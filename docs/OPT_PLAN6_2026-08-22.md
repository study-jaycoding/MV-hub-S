# 최적화 라운드 7 설계 v2 (2026-08-22, 코덱스 검토 반영 — 시리즈 마지막)

검토 판정 반영: ①보안 선행 웨이브 신설 — publish-1·db_transfer-1 을 모든 최적화보다
먼저 ②assets-1 은 웨이브 2 로(SMB·원자 커밋·취소 결합 — 소형 아님) ③비동기 취소
non-abandon 계약(스레드 완료 전 lock 해제·임시파일 삭제·반환 금지) 전 항목 공통.

## 웨이브 0 — 보안 선행 (P1 2건)

0-A. **publish-1** /shared-server 7개 라우트(status·login·register·logout·elevate·
  de-elevate·url)에만 loopback 가드(require_loopback_request 래퍼 — 기존 의존성 없음,
  신설). /share/publish-bundle·/publish-to-shared 는 제외 필수. URL 은 공통 정규화
  (http/https·호스트 필수·사설 IP/localhost 허용·실패=외부 요청 전 400) — login 입력·
  저장값·register/elevate 재사용 모두 통과. ★호환성 변경 문서화: LAN 직결·역프록시
  에서 이 7개는 403. 공유 서버(auth_enabled) UI 는 서버 설정·elevation 숨김.
0-B. **db_transfer-1** 계약(코덱스 확정): 보안 초기화는 '한 SQLite 트랜잭션', 파일
  교체~보안 초기화~clear_active 는 '파일 세트 보상 롤백 경계'. 성공 경계=게이트→백업→
  교체→init_db(명시 경로)→같은 경로 직접 연결 BEGIN IMMEDIATE→비밀 7종+auth_secret
  제거→새 auth_secret→전 계정 password_changed_at 회전(★환경변수 AUTH_SECRET 이면
  토큰 무효화가 이것뿐)→COMMIT→strict clear_active→캐시 초기화→응답. 실패 시 content·
  trash 파일 복구+active.json 원상, 롤백 실패=500+운영 로그. 게이트 안 set_setting
  금지(직접 연결)·strip_transfer_secrets 재사용 금지. 역할 제거=shared_server_roles
  캐시만(account.global_role 불가침).

전제 동일: 배포 기능 불변·계약 테스트 선행·독립 커밋·웨이브 리뷰. 범위=라우터 계층+
앱 루트(서베이6) 15후보 — 전량 채택(마지막 라운드, 가치 소진 관점).

## 웨이브 1 — 저위험 소형 (7건)

1-B. **db_transfer-3** 내보내기 임시파일을 OS 고유 파일(mkstemp/난수)로 — 같은 초
  동시 요청의 파일 공유·교차 삭제 제거. FileResponse 전 실패 정리.
1-C. **manage-2** 담당자 20명 초과 무음 절단 → 400 반환(부분 성공 오보고 제거).
1-D. **generation-1** 배치 API 내부 최대 500회 finalize_id_map 재조회 제거 —
  workspace 는 row job_id 직접, 코멘트는 resolve_generation_meta_batch 1회.
1-E. **comfy-1** 입력 계보 권한 확인 배치화 — get_generations_batch+
  batch_view_member_projects, 열람 불가=None 의미 유지.
1-F. **library-2** 코멘트 보강 500개 순차 chunk — 실패 chunk 만 로컬값 유지(성공
  chunk 는 반영), chunk 번호·크기 warning 로그, private count 는 1회 계산.
1-H. **generation-2** cache_one preflight(해석·권한·큐 등록)·조립 to_thread —
  로컬 miss 시 최대 60초 동기 프록시가 루프를 막던 것 제거.
1-I. **hf_missing-1** gens_with_job_id to_thread 1줄.

## 웨이브 2 — 중위험 (6건)

2-G'. **assets-1(웨이브 2 이동)** 업로드의 SMB 경로 해석·배치 검사·최종 커밋 to_thread
  — hardlink/O_EXCL 불변, 취소 시 스레드가 임시파일 소유·정리 완료 전 반환 금지.
2-B. **db_transfer-2** validate_hub_db·_install_db 전체 to_thread(예외 매핑 유지) —
  대형 DB 가져오기 중 전체 API 정지 제거. 유지보수 게이트·롤백 테스트 스레드 재검증.
2-C. **library-1** 각인 읽기(업로드 복사·크기 검사·read_stamp)를 sync helper 로 묶어
  to_thread — 413·finally 삭제 계약 유지, 취소 시 임시파일 정리 검증.
2-D. **resolve_integration-1** 최대 500건 전송 판정 배치화 — 로컬 1회+멤버십 1회+
  원격 /generations/batch 1회, 입력 순서 재조립. 첫 missing 404 문구·팀 가시성 동일.
2-E. **manage-1** 완료본 저장 프로젝트별 asyncio.Lock(refcount 레지스트리 — R6
  목적지 lock 동일: 대기 취소=refcount 만 감소, 보유 취소=release 후 감소, 마지막
  사용자 제거, 레지스트리 lock 잡은 채 await 금지). lock 범위=저장 대상 조회~
  record_export 끝. NAS/DB 구간 to_thread+non-abandon. 단일 프로세스 한계 명시.
  final_export 정책·쿼리 불변(전역 lock 금지).
2-F. **publish-2** 벌크 발행 읽기 배치화 — 수신=resolve_generation_meta_batch 1회,
  비프록시=get_generations_batch. 권한 검사·항목별 repo.publish 순서·published 집계
  재현.

## 하지 않는 것(서베이6 금지 목록)

publish.router 전체 loopback 가드 금지(/share/publish-bundle 보호) · 공유서버 URL
public-IP 검사 금지(사설 LAN 서버 허용) · final_export 정책·쿼리 재수정 금지(manage 는
async 경계만) · 단건 원격 500개 gather 병렬화 금지(배치 API 사용) · 완료본 저장 전역
lock 금지(프로젝트별만) · 보안 키 제거 실패 best-effort 유지 금지 · 임시파일 ms
timestamp 완화 금지(OS 고유 파일) · 인증 TTL·미들웨어 결합·자동 리프레시·워크스페이스
가시성·ingest/push 앵커·유료 CLI 순서·경로 prefix 불변.
