---
updated: 2026-08-22
status: archived
---

# 최적화 라운드 8 (2026-08-22) — 코덱스 주도 구현 체제 ✅종결

★결과: 13건 전부 반영 + 적대 리뷰 P1 3건 수정. 최종 회귀 백엔드 **1,401**(+51, 기준선
1,350)·프런트 616. 커밋: cb121bf5(웨이브1) 268afe03(AT-1/AIO-2) c3e33edd(SY-1)
31b93fc1(GR-1/BK-2/TD-1) a15a9081(MP-1) 68bcacc1(GMC-1) 163f7486(GPM-1)
5990c780(리뷰 P1 3건).

리뷰 P1(수정 완료): ①stuck synced 정리가 공유 서버에서 남의 잡 오살(하우스 creator_uid
스코프+미확정 시 스킵) ②telemetry (account_key,my_uid) 짝 어긋남 → B tombstone 무전송
소거 ③account-report 미고정 → B 계정을 A UID 로 오연결(_drain_once 원자 캡처+두 drain
전체 override 유지).

리뷰 P2 6건 → R9 이월: assets non-abandon commit 후 취소 시 캐시 무효화 생략 ·
GMC 세마포어 호출별 생성(프로세스 상한 아님) · gather 예외 시 형제 task 미회수 ·
백업 due 판정 scope 와 실행 scope 재캡처 창 · quiet-dirty 백업 실패 삼킴 후 상태 소거 ·
trash tombstone 기록이 본체 COMMIT 밖.

★운영 변경(Jay 지시): R8~R10 은 코덱스가 서베이+구현+계약 테스트 작성+자체 검증까지
주도(쓰기 샌드박스). 클로드=범위·계약 승인, diff 검토, 전체 회귀, 커밋·푸시(최종 반영
책임 — CLAUDE.md "코덱스 코드는 클로드가 검토 후 반영" 유지).

범위=마지막 미조사 잔여: usecases 계층(gen_requests 1,476줄 등 4모듈)+서비스 소형
9모듈(backup·syncer·asset_tree·file_stamp·asset_io·telemetry_drain·media_preservation·
project_folders). 코덱스 서베이#7 13후보 — 클로드 전량 승인(계약이 누적 금지 목록·
R7 교훈과 정합함을 확인).

## 웨이브 1 — 저위험 (4건)

- BK-1 주기 백업 탐색을 단일 스캔 동기 helper 로 모아 to_thread(NAS 루프 블로킹 제거).
- FS-1 각인 임시파일 mkstemp 고유화+실패 잔여물 정리(동시 각인 충돌 제거).
- AIO-1 업로드 chunk write 를 to_thread_non_abandon(삭제 경쟁 제거).
- SY-2 duplicate reconcile 예외 무음 삼킴 → 구조화 경고(오류 원문 미기록).

## 웨이브 2 — 중위험 (9건, 코덱스 권장 순서)

AT-1 asset_tree 캐시 epoch(오래된 스캔의 캐시 재오염 차단) → AIO-2 동일 미디어
check-and-commit 원자화(refcount lock) → SY-1 stuck synced 삭제 직전 재검증
transaction-root(move_to_trash_if_stuck_synced) → GR-1 요청 usecase 계정 범위 고정+
_sync_io non-abandon → BK-2 백업 (src,dir) 캡처 고정+전체 non-abandon+콜백 실패
분리 기록 → TD-1 telemetry prepare/push/settle 계정 범위 고정 → MP-1 보존
claim~finish 비포기 실행 → GMC-1 미디어 캐시 동시성 6 제한+DB 일괄 반영
transaction-root → GPM-1 개인 메타 로컬+shadow 단일 트랜잭션.
★SY-1·GMC-1·GPM-1 은 각각 독립 diff 로 구현·검토.

## 보고만(구현 제외)

project_folders 중복 조회 1회(효과 소) · cache_all_generation_media=사장 코드(삭제
금지 지침 준수) · syncer._house_email 무기한 캐시(자동 refresh 계약 연동이라 제외).

## 하지 말 것(서베이#7 — 코덱스 자체 명시)

gen_requests 라우터·repo·idempotency/CAS 재수정 금지 · 견적을 응답 전 대기 금지 ·
recovery_required 자동 재큐잉 금지 · syncer 유료 CLI·0.75s 캐시·refresh 정책 불변 ·
온라인 백업에 maintenance_gate 금지 · 각인 3키·무재인코딩 불변 · 보존 실패값 캐시
금지 · dedup 을 content-address 파일명으로 바꾸지 않음 · asset_tree lock registry
삭제 금지 · 사장 코드 삭제 금지 · R1~R7 재조사 금지.
