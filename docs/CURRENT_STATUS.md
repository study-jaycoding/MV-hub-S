# MV Hub 현재 작업 현황

기준일: 2026-08-18

마지막 전체 검증 구현 기준: `dev`의 `39c0c26e`

현재 문서 기준: 2026-08-18 적대적 재검토와 비공개 코멘트·캔버스 소속·다운로드 경계 보강 완료

비교 기준: `main`의 `3b7cf077`

이 문서는 긴 감사 기록을 읽기 전에 보는 **한 장짜리 현황판**이다. 위험 항목의 상태를 바꾸는
단일 기준은 [RISK_REDUCTION_PLAN_2026-08-15.md](RISK_REDUCTION_PLAN_2026-08-15.md)의
`Gate 0 산출물 — 정규화 잔여 목록`이다. 두 문서가 다르면 Gate 0 표를 우선한다.

## 지금 상태

- 현재 작업 브랜치는 `dev`다. 최신 안정화 구현은 `39c0c26e`이며 전체 회귀·프로덕션 빌드·
  격리 브라우저 핵심 화면 실측을 통과했다.
- 잘못된 데이터 표시·오귀속으로 분류한 미해결 P0는 없다.
- 내부 전체 회귀와 관리 화면 실측은 통과했다. 실제 Higgsfield·공유 서버·Comfy·Resolve·NAS·
  릴리스 설치본을 한 번에 잇는 외부 통합 검증은 아직 남아 있다.
- 따라서 **개발 기준선은 안정화됐지만 운영 배포 완료를 선언한 상태는 아니다.**

## 완료한 핵심 위험

| ID | 결과 | 근거 커밋 |
|---|---|---|
| RL-01 | 생성 당시 워크스페이스 귀속을 보존하고 다른 공간으로 추측하지 않음 | `898b703b` |
| RL-02 | 프로젝트·요약·작업이 같은 워크스페이스 범위를 사용하고 과거 작업을 별도 표시 | `3d4107e3` |
| RL-03 | 주기 동기화 적재와 관리 집계 전송 예약을 같은 트랜잭션으로 처리 | `66ea9805` |
| RL-04 | 확인되지 않은 워크스페이스 생성 요청을 CLI 실행 전에 차단 | `2598916a`, `955d8d61` |
| RL-05 | 유료 제출 전·후를 분리하고 불명확한 제출은 자동 재실행하지 않음 | `1252b52d` |
| RL-06 | 같은 캔버스 시도의 동시·중단·재시작 재시도를 하나의 생성으로 복구 | `df2d1fd5` |
| RL-07 | 완료된 생성이 늦은 응답 때문에 실행 중·실패로 되돌아가지 않음 | `cb373035` |
| RL-08 | 견적 CLI 동시 실행을 제한하고 취소·종료 때 자식 프로세스까지 회수 | `28f49cc6` |
| RL-09 | 요청별 401과 실제 세션 만료를 구분하고 유효한 로그인은 보존 | `590298cb` |
| RL-10 | 생성 적재 응답과 원격 텔레메트리 전송을 분리하고 늦은 정산을 revision으로 차단 | `dfaa2672` |
| RL-11 | 공유 해제·최종 해제 실패가 로컬과 서버 상태를 다르게 남기지 않도록 차단·재시도·보상 | `18d63560` |
| RL-12 | 실제 반영 성공 시각을 재시도 행과 분리 보존하고 로컬 계정 메뉴에 표시 | `46c6198b` |
| RL-13 | 계정 상태·거래 보고를 별도 영속 큐로 전환하고 명시적 서버 ACK 뒤에만 완료 처리 | `9216ee96` |
| RL-14 | Assets 원본을 지원 미디어로 제한하고 고정 MIME·nosniff·동일 출처 경계를 적용 | `2a4a46f4` |
| RL-15 | 업로드 본문을 multipart 파싱 전에 제한하고 파일 수·개별·합계를 다시 검증 | `5b2cf434` |
| RL-16 | 폴더 감시를 등록자 단위로 추적하고 이동·삭제·종료 때 마지막 핸들을 해제 | `bb8b5843` |
| RL-17 | Comfy 입력을 앱 TEMP 파일 하나로 소유하고 multipart를 1MiB 청크로 전송 | `d5077be6` |
| RL-18 | 앱 임시파일을 공통 수명주기와 안전한 접두 범위로 청소 | `a2fbbe0e` |
| RL-19 | 느린 WebSocket 하나를 격리하고 재연결 분산·인증 만료 표시를 통일 | `1f096796` |
| RL-20 | busy·DB 유지보수·실제 무응답을 분리하고 무응답에만 재시작 개입 | `f56811b9` |
| RL-21 | 동일 시각의 DB 3개를 복원 사본으로 검증하고 격리 서버 ready·로그인·핵심 수를 대조 | `562f83c7` |
| RL-22 | 공유·최종 결과 원본을 영속 큐로 보존하고 용량 한도·재시작 복구·실패 재시도를 표시 | `2a8dab18` |
| RL-24 | CLI 파라미터 스키마 조회 실패를 영구 캐시하지 않고 짧은 백오프 뒤 재조회 | `8f09856e` |
| RL-25 | 빈 TEMP와 PowerShell 모듈 자동 로딩 실패에도 Git 업데이트 bootstrap 실행 | `ce346560` |

현재 부분 해결 위험은 RL-02와 RL-23이다. RL-02의 화면 범위·수명주기는 구현됐지만 프로젝트 이동
뒤 과거 작업의 귀속 모델은 설계 결정을 남겨 두었다. RL-23은 자동 업로드·세트 저장·재시도·운영
상태까지 구현했지만 실제 NAS/다른 물리 장치의 복제본만 사용한 예비 PC 복원 훈련 전에는 완료로
올리지 않는다.

## 2026-08-18 추가 안정화

- **비공개 코멘트 경계:** 공유 서버의 공개 코멘트 수와 계정 로컬 비공개 코멘트 수를 정확히 합치고,
  다른 계정으로 바뀌면 이전 계정의 코멘트 캐시를 재사용하지 않는다. 비공개 코멘트는 팀 공유·발행·
  통계에서 제외된다는 실제 보존 범위로 안내 문구도 바로잡았다.
- **캔버스 생성물 제거 복구:** 카드에서 생성물을 뺀 의도를 계정별 영속 대기열에 네트워크 전부터
  기록한다. 전송 실패·오프라인·앱 재시작과 늦은 서버 조회 뒤에도 자동 백필이 제거 항목을 되살리지
  않는다.
- **원격 다운로드 상한:** 크기를 미리 알 수 없는 원격 파일이 각인 상한을 넘으면 임시 디스크 사용을
  제한한 채 각인 없이 전체 원본을 스트리밍한다. 중단·완료 뒤 upstream과 임시파일을 회수한다.
- **예상 크레딧 폴백:** 가격 갱신용 CLI가 순간 실패하더라도 검증된 이전 가격이 있으면 화면과 로그에
  502를 남기지 않고 그 값을 일시 재사용한다.

## 최근 내부 구현·검증을 완료한 작업

- **RL-23 — 작업자 PC 오프디스크 백업**

  작업자 로컬 백업의 `content + trash`를 한 세트로 묶어 공유 서버에 자동 업로드한다. 세트는
  별도 영속 outbox에 먼저 등록하고, 네트워크 실패·로그인 만료·앱 종료 뒤에도 폐기하지 않는다.
  공유 서버는 세션 계정별로 크기·SHA-256·SQLite 무결성을 확인한 뒤에만 세트를 공개하며, 정확한
  세트 ID와 역할별 ACK가 모두 맞아야 작업자가 성공 처리한다.

  서버의 외부 복제는 기존 단일 DB와 새 완성 세트를 모두 처리하고 결과를 구조화 상태로 남긴다.
  설정 화면은 로컬 세트 구성, 공유 서버 백업 상태·마지막 성공·대기 수와 재시도 버튼을 표시한다.
  릴리스 업데이트는 상태 DB·outbox·staging이 있는 `backend/data`를 교체하지 않는다.

  동일 세트 동시 저장 20회, 관련 회귀 84개, 백엔드 전체 887개·21 subtests, 프론트 76개
  파일·535개, 프로덕션 빌드와 아키텍처 검사를 통과했다. 격리 브라우저에서 상태 표시와 앱 출처
  콘솔 오류 0건을 확인했고, 100명 저사양 단기 실측도 통과했다. 실제 다른 물리 장치 복원 훈련은
  남아 있어 RL-23 상태는 부분 해결이다.

## 그다음 권장 작업

서로 섞지 않고 아래 순서로 각각 설계·구현·회귀·실측·커밋한다.

1. **RL-23 외부 완료 조건 — 실제 다른 물리 장치 복원 훈련**

   공유 서버의 실제 NAS/다른 디스크 대상을 설정하고, 작업자 세트 하나를 복제한 뒤 그 복제본만으로
   예비 PC에 `content + trash`를 복원한다. 결과와 걸린 시간을 월간 훈련 기록에 남긴다. 상세 조건은
   [WORKER_OFFDISK_BACKUP_CONTRACT.md](WORKER_OFFDISK_BACKUP_CONTRACT.md)를 따른다.

2. **RL-02 잔여 설계 결정 — 이동한 프로젝트의 과거 작업 귀속**

   프로젝트를 다른 워크스페이스로 옮긴 뒤 기존 작업을 어느 공간의 과거 기록으로 볼지 결정한다.
   작업 생성 당시 공간을 고정 보존하는 방식과 생성물 기준으로 다시 모집하는 방식 중 하나를 먼저
   확정한 뒤 별도 작업으로 구현한다.

3. **Gate 6 — 실제 외부 프로그램과 릴리스 설치본 통합 검증**

   실제 Higgsfield 1건, 두 계정 공유 왕복, Comfy·Resolve, NAS 복제, 릴리스 업데이트·자동 재시작을
   운영과 같은 순서로 한 번에 확인한다. 이 단계 전에는 운영 배포 완료로 표현하지 않는다.

## 마지막 검증 결과

| 검증 | 결과 |
|---|---|
| 백엔드 전체 테스트 | 935개, 21 subtests 통과 |
| 프론트 전체 테스트 | 78개 파일, 566개 통과 |
| 프론트 아키텍처 검사 | 통과 |
| 프론트 프로덕션 빌드 | 통과 |
| 2026-08-18 안정화 집중 회귀 | 비공개 코멘트 12개, 계정별 코멘트 캐시 1개, 캔버스 소속 19개, 다운로드 각인 6개, CLI 견적 계약 8개 통과 |
| 2026-08-18 격리 브라우저 실측 | 메인·캔버스·Assets, 관리 대시보드·작업 테이블·보드·캘린더·완료 로딩 및 서버 응답 확인, 앱 경고·오류 로그 0건 |
| 업데이트 경로 테스트 | RL-25 직접 대상 4개, 백엔드 관련 40개·프론트 3개 통과 |
| RL-05 대상 테스트 | 백엔드 98개, 프론트 2개 파일·3개 통과 |
| RL-05 중단·재시작 DB 드릴 | 통과, `paid_cli_called=false`, `ok=true` |
| RL-05 격리 브라우저 실측 | 복구 표시·일반 재생성 차단·명시 확인 버튼·콘솔 오류 0건 확인 |
| 단일 DB 백업·복원 드릴 | 통과 |
| RL-21 DB 세트 복원 집중·관련 회귀 | 8개·39개 통과 |
| RL-21 Windows 격리 서버 실측 | content·trash·manage ready, 로그인, 핵심 수 대조, 원본 불변·프로세스 회수 통과 |
| RL-22 원본 보존 집중·동시성 회귀 | 집중 31개 및 동시 공유·최종 요청 20회 반복 통과 |
| RL-22 격리 브라우저 실측 | 기존 최종본 자동 백필, 용량 부족·재시도·완료 표시, 앱 콘솔 오류 0건, 5173/8012 회수 |
| RL-23 관련 회귀 | 작업자 세트·복제·복원·운영·업데이트 84개 통과 |
| RL-23 동시·중단·혼합 버전 | 동일 세트 20회 동시 저장 1세트, 재시작·로그인 만료·네트워크 복구·신구 서버 호환 경계 통과 |
| RL-23 격리 브라우저 실측 | 로컬 백업 수·최신 세트 구성·공유 서버 상태·재시도 표시, 앱 콘솔 오류 0건, 5173/8012 회수 |
| RL-23 100명 저사양 단기 실측 | 요청 2,920건 모두 200, p95 36.68ms·p99 231.80ms, WS·장기 폴링 각 100개, 오류·SQLite lock 0, 최대 RSS 196,182,016바이트 |
| 관리 화면 실측 | Personal → MILLIONVOLT → R&D 전환, 과거 기록 토글, 반복 레이아웃 안정성 확인 |
| RL-06 대상 테스트 | 백엔드 19개, 프론트 1개 파일·2개 통과 |
| RL-06 동시·중단·재시작 드릴 | 동일 생성·요청 1건, 신호·저널 1회, 재생성 옵션 복원 확인 |
| RL-06 격리 브라우저 실측 | 로그인·워크스페이스·캔버스 로드, 콘솔 오류 0건 확인 |
| RL-08 대상 테스트 | 백엔드 24개 통과 |
| RL-08 동시 견적 부하 실측 | 100명 동시 조회를 가정한 100개 요청에서 실제 CLI 구간 최대 2개, 캐시 100개 보존 |
| RL-08 Windows 취소 실측 | 테스트 부모·손자 프로세스 종료, 생존 마커 없음 |
| RL-09 대상 테스트 | 백엔드 12개, 프론트 1개 파일·5개 통과 |
| RL-09 동시·TCP 회귀 | 100개 동시 401에서 확인 1회, 느린 확인 비대기, 실제 TCP 만료·보존 판정 통과 |
| RL-09 격리 브라우저 실측 | 로그인·작업 공간↔공유 & 리뷰 전환, 강제 로그아웃 없음, 콘솔 오류 0건 |
| RL-10 대상 테스트 | 백엔드 37개, 5회 연속 통과 |
| RL-10 동시·느린 전송 회귀 | 동기 예약 100개 합치기, 두 번째 drain 0.2초 이내 반환, 실패 뒤 재시도 통과 |
| RL-10 DB 경계 회귀 | 원격 전송 대기 중 유지보수 게이트·새 dirty 쓰기·동일 시각 revision CAS·구버전 DB 마이그레이션 통과 |
| RL-10 격리 앱 실측 | test_dev 준비·로그인 유지·작업 공간→공유 & 리뷰→캔버스·앱 콘솔 오류 0건·5173/8012 반환 통과 |
| RL-11 직접 대상 테스트 | 공유·최종 상태 일관성 12개 통과 |
| RL-11 관련 회귀 | 공유·프록시·감사 묶음 62개 통과 |
| RL-11 로컬 DB·보상 회귀 | 실제 임시 SQLite의 최종→공유 불변식, 재시도·재조회·원격 보상 성공/실패 통과 |
| RL-11 격리 앱 실측 | 로그인 유지·작업 공간→공유 & 리뷰→캔버스·앱 콘솔 오류 0건·5173/8012 반환·운영 에이전트 보존 통과 |
| RL-12 대상 테스트 | 백엔드 38개, 프론트 1개 파일·3개 통과 |
| RL-12 DB·경쟁 회귀 | 기존 성공 이관·재dirty 보존·늦은 ACK·전송 없는 큐 정리의 거짓 성공 차단 통과 |
| RL-12 로컬 브라우저 실측 | 계정 메뉴 마지막 성공·대기 없음 표시, 앱 콘솔 오류 0건, 5173/8012 반환·기존 8010 보존 통과 |
| RL-13 대상 테스트 | 백엔드 19개, 프론트 동기화 상태 7개 통과 |
| RL-13 큐·경쟁 회귀 | 계정 최신 상태 단일화·거래 안정 키 중복 방지·모델 보강·동일 보고 백오프 유지·늦은 ACK revision 차단 통과 |
| RL-13 두 서버 브라우저 실측 | 로컬 8012→임시 AUTH 공유 서버 8201 실패 표시·명시적 ACK 복구·대기 0·앱 콘솔 오류 0건·5173/8012/8201 반환 통과 |
| RL-14 대상 테스트 | Assets 응답·서비스·권한 백엔드 19개와 허용 확장자 21개 하위 검증 통과 |
| RL-14 실제 HTTP·브라우저 실측 | PNG 200·정상 표시·고정 MIME/inline/nosniff/CSP/CORP/no-cache, HTML 415, 테스트 포트 8214 반환 통과 |
| RL-15 관련 회귀 | 업로드 미들웨어·Assets·Comfy·DB·임시파일 경계 79개 통과 |
| RL-15 실제 HTTP 실측 | 정상 소파일 200, Assets 합계·Comfy 개별·DB·청크 요청 413, 부분파일·민감 로그 없음, ready 유지·테스트 포트 반환 통과 |
| RL-16 집중 회귀 | watcher·Assets 트리·권한·프로젝트 폴더·예산 48개 통과 |
| RL-16 Windows watchdog 실측 | 등록 경로 100회 이동과 같은 폴더 50개 공유 등록에서 핸들·emitter 1개 유지, 마지막 해제·종료 뒤 0개·Observer 종료 확인 |
| RL-16 HTTP/WS 실측 | 수동 마운트 경로 교체 뒤 이전 경로 무알림·새 경로 알림, 삭제 뒤 무알림, 종료 뒤 8216 포트·프로세스 반환 통과 |
| RL-17 대상 테스트 | Comfy 클라이언트·라우터·업로드 제한·영상 변환·임시파일·저장 99개 통과 |
| RL-17 64MiB 스테이징 실측 | 67,108,864바이트 일치·정리 완료, Python 추적 최대 할당 2,232,179바이트 |
| RL-17 64MiB 실제 HTTP 실측 | multipart 67,109,276바이트 완전 수신·Content-Length 일치·chunked 없음, Python 추적 최대 할당 4,554,937바이트 |
| RL-19 대상 테스트 | 느린·실패 소켓 격리, 중첩 방송 단일 집계, 운영 카운터 백엔드 17개와 재연결 jitter·1008 표시 프론트 4개 통과 |
| RL-19 100명 저사양 단기 실측 | 3,700개 요청 모두 200, 작업 p95 59.44ms·p99 176.75ms, WS 100개 유지·오류 0·SQLite lock 0·최대 RSS 198,844,416바이트 |
| RL-19 격리 브라우저 실측 | 임시 계정 로그인 뒤 서버측 거부를 1008로 수신, 재접속 반복 없이 로그인 화면 표시·앱 출처 콘솔 오류 0건 확인 |
| RL-20 집중 회귀 | ready 유지보수 즉시 응답·상태머신·HTTP 오류 분류·서브프로세스 감시 21개 통과 |
| RL-20 busy·maintenance 실측 | 별도 TCP 서버의 ready→busy→maintenance→ready에서 경보만 남고 개입·taskkill 없음, 응답의 민감 필드 미기록 확인 |
| RL-20 hang 실측 | 실제 Windows 포트 소유 프로세스의 연속 timeout에서 정확한 PID만 dry-run 개입 대상으로 판정, 테스트 프로세스만 회수 |
| RL-20 100명 저사양 단기 실측 | 요청 3,706건 모두 200, 작업 p95 64.69ms·p99 173.56ms, WS·장기 폴링·SQLite lock 오류 0, 최대 RSS 198,799,360바이트 |
| 서버 부팅 smoke | 기존 dist만 사용, npm 호출 없음, 임시 DB ready·프로세스 정리 통과 |

위 결과는 해당 커밋 또는 명시한 현재 작업 트리의 내부 계약을 확인한 기록이다. 새 커밋이나 외부 프로그램 버전 변경 뒤에는
다시 실행해야 하며, 실제 크레딧을 사용하는 생성 실측을 대신하지 않는다.

RL-05 브라우저 실측은 임시 SQLite DB와 테스트 계정만 사용했고 실제 Higgsfield CLI는 호출하지
않았다. 따라서 이 결과는 중복 과금 방지 내부 계약을 증명하지만 실제 크레딧 생성 왕복을 대신하지 않는다.

## 운영 배포 전 남은 확인

- 실제 Higgsfield 생성 1건의 제출 → 직접 상태 조회 → 결과 URL → 서버 자산 저장 전체 왕복
- 공유 서버에서 서로 다른 두 계정의 publish → 조회 → unpublish 왕복
- 실제 Comfy 실행과 Resolve 가져오기·내보내기
- 실제 NAS 백업 복제의 최신성·오프디스크 복사와 예비 PC 복구 시간 측정
- 운영 설치본의 업데이트 → 자동 재시작 → ready → 로그인 → 주요 화면 확인

상세 합격 조건은 위험 계획의 `Gate 6 — 외부 통합과 병합·배포 검증`을 따른다.

## 문서 사용 순서

1. **현재 판단:** 이 문서
2. **위험 상태와 다음 작업:** [RISK_REDUCTION_PLAN_2026-08-15.md](RISK_REDUCTION_PLAN_2026-08-15.md)
3. **현재 구조:** [ARCHITECTURE.md](ARCHITECTURE.md), [AI_CONTEXT.md](AI_CONTEXT.md)
4. **데이터·권한 계약:** [DATA_OWNERSHIP.md](DATA_OWNERSHIP.md),
   [WORKSPACE_DATA_CONTRACT.md](WORKSPACE_DATA_CONTRACT.md),
   [신원과_모드_가이드.md](신원과_모드_가이드.md)
5. **운영·복구·테스트:** [SERVER.md](SERVER.md), [SERVER_RECOVERY.md](SERVER_RECOVERY.md),
   [TESTING.md](TESTING.md)
6. **과거 발견 근거:** [AUDIT_2026-08-15.md](AUDIT_2026-08-15.md)

## 갱신 규칙

- 위험 상태는 Gate 0 표에서 먼저 바꾼 뒤 이 문서의 요약을 맞춘다.
- 테스트 숫자는 전체 회귀를 실제로 다시 실행했을 때만 갱신한다.
- 브랜치의 앞선 커밋 수처럼 매번 변하는 숫자는 문서에 고정하지 않는다.
- 외부 실측을 하지 않았다면 `검증 완료`나 `배포 가능`으로 표현하지 않는다.
