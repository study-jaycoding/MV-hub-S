---
aliases:
  - 현재 작업 현황
tags:
  - mvhub
  - mvhub/현황
status: active
updated: 2026-09-18
---

# MV Hub 현재 작업 현황

기준일: **2026-09-18** · **`56e53477` 기준 정식 배포 전 게이트 통과 · 푸시·병합·배포 미실행**.

## 최신 판정

- 09-18 08:25의 **R3 단일 전체 실행**에서 정식 게이트를 통과했다. 새 전체 시험·빌드·구조 검사·합성 백업 복원·100명 부하 결과와 종료 확인은 아래 절을 따른다. 검사 전후 및 종료 후 추적 파일1,160개 해시 불일치0, 검사 커밋 동일·작업 트리 clean이다.
- 앞선 종합 점검에서는 실제 Claude R8 승인 후 필수 기록 보완5건을 [종합 점검 장부](PROGRAM_AUDIT_2026-09-17.md)에 반영했고, 후속 Claude `8f39cf94…`가 기록·사실 정정·종결 문구까지 재확인 승인했다. 그 감사의 종결이며 운영 배포 승인이 아니다.
- 종합 점검 당시 최종 백엔드4,152 PASS +815 subtests, 권한1 SKIP, 폐기예고2경고. 프런트182파일/1,909 PASS·타입 포함 빌드·구조 검사 통과; 기존 번들 경고 유지.
- 이전 종합 점검의 100명·15분 혼합 부하는171,283건 논리 정상(캐시304 포함), 오류/DB잠금0, 일반 요청p95 50.47–50.82ms, 최대257.5MiB였다. 로그인p95 7660.6ms와 제어요청46.74ms, 롱폴 포함 서버p99약25초는 별도 지표다. 이번 정식 게이트의 부하는60초×2회로 구분한다.
- 이 부하는 목록/집계/상세/미디어/색 저장 중심이며 Comfy·공유 변경·Resolve·백업/업데이트·제출의 동시 부하를 대신하지 않는다. 전체 앱 성능 보증이 아니다.
- 종합 점검 종결 당시 승인 소스473/553/167개 대조 불일치0·시험용 프로세스/포트 종료를 확인했다. 이 수는 저장소 전체 파일 해시가 아닌 당시 시험별 선택 집합이다.
- U2의100저장63.26초/읽기오류46회, B2의 정확성 확보에 따른 지연·전송량 비용은 남는다. 유료 생성·실제 Resolve·운영 적용·장시간 운용/RTO는 미검증이다.
- 사용자 승인에 따라 코드·시험99파일을 `dev`의 `80e0b8a1`, 감사·계약·현황 문서6파일을 `56e53477`로 분리 커밋했다. 승인된 소스473/553/167개와 문서 해시를 먼저 대조했고, 커밋 검사에서 신규 시험2파일 끝의 불필요한 빈 줄만 제거했다. 아래 종료 기록은 제품 코드가 바뀌지 않는 문서 후속 커밋으로 남긴다.
- 이번 요청 범위는 **분리 커밋과 깨끗한 커밋 기준 배포 전 검사**다. 푸시·병합·태그·릴리즈 제작·NAS 게시·운영 적용은 하지 않는다. 앞의 종합 점검 수치는 이전 증거이며 새 정식 게이트의 결과로 재사용하지 않는다.
- DB 호환성 별도 읽기 전용 검토에서 새 배포 차단 문제는 발견되지 않았다. SQL 스키마 변경 없이 기존 설정에 Comfy 원장을 추가하며, 구버전 복귀 시 원장 처리 제한은 [Comfy 계약](COMFY_RESULT_RECOVERY.md)의 경고를 따른다. 운영 DB 세트 백업·실행 중 작업 확인 등 Gate 6-B는 별도다.
- NAS의 마지막 확인 기록은 09-17 19:11:54 게시한 이전 배포본 `2026.09.17-1900`이다(당시 ZIP78,700,236B·SHA256 일치). 이번 사전 게이트에서는 NAS를 다시 검사하거나 게시하지 않았으며 설치본도 적용하지 않았다.

### 09-18 정식 배포 전 검사

- owner: Codex, reviewer: 제품은 앞선 실제 Claude 독립 승인; 이번 DB 호환성·검사 환경 교정·R3 집계는 별도 Codex 읽기 전용 검토.
- status: 완료 (사전 점검 한정).
- touched_paths: 종료 기록은 이 문서; 제품 변경은 `80e0b8a1`, 정식 검사 기준은 문서 커밋을 포함한 `56e534777f8baf80d20cf6f795ece8bbb604080d`다.
- **공식 게이트와 판정 기준은 수정하지 않았고 `AllowDirty`·시험 생략 옵션도 사용하지 않았다.** 격리 보조 실행기는 사용자 DB 대신 새 합성 데이터·임시 폴더를 사용하고, 백엔드 시험의 격리 밖 SQLite/외부 네트워크 접근을 차단했다. 차단 시도 기록0이다.

| 실행 | 결과 | 근거와 처리 |
|---|---|---|
| 1차 | 게이트 실패 | 백엔드·프런트·빌드·구조·합성 복원은 통과했지만 부하 첫 구간p95 534.51ms가500ms 기준을 초과했다. 검사 보조 실행기의 시험용 `CONTENT_HUB_DB_POOL=0`이 부하 서버에도 상속된 것을 확인했다. 원자료는 실패로 보존했다. |
| R2 | 미완료 | DB 연결 재사용 비활성화를 pytest 프로세스에만 한정하고 부하 서버는 제품 기본값을 쓰도록 분리했다. 백엔드31%에서 프로세스가 종료됐고 완료 기록이 없다. 종료 원인은 단정하지 않으며 통과로 세지 않는다. |
| R3 | **단일 전체 통과** | R2와 동일한 보조 파일3개를 별도 숨김 백그라운드로 실행했다. 07:50–08:25에 전체 게이트를 완료했고 두 완료 JSON 모두 `passed=true`다. 제품·시험 코드와500ms 기준은 그대로다. |

1차의 결과 XML은 Windows 인자 해석으로 다른 위치에 생성되어 보고서 폴더로 옮겼다. 이동 전후 SHA256은 동일하다. R2부터 출력 경로만 정규화했다. 설정 누출 확인과 R3의 통과가 pool off를 첫 지연의 유일 원인으로 확정하는 반복 A/B 실험은 아니다.

R3 결과:

- 백엔드 **4,152 PASS +815 subtests**, 실패/오류0, Windows 심볼릭 링크 생성 권한 부재1 SKIP, 웹소켓 의존성 폐기예고2경고. 시험 시간1,884.79초.
- 프런트 **182파일/1,909 PASS**, 타입 포함 빌드·구조 검사 통과. React `act` 시험 환경 경고606줄과 큰 번들 경고는 남는다. 무경고 또는 실제 UI 운용의 완전한 보증으로 해석하지 않는다.
- 합성 단일 DB 온라인 백업·복원: 생성물3개, 무결성 `ok`, 외래키 오류0. **운영 DB3종 세트 또는 실물 NAS 복원 검증이 아니다.** 개발 환경의 의존성 일관성 검사도 통과했다(Python3.14.3·Node24.11.1).
- 100명·60초×2회 혼합 부하 **22,674건 논리 정상**(캐시 재사용 포함, 로그인·제어 요청은 별도), 오류/DB잠금0. 각 구간p95 **56.55/59.46ms**, p99 129.47/156.88ms. 로그인p95 7,742.87ms·로그인 중 제어 요청p95 40.96ms는 일반 요청과 구분한다.
- 격리 서버의 실제 처리 프로세스에2코어·낮은 우선순위·512MiB 상한 적용, 최대RSS **253.65MiB**, 워밍업 후 증가 **0.83%**. 두 구간의 연결100명과 전체 **17개 판정 모두 통과**했다. 혼합 부하의 작업 목록 무쓰기 전용 지표 `null`은 미측정이며 통과로 바꾸지 않는다.
- 루트가 완료 후 PID와 생성 시각으로 시험용 프로세스 잔여0, 시험 포트10011의 LISTEN0을 확인했다. 종료 후 파일1,160개 해시 재대조도 일치했다. 마지막 읽기 전용 독립 대조에서 집계 오류나 빠진 실패 판정이 발견되지 않았다.

원자료: `predeploy-reports/predeploy-20260918-56e53477-r3/`의 `predeploy-latest.json`, `verification.json`, `final-evidence.json`, `backend-results.xml`, `console.log`, `load-100-users-20260918-082245.json`. 앞선 실패·중단 자료는 각각 접미사 없는 폴더와 `-r2` 폴더에 보존했다. 과거 종합 점검의 완료 기록은 소급 변경하지 않았다.

남은 범위는 [Gate 6-B/6-C](RISK_REDUCTION_PLAN_2026-08-15.md)의 운영 DB 세트 백업·non-terminal 요청/작업자 작업 확인·업데이트/복귀 절차 및 실제 왕복 확인이다. 구버전 Comfy 원장 복귀 제약과 위 U2/B2 비용도 유지한다. 이번에는 푸시·병합·태그·릴리즈 제작·NAS 게시·운영 재시작을 실행하지 않았다.

## 종합 점검 진행 이력 (각 기록 당시 상태)

아래 진행 중·미승인·검사 수치는 그 시점의 기록이다. 최신 판정은 위 요약과 종합 점검 장부의 최종 통합 결과를 따른다.

현재 기준은 `dev` = `origin/main` `4df0eb93`이며 `2026.09.17-1900`을 NAS에 게시했다.
새 사용자 요청에 따라 제품 코드 수정 전에 문제를 모으고 Claude와 설계를 합의한다.
첫 12영역 경계 조사 후 계정 고정(B1)은 Claude 승인·77개 재검증을 마쳤다.
이름 통일(B3), Comfy 준비/업데이트 경합(E1), 이름 캐시 복구(M1)도 독립 승인을 받았다.
댓글/재인증 계정 고정(B4/B5), 시각 기반 목록 갱신(B2), 미디어 무결성(D1/D2)은 승인 후 시험 보강을 마쳤다.
카드 빠른 클릭(C1), 제출 후 기록 실패(E4), 원본 열기 오류 안내(C6)도 독립 승인을 받았다.
프런트 1회차 전체 1,602개·빌드·구조 검사를 통과했고, C1 후속 51개 검사도 통과했다.
업데이트 상태 안전 거부(U1)와 계정 전환 중 복원(R1)은 독립 승인을 받았고 관련 215개/188개 검사를 통과했다.
별도 격리 Windows 시험9개도 통과했다. W1/C4/C5도 독립 승인을 받았으며 후속 회귀를 보강했다.
프런트 후속 전체1,727개 통과. 백엔드3,662개+815 subtests 통과/환경 충돌7개/권한 제외1개 후,
환경 충돌의 해당 묶음26개를 설정 분리만으로 재검증해 통과했다. 두 실행을 단일 전체 green으로 표기하지 않는다.
C4 hover/Canvas 전달, C3 색상, E3 삭제 대상 복구는 독립 코드 승인을 받았다. C3 검토 후속을 보강 중이다.
C3 포함 프런트 전체는 175파일/1,781개 통과했다. E3 신규106개·관련441개+6 subtests도 통과했다.
E3 후속은186개 통과·해시 대조 완료. R1/U2 독립 코드 검토도 승인받았다.
U2 후속47개·PS 계약 검사와 제품 해시 대조를 마쳤고 R1 후속333개도 통과·해시 대조 완료했다.
E2/E5 실행 원장은 회수/정리 동시 요청 CAS 보강 뒤352개 관련 backend·39개 frontend 검사와 독립 코드 승인을 마쳤다.
U2는 격리 Windows 시험14개를 마쳤다.
U2 동시 읽기/100회 저장에서 손상 JSON은 없었지만 읽기 실패46회와 63.26초의 저장 지연을 함께 기록했다.
15분/100명 기준선은 170,886건 논리 정상(캐시304 포함), 영역 증거 진척은 47/48(97.9%)다.
R4 실제 Claude가 정상/실패11칸·실측2칸을 추가 승인했다. A05는 새B9·댓글후속 포함 최종전체334PASS/missing0/중복0이며 종결 독립 검토 대기다. A12 단계별실측은 R7승인 완료다. 최신 backend473은 전체검사 전후/현재와 일치하고 frontend553도 현재와 일치한다.
후보2건은 실제 Claude 최소 설계 합의 후 독립 검증을 마쳤다. B9는6FAIL 반례→최소계정pin→신규10PASS·관련98PASS·실제Claude독립승인과 현재해시 일치를 확인했다. F2 결과불명 안내는29FAIL→최소구현→68PASS/실제Claude독립승인 및 전체182파일1909PASS·빌드·구조검사/553해시동일을 마쳤다.
화면 정상/실패8칸을 실제 Claude와 검토해 추가 확인했다. 공유·공간·폴더 설정의 계정 전환3곳은 합성 A/B DB에서 재현했다.
초기22묶음과 후속 이동 안내F2·계정 고정4건 및 B4 댓글후속7반례의 수정·독립 검토·관련 재검증을 마쳤다. 현재 승인수정묶음 완료는27/27(100%)이며 최종전체도 통과했다. 15분부하와 A05실패 증거칸의 종결 검토는 미완료다.
계정3건 신규25개·관련420개 검사와 실제 Claude 승인을 마쳤으며 개별 묶음 완료가 최종 전체 검증이나 운영 안전 보장을 뜻하지 않는다.
E2 별칭/목록 역전 후속 합의·구현 뒤 프런트 전체179파일1,824개·타입 포함 빌드·구조 검사 통과.
C3 후속 독립 승인·제품 무변경 회귀13개 보강 뒤 관련133개와 해시 대조를 완료했다.
순증분 포함 프런트 전체179파일1,837개·타입 포함 빌드·구조 검사 통과(`frontend-final-reviewed`).
F2와E2 후속까지 포함한 최종 프런트는180파일1,864개·타입 포함 빌드·구조 검사 통과,551파일 해시동일(`frontend-final-676928677a104047adc77e04c1fa98f3`).
앞선 백엔드 전체는 E2 보완을 위해63%에서 중단했으며 통과로 세지 않는다. 01:38 시작 재실행은4,120PASS+815subtests/4FAIL/1SKIP으로 종료했고 소스 동일·격리 밖 접근0이다(`whole-backend-dxrehxkd`).
실패4건은 updater 시험의 코드 추출 표식 오류로, Claude 승인 후 표식2줄만 고쳤다. 해당4건 포함 업데이트 관련209PASS 뒤 최종전체에서도 네건 모두 통과했다. updater 제품은 이 보완에서 무변경이다.
초반 실패 분류용28파일은 LOCALAPPDATA 고립을 보강한 동일 가드 아래488개+27 subtests 통과, 제품 해시 변화0이었다.
실제 사이드바의 생성자별 수치/폭200–480px·긴 이름을 합성 브라우저에서 확인했다. Resolve 합성8시나리오와 미디어 실제loopback 전송/정리 실측도 완료했다(실기 검증 아님).
실제 SceneBoard의1/50장 선택·색 변경·이동 위치 저장/재열기를 합성 브라우저에서 확인했다. 생성추적5케이스와결과회수1/200기록실측도통과·정리완료했다. 회수profile-off 6child대조와A03 실제폴더이동12관측도통과·정리완료했다. A06 관리30/300행57관측을마쳤다.
R5 실제Claude는 A03/A08/A11 실측3칸을 승인했다. A11은 합성카드1개·두화면의 실제WS 자동반영과 해제 뒤 갱신중지/정리를 확인했고, 전체앱·인증·장시간메모리까지 검증한 것은 아니다. 관련제품19/6/12해시 재계산 불일치0이다.
A05 인증실측은 첫1회login2+GET30/oracle30PASS·당시18소스해시동일·종료정리 후 실제Claude R6승인을 받았다. ASGI단일합성DB의 순차인증/가시성만 확인했으며 A05실패 최종전체조건은 그대로 남는다. A12는 준비도구 첫실패를 보존한 뒤 원본2시험 PASS·합성복원/실제serve 단계실측·종료정리와 실제Claude R7승인을 마쳤다. 작은 단회측정이며 실NAS·정상종료·RTO 보장은 아니다.
B4댓글후속은 정정BEFORE 7FAIL/11PASS 뒤 네 경로만 기존계정scope로 감쌌다. AFTER112PASS·실제Claude 독립승인·루트473해시/원자료/정리 대조 완료다. 최초시험 입력누락 실패기록도 보존했다. 최종전체 `whole-backend-8lwer3pd`는04:32에4152PASS+815subtests/기존권한1SKIP으로 종료했다.
첫 부하 `whole-load-n5q913yk`는 wrapper 미디어 경로와 원본 시드 위치가 달라 중단·보존했고 제품 판정/PASS로 세지 않는다. 실제Claude 승인 후 검사 설정 한 줄만 교정했다. 새15분 `whole-load-s5vy_dvm`(세션44048)은04:42:59 시작, 첫 관측은 미디어262144B·4xx없음·WS/agent100이다. 아직 진행 중이며 제품/시험은 동결한다.
전체 범위·실측 근거·진행률·검토 상태는 [프로그램 종합 점검 장부](PROGRAM_AUDIT_2026-09-17.md)에 기록한다.
운영 적용·유료 생성·사용자 데이터 수정·재배포는 이번 점검의 자동 단계가 아니다.

직전 릴리즈 작업 당시 기록:
코드 기준은 `dev` `2c24d34c`다. 사용자가 작업 커밋과 점검 통과 후 dev 푸시·main 병합·릴리즈 생성을
승인했다. 병합 전 main 기준은 `6a7430d1`이며 앞서 커밋한 생성 작업/공유 범위 보완도 포함한다.
보류 사용 이후 단순 구버전 복귀는 안전하지 않다. 장애 시 DB·원장을 보존하고 수정판 복구를 우선한다.
서버·설치본 적용과 dev 재시작은 하지 않는다. 새 API는 서버 먼저 업데이트해야 하며 NAS 게시는
사용자 선택을 별도로 확인한다. 아래 세부 항목의 미커밋/검증 수치는 각 작업 완료 당시의 기록이다.
검증 기준 커밋은 `1262b249`이며 이후 변경은 이 결과를 기록하는 문서뿐이다. 제작 버전은
`2026.09.17-1900`(CLI 1.1.25)으로 지정했고 NAS 게시는 아직 승인되지 않았다. 최종 ZIP 검증·게시 여부는
고정 릴리즈 폴더의 `predeploy-reports/release-2026.09.17-1900/REPORT.md`에서 확인한다.

직전 작업(생성자 범위·배치 활동):
기준 `dev` `ccf47d95`에서 설계와 Claude 검토 후 구현했고, 사용자 승인으로 코드 `fa3ac6e1`을 dev에 커밋했다.
푸시·main 병합·배포·운영 서버 적용·설치본 재시작은 하지 않았다.
사용자 승인대로 dev를 재시작하고 e001/c0010 본인 생성물 5개의 작업만 정상 화면에서 복원했다.
직전 공유 자동 필터·생성자 집계는 코드 `23344da9`/문서 `ccf47d95`로 dev에 커밋된 상태다.
아래 이전 항목의 브랜치·배포 상태와 승인은 각 기록 당시 기준이다.

직전 작업 기록(2026-09-17): **오류별 한영 안내·HTTP 지연 로그 구분 최종 검증 완료**. 당시 직전 main은 `39bcdb52`, NAS 배포는 `2026.09.16-1209`(CLI 1.1.25)다. Jay가 그 작업의 dev 커밋·푸시·main 병합과 릴리즈 생성·NAS 게시를 승인했으며 서버 적용은 직접 진행한다. CLI는 1.1.25를 유지한다. 실제 게시 버전은 NAS `latest.json`과 고정 릴리즈 폴더의 `predeploy-reports/release-<버전>/REPORT.md`에서 확인한다.

배포 확인 이력(**2026-09-11 당시**): 코드 기준선 `dev` = `main` = `origin/main` `1f383b84` = 팀 릴리스 **`2026.09.11-1647`**(2026-09-11 16:50 NAS 게시 · Jay 서버 적용) · 롤백 기준 태그 `predeploy-baseline-c9677511`(직전 main `c9677511` = 릴리스 `2026.09.10-1322`). 직전 릴리스 이후 커밋 25개가 실렸다. **배포 후 확인 완료**(가동 17분 · 5xx 0 · p95 14.4ms · SQLite 잠금 0 · 새 라우트·가드 동작 확인) — 실제 브라우저 동작(붙이기 창 두 칸·씬 탭 드래그·테두리)은 사람 확인 대기.

> [!NOTE]
> 이 문서는 긴 기록을 읽기 전에 보는 **한 장짜리 현황판**이다. 날짜별 상세 기록은 아래
> [자세한 기록](#자세한-기록) 의 `status/` 노트로 옮겼다(내용은 그대로 보존).
> 위험 항목의 상태를 바꾸는 단일 기준은
> [RISK_REDUCTION_PLAN_2026-08-15.md](RISK_REDUCTION_PLAN_2026-08-15.md) 의
> `Gate 0 산출물 — 정규화 잔여 목록`이다. 두 문서가 다르면 Gate 0 표를 우선한다.

## 지금 상태

아래는 각 기능 작업 완료 당시 기록이다. 최신 종합 점검·배포 상태는 문서 맨 위의 최신 판정을 우선한다.

- **공유 검토·폴더 UI 병합 게이트 통과(2026-09-17, owner Codex · status 완료).**
  코드 `2c24d34c`(93파일), 설계·현황·워크스페이스 계약은 별도 문서 커밋으로 보관한다.
  정식 사전 게이트는 깨끗한 dev에서 전체 테스트·빌드·격리 SQLite 백업/복원·100명/60초/2회 부하를
  생략 없이 실행한다. 구조 검사와 기존 DB 하위 호환성도 확인한다. 운영 DB/유료 호출은 사용하지 않는다.
  통과 후 dev 푸시와 `HEAD:main` fast-forward, 고정 `D:\ClaudeCode\MV-hub-S-release`의 깨끗한
  main을 원격과 일치시켜 릴리즈를 만든다. ZIP·런타임·금지 파일·SHA256을 검증하고 서버 적용은 별도다.
  첫 전체 실행은 백엔드 3,258개·815 subtests 통과, 1개 제외와 테스트 환경 충돌 1건으로 중단했다.
  외부 복구를 끈 격리 환경에서 ON을 가정한 기존 Comfy 복구 테스트가 실패했으며 실제 호출은 mock이다.
  해당 테스트만 ON 조건을 명시하고 OFF 안전 검사·제품 구현은 유지했다. 독립 검토 승인 및 관련 27개 통과.
  보완은 `1e8a220b`로 커밋했고 `1262b249`의 깨끗한 작업 트리에서 전체 게이트를 다시 통과했다.
  최종 백엔드 **3,259개 +815 subtests 통과, 1개 제외**(Windows 심볼릭 링크 권한, 335.37초),
  프런트 **165파일/1,555개**, 타입 포함 빌드·구조 검사·격리 SQLite 백업/복원 통과.
  2코어·낮은 우선순위·100명·60초×2회 부하: 요청 **22,585건 모두 200**, 5xx·전송·SQLite 잠금 오류 0,
  작업 p95 61.99/62.09ms, 최대 RSS 253,210,624바이트(512MiB 이내), 워밍업 후 메모리 증가 0.26%.
  결과는 dev `predeploy-reports/release-20260917-1262b24/predeploy-latest.json`과 같은 폴더의 로그/부하 JSON에 있다.
  첫 실패 기록도 보존한다. 백업 훈련은 합성 생성물 3건의 임시 DB이며 운영 DB 3종 백업을 대신하지 않는다.
  DB 추가형 마이그레이션·과거 NULL 보존·구서버 응답 보호의 독립 검토에서 차단 결함은 없었다.
  서버 선행 업데이트와 보류 사용 후 복구 제약은 [공유 상태 설계 §10](SHARE_STATE_RECONCILIATION_DESIGN.md#조건부-검토에-대한-codex-보완)에 기록했다.

- **폴더 사이드바 폭 조절(2026-09-17, owner Codex · reviewer Claude · status 완료 · updated 2026-09-17).**
  Claude 설계에 따라 두 사이드바의 공통 껍질만 교체했다. 오른쪽 6px 경계 드래그로 작업 공간·공유 및
  리뷰·캔버스에서 같은 폭을 사용한다. 기본/최소 200px, 최대는 480px와 창 너비 절반 중 작은 값(최소 200px 보장)이다.
  더블클릭/Home은 기본 복원, ←/→는 8px 조절, End는 최대, Esc/포인터 취소/창 이탈은 드래그 취소다.
  로컬 UI 설정 `ch.sidebarWidth`는 완료 때만 저장하며 창 축소는 표시폭만 줄여 선호폭을 보존한다.
  Codex 검토 후 CSS 변수·형제 손잡이·포인터 고정·종료 정리 설계로 구현했다. 드래그 중 전체 화면/폴더
  목록을 다시 렌더하지 않으며 폴더 선택·집계·상단 탭 위치·DB/API는 바꾸지 않는다.
  touched_paths: `components/common/ResizableSidebar.tsx`, `FilterSidebar.tsx`, `sidebar/CanvasFolderSidebar.tsx`,
  `styles/app-shell.css`, `lib/i18n.ts`, `tests/resizableSidebar.test.tsx`, 이 현황 문서(프런트 경로 기준).
  신규 **20개**, 최초 관련 **46개**, 최종 프런트 전체 **165파일/1,555개**, 타입 포함 빌드·구조 검사 통과.
  격리 브라우저의 실제 두 사이드바에서 200→360px 드래그·긴 폴더명·한영 안내·세 탭/재열기/새로고침
  폭 유지·480px 상한·키보드·더블클릭 복원·폴더 내부 스크롤을 확인했다. 운영 데이터 사용 없음.
  Claude 독립 코드 리뷰 승인(정적 검토, 차단 문제 없음). 권고에 따라 조절 중이 아닌 Esc는 기존 취소
  기능으로 전달하도록 보완했고 회귀 테스트를 추가했다. 검증용 임시 화면 3개·탭·5180 서버는 정리했다.
  기존 큰 번들 경고는 유지된다. 설치본·DB·서버 변경·dev 재시작·커밋·배포는 하지 않았다.

- **폴더 마지막 숫자 의미 정정(2026-09-17, owner Codex · 자체 검토 완료).**
  `R1 · 2`는 전체 2개 중 보류 1개다. 마지막 숫자는 최종·보류·일반 공유를 모두 포함한다.
  ★/R은 전체 숫자의 90% 크기·굵기 400, 전체 숫자는 굵기 700이며 한영 설명도 전체로 맞췄다.
  생성자 선택·하위 폴더 누적·DB/API는 유지한다. 프런트 **164파일/1,535개**, 집중 **52개**,
  타입 포함 빌드·구조 검사 통과. 이번 변경의 실제 앱 육안 확인·재시작·커밋·배포는 하지 않았다.
  상세: [설계 §11 후속 정정](SHARE_STATE_RECONCILIATION_DESIGN.md#후속-정정--마지막-숫자는-전체-수량-2026-09-17).

- **생성자 선택과 폴더 수량 연동(2026-09-17, owner Codex · reviewer Claude · status 완료 — 서버 적용/재시작 보류).**
  생성자 선택 시 폴더·최종/보류/공유·프로젝트·미분류·신규 +N을 같은 사람 기준으로 표시한다.
  선택 해제는 기존 전체 수량, 선택 생성자의 작업이 없는 폴더는 `-`다. +N의 모두 확인도 같은 범위다.
  서버 단일 집계에 루트/미분류를 포함하고 기존 권한에 AND로 적용한다. DB 스키마 변경 없음.
  구서버·불완전 응답은 전체값/0 대신 숫자를 숨기고 안내한다. 늦은 이전 생성자 응답도 폐기한다.
  프런트 **164파일/1,531개**, 관련 백엔드 **86개**, 타입 포함 빌드·구조 검사 통과. Claude 독립 리뷰 승인.
  격리 브라우저에서 실제 생성자 A/B 클릭·해제·폴더/프로젝트/미분류 전환과 한영 구서버 안내 확인.
  운영 앱 확인·서버 적용·재시작·커밋·배포 없음. 기존 공간/기타 필터 범위는 유지한다.
  상세: [설계 §12](SHARE_STATE_RECONCILIATION_DESIGN.md#12-생성자-선택과-폴더-수량-연동-2026-09-17).

- **공유 검토 보류·카드 메뉴(2026-09-17, owner Codex · reviewer Claude · status 완료 — dev 재시작/실화면 확인 보류).**
  공유/보류/최종 카드에서 현재 상태를 제외한 3개 전환을 제공하고 각각 Yes/No로 확인한다.
  보류는 흰 S·빨강 `#c9384a`, 상단 S 우클릭은 반대 색 S 하나를 바로 아래에 표시한다.
  보류 등급 ↓는 일반, ↑는 공유. 관리 보드는 생성→보류→공유→완료이며 테이블/캘린더도 연동한다.
  메뉴는 크기 상한 없이 카드 안쪽을 3등분하고 짧은 변에 맞춰 글자·아이콘을 확대한다.
  Yes/No는 동일 폭·높이 28px다. 우측 설명 제거·SVG 별 중앙 정렬을 포함한다.
  바깥 색 면과 안쪽 단일 clip을 분리해 모서리를 정리했고 선택 전후 배치·포커스/Resolve 강조를 유지한다.
  확인 질문·취소·중복 클릭 방지는 공통 UI로 묶고 단건/등급/결과 알림 번역을 보완했다.
  서버의 공유/최종/보류 세 축을 원자 반영하고 로컬은 관측 기반 CAS 미러로 맞춘다.
  Claude 독립 리뷰 후 응답 필드 누락·서버 관측 불일치의 거짓 성공을 차단하고 한정 재승인받았다.
  백엔드 전체 **3,207개 +815 subtests 통과·1개 제외**, 이후 최종 보완 집중 **102개 통과**.
  프런트 전체 **162파일/1,492개**, 타입·빌드·구조 검사 통과. 이후 긴 리스트 보완 집중 **64개 통과**.
  격리 브라우저 24개 크기/배율/레이아웃 조합에서 선택 전후 배치가 같고 3배 확대 모서리도 확인했다.
  상태 메뉴 6조합·한영 단건/등급 확인을 검사했고 공통 UI는 Claude 독립 리뷰를 통과했다.
  서버 먼저 업데이트해야 하며 기존 폴더 메뉴 미커밋은 보존했다. 재시작·실제 상태 변경·커밋·푸시·배포 없음.
  상세 계약·검증·touched_paths: [공유 상태 설계 §10](SHARE_STATE_RECONCILIATION_DESIGN.md#10-공유-검토-보류-확장-2026-09-17).

- **공유 폴더 상태별 개수(2026-09-17, owner Codex · reviewer Claude · status 완료 — 서버 적용/재시작 보류).**
  폴더 행에 `★1 · R2 · 5`(최종·보류·전체, 앞 두 값은 전체에 포함)를 표시한다. 최종/보류 0건은 생략한다.
  총계와 상태별 수량은 동일 서버 조회에서 집계하고 하위 폴더를 누적한다. DB 스키마 추가 없음.
  작업 공간/캔버스와 프로젝트 전체 합계는 유지하며 구서버·상세 불일치는 `전체 N / Total N`으로 표시한다.
  계정/워크스페이스/탭 변경 중 이전 수량은 숨기고 늦은 응답을 폐기한다. Claude 독립 리뷰 승인.
  최신 프런트 **163파일/1,510개**, 관련 백엔드 **90개**, 타입 포함 빌드·구조 검사 통과.
  격리 브라우저 200/160px 폴더 목록·한영·수량 변경·구서버/작업 공간 표시 확인. 운영 데이터 사용 없음.
  실제 사용은 서버 먼저 업데이트해야 한다. 재시작·커밋·배포 없음. [설계 §11](SHARE_STATE_RECONCILIATION_DESIGN.md#11-공유-폴더-상태별-개수-2026-09-17).

- **폴더 우클릭 메뉴(2026-09-17, owner Codex · reviewer Claude · status 완료 — dev 재시작 보류).**
  공유&리뷰 폴더의 저장 버튼·확인·결과 안내를 ‘골드만 저장’으로 통일했다. 기존 최종본 판정·저장 API는 유지한다.
  ‘원본 위치 열기’는 메뉴 우상단의 작은 폴더 아이콘이며 작업 공간·공유&리뷰·캔버스에 공통 제공한다.
  본인 PC의 연결된 Render 하위 폴더만 탐색기로 연다. 경로 없음/연결 끊김은 안내하고,
  새 폴더 생성·원본 다운로드·DB 변경·변경 알림은 없다. 로컬 브라우저 문맥·공유 서버 거부·프로젝트 읽기 권한,
  계정 쌍 고정·상대경로/링크 탈출 검사 적용. 원격 호스트 접속에는 버튼을 숨긴다.
  Claude 설계와 Codex 보완 합의 후 구현. 백엔드 관련 **94개**, 최종 UI 변경 후 프런트 전체
  **156파일/1,398개**, 집중 27개·빌드·구조 검사 통과. Claude 독립 코드 리뷰의 P2(탐색기 실행 파일 부재를
  폴더 부재로 오인하는 오류 분류)를 수정하고 회귀 검사를 추가했다. 수정 코드의 한정 재검토도 승인받았다.
  실제 dev Chrome에서 세 탭 모두 우상단 아이콘(24px, 위/오른쪽 여백 약 7px)을 확인했다.
  OS 실행은 모킹으로 검사했으며 실제 탐색기 실행·dev 재시작은 보류했다. 검증 탭 정리 완료.
  커밋·푸시·배포 없음.
  touched_paths: `project_folders.py`, `routers/manage.py`, `mutation_notify.py`, `App.tsx`,
  `ProjectSection.tsx`, `FilterSidebar.tsx`, `folderContextMenu.ts`, `projectApi.ts`, 사이드바 CSS,
  관련 테스트와 현황/워크스페이스 계약.

- **작업 공간 집계·배치 활동 후속(2026-09-17, owner Codex · reviewer Claude · status 완료).**
  작업 공간 생성자는 실제 본인/공간/프로젝트/폴더 범위로 집계하고 타인 멤버 명단을 섞지 않는다.
  두 탭 휴지통은 일반 집계에서 제외한다. 새 로컬 API 범위 확인값이 없으면 옛 숫자를 숨긴다.
  콘텐츠/관리 팩트에 nullable `task_activity_at`을 추가하고 실제 배치·명시 재담기만 기록한다.
  원래 생성일·PM 편집·작업 ID는 보존하며 자동 동기화/조회는 보관 작업을 되살리지 않는다.
  기존 행의 일괄 시각 보정/복원은 하지 않는다. 새 원본 미디어 저장·새 DB·유료 호출 없음.
  Claude 독립 리뷰의 AUTH off UID 누락·부분 조회 보관 복원·unknown 공간 보충 오인식을 수정하고 재승인받았다.
  프런트 **155파일/1,393개**, 백엔드 **3,074개 +813 subtests 통과·1개 제외**, 타입·빌드·구조 검사 통과.
  dev 실측: 해당 폴더 카드 5개/제이 5 일치, 같은 폴더 재담기로 기존 작업 1개가 생성 상태로 복원됨.
  나머지 작업 61개·원래 생성일·PM 편집값은 보존했다. [작업 표시 계약](TASK_ACTIVITY_FACTS.md)에 근거를 기록했다.

- **작업 탭 미공유 생성 활동(2026-09-17, owner Codex · reviewer Claude · status 완료 · updated 2026-09-17).**
  기존 관리 DB의 생성 메타로 폴더 작업·생성 상태·건수·크레딧·시간을 보강하고 공유 콘텐츠와 중복 제거한다.
  본인 미공유는 로컬 전용 API로 썸네일을 보강하며 타인 미공유는 기존 관리 권한 내 메타만 표시한다.
  당시 서버 미디어/프롬프트 추가 저장과 새 DB·스키마 변경은 없었다(배치 시각은 위 후속 보완).
  개인/미상 공간은 팀 작업에 추측 합류하지 않는다.
  계정·권한·공간별 캐시와 늦은 응답 폐기, 공유 서버 직접 미리보기 차단을 적용했다.
  PM 일정·메모·배정·수동 링크와 완료본 저장 판정은 보존했다. 보관된 타인 미공유 자동행의
  이름 노출과 안정된 보관 행의 반복 GET 쓰기도 보완했다.
  프런트 전체 **154파일/1,378개**, 타입·빌드·아키텍처 통과. 격리 실제 브라우저에서
  테이블/보드/캘린더·본인 이미지/타인 메타·내 작업 필터·공간 전환 확인. 모의 데이터이며 운영 실측이 아니다.
  백엔드 최종 전체 **2,915개 +810 subtests 통과·1개 제외**(Windows 심볼릭 링크 권한, 392.94초).
  최종 보완 집중 111개 +14 subtests, 추가 프록시 실패 회귀 포함 HTTP/로컬 22개 +13 subtests 통과.
  초기 전체 검사 환경의 임시 DB 경로 충돌 9건은 올바른 격리 초기화로 재검증해 해소했다.
  Claude 최종 리뷰에서 발견된 단독 로컬·개인 썸네일 회귀는 공식 실행 모드 분리로 복원했고 재승인받았다.
  실제 공유 서버만 새 메타 병합, 단독/격리 dev는 루프백 검사 후 기존 콘텐츠 표시를 유지한다.
  새 팀 서버 메타 계약은 서버 모드가 명시된 격리 fixture로 검사했다. 임시 화면·탭·5181 서버 정리 완료.
  상세 합의·권한·검증·제한과 touched_paths는 [TASK_ACTIVITY_FACTS.md](TASK_ACTIVITY_FACTS.md).
  과거 미보고 기록 자동 복구는 포함하지 않으며 클라이언트·서버 업데이트가 함께 필요하다.

- **공유 생성자 집계 연동(2026-09-17, owner Codex · reviewer Claude · status 완료 · updated 2026-09-17).**
  공유 카드와 같은 유효 워크스페이스·프로젝트·조회 권한으로 생성자와 전체 건수를 집계한다.
  개인은 권한 내 모든 개인 공유물, 미분류는 프로젝트 미지정, 기본 라이브러리는 보관 프로젝트 제외다.
  당시 내 작업 멤버 합집합/0건 행은 보존했으나 위 후속 보완에서 실제 본인 범위로 변경했다.
  공유 생성자 자신·검색·색·태그·폴더·휴지통은 집계 조건 밖이다.
  구서버의 범위 무시를 막기 위해 scoped 응답 확인값을 요구하고 누락 시 409 업데이트 안내한다.
  공간/계정/준비 상태 키와 요청 순서로 옛 행·역순 응답을 차단하고 변경 알림으로 갱신한다.
  공간 전환/자동↔전체 전환에 생성자 선택 해제, 성공 집계에서 사라진 생성자만 선택 해제한다.
  백엔드 신규 80개 포함 집중 **108개 통과**, 전체 **2,854개 +800 subtests 통과·1개 제외**(364.52초).
  제외는 기존 Windows 심볼릭링크 권한 항목이며 임시 데이터 경로만 사용했다.
  Claude 독립 리뷰는 차단 결함 없이 승인했다.
  비차단 지적이던 휴지통에서 일반 공유물 집계로 생성자 선택이 풀리는 예외도 보완했다.
  공유 휴지통은 생성자 집계 무조회/건수 숨김, 기존 선택 보존/명시 해제 버튼을 제공한다.
  프런트 집중 **29개 통과**(휴지통 회귀 2개 추가), 최종 전체 **150파일/1,351개 통과**.
  타입·빌드·아키텍처·공백 검사 통과(기존 500kB 번들 경고 유지), Claude 휴지통 보완 재검토 승인.
  격리 실제 브라우저에서 팀 A Jay2/B River1/개인 Jay1+River1/전체 Jay3+River2, 변경 알림
  집계 갱신·선택 해제·느린 이전 응답 폐기·미준비 무조회·409 한영 안내와 회복을 확인했다.
  50ms/1초 모의 응답 검증이며 운영 성능 실측이 아니다. 외부 API 시도 0, 임시 파일/탭/5178 서버 정리 완료.
  기존 팀 유휴 15초 알림에 집계 요청 1회가 추가되므로 다수 클라이언트 운영 부하는 별도 관찰한다.
  단순 10초 skip은 실제 공유 변경 직후 갱신을 놓칠 수 있어 적용하지 않았다. 새 타이머/카드 수집은 없다.
  내 작업 탭 전환에서 이전 생성자 선택이 남는 기존 동작은 이번 공유 범위 변경에 포함하지 않았다.
  비차단 관찰: 휴지통→동일 일반 범위 복귀 시 새 요청 effect 전 한 프레임 동안 이전 집계가 보일 수 있다.
  공간/계정이 다른 행은 표시하지 않으며 복귀 즉시 다시 조회한다. 운영 데이터 왕복/성능 보장은 아니다.
  DB 구조·원본·소유자·과금·미디어 저장 정책 변경 없음. 서버와 클라이언트 업데이트가 함께 필요하다.
  touched_paths: `backend/app/repo/identity.py`, `backend/app/routers/generation.py`,
  `backend/tests/test_creator_workspace_scope.py`, `frontend/src/lib/useLibraryCreators.ts`,
  `frontend/src/lib/projectApi.ts`, `frontend/src/components/sidebar/CreatorSection.tsx`,
  `frontend/src/components/FilterSidebar.tsx`, `frontend/src/App.tsx`, 필터/공간이동 훅·i18n·생성자 CSS,
  생성자/공간 회귀 테스트·이 문서·[워크스페이스 계약](WORKSPACE_DATA_CONTRACT.md#ui-의미현행-구현).

- **공유&리뷰 워크스페이스 자동 필터(2026-09-17, owner Codex · reviewer Claude/독립 프런트 검토 · status 완료).**
  상단에서 선택한 팀 ID 또는 personal 범위를 공유 목록에 자동 적용한다. 개인은 Jay 확인대로
  현재 조회 권한이 있는 모든 개인 공유물을 포함한다. `W 이름 · 자동` 메뉴의 전체 보기는 임시이며
  공간 변경·재시작 후 자동으로 복귀한다. 전역 태그 영역의 수동 W 칩은 공유 탭에서 숨기되 보존한다.
  과거 unknown은 자동 제외·전체 보기로 확인하며 공간 미확인은 안내/명시 전체 보기만 제공한다.
  생성 목적지·무장 태그·과금·일반 전역 태그·원본 데이터·기존 권한은 변경하지 않았다.
  이전 폴더·카드 선택·미리보기 히스토리와 늦은 목록/추가 페이지/Resolve 응답을 무효화한다.
  Resolve 복수 선택도 필터 후 폴더를 결정하며 구서버의 적용 확인 누락은 409 안내한다.
  독립 검토에서 시작 시 이전 폴더 복원/뒤로가기 미리보기 재등장/GET 공백 ID 불일치를 보완했다.
  최종 리뷰에서 휴지통 필터 후 표시 건수를 offset으로 쓰는 반복 조회를 발견해 원본 수신 건수로
  변경했고 탭 캐시에도 보존한다. 추가 조회 실패는 자동 당김을 멈추고 새로고침을 안내한다.
  빈 목록은 회색 필터 OFF에서도 자동 최대10회 조회하고 이후 더 보기로 이어간다.
  실제 App+목록 훅 결합 검사에서 원본 offset 0/200/400→일치 결과, 상한 후 정지·더 보기 클릭도 검증했다.
  프런트 전체 **148파일/1,320개 통과**, 구조·타입·빌드·공백 검사 통과(기존 큰 번들 경고 유지).
  백엔드 전체 **2,774개 +800 subtests 통과·1개 제외**(343.24초). 제외는 기존 Windows 심볼릭링크
  생성 권한 부족이며, 기존 테스트 문자열 escape 경고 1개만 남는다. 임시 DB만 사용했다.
  격리 실제 브라우저에서 팀 A2/B1/개인2/전체6, 공간 변경 자동 복귀, 미확인 보류와 전체 보기,
  다른 공간 Resolve 무동작·혼합 선택의 현재 공간만 강조, 한영/480·1100px/긴 이름 배치를 확인했다.
  요청은 80ms 모의 응답이며 실제 운영 성능 실측이 아니다. 미디어 다운로드·실제 API 시도 0건이다.
  실측 도중 임시 페이지 HMR의 createRoot 중복 경고가 1회 있었고 제품 런타임 오류로 판정하지 않았다.
  임시 페이지 2개·검증 탭·5178 검증 서버는 종료/제거했다. Claude는 백엔드·프런트·휴지통 보완
  최종 재검토를 승인했으며 남은 차단 결함은 없다. 운영 실데이터 왕복/성능 검증을 뜻하지 않는다.
  DB 스키마·마이그레이션·CLI 변경 없음. 당시 사이드바 집계는 범위 밖이었으나 생성자는 위 후속 변경에 포함한다.
  프로젝트/폴더 집계와 기존 수동 다중 W 필터의 약12,000행 제한은 여전히 범위 밖이다.
  배포 시 공유 서버와 클라이언트를 함께 업데이트해야 자동 범위의 Resolve 연동을 사용할 수 있다.
  touched_paths: `backend/app/repo/generations_query.py`, `backend/app/routers/library.py`,
  `backend/app/usecases/generation_locate.py`, `frontend/src/App.tsx`, 조회/필터/Resolve/미리보기/태그 훅,
  툴바 워크스페이스/그리드 빈 상태 컴포넌트·i18n·타입·CSS·관련 테스트·이 문서·아래 계약.
  상세 계약: [WORKSPACE_DATA_CONTRACT.md](WORKSPACE_DATA_CONTRACT.md#ui-의미현행-구현).

- **오류 안내·서버 로그 최종 점검(2026-09-17, owner Codex · reviewer Claude Opus/독립 코드·배포 검토 · status 완료).**
  크레딧 한도/잔액·인증·권한·입력·요청 제한·서비스·통신 오류를 카드/목록/캔버스/정보창/콘솔에
  한영으로 구분한다. 제출 불명확 작업의 자동 재실행 차단·명시적 미제출 확인 조건은 유지한다.
  서버 콘솔은 정상 HTTP 지연과 요청/서버 오류를 구분하고 알려진 DB 백업 업로드만 작업명을 표시한다.
  기존 dev 커밋 `7ce4732d`의 프로젝트 폴더 자동 펼침 제거도 포함해 독립 검토했으며 차단 결함은 없었다.
  전체 백엔드 **2,747개 + 800 subtests 통과·1개 제외**(334.71초), 프런트 **145파일/1,285개 통과**,
  구조/타입/빌드/공백 검사와 비용 없는 제출 중단·재시작 드릴 통과. 제외는 Windows 심볼릭 링크 권한이다.
  격리 실제 브라우저에서 오류 9종+미분류의 한영 카드, 목록, 상세 원인/조치, 원문 접기/펼치기를 확인했다.
  DB 스키마·API·의존성·CLI pin 변경 없음. 운영 DB/설치본/공유 서버·유료 생성은 건드리지 않았다.
  검사 준비 중 전역 DB/복구 설정 충돌 및 빈 임시 DB 미초기화를 바로잡은 뒤 전체를 다시 통과했다.
  격리 준비도 독립 승인받았다. 실앱 격리 기동 시 동기화 차단 이름은 `CONTENT_HUB_SERVER_SYNC=0`이다.
  Gate 6-A 확인이며 서버 설치/운영 왕복 Gate 6-B/C 완료를 뜻하지 않는다. 기존 경로 escape·번들 크기 경고 유지.
  상세 계약: [GENERATION_SUBMISSION_RECOVERY.md](GENERATION_SUBMISSION_RECOVERY.md#10-오류-원인-안내와-복구-판단의-분리-2026-09-16).
  touched_paths: `agent_push.py`, 오류 표시 컴포넌트·공통 분류/i18n/CSS·테스트, `tools/log_viewer.py`, 관련 테스트·이 문서·복구 계약.

- **CLI 1.1.25 전환 검증(2026-09-16, owner Codex · reviewer Claude Opus · status 완료).**
  제품 변경은 `hf_cli_version.txt` 한 줄이다. 별도 prefix의 후보 실제 버전 1.1.25(`b56caad0`)를
  확인했고 전역 CLI·실사용 설치본은 유지했다. 무료 스모크 34개와 모델 8종/거래/조회 등 추가 29개 통과.
  모의 gateway 재지정은 두 CLI에서 모두 적용되지 않아 안전 중단했다. 이후 Jay의 별도 승인으로
  R&D에서 최대 1크레딧 테스트 이미지 1장을 생성해 정상 완료, R&D만 정확히 -1크레딧·거래 1건,
  다른 공간 변화 0과 전역 선택 보존을 확인했다. 실제 생성 요청의 config/env 우선순위를 검증했으며
  영상 실제 생성까지 검증했다고 주장하지 않는다. 전체 백엔드 2,660개+800 subtests·프런트 1,207개,
  구조/타입/빌드 통과(Windows 권한 제외 1개, 기존 경고 유지). Claude 최종 리뷰는 차단 결함 0건이며,
  고정 폴더 clean/원격 일치·완성 ZIP 실제 CLI 버전·해시 검증 후 latest 마지막 게시를 조건으로 승인했다.
  서버와 PC의 CLI pin 불일치 시 에이전트 시작 때 생성이 꺼질 수 있으므로 양쪽 업데이트·재시작이 필요하다.
  근거·절차·복귀 기준은 [HF_CLI_UPGRADE.md](HF_CLI_UPGRADE.md).
  touched_paths: `hf_cli_version.txt`, `docs/HF_CLI_UPGRADE.md`, `docs/CURRENT_STATUS.md`.

- **기능별 커밋·병합 전 최종 검사(2026-09-16, owner Codex · reviewer Claude Opus/독립 UI·호환성 검토 · status 완료).**
  남아 있던 변경 14개를 검토하여 코드와 문서를 분리했다. 코드 커밋은 `4dd15021`(Windows 경로 안전 비교·
  대상 잠금 통일), `54da4d03`(선택 메뉴 글자 줄바꿈 방지), `1dfacaad`(전송과 수동 업데이트 동시 실행 보호)다.
  Claude Opus가 현재 경로·부모/자식 핸들·업데이터를 다시 독립 검토해 **차단 결함 0건·승인**했다.
  UI·main 대비 DB/HTTP 하위 호환성 독립 검토에서도 새 차단 결함은 없었다. 추가 제품 코드 수정은 필요하지 않았다.
  집중 회귀 **148개 통과·1개 제외**, 격리 전체 백엔드 **2,660개 통과·1개 제외 + 800 subtests 통과**
  (296.52초), 프런트 **143파일/1,207개 통과**, 구조 검사·타입 검사·빌드 통과다.
  제외 1개는 Windows 심볼릭 링크 생성 권한 부족이며 실제 junction 이탈 검사는 통과했다.
  기존 React act 경고와 큰 번들 경고는 남지만 검사 실패는 없다. 운영 DB·설치본·실제 서버에는 쓰지 않았다.
  실제 컴포넌트/CSS를 렌더한 임시 브라우저 검사에서 내부 폭 356/596/896/1196px × 작업 공간/공유/혼합 선택
  **12조합 모두 글자 줄바꿈·잘림 0건**이었다. 재시도·복구·영구삭제 표시도 포함했고 임시 파일과 창은 정리했다.
  최신 원격 조회 기준 `origin/main`은 `56f1cabd`이며 코드 HEAD까지 58커밋 뒤다. DB 스키마·마이그레이션·
  의존성 정의 변경이 없고 기존 단수 workspace 요청·옛 에이전트의 본문 없는 요청도 유지한다.
  Gate 6-A 전체 검사·하위 호환성 조건을 확인했으며 병합 대상은 이 코드와 별도 문서 커밋을 포함한 dev HEAD다.
  운영 배포 Gate 6-B/C 완료를 뜻하지 않는다. **공유 서버도 업데이트해야 공유&리뷰 선택 따라가기가 작동**하고,
  전송 보호의 **최초 적용은 전송 완료 → 옛 MV Hub 완전 종료 → 수동 업데이트**가 필수다.
  멈춘 Resolve 가져오기 자식이 있으면 강제 업데이트도 OS 보호를 우회하지 않는다. 해당 자식의 작업·복구 상태를
  먼저 확인해야 하며 자동 종료하지 않는다. 실제 릴리스 설치 왕복과 메뉴 Importer 동시 반입은 별도 운영 확인 대상이다.
  touched_paths: 위 코드/테스트 12개, `docs/ARCHITECTURE.md`, `docs/CURRENT_STATUS.md`.

- **Resolve 선택 표시 지연 최적화(2026-09-16, owner Codex · reviewer Claude Opus · status 완료).**
  사용자 승인으로 빨간 테두리/글로우를 즉시 적용·해제하는 독립 레이어로 분리하고 일반 라임 선택은 유지했다.
  감지 기본/최소 대기 간격은 100→50ms로 줄였고 연결 없음 500ms 백오프·200개 상한·변경 시만 상세 조회는 유지한다.
  같은 폴더의 이미 표시된 대상에 한해 신선한 정상 페이지를 재사용한다. 최초 서버 권한 locate와 모든 정상 갱신의
  공유/삭제/이동 재검증은 생략하지 않는다. 캐시 15초 수명·커서·hasMore를 보존하고 필터/탭 변경·대기 응답을 방어한다.
  Claude Opus 설계·독립 코드 리뷰 승인(차단 이슈 없음). 리뷰 권고로 실제 부모 강조 셀의 양성 단언도 추가했다.
  프런트 전체 **143파일/1,207개 통과**, 관련 UI 재실행 15개·구조 검사·빌드 통과(기존 act/큰 번들 경고 유지).
  백엔드 감시/매핑/locate 관련 **80개 + 13 subtests 통과**. DB 스키마·인증 정책·원본 미디어 보존 변경 없음.
  브라우저 CSS 실측은 빨간 적용/이동/해제의 transition 0s, 다음 프레임 9.1~12.1ms였고 직접 선택 라임 140ms는 유지됐다.
  사용자 요청으로 공식 Resolve `SetSelectedClip`을 통해 현재 폴더의 연결 클립을 10회 직접 선택했다(성공 10/10).
  실제 dev는 대상 일치 10/10, 수신→DOM p50 23.95ms/최대 30.10ms, 빨간 레이어 즉시 적용 10/10이었다.
  연속 선택의 locate 1회·목록 0회 경로도 관측했다. 단, 다른 창 뒤 브라우저의 다음 프레임은 124.2~994.5ms로
  늦었다. 이전 실측과 DOM 시간은 거의 같으며, 제거한 것은 140ms 효과 대기와 불필요한 후속 요청이지 무지연 보장은 아니다.
  공식 선택 API 자체는 223~399ms가 걸려 물리 클릭→화면 지연과 동일시하지 않는다. Windows 캡처 오류
  `0x80004002`로 직접 마우스/복수 선택은 수행하지 못했다(복수·해제 동작은 회귀 테스트로 확인).
  실제 1개 선택 상태의 50ms 감시 두 구간은 초당 17.87~18.86회, SDK 조회 p50 1.64~2.60ms/
  p95 4.17~11.02ms였다. 한 번 163.26ms 지연도 관측되어 상한 지연이 사라졌다고 보지 않는다.
  API 대기 비중 4.72~9.83%는 CPU 사용률이 아니다. 200개 대량 선택·재생 중 부하는 별도 검증 대상이다.
  API는 단일 선택만 지원한다. 초기 선택 0개에서 시험 후 마지막 시험 클립 1개가 선택되어 있으며 프로젝트 내용은 수정하지 않았다.
  dev 감시 자식만 새 50ms 코드로 재시작했다. 코드·회귀 테스트 6개 파일은 `cb26cc89`로 커밋하여
  `origin/dev`에 푸시했다(09-16). 푸시 직전 프런트 관련 48개·백엔드 80개 + 13 subtests 재실행도 통과했다.
  main·공유 서버·실사용 설치본·릴리스에는 미반영이다. 별도 전송 보호·선택 메뉴 레이아웃 작업은 이 커밋에 포함하지 않았다.
  touched_paths: `resolve_selection_worker.py`, `useGenerationLibraryData.ts`, `resolve-library.css`, 관련 테스트 3개·현행 구조/현황 문서.

- **Resolve 탭별 위치 따라가기(2026-09-16, owner Codex · reviewer Claude Opus · status 완료).**
  사용자가 필터 자동 해제를 승인했다. 캔버스는 기존 결과 창을 유지하고, 작업 공간·공유 & 리뷰는
  현재 탭 안에서 실제 프로젝트/폴더로 이동한다. 공유 탭은 서버가 현재 공유·접근 권한을 확인한
  항목만 처리하며, 비공유·삭제·권한 없음·조회 실패는 이동/필터 해제/알림 없이 강조만 지운다.
  읽기 전용 `generations/locate`로 대상 메타만 조회하고, 첫 페이지 밖 대상은 정렬을 유지해 합치되
  페이지 커서는 일반 목록만 따른다. 표시 필터와 생성 목적지/태그를 분리해 자동 이동이 다음 생성 설정을
  바꾸지 않는다. Claude Opus 설계·독립 코드 리뷰 **승인**(09-16). 리뷰에서 찾은 조회 중 탭 복귀의
  캐시 혼입을 수정 전 실패/수정 후 통과 회귀로 고정했고, 재리뷰에서 승인 조건 충족을 확인했다.
  프런트 전체 **143파일/1,196개 통과**, 구조 검사·빌드 통과(기존 큰 번들 경고 유지).
  실제 브라우저의 격리 화면에서 200개 정상 페이지 뒤 대상 1개 주입(총 201개), 정상 페이지 조회 1회만
  추가된 자동 스크롤, 빨간 글로우/직접 선택 라임, 공유 탭 유지/비공유 무반응, 생성 위치·태그 보존을
  확인했다. 시험 중 쓰기 요청 0건이며 임시 화면은 제거했다. 실제 Resolve 프로젝트 조작·운영 왕복은 하지 않았다.
  백엔드 관련 155개(신규 36개 포함) 및 전체 **2,660개 통과·1개 제외 + 799 subtests 통과**(308.18초).
  첫 전체 실행은 장기 대기로 시험 프로세스만 종료했다. 상세 진단을 켠 격리 재실행은 전부 통과했고,
  첫 대기의 정확한 원인은 확정하지 않았다(제품 결함을 고쳤다고 주장하지 않음).
  touched_paths: backend library router/query/locate usecase/mutation_notify, frontend App/API/library hooks,
  ThumbnailGrid/GenerationCard/FilterSidebar/ProjectSection/FolderTreeView, 관련 테스트·스타일·본 문서.
  DB 스키마·원본 미디어 다운로드·실사용 서버·릴리스에는 변경하지 않았다. 코드 dev 커밋 `8a3b451f`,
  문서 `27e89cab`은 최적화 `cb26cc89`와 함께 09-16 `origin/dev`에 푸시했다.
  사용자가 작업 없음과 재시작을 승인하여 dev만 재시작했다. 8012 health `ok`, OpenAPI의 locate POST
  등록, 5173 프런트 HTTP 200을 확인했다. 실사용 설치본·Resolve·공유 서버는 재시작하지 않았다.
  실제 공유 서버에서도 쓰려면 서버가 locate 지원 버전이어야 한다(구서버는 화면 유지).
  커밋 후 관련 프런트 77개·백엔드 36개를 다시 실행해 모두 통과했다. 실제 Resolve 선택 → dev 화면의
  첫 7회에서 수신→DOM 강조 22.7~28.1ms, 다음 프레임 106.7~983.9ms였다. 보이는 창에서 재측정한 단일 선택
  14회는 수신→DOM p50 23.95ms/최대 32.5ms, 다음 프레임 p50 28.15ms/최대 37ms였다(클릭 시점은 미포함).
  창 가림·다중 MV Hub 창 활성 상태에 따라 화면 프레임이 늦어질 수 있으며, 무지연으로 검증됐다고 보지 않는다.
  Resolve 현재 4개 선택 조회 100회는 p50 2.37ms/p95 4.33ms/최대 268.3ms, 전체 메타 재조회 20회는
  p50 8.93ms/p95 12.03ms/최대 12.56ms였다. 당시 제시한 후속 최적화 권장안은 다음과 같다(현재 구현 결과는 위 항목).
  ① Resolve 강조 적용/해제의 테두리·글로우 transition 제거(현재 실제 computed style 140ms, 안쪽 링은 즉시)
  ② 기본 100ms 감지 간격 축소와 부하 재측정 ③ 선택 직후 중복 locate/목록 재조회 축소다. 후속 locate는
  첫 강조 뒤 재검증이므로 그 시간을 최초 표시 지연에서 그대로 빼서는 안 된다. 공유/삭제 권한 재검증은 유지한다.

- **Resolve 동시 전송 경로·수동 업데이트 보호(2026-09-16, owner Codex · reviewer Claude Opus · status 완료).**
  Claude Opus 설계 검토를 반영해 알려진 Windows extended 경로 표기를 비교할 때만 통일하고 실제 IO 경로는
  유지했다. 최종 코드 동시 복사 1,000회/출력 2,000개에서 오류 0·원문 바이트 일치를 확인했다(20.391초, 임시 로컬 폴더).
  Windows 릴리스의 부모·가져오기 자식은 별도 공유 핸들로 전송 전체를 보호하고, 업데이터는 같은 파일을
  배타적으로 열어 동시 시작을 막는다. 기존 업데이트 중복 17과 전송 busy 18을 분리하며, 빈 파일 잔재는 차단하지 않는다.
  Claude Opus 독립 코드 리뷰는 실제 차단 결함 0건·회귀 보강 조건부 승인이다. 지적된 extended `..` 이탈은
  번들 Python 3.14.3에서 실제 재현되지 않았지만, 해석 결과에 `..`가 남으면 거부하는 방어와 실제/모의
  회귀를 추가했다(관련 81개 통과·심볼릭 링크 권한 부족 1개 제외, 실제 junction 이탈 검사는 통과).
  전송·업데이트 관련 95개도 통과했다(실제 시스템 Python 3.11/3.14 자식, 비상속, 취소, 최초 busy/재시도 포함).
  따라서 리뷰의 회귀 보강 조건을 충족했다. 최종 백엔드 **2,624개 통과·1개 제외 + 799 subtests 통과**
  (303.52초), 프런트 139파일/1,148개·구조 검사·빌드 통과(기존 큰 번들 경고 유지).
  초반 전체 검사에서 테스트 실행 환경 문제가 있었다. 임시 `CONTENT_HUB_DATA` 아래 `db` 폴더를 준비하고
  `CONTENT_HUB_DB` 개별 덮어쓰기는 해제해야 기존 서버 이전/미디어 캐시 테스트의 경로 전제와 맞는다.
  환경 수정 뒤 해당 31개와 최종 전체 검사가 통과했다. 이 때문에 제품 DB/미디어 코드는 변경하지 않았다.
  실사용 Resolve/설치본/NAS는 변경하지 않았다. 실제 Resolve 전송·실제 패키지 설치 왕복은 별도 확인 대상이다.
  최초 적용은 전송을 모두 끝내고 옛 MV Hub를 완전히 종료한 뒤 수동 업데이트한다.
  Resolve 종료·DB 변경·새 서버 미디어 보존은 없다.
  touched_paths: `path_safety.py`, `resolve_transfer.py`, `resolve_transfer_gate.py`, `resolve_import_worker.py`,
  `resolve_integration.py`, `update_release_worker.bat`, 관련 회귀 테스트, 현행 구조 문서.
  후속 최종 검토와 dev 커밋은 위의 2026-09-16 기능별 검사 기록을 따른다. 푸시·병합·배포는 하지 않았다.

- **연결 Text 프롬프트 반복 생성 유실 수정(2026-09-15, owner Codex · reviewer Claude Opus · status 완료).**
  Canvas Text 노드는 그대로인데 성공 후 입력창만 비워, 같은 카드의 두 번째 요청이 `(no text)`가 되는
  경로를 실제 제출 훅으로 재현했다. 파생 Text는 성공 후 유지하고, 일반 입력은 제출 때와 작업 위치·내용이
  같을 때만 비운다. 대기 중 새 입력·다른 카드/워크스페이스 왕복은 보존하며, 수동 편집·참조만 생성은 유지한다.
  훅 회귀 15개(수정 전 9개 실패)·실제 SpotlightPrompt 반복 클릭/히스토리 3개 통과. 프런트 전체 136파일 1,129개,
  구조 검사·빌드 통과(기존 act/큰 번들 경고는 있음). Chrome 별도 포트의 실제 제출 훅+가짜 응답으로
  같은 Text·Seedance 2.5·30초·1080p·참조 10장 연속 20회에서 본문/표시 원문/참조 일치, 예기치 않은
  `(no text)` 0건. 수동 편집·일반 입력 비우기·히스토리 재사용·명시적 참조 전용도 확인했다.
  외부 생성 호출 0건이며 dev 화면 로딩도 확인했다. 실서비스 유료 생성 왕복은 실행하지 않았다.
  과거 빈 프롬프트는 당시 원문이 확인되지 않아 임의 복원하지 않는다. touched_paths: App·SpotlightPrompt·
  useSpotlightSubmit, spotlightSubmitRender·spotlightPromptRepeatRender 테스트, 현행 구조 문서.
  Claude Opus 최종 리뷰 승인(P0/P1 없음). 리뷰에서 지적한 히스토리 탐색 위치 P2도 실제 컴포넌트로
  재현 후 수정했다. 저장 시 탐색 위치를 별도로 초기화해 입력 보존과 양립한다. 생성 성공 후 같은 카드·
  참조 바인딩이 유지되는 App/SceneBoard 경로는 소스 추적으로 확인했으며, 전체 App의 유료 생성 실측은 아니다.
  코드·테스트 5개 파일은 `dev`의 `1e5378ab`로 커밋했다. 문서는 별도 커밋하며 푸시·릴리스는 하지 않았다.

- **Resolve 선택 → MV Hub 생성물 따라가기(2026-09-15, Jay 요청 · Claude Opus 설계 검토 + Codex 구현).**
  Resolve Media Pool에서 MV Hub가 보낸 클립을 고르면 별도 창 없이 마지막으로 사용한 MV Hub 탭이
  해당 캔버스의 기존 결과 팝업을 열고 그 생성물에 빨간 테두리와 글로우를 표시한다. 0.1초 로컬 감시이며
  `fusionscript`는 호환 Python 자식에 격리한다. 새 클립은 숨은 generation/project ID 메타데이터,
  과거 클립은 검증된 전송 manifest의 절대 경로로만 찾고 파일명은 추측하지 않는다. 계정 범위 WS에는
  생성물 ID만 보내며 미디어·프롬프트·로컬 경로는 보내지 않는다. Claude Opus 최종 리뷰에서 P0/P1 없음·
  승인. `test_dev` 로그인 계정 미수신 가능성과 일시 오류 뒤 감시 정지 가능성도 후속 보완했다.
  관련 백엔드 73개, 프런트 전체 1,064개, 구조 검사·빌드 통과. Resolve Studio 20.3.2.9 실기기에서
  다른 클립 클릭→dev 브라우저의 10개 결과 팝업 자동 열기→목표 1개 선택까지 왕복 확인했다.
  선택된 ID와 기존 대표·Last viewed ID가 서로 달라, 두 기존 상태를 바꾸지 않은 것도 DOM으로 확인했다.
  **후속: 첫 선택 누락 수정 + 실시간 체크 체크박스.** 작업 공간·공유 & 리뷰에서 첫 알림으로 창만 열리던
  StrictMode 초기화 경합을 실제 SceneBoard 렌더 시험으로 재현 후 수정했다. 설정 → 외부 프로그램 →
  DaVinci Resolve의 `실시간 체크` 기본 체크: 켜면 자동 이동·열기·선택, 끄면 현재 열린 결과 창의 일치 항목만
  표시한다. 닫힌 창의 신호는 재생하지 않고 설정 변경 시 대기도 취소한다. 이번 프런트 전체 1,077개·구조 검사
  통과, 빌드 통과. App·SceneBoard 실제 React DOM 검사로 두 탭의 첫 이동과 선택·해제 동작을 확인했고,
  dev Chrome에서 기본 체크·해제·다시 열기 유지·체크 복귀를 실측했다. 이 후속 변경의 Resolve 실클릭 왕복은
  아직 재실측하지 않았다. Claude Opus 후속 최종 리뷰 승인(P0/P1 없음).
  **후속: 선택 감시 지연 개선·복수 선택(2026-09-15, owner Codex · reviewer Claude Opus · status 완료).**
  기존 일반 선택 8회 실측에서 Resolve 선택 처리 완료→브라우저 DOM 표시 60~403ms(중앙 218ms),
  빠른 선택 12회에서 화면 변화 2회였다. 가벼운 0.1초 별도 시험은 12회 모두 감지했다.
  Claude와 조율해 0.1초 식별 조회·변경 시 상세 조회·최신 한 개 슬롯·낡은 매핑 결과 폐기를 반영했다.
  개선 후 동일한 실기기 시험에서 일반 8회 모두 반영(51~91ms, 중앙 75ms), 빠른 12회도 모두 반영했다.
  선택은 원래대로 복원했다. 수치는 이 PC의 Resolve 선택 API 완료→dev Chrome DOM 반영 기준이다.
  복수 선택은 **열린 결과 창 우선·일치 항목 전체 강조**, 창이 없으면 한 묶음만 연다(Jay 선택).
  직접 선택은 라임, Resolve 선택만 빨강/글로우로 구분하며 빈 배열은 Resolve 강조만 지운다.
  최대 200개·긴 파이프 메시지 상한에는 자동 이동을 막고 원본 선택 수를 유지한다.
  관련 백엔드 110개·추가 조건 12개, 프런트 전체 1,109개(관련 53개·실제 React 렌더 32개), 구조·빌드 통과.
  dev 재시작 후 Jay의 실제 복수 선택 3개와 Chrome의 빨간 카드 3개 ID 완전 일치를 확인했다.
  대표·Last viewed는 그대로였다. Claude Opus 설계 합의·코드 리뷰 완료. 리뷰의 조건부 승인 항목인
  체크 OFF→다른 묶음 단일 선택 때 이전 빨강이 남는 P1을 실제 렌더 시험에서 재현한 뒤 수정했다.
  교집합 0이면 항상 Resolve 강조만 지우며, 직접 라임 선택·현재 창·씬 보존도 회귀로 검증했다.
  참고: 대상 카드가 아직 없는 신호의 30초 만료는 다음 effect 실행 때 평가하는 기존 방식이다.
  이 대기 객체가 남더라도 만료 후 화면을 여는 신호로 재생하지 않는다. 대량 200개 실기기 성능은 미측정.
  touched_paths: `resolve_selection_worker.py`, `resolve_selection_monitor.py`, App·SceneBoard·SceneVariantPopup·
  resolveSelection·progressSocket·types, 관련 테스트·현행 구조 문서. 코드·테스트 30개 파일은 `dev`의
  `3555a076`으로 커밋했고 문서는 분리 커밋한다. 커밋 직전 백엔드 110개·추가 조건 12개와 프런트 관련
  53개를 재실행해 통과했다. 다른 작업의 변경 2개는 제외했으며 푸시·배포는 아직 하지 않았다.

- **크레딧 추적 정확도 3건(2026-09-11 밤~09-12, Jay 지시 · Claude·Codex 4라운드 검토 후 구현).**
  Jay 질문("견적·실제 소비량을 제대로 쓰고 있나")에서 시작해 실측으로 세 가지를 고쳤다.
  ①**소수를 버리고 있었다** — 크레딧은 소수다(`Nano Banana 2` −1.5, `Soul V2` −0.12, 견적 `seedance_2_0` 22.5;
  MILLION VOLT 100건 중 58건이 소수). 매칭·견적 산출·**견적 캐시 로더**·요청 견적·그룹 이월(DB 저장)·화면 8곳에서 정수로 깎아
  −1.5 를 2 로(33% 과대), −0.12 를 0 으로(전액 소실) 적었다. 견적은 실제 청구와 **정확히 일치**한다 — 힉스필드는 맞는 값을 주고 있었다.
  ②**다른 워크스페이스 거래를 아예 못 봤다** — 거래는 공간별인데 전역 공간 하나만 읽었다(전역=뻘뻘뻘 조회에 그날 R&D 의 1크레딧이 안 보였다).
  이제 공간마다 env 를 박아 걷고 `--cursor` 로 이어 읽는다(100건 = 실측 7시간 치).
  ③**애매한 배정을 확정하고 있었다** — 거래에 job_id 가 없어 시각으로 짝을 맞추는데, 그리디라 금액이 뒤바뀌었다.
  이제 **가능한 모든 최대 매칭에서 금액이 같을 때만 확정**하고 아니면 보류한다('고정요금' 가정을 넣지 않는 기준).
  **09-12 후속**: 환불 480크레딧이 어디에도 안 빠지던 것을 고쳤다 — 사용/환불/순사용을 거래 원장으로 따로 집계해 `/team-overview` 의 `ledger` 필드로 준다(생성물 팩트와 **더하지 않는다**·드릴에서는 주지 않는다·멤버는 본인 것만).
  환불 **개별 귀속은 접었다**: 480 중 유일하게 정해지는 것이 97(20%)뿐이라 효과가 작다. 로컬 장부와 전송 대기열의 공간값이 어긋나던 것(P2)과 ACK 정확도도 함께 고쳤다.
  검증: backend 2087 · vitest 822 · **되돌려 확인 26건**(Codex 코드 리뷰 2회로 ②에서 P1 3건 — 상한 뒤 이어읽기 상실, 200 만 보고 기준 이동, 프록시 대기열 실패를 성공으로 답함 — 을 잡아 고쳤다). **남은 것**: 과금 당시 공간 스냅샷(현재 귀속 공간은 손으로 바꿀 수 있어 증거가 아니고, **금액 확정과 공간 확정은 별개**다) · 과거에 정수로 저장된 크레딧 되살리기(매칭이 `real_credits IS NULL` 만 봐서 자동으로 안 고쳐진다) · 그룹 한도에 환불 되돌리기(공간을 옮긴 생성물 때문에 엉뚱한 공간에 돌아갈 수 있고 힉스필드 실제 규칙이 미확인).

- **100명 전제 적대적 리뷰 라운드(2026-09-09, Claude + Codex gpt-6-astra).** 4개 레인 병렬 리뷰 →
  P0 0 · P1 4 · P2 8 → 전부 최소 수정으로 반영(dev 수정 커밋 8개 `203905e9`~`7e9c542f`). 핵심은 **권한 경계 3건**
  (계보 부모로 남의 비공개 생성물 연결 → 내용 조회 / 권한 잃은 뒤 답글 알림 본문·썸네일 수신 /
  `?tab=invalid` 로 필터 없는 폴더 집계)과 비동기 라우트 안 동기 호출 3곳, 프런트 응답 역전 5곳.
  Codex 코드 리뷰 후속으로 **첫 수정의 회귀 1건**(부모 후보 전체 검사가 정상 복수 선택을 404 로)을
  전이 축소 뒤 검사로 정정. 볼트 위키 12 수정·4 신설(Codex 반영, Claude 검증). `main` 미병합(09-09 당시 — 09-10 `f3181c48` 로 병합·배포).
  같은 날 오전 **Codex 4레인 실측 재검증**: 백엔드 회귀·프런트(701)·격리 서버 HTTP·100명 부하(190 RPS · p95 36.9ms ·
  잠금 0 · 17/17) 전부 통과. 드러난 결함 2건은 도구·테스트뿐(업데이트 상태 파일 격리 누락 `271c58a2`, 지구력 프로브 백업 래퍼) — 수정.
  오후 **캔버스 손질 3건** `5123f0c4`(Codex 3회 리뷰): 리스트·렌더 항목 순서=연결한 순서(새 연결 order + 기존 씬 freeze),
  'c' 자동연결은 텍스트→생성·레퍼런스/리스트→텍스트로 위치 무관, 생성 카드에 Set 전용 입력 레인(4레인 대칭).
  늦은 오후 **Seedance 2.5 `mode` 누락**: 힉스필드 잡 기록엔 mode 가 없고 동기화가 params 를 통째로 덮어써 재생성·레시피→씬·
  모델 카드 경로가 `--mode` 없이 나가 CLI 기본 t2v 가 레퍼런스를 거부 → 에이전트 제출 직전 보정(레퍼런스 유무로 omni/t2v) +
  동기화 합치기 + 프론트 기본값 병합. 에이전트 수정은 릴리스 `2026.09.10-1322` 에 포함 — 업데이트한 PC 부터 적용(에이전트 재실행 필요).
  저녁 **폴더 우클릭 메뉴**(라이브러리·캔버스 공통): 내 작업 탭 "팀에 공유"(하위 포함, 로컬 후보 → 200건 분할 발행),
  팀 탭 "최종 경로로 저장"(저장 대상 최종본만 렌더 폴더에 폴더 구조대로). 버튼 색=폴더 아이콘 색. Codex 설계 검토 P1 2·P2 3 반영.
  밤 **캔버스 '폴더 보기'**: 활성 씬에서 폴더를 고르면 가운데 플로팅 창에 라이브러리 격자(같은 요소·같은 필터)를 띄운다.
  밤2 **'관리 요약' 사용량을 팀 기록 장부(팩트)로**: 프로젝트 요약·에피소드·시퀀스 표가 content(발행분만)가 아니라 `team_generation_fact` 를 읽어
  공유 안 한 생성물의 크레딧·생성시간·기간까지 센다(뻘뻘뻘 455→653건, e020 153→8,451cr). 실제/견적/미상 건수 노출. 09-10 서버 적용 완료.
  09-10 Jay 결정(두 번 바뀜, 최종) **일반 멤버=내 사용량만 · 매니저(read_all)=팀 전체** — 서버가 (uid, 이메일) 쌍으로 강제(`usage_scope: all|mine`),
  멤버에게도 '내 사용 현황' 패널(프로젝트·모델·Yield·기간별·CSV)을 연다. 한도 열은 팀 전체 예산(툴팁 명시).
  같은 날 낮 **크레딧 풀 · 그룹 한도 · 잔액 추이**: 힉스필드 서브 워크스페이스 월 충전(프로젝트 '예산 한도 매월' 합에서 파생)·User Group 한도(매일·매주·매월, 전부 이월)·멤버 배정(이메일 키)·충전 기준일·긴급 충전 기록(손 입력)을 우리 대시보드가 알게 —
  풀 카드(충전·이번 달 사용·현재 잔액·월초 관측 잔액)·그룹 표(남은 양 이월 포함·미상은 추정)·잔액 추이(관측선·충전 계단·예상 소진). 한도 강제는 힉스필드, 우리는 표시·경고만.
  설정은 프로젝트 설정 창(전역 PM), 저장은 revision 잠금. 멤버는 '내 그룹' 카드만.
  같은 날 **사이드바 우클릭 '모두 확인'**: 공유&리뷰 탭 프로젝트 행·폴더 행 우클릭 → 그 범위의 새로 들어온(+N) 항목을 한꺼번에 확인 처리(로컬 ack 기록).
  같은 날 오후 **'프로젝트 상세 보고서' CSV**: 기존 Export usage report(힉스필드 호환 합계) 옆에 생성물 1건 = 1행 단추 — 프로젝트·에피소드·씬/컷·작성자·모델·크레딧(근거)·
  생성 소요시간·최종본/공유/삭제·Job ID(`/api/manage/usage-detail-export`, 열람 범위는 동일). 소요시간은 잡 실행 시간이지 사람 작업시간이 아님.
  근거는 [status/최근작업_2026-09-09.md](status/최근작업_2026-09-09.md).

- **워크스페이스 선택을 앱이 소유(2026-09-11, Jay 지시 · Codex 2라운드).** 허브가 CLI 전역을 바꾸던 것을 없앴다 —
  전환이 **네트워크를 안 타서 즉시**가 되고(직전까지 1.4초), 과금 경합의 **원인**도 사라졌다(피해는 봉투 고정이 이미 막았다).
  ★`is_selected` 는 이제 '마지막으로 생성한 공간' 이다 — **앱 선택으로 채택하면 안 된다.**
  ★POST 선택 API 는 **410 폐기** — 조용한 200 은 옛 프론트에서 선택을 되돌리고 그 뒤 생성이 엉뚱한 공간으로 작성된다.
  **배포 시 열린 옛 탭은 새로고침 필요.** 잔액은 목록에서 고르고 **폴백 없음**(못 찾으면 미확인).
  실측: `generate list`·`generate cost` 는 공간과 무관 → 전환 뒤 동기화는 필요 없던 일. 순 -629줄.
  달라지는 동작(탈퇴 공간은 생성 시점에 거부·잔액 미확인 표시)은
  [status/최근작업_2026-09-09.md](status/최근작업_2026-09-09.md) 해당 절.
- **과금 공간 고정(2026-09-11, Jay 지시 · Codex 평가+리뷰).** 힉스필드 CLI 의 워크스페이스 선택은 **전역 한 개**라,
  에이전트가 확인한 뒤 생성이 나가기까지의 틈에 허브가 바꾸면 **요청은 B 인데 과금은 A** 로 갔다.
  실측으로 `HIGGSFIELD_WORKSPACE_ID` 가 `generate create` 의 과금 공간을 정한다는 것을 확인하고(전역=뻘뻘뻘 /
  env=R&D → R&D 만 1크레딧 차감), **생성 호출마다 검증한 공간을 박도록** 고쳤다. 잡 조회는 계정 단위라 그대로 뒀다.
  ★핀(`hf_cli_version.txt`)을 올릴 때 이 계약을 다시 확인해야 한다 — env 를 무시하는 CLI 에서는 옛 경합이 그대로다.
  ★남은 위험 순위와 후속((e) 허브 set 제거 · R6 계정별 드레인 · R2 403 사유 코드)은
  [status/최근작업_2026-09-09.md](status/최근작업_2026-09-09.md) 해당 절.
- **워크스페이스 전환 딜레이·씬 탭 알림(2026-09-11, Jay 지시 · Codex 적대적 검토 4라운드 · P1 11건 반영).**
  전환 한 번이 CLI 를 직렬로 3번 쳐서 3.5~4초였다(실측: `workspace list` 0.7초, `generate list --size 100` 1.9초).
  동기화(1.9초)를 응답 뒤로 빼고, 알림을 라이브러리 재조회 앞으로 옮기고, 중복 재조회를 없앴다 — set+확인 조회만 남아
  **1.4초 안팎 예상**(클릭→알림 실측은 Jay PC 대기). 씬 탭으로 공간이 바뀌면 **CLI 반영이 확인된 뒤에** 알림이 뜬다.
  ★전환 중 다른 탭을 누르면 그 클릭의 전환 의도가 **버려지던 실제 버그**를 고쳤다(씬은 열리고 공간은 안 바뀌어 생성이 막혔다).
  ★`sync` 응답 계약이 완료 건수 → `{"scheduled": 0|1}` 로 바뀌었다.
  ★**미해결**: 이미 접수된 대기 잡과 허브 전환 사이의 과금 공간 경합(호출별 워크스페이스 고정이 별건 후속).
  근거는 [status/최근작업_2026-09-09.md](status/최근작업_2026-09-09.md).
- **배포 직전 성능 점검(2026-09-11, Jay 지시 · Codex 점검 1 + 코드 리뷰 3 · P1 5건 반영).**
  붙이기 창의 이력 조회가 데이터에 비례해 느려지던 것(생성물 5만 건 116ms)을 **범위로 갈라** 고쳤다 — 이 캔버스는 인덱스를 타 3ms,
  다른 캔버스는 누를 때만. 상한 대신 **커서 이어 읽기**(키는 sort_ts·생성물 id·scene·card 네 개 — 앞 둘만 쓰면 한 생성물의 나머지 소속이 영구 누락).
  두 칸이 서로를 기다리던 `Promise.all` 제거, 창 재열기 경쟁은 열기 순번으로, 정책 로딩이 생성을 막는 시간은 8초로 제한, 씬 탭 드래그는 측정 1회로.
  ★부하 게이트 통과가 새 경로의 성능을 보장하지 않는다. 근거는 [status/최근작업_2026-09-09.md](status/최근작업_2026-09-09.md).
- **생성 카드 '복구' → 두 칸(2026-09-11, Jay 지적 · Codex 설계 검토 1 + 코드 리뷰 1).**
  옛 복구 목록은 **카드에서 나온 생성물이 원리적으로 들어올 수 없는 질의**였다(`canvas_attempt_id IS NULL`) — 그래서 최신 생성물만 나와 '삽입'처럼 보였다.
  창을 **카드 생성물 복구**(어느 카드에 있었는지 기록이 있는 것, 가까운 순 꼬리표)와 **카드 생성물 담기**(기록이 원래 없는 것, 검색·더 보기)로 나눴다. 카드 단추는 세로점 3개.
  서버는 '있었던 자리'만 주고(요청표 ∪ 소속표) **'지금 붙었나'는 로컬 씬 목록으로 판정**한다 — 소속표는 더하기 전용이라 카드 삭제를 모르기 때문이다.
  붙이기는 요청표를 안 건드리고 `reviveCardGenerations` 로 소속만 추가하며, `addedForward` 전이 메타로 undo 가 실제로 떼어진다.
  근거는 [status/최근작업_2026-09-09.md](status/최근작업_2026-09-09.md).
- **캔버스 씬 탭 — 순서 바꾸기·캔버스별 워크스페이스(2026-09-11, Jay 요청 · Codex 설계 검토 1 + 코드 리뷰 5).**
  씬 탭을 **끌어서 순서**를 바꾸고, **우클릭으로 그 캔버스의 팀 워크스페이스를 지정**한다. 다른 공간을 보고 있으면 그 탭은 회색이고,
  누르면 **그 공간으로 바뀌면서** 열린다(확인창 없음 — 제출 완료·대기 중 잡은 모두 요청에 박힌 공간으로 나가 영향이 없다).
  순서 저장은 목록 스냅샷이 아니라 '이 씬을 저 씬 앞으로'라는 연산으로 넘겨 저장 직전에 최신 목록을 다시 읽는다(다른 창이 추가한 씬 유실 방지).
  워크스페이스 변경에 **순번(epoch)** 을 하나 두어 늦은 응답이 새 선택을 덮지 못하게 했다(계정 메뉴 목록 조회 포함).
  지정과 탭 순서는 이 브라우저에만 남는다. 근거는 [status/최근작업_2026-09-09.md](status/최근작업_2026-09-09.md).
- **그룹 사용 모델(2026-09-11, Jay 요청 · Codex 설계 검토 1 + 코드 리뷰 4).** 크레딧 그룹 창에 "사용 모델"을 넣고, 그 그룹 멤버의
  **생성 창 모델 드롭다운·캔버스 모델 설정 노드**에 체크한 모델만 보인다. **빈 목록 = 제한 없음(전부 사용)** — 새 그룹이나 설정 안 한
  그룹에서 아무도 못 만드는 일이 없게. 이미 골라 둔 모델은 자동으로 바꾸지 않고 '사용 불가' 표시 + 제출만 막는다(Jay 결정).
  ★힉스필드 관리 화면은 반대로 차단 목록(Restricted Models)이다 — 처음엔 그에 맞췄다가 **화면에서 헷갈린다는 Jay 지적으로 허용 목록으로 뒤집었다**.
  **힉스필드 CLI·개발자 API 는 워크스페이스별 모델 권한을 주지 않는다**(5개 워크스페이스 실측 동일) — 크레딧 풀처럼 손 입력이고 강제는 힉스필드가 한다.
  저장 계약의 핵심은 **필드가 없으면 기존값 유지**(구버전 앱·'추정' 맞추기가 설정을 지우지 않게). 정책은 앱에서 한 번만 구독하고
  제출 직전 가드를 생성 창·캔버스 Render·재생성·복구 재실행까지 덮었다. 다음 릴리스에 포함해야 작업자 PC 에 반영된다.
  근거는 [status/최근작업_2026-09-09.md](status/최근작업_2026-09-09.md).
- **개발 기준선은 안정적이다.** 잘못된 데이터 표시·오귀속으로 분류한 미해결 P0는 없다.
- **업데이터 결함과 그 대응(2026-09-02).** `2026.09.02-0951` 을 받은 PC 는 이후 모든 업데이트가 90%
  에서 실패하고 롤백됐다. 원인은 완료 마커 커밋의 `[System.IO.File]::Replace(..., $null)` —
  PowerShell 5.1 이 `[string]` 인자의 `$null` 을 빈 문자열로 바꿔 넘긴다(진짜 null 은
  `[NullString]::Value`). **업데이트를 실행하는 주체가 그 PC 의 설치본이라 새 릴리스로는 자가
  복구가 안 된다** — 외부 복구 스크립트(NAS `MVHub_Fix_Updater.bat`)로만 풀린다(팀 공지 완료).
  재발 방지는 3층: 수정(`476117f1`) + 패키징 전 워커 검사로 빌드 차단(`d9a82e8e`) + 복구 스크립트.
  Jay PC 업데이트 성공까지 실측. 근거는
  [status/최근작업_2026-09-03.md](status/최근작업_2026-09-03.md) §1.
- **캔버스 씬 백업이 서로를 지우던 사고(2026-09-03).** `fe5b6778`(09-01)이 앱 창을 **전용 브라우저
  프로필**로 띄우면서 한 PC 에 프로필이 여럿 생겼고, 씬 원본이 `localStorage` 에 있어 작업물이
  사라진 것처럼 보였다. 더 심각한 것은 삭제 미러가 "서버에만 있는 id 는 전부 삭제" 로 계산해
  **프로필을 번갈아 열 때마다 서로의 백업을 지운 것**이다(코덱스 적대적 검토에서 확인).
  삭제 미러를 "이 세션에서 로컬에 있던 씬" 으로 좁히고, 복구를 합집합으로 바꾸고, 명시적
  가져오기를 노출하고, 런처가 브라우저를 고정하게 했다. **이미 지워진 백업은 복원 경로가 없다**
  (실제 DELETE). 상세는 [status/최근작업_2026-09-03.md](status/최근작업_2026-09-03.md) §2.
- **업데이트 알림은 관리자 공지에만 뜬다(2026-09-03, `86242e2c`, Jay 결정).** 릴리스를 만들 때마다
  각 PC 가 스스로 알림을 띄워 업데이트 시점을 일괄 관리할 수 없었다. 이제 알림 센터의 업데이트
  알림은 관리자가 [공지] 를 누른 것만이다(자동 감지는 살아 있어 설정 화면 표시·버튼은 그대로).
  ★이 변경을 받은 PC(`0913` 이상)부터 적용 — **그 뒤 릴리스는 공지를 누르지 않으면 아무도 모른다.**
- **최적화 라운드 R8~R14 종료**(2026-08-24 수렴 선언) — 더 고칠 것이 남지 않았다는 판정이다.
  새 코드가 지켜야 할 규칙은 [루트 ARCHITECTURE.md](../ARCHITECTURE.md) `§6 동시성·상태 계약`에 있다.
- **Resolve 전송 = 직접 전송이 현행.** 서버 영구 큐 없이 **요청 한 건 안에서 준비·반입·결과 저장**을 끝낸다
  (2026-08-25 `aa0985b9`, -404줄). 브라우저의 짧은 직렬화만 남겨 Resolve API 동시 호출을 막는다.
  직접 전송은 릴리스 `2026.08.25-0847` 부터 배포됐다.
  **전송 파일명은 `<폴더경로>_<번호 2자리>`, 번호는 "그 폴더에 남아 있는 마지막 번호 + 1" 하나가
  전부다**(2026-09-02, Jay 규칙 확정 — 장부·알파벳 꼬리는 설계 과정에서 폐기). 스틸 이미지는
  **한 경로씩** 반입한다 — 여러 장을 한 번에 넘기면 Resolve 가 이미지 시퀀스로 묶어 `[00-01]`
  한 클립이 된다(실측).
  큐 v3(08-23~24)는 **이력**이며 현재 확인된 재도입 계획은 없다. 2026-08-27 에 큐 라우트 3개(`/api/resolve/queue*`)·
  워커 콜백·`/status` 의 v3 재평가·프론트 큐 헬퍼를 **제거**했고, 업데이트 차단은 v3 스캔 대신 진행 중 직접 전송 카운터를
  본다(ARCHITECTURE §7.6). `services/resolve_queue.py` 는 현행이 쓰는 `run_non_abandon` 하나만 남긴 33줄 모듈이다
  (2026-08-27 기준선 정리 — 직접 전송 경로가 닿지 않는 접수·상태 전이·claim·취소·복구·스캔 코드 1,970여 줄과 그 코드
  없이는 못 도는 테스트 57개를 삭제하고 그 안에 섞여 있던 살아있는 계약 2개는 큐 코드 없이 다시 씀, 동작 코드는 옮기거나 바꾸지 않음). 휴면 워커 `resolve_queue_worker.py`(1,127줄)도
  같은 날 삭제. `resolve_lock.py`·`resolve_import_worker.py` 는 남는다(현행 사용: `/locks`·status runner 자식).
  `/resume` 이 하던 v3 manifest 수동 복구 경로는 사라졌다(파일은 보존). 반입 자식의 시간제한 없음은 **의도적 현행 유지**
  (실제 발생 0, ARCHITECTURE §7.6 알려진 한계) — 재설계 제안 없음.
- **휴면 코드 정리(2026-08-27, Claude 스캐너 + Codex 독립 조사 교차).** 기준 = "현행 동작 경로가 닿지 않는 정의만, 동작
  코드는 옮기거나 바꾸지 않음". 백엔드: 모듈 최상위 정의 2,600개 중 호출자 0 인 함수·상수 약 45개(mutation_notify 의
  `should_notify_*` 3종 → `notification_domains` 로 통합됨, share_state 단건 lock·claim, backup·cli_bridge·comfy_client 의
  구형 helper, Resolve Bin 실사 클러스터·큐 v3 복사·바이트락 `FileLock`/`process_liveness`/`terminate_process` 등)와 테스트만
  쓰던 wrapper 12개를 제거. 살아있는 계약을 검사하던 테스트는 현행 API 로 바꿔 유지(`notification_domains`,
  `update_project_identity`, `share_state_action_locks`, `job_id_sync_diff`, 메뉴 Importer 잠금은 Importer 자체 `FileLock`).
  의도적 보존: `_REMAP_EXEMPT`(신원 스키마 감사 규칙)·`clear_task_read_cache`(테스트 격리 seam)·도구(`tools/*.py`)가 쓰는
  복구 검증 함수들. 프런트: 다른 파일·테스트가 전혀 안 쓰는 export 10개(`ProjectPlanningDialog` 컴포넌트 포함)와 테스트만
  쓰던 3개(`hasMediaRefTokens`·`applySceneGenerationResults`·`patchOwnedComfyRun`) 제거. "파일 안에서는 쓰지만 export 만
  불필요한" 선언(약 190개)은 손대지 않음. 미참조 모듈·파일은 양쪽 모두 0. "하위 호환용" 이라 적혀 있던 `unknown_job_ids`·
  `SCRIPT_RELATIVE_DIR` 은 저장소 안(도구·bat·프런트 포함) 호출자 0 을 확인하고 제거했다(저장소 밖 호출자는 없다고 봄 — 필요하면
  한 줄 wrapper 로 복원). 메뉴 Importer 잠금 테스트는 허브 `FileLock` 대신 Importer 자체 `FileLock` 을 보유자로 써서 유지.
- **공유 서버 이사 공지** 구현·실환경 검증 완료. 관리자 [팀에 공지] → 작업자 알림 → 한 번 눌러 전환.
- **영상 포스터 오염 처방(2026-08-27).** 힉스필드 MCP `show_generations` 의 영상 `results.thumbnailUrl` 은 결과
  포스터가 아니라 **첫 입력 이미지**다(실측). 기동 시 이력 보충이 이를 저장해 7월 시댄스 영상 63건에 레퍼런스 시트가
  포스터로 떴다(주기 동기화는 최신 100건만 CLI 로 되돌려 8월 잡만 정상). 처방 = MCP 변환에서 영상 썸네일 폐기 +
  같은 결과물일 때만 기존 포스터 승계 + 로컬 허브 기동 시 CLI `generate get` 으로 진짜 포스터 재조정(CAS).
  **실기기 검증 완료**: 릴리스 `2026.08.27-1404` 로 Jay PC 를 갱신한 첫 기동에서 `thumbnail_repair_done candidates=75
  replaced=75 cleared=0 skipped=0`(14:14), DB 대조 영상 asset 78건 중 입력 이미지와 같은 썸네일 0·진짜 포스터 75.
  근거·검증은 [status/영상_포스터_오염_2026-08-27.md](status/영상_포스터_오염_2026-08-27.md).
- **캔버스 렌더 노드의 모델 규칙 통일(2026-08-27, `8f6f9cbb`, Jay 결정 B).** 모델 노드가 없는 생성카드는 Render 클릭
  시점의 하단 프롬프트 모델·옵션으로 생성한다(카드 아래 Generate 바와 같은 규칙 — 종전엔 렌더 노드만 건너뛰어
  "모델이 연결된 생성 카드가 없습니다"). 모델 노드가 2개 이상이거나 모델 미설정 노드면 여전히 건너뛰고, 알림에
  `모델 노드 없음 → 하단 모델(이름) N개` 를 표시한다. 폴백은 실행 훅이 클릭 시점에 1회 읽어 comfy 완료 뒤 제출까지
  전달하므로 실행 중 하단 모델을 바꿔도 섞이지 않는다(단, 그 변경은 감지 대상이 아니다).
- **백로그 잔여 1건** — 휴지통 교차-WAL 전원손실(구조 재설계급, 보류 확정). 나머지는 실측으로
  종결하거나 처리했다.
- **부분 해결 위험은 RL-23 하나다.** 실제 다른 물리 장치 복원 훈련 전에는 완료로 올리지 않는다.
- 실제 유료 Comfy Cloud 작업, NAS 물리 복원, 운영 업데이트·자동 재시작을 한 번에 잇는 통합 검증
  (Gate 6)은 아직 남아 있다. 따라서 **운영 배포 완료를 선언한 상태는 아니다.**

### 마지막으로 기록된 전체 회귀 (2026-09-03 실행)

> [!IMPORTANT]
> 아래 숫자는 2026-09-03 `dev` `54768486`(= 릴리스 `2026.09.03-1014`) 에서 격리 환경
> (`CONTENT_HUB_NO_PROXY=1`, 임시 DB)으로 실행한 결과다.
> 이후 커밋의 전체 회귀를 자동으로 보증하지 않으며, 새 릴리스 전에는 Gate 6 절차로 다시 확인한다.
> Resolve·이사 공지 행은 2026-08-24 실측 기록이다(Resolve 는 당시 큐 v3 경로 — 직접 전송 경로의 실기기 반입 재실측은 아직 없다;
> 2026-08-27 실측은 반입을 일부러 거부시킨 채 잠금·런처만 확인). 이 PC 는 5173 이 Windows 예약 포트 범위(5141–5240)에
> 들어가 `test_dev.bat` 의 Vite 가 `EACCES` 로 못 뜬다 — 환경 문제(코드 아님). 런처가 `tools/pick_dev_port.ps1` 로 예약 범위를
> 읽어 5173 이 막혀 있으면 `[dev] Port 5173 is reserved by Windows` 를 찍고 3173(다음 3174…)으로 자동 이동한다(더블클릭만으로
> 동작). 고정하고 싶으면 실행 전 `$env:FRONTEND_PORT=…`.

| 검증 | 결과 |
|---|---|
| 백엔드 전체 테스트 | **1,879개 통과 + 46 subtests** (3분 32초) — 08-27 대비 +92. 런처 앱 창 계약(브라우저 고정 6건 포함) 27건, 업데이터 `$null` 금지 계약, 씬 백업·카드 소속이 늘었다 |
| 프론트 전체 테스트 | **100개 파일·695개 통과** — 08-27 대비 +22. 씬 백업 14건(삭제 미러 새 계약·프로필 분리 회귀·합집합 복구·명시적 가져오기), 마퀴 판정(걸침/감쌈) 포함 · `tsc -b` 통과 · `lint:architecture` 이상 없음 · `build` 성공(릴리스 빌드에서 확인) |
| 운영 설치본 업데이트 | 통과 (2026-09-02, Jay PC) — `0951`(결함 버전) → `1726` 을 고쳐진 워커로 실행해 `[3/3] Update complete.` 확인. 잔재 백업은 격리 보존됨 |
| Resolve 전송 파일명·스틸 반입 | 통과 (2026-09-02, Jay PC 실기기) — 폴더 스캔 연번으로 반입, 스틸을 한 건씩 넘겨 `[00-01]` 시퀀스 묶임 해소 |
| 캔버스 조작(그룹 선택·이동, 크기 조절, 오류 문구) | 통과 (2026-09-03, Jay dev 실기 — 지적 2건 반영 후 재확인). ⚠️ **드래그 동작은 자동 테스트가 없다**(마우스·DOM 의존) — 마퀴 판정만 테스트로 고정 |
| Resolve 실기기 반입 | 통과 (2026-08-24, 큐 v3 기준 — 클립 3개, 중복 0, 원상 복원 확인) |
| 업데이트 차단 게이트·카운터 실측 | 통과 (2026-08-27, `3fa4e862`, 격리 서버 8000·`data_test`) — `checking` 상태 파일 → `POST /api/resolve/transfers` **409**, 삭제 후 해제. 40건(1.1GB) 직접 전송 중 `resolve_active=1`·`can_update=false`(Claude 12샘플 중 8, Codex 19샘플 중 15), 종료 후 0. Resolve 대상 불일치로 가져오기 거부(클립 미삽입), 렌더 루트 원상복구 |
| 런처 창 닫기 → 브라우저 유지 | 통과 (2026-08-27, `c243b211` 포함 dev, `test_dev` 포트 3173 사본) — 허브·Vite·에이전트 기동 후 콘솔 X 닫기: 11초 뒤 8012/3173 리스너 0·프로세스 트리 0, Chrome 프로세스 32→37→37 유지. 한계: Chrome 이 이미 켜진 상태(미실행 케이스는 OS 단위 테스트 `test_explicit_breakaway_child_survives_guard_cleanup` 만), 특정 탭은 미식별, 1회 실행. Codex 는 샌드박스 제약으로 단위 테스트(2 passed)·증거 감사만 |
| 공유 서버 이사 실환경 | 통과 (발행 → 수신 → 원클릭 전환 → 위조 거부) |

## 위험 항목

RL-01 ~ RL-25 중 **RL-23만 부분 해결**이고 나머지는 완료다. 항목별 결과와 근거 커밋은
[status/RL_완료목록.md](status/RL_완료목록.md) 에 있다.

## 그다음 권장 작업

서로 섞지 않고 아래 순서로 각각 설계·구현·회귀·실측·커밋한다.

1. **`2026.09.10-1322` 배포 뒤 남은 것** — 공지는 09-10 13:35 발표됨. Gate 6-C 는 당일 실사용(9명 62건)으로 충족(Jay 판단), 일시중지 확인은 불필요로 종결. 남은 건 ①크레딧 그룹 입력(워크스페이스 5곳 모두 그룹 0개, 힉스필드 값이 오면) ②배포 후 수정 2건(`99726486`)은 다음 릴리스 때 ③공유 서버 시간대가 UTC — 지금은 그대로(Jay 결정, 이사 때 A/B 안). 아래 `1014` 항목은 이전 릴리스 기록.
2. **`2026.09.03-1014` 공지 — 배포는 끝, 알림만 남음**(이전 릴리스 기록)
   팀 릴리스 `2026.09.03-1014`(`54768486`, hf_cli 1.1.24)를 NAS `Z:\mvutil\MV_hub_S\packages` 에 올렸다(sha256 은
   `latest.json` 참조 — 매 릴리스 바뀌는 값이라 여기 고정하지 않는다). **`0913` 이상을 받은 PC 는 관리자가 [공지] 를
   누르지 않으면 이 업데이트를 모른다**(위 알림 정책). 관리자 화면에서 공지하고, 중요 릴리스면 [고정]도 함께 켠다.
   `0951` 에 갇힌 PC 가 남아 있으면 NAS `MVHub_Fix_Updater.bat` 을 한 번 실행해야 업데이트가 가능해진다(팀 공지 완료).
   작업자 PC 는 업데이트 후 캔버스를 열어 씬이 돌아오는지 보고, 안 보이면 씬 탭 줄의 **[백업에서 씬 N개 가져오기]** 를 누른다.

3. **RL-23 외부 완료 조건 — 실제 다른 물리 장치 복원 훈련**
   공유 서버의 실제 NAS/다른 디스크 대상을 설정하고, 작업자 세트 하나를 복제한 뒤 그 복제본만으로
   예비 PC에 `content + trash`를 복원한다. 결과와 걸린 시간을 월간 훈련 기록에 남긴다. 상세 조건은
   [WORKER_OFFDISK_BACKUP_CONTRACT.md](WORKER_OFFDISK_BACKUP_CONTRACT.md)를 따른다.

4. **Gate 6 — 실제 외부 프로그램과 릴리스 설치본 통합 검증**
   이미 격리 환경에서 통과한 Higgsfield 1건과 두 계정 공유 왕복을 포함해 Comfy·Resolve, NAS 복제,
   릴리스 업데이트·자동 재시작을 운영과 같은 설치본·순서로 한 번에 재확인한다. 이 단계 전에는
   운영 배포 완료로 표현하지 않는다.

## 배포 후 운영 검증 잔여

2026-09-10 `2026.09.10-1322` 배포 직후 확인된 것: 공유 서버 새 코드 응답(상세 보고서 API 200) · `/api/ready` ready · 30분 지표 관찰(15:00~15:29, 7표본) 5xx 0 · SQLite 잠금 0 · p95 ≤ 30ms · RSS 증가 없음 · 디스크 여유 99.9% · 웹소켓 11 안정 · 4xx 8건(원인 미상) · 잔액 관측 4 워크스페이스 09-10 시작(Education 0건). 서버 쪽 에이전트 지표 0 은 정상(에이전트는 각 PC 로컬 허브에 붙음).
아래는 아직 사람이 확인하지 않은 것(Gate 6-C):

- 최신 `dev` 격리 환경의 실제 Higgsfield 1건 왕복은 통과했다. 운영 설치본 배포 직후 같은 경로를
  다시 확인(09-10 미실시 — 과금이라 Jay 가 직접)
- 최신 `dev` 격리 서버의 두 계정 publish → 조회 → unpublish 왕복은 통과했다. 운영 공유 서버의
  실제 계정·혼합 버전 배포에서도 같은 결과와 감사 이력을 재확인
- 실제 Comfy 워크플로우 실행과 Resolve의 실제 가져오기·내보내기(격리·실기기 반입은 통과)
- 실제 NAS 백업 복제의 최신성·오프디스크 복사와 예비 PC 복구 시간 측정
- 운영 설치본의 업데이트 → 자동 재시작 → ready → 로그인 → 주요 화면 확인

상세 합격 조건은 위험 계획의 `Gate 6 — 외부 통합과 병합·배포 검증`을 따른다.

## 자세한 기록

| 기록 | 내용 |
|---|---|
| [status/최근작업_2026-09-09.md](status/최근작업_2026-09-09.md) | 100명 전제 적대적 리뷰 라운드(Codex 4레인) — 권한 경계 3건·루프 정지 3곳·프런트 응답 역전 5곳·볼트 정비 · 09-10: 대시보드 열람 범위(멤버=내 것)·크레딧 풀/그룹 한도/잔액 추이·충전 기준일·프로젝트 상세 보고서 CSV · **릴리스 `2026.09.10-1322` 배포 기록·검증 잔여** |
| [status/최근작업_2026-09-03.md](status/최근작업_2026-09-03.md) | 업데이터 결함·복구 3층, 캔버스 백업 상호 삭제 사고, 알림 정책 변경, Resolve 파일명 규칙, 캔버스 조작 |
| [status/최근작업_2026-08-24.md](status/최근작업_2026-08-24.md) | 최적화 R8~R14, Resolve 큐, 서버 이사, 백로그 정리 |
| [status/영상_포스터_오염_2026-08-27.md](status/영상_포스터_오염_2026-08-27.md) | MCP thumbnailUrl=입력 이미지 실측, 처방 3건, 릴리스 `1404` 실기기 재조정 75/75 완료 |
| [status/RL_완료목록.md](status/RL_완료목록.md) | RL-01~RL-25 결과와 근거 커밋 |
| [status/검증기록.md](status/검증기록.md) | 항목별 회귀·부하·브라우저·릴리즈 실측 기록 |
| [status/구현완료_RL-02_RL-23.md](status/구현완료_RL-02_RL-23.md) | RL-02 워크스페이스 스냅샷·RL-23 오프디스크 백업 상세 |
| [status/사전배포검증_2026-08-19.md](status/사전배포검증_2026-08-19.md) | 사전 배포 적대 검증 |
| [status/안정화_2026-08-18.md](status/안정화_2026-08-18.md) | 추가 안정화 |
| [status/코드대조_2026-08-26.md](status/코드대조_2026-08-26.md) | 기준선 `8133c625` 코드↔문서 전수 대조(29개)·Claude/Codex 교차검증·finding ledger |

## 문서 사용 순서

1. **현재 판단:** 이 문서
2. **위험 상태와 다음 작업:** [RISK_REDUCTION_PLAN_2026-08-15.md](RISK_REDUCTION_PLAN_2026-08-15.md)
3. **코드를 어디에 둘지:** [루트 ARCHITECTURE.md](../ARCHITECTURE.md) — 계층·경계·동시성 규칙
4. **현재 구조·데이터 흐름:** [docs/ARCHITECTURE.md](ARCHITECTURE.md), [AI_CONTEXT.md](AI_CONTEXT.md)
5. **데이터·권한 계약:** [DATA_OWNERSHIP.md](DATA_OWNERSHIP.md),
   [WORKSPACE_DATA_CONTRACT.md](WORKSPACE_DATA_CONTRACT.md),
   [신원과_모드_가이드.md](신원과_모드_가이드.md)
6. **운영·복구·테스트:** [SERVER.md](SERVER.md), [SERVER_RECOVERY.md](SERVER_RECOVERY.md),
   [TESTING.md](TESTING.md)
7. **과거 발견 근거:** [AUDIT_2026-08-15.md](AUDIT_2026-08-15.md)

## 갱신 규칙

- 위험 상태는 Gate 0 표에서 먼저 바꾼 뒤 이 문서의 요약을 맞춘다.
- 테스트 숫자는 전체 회귀를 실제로 다시 실행했을 때만 갱신한다.
- 브랜치의 앞선 커밋 수처럼 매번 변하는 숫자는 문서에 고정하지 않는다.
- 외부 실측을 하지 않았다면 `검증 완료`나 `배포 가능`으로 표현하지 않는다.
- **이 문서는 요약만 유지한다.** 날짜별 상세 기록이 생기면 `status/` 에 새 노트로 넣고 여기에는
  한 줄 링크만 추가한다(이 문서가 다시 길어지지 않게).
