---
updated: 2026-08-22
status: archived
---

# 최적화 라운드 9 (2026-08-22) — 코덱스 주도, R8 이월 P2 + 앱 코어 ✅종결

★결과: 승인 13건 전부 반영. 최종 회귀 백엔드 **1,433**(+32, 기준선 1,401). 커밋:
ca2dce4b(웨이브1 5건) e667e029(BK-2) 487b98c1(TR-1/2) 5b68cee7(BK-1)
f24a70fa(GMC-1/2) efbb536f(DB-2) fde5bf35(LC-1).

적대 리뷰 P1 1건 = ★구조 항목으로 강등(클로드 판정): WAL main+ATTACH trash 교차
트랜잭션은 전원 손실 시 파일 세트 원자성이 없음(trash.py:268·433·592). R9 회귀 아님
(휴지통 분리 DB 구조의 태생 한계, R9 는 소프트웨어 예외/롤백 원자성 개선). 수정=
아키텍처 재설계급(payload main DB 이전 or 2단계 intent+부팅 복구) → Jay 결정 대기.

리뷰 P2 9건 → R10 후보: purge sidecar best-effort 고아 · GMC 세마포어 loop당 6(다중
loop 시 초과) · GMC 예외 재전파 시 호출자가 성공 수 미수신(claim 카운트 왜곡) ·
전환 init_db 가 transition_lock 아래(대형 마이그레이션 시 로그인 지연) · 포인터 공개가
세션 설정 저장 전(짧은 stale 창) · periodic_worker_backup._task private 접근 ·
AUTH-off 종료 periodic_sync.stop 추가 호출(무해 no-op) · 대시보드 읽기 트랜잭션의
checkpoint 지연 · AST-1 재사용 경로 불필요 epoch 증가(무해).

체제=R8 과 동일(코덱스 서베이·구현·자체검증, 클로드 검토·회귀·커밋). 범위=R8 리뷰 P2
6건 이월분 전량 + 코어(main/db/manage_db) 신규 6건 + 휴지통 인접 1건. 서베이 상세는
scratchpad codex_r9_survey_out.txt(요지는 아래), 클로드 전량 승인.

## 웨이브 1 — 저위험 (6건)

- BK-2 quiet-dirty 백업 실패 삼킴 후 dirty 소거 → _backup_once 성공 반환·성공시만 정산. ★독립 diff
- MDB-1 elapsed_by_job_ids 가 부재 manage_hub.db 를 생성(빈 DB→백업 검증 실패) → mode=ro URI.
- AST-1 capture/ref-import 커밋 후 취소 시 트리 캐시 무효화 생략 → commit_attempted+finally 선례 적용.
- LC-2 worker-backup bootstrap 수제 1회 shield → to_thread_non_abandon 교체.
- DB-1 풀 컨텍스트가 Exception 만 롤백 → BaseException(취소 시 미완료 트랜잭션 풀 잔류 차단).
- MDB-2 team_overview 다중 SELECT 시점 불일치 → 명시 읽기 BEGIN(단일 WAL snapshot).

## 웨이브 2 — 중위험 (7건, 각 독립 diff)

- TR-1+TR-2 삭제 tombstone·복원 dirty 를 본체와 한 transaction-root 로(같은 content DB 라 가능,
  caller-supplied connection helper, 중첩 get_connection 금지).
- BK-1 백업 due 판정 scope 를 실행까지 전달(재캡처 창 제거)+전환 시 상태 재설정.
- GMC-1+GMC-2 다운로드 세마포어를 event-loop 귀속 공유로(프로세스 상한 6, 모듈 전역 금지)
  + gather return_exceptions=True 전체 회수 후 재전파(형제 즉시 취소 금지).
- DB-2 기존 계정 DB 파일도 init_db(path) 멱등 마이그레이션(비활성 계정 스키마 누락 해소).
- LC-1 lifespan cleanup 경계를 첫 start 전으로 확장+각 cleanup 끝까지 시도 후 원예외 재전파.

## 보고만(구현 제외)

ws overflow/debounce task shutdown registry 없음(경고 재현 시 별도 diff) ·
periodic sync 종료 늦은 DB 쓰기 가능(R8 조사 완료 범위) · thumbnail prewarm daemon
thread 미회수(중단 토큰 필요, 범위 대비 큼).

## 반대 확정(재제안 금지)

manage_db 연결 풀 신설 · team_overview 단일 SQL 통합(실측 20~34ms) · 3-E(startup
CLI·마이그레이션 중첩=CLI 재정렬) · shutdown 무조건 flush_pool.

## 하지 말 것(서베이#9 — 코덱스 자체 명시)

R1~R8·A5 재조사 금지 · CLI 추가·재정렬 금지 · syncer 정책 불변 · recovery_required
비재큐잉 · 온라인 백업 maintenance_gate 금지 · GMC 형제 다운로드 즉시 취소 금지 ·
모듈 전역 Semaphore 금지 · tombstone 원자화에 중첩 get_connection 금지 ·
manage_hub.db 통합·풀 금지 · WS sender 계약 재설계 금지 · 각인 3키·실패값 미캐시 ·
attempts 정렬 금지 · 사장 코드·lock registry 삭제 금지.
