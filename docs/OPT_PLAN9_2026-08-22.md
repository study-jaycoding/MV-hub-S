# 최적화 라운드 10 (2026-08-22) — 코덱스 주도, 시리즈 마지막 라운드

체제=R8·R9 동일. 범위=R9 P2 이월 중 3건+신규 서비스 실경합 2건, **5건 전부 독립 diff**.
서베이 상세=scratchpad codex_r10_survey_out.txt, 클로드 전량 승인. R10 종료 후 시리즈
(R8~R10) 마감.

## 웨이브 1 — scope/capture 패턴 (2건)

- RR-1(보안성 높음): remote_realtime config provider 가 token(A DB)과 base URL(B DB)을
  다른 시점에 읽어 A 의 Bearer/Cookie 가 B 서버로 전송될 수 있음 → transition_lock
  acquire(blocking=False) 로 URL·token 두 읽기만 스냅샷(전환 중=None 반환, loop 비차단).
- HAF-1: history import(최대 10,000페이지 detached task)가 계정 범위 미고정 — A import
  중 B 전환 시 완료 audit·자동 claim 이 B DB 에 기록될 창 → task 시작 전 scope 캡처+
  전체 override(네트워크·순회 중 lock 비보유).

## 웨이브 2 — 상태·트랜잭션 계약 (3건)

- P2-3(실버그성 높음): GMC 예외 재전파 시 보존 caller 가 부분 성공 수를 못 받아
  claim 을 0/1/internal_error 로 오기록 → 내부 outcome helper(부분 결과+예외 동반 반환),
  공개 함수는 원예외 재전파 유지, 보존 서비스만 outcome 소비(6/1/partial 정산).
- P2-1: purge 가 trash 삭제(autocommit) 후 sidecar 정리를 best-effort — 실패 시
  metrics·task·export 고아 영구 → _with_trash BEGIN IMMEDIATE 안에서 trash 삭제+
  caller-supplied sidecar helper 동시 COMMIT. ※소프트웨어 예외 원자성만(교차-WAL 전원손실 제외).
- P2-5: 로그인/가입이 B 포인터 공개 후 세션 설정(URL·token 등)을 순차 저장 — 일반
  요청이 준비 중 B DB 를 읽는 창, 실패 시 B 포인터 잔류 → lock 아래 B DB 준비+override 로
  설정 저장 완료 후 set_active(B) 를 마지막에 1회.

## 반대/제외 확정(재제안 금지)

GMC cross-loop 상한(P2-2, loop당 6 유지) · 전환 init_db lock 밖 이동(P2-4) ·
periodic_worker_backup._task 정리(P2-6) · AUTH-off no-op stop(P2-7) · 대시보드 읽기
트랜잭션(P2-8) · AST reuse epoch(P2-9, side channel 금지). 보고만: HAF starter
registry 부재(REP-HAF-2).

## 하지 말 것(서베이#10)

trash 교차-WAL 구조 재개 금지 · purge 중첩 get_connection·MANAGE-off 스키마 생성 금지 ·
GMC module-global/threading 세마포어 금지 · 형제 즉시 취소·CancelledError 계약 변경 금지 ·
init_db lock 밖 이동 금지 · history 순회 중 lock 보유 금지 · realtime 설정 읽기로 loop
blocking 대기 금지 · plain to_thread 기계적 전수 교체 금지 · 프런트/A5·CLI·syncer·
recovery·OPT_PLAN1~8 누적 금지 전부.
