---
updated: 2026-08-24
status: archived
---

# 최적화 라운드 13·14 (2026-08-23~24) — 시리즈 종료: 수렴 선언 ✅

## R13 = R12 적대 리뷰(코덱스) + 수정 4건

리뷰 판정: R12 5건 승인, P1 2건+P2 2건 발견, 관측 플레이크=실제 TOCTOU 확정.
수정(전부 반영, 커밋 2개 — Opus 분·코덱스 분 분리):

- AUTH-1(P1): verify_token·token_password_stamp 에 opt-in 센티널 — "시크릿 사용 불가"와
  "진짜 무효"를 검증 순간의 원인으로 분기, 게이트 재표본 완전 제거(복원 롤백 시 유효 토큰
  삭제 잔여 창 봉쇄). WS 45초 재검증은 판정 불가 시 다음 주기 유예.
- IMPORT-1(P1): active_uid 를 override 인지형으로 — (account_key, uid) 를 transition_lock
  한 구간에서 원자 캡처(_capture_account_pin), 라우트 본문 무변경.
- WS-1(P2): 5초 이상 유지된 연결만 백오프 리셋(1013 반복 거부 지수 백오프 유지).
  R14 후속: openedAt 을 performance.now()(단조 시계)로 — 시각 점프 오판 차단.
- IDEM-1(P2, 코덱스): classify_placeholder_collision — 예약+재개 가능성을 BEGIN IMMEDIATE
  한 번에 completed/resumable/conflict 판정. 일반·캔버스 통합(R6 재개 진리표 1:1 보존),
  가짜 409·플레이크 해소(30회 반복 0 실패). 부수: payload 비-dict 500→409.

최종 회귀: 백엔드 **1,549**(+29 subtests)·프런트 **645**. 커밋 6c9dc907 까지.

## R14 = R13 교차 리뷰(Opus) + 최종 수렴 판정

교차 리뷰: **P1 0건** — IDEM-1(코덱스 작성) 포함 전부 승인(R6 진리표 1:1·transaction-root
계약·센티널 누출 없음·override 영향 전수 확인). R13 이 넣은 회귀 0건(회귀 3회 완주 실측).

### ★수렴 선언

실질 가치가 있는 신규 최적화 후보 = **0건**. 잔여 분류:
- Jay 대기 4건: C3 Resolve · trash 교차-WAL · main 병합/릴리스 · R7 호환성 확인.
- 반대 확정(재제안 금지): R6~R12 누적 전부.
- 조건부 백로그(조건 미충족 유지): D-3·manage_telemetry-2·lineage-1·bridge-3(실기기) 등.
- 테스트 위생(최적화 아님): r8 전환 테스트의 0.5s 벽시계 대리 측정(→ lock.acquire(timeout)
  계약 판정 권고) · AUTH-1 WS 45초 유예 분기 무테스트 · IDEM 신규 2상태 무테스트 ·
  이 머신 한정 환경 플레이크(AV 임시잠금=AUDIT_2026-08-15 기록, release_update=bat cwd 함정).
- P2-1 기록: IDEM-1 이 일반 경로 409 문구를 "같은 생성 요청 키의 placeholder를 안전하게
  이어갈 수 없습니다"로 변경(파싱처 없음 확인 — 계약 파손 아님).

**시리즈 전체(R8~R14) 합계**: 반영 약 90건, 테스트 백엔드 1,350→1,549(+199)·프런트
616→645(+29), 전 구간 무회귀. 종료 조건("코드에 최적화가 필요 없어질 때까지") 충족.

---

## 후속 기록 (2026-08-24, Jay 판정)

- 휴지통 교차-WAL 전원손실: **백로그 확정**(Jay) — 발생 확률 극저·수정=구조 재설계급.
- Resolve 큐·서버 이사·이름 주소: 코덱스 실환경(라이브) 통합 테스트 수행 —
  이사 공지·이름 주소 전 시나리오 통과(문제 0), Resolve 큐에서 릴리스 차단 2건
  발견(동일 초 FIFO·사망 프로세스 alive 오판)→수정(7025dc86)→재테스트 "두 건 해소".
  잔여=Resolve 실반입 1회(Jay 실기기 — 샌드박스는 fusionscript/UI 권한 제약).

## 백로그 실측 판정 (2026-08-24, 코덱스 실측 — Jay 지시)

- ★종결 3건: manage_telemetry-2(30만 행 벤치 이득≤3.4%, 인덱스 미선택/역효과) ·
  D-3(10만 행에서도 체감 없음, 반환 비용 지배) · AV 추가 완화(현 10회 재시도 실효,
  광범위 제외는 진짜 누수 은폐 위험).
- 착수 3건(구현 진행): lineage-1(정확성 버그로 승격 — 넓은 계보 SQL 변수 한도 실패+
  limit 위반, CTE 15배·동일집합 실측) · periodic sync 종료 후 DB 쓰기(10/10 재현) ·
  thumbnail prewarm 스레드 회수(파일 잠금 실증).
- 보류 유지 2건: bridge-3(실기기 실측 계획 산출 — 폐기용 Resolve 프로젝트에서 prefix
  캐시 p95 비교) · WS task registry(정상 종료에선 경고 미재현 — 강제 loop close 한정).

## 조건부 보류 최종 판정 (2026-08-24, 실측 완료)

- ★bridge-3 → **종결**(리졸브 실기기 실측): 이득은 조회 경로뿐(단발 프로세스 오버헤드에
  묻힘, 실사용 규모 절감 9~259ms < AddSubFolder 1회 105ms), 생성 경로는 손해(+318ms).
  결정타=캐시의 TOCTOU 확장으로 같은 이름 중복 Bin 실기기 실증(기존 가드 미작동, 이후
  전 작업 하드 에러). 부수 발견: AddSubFolder ~1,000회/프로세스에서 클라이언트 사망(255)
  — 단일 요청 1,000 Bin 은 애초 도달 불가.
- ★WS task registry → **사실상 종결**(실서버 실측): 실제 uvicorn+실 WS 8연결+debounce
  pending 상태에서 정상 종료 2종×연결상태 2종×3회=12/12 경고 0. overflow 분기는 2초
  send timeout 이 먼저 작동해 운영 경로 도달성 낮음 실증. 재개 조건(좁힘): 실서버 정상
  종료에서 pending 경고가 _debounced_notify/_collect 로 식별되거나 queue/timeout 정책
  변경 시.

→ 조건부 보류 0건. 잔여 백로그 = 휴지통 교차-WAL(확정 백로그) 1건뿐.
