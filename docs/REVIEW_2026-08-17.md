# Claude 전영역 교차 리뷰 (2026-08-17)

대상: Codex 구현 51커밋(`2598916a`~`729b1904`, RL-01~23·25). 방식: 5영역 병렬 적대적 리뷰
(코드·테스트 전량 대조). 결과: **P0 0건 · P1 4건(수정 완료) · P2 약 25건(백로그)**.
자동 검증: 백엔드 887+21 subtests·프론트 535·tsc 전부 통과(리뷰 시점·수정 후 재확인).
Codex 는 사용 한도로 8/20 복귀 예정 — 아래 "P1 수정"의 Codex 교차 확인이 그때 첫 작업.

## P1 — 수정 완료 (2026-08-17, Claude)

1. **복원 드릴의 외부 부수효과** — 드릴 격리 서버가 부팅 시 복원 사본의 in-flight 마커·API
   키로 실제 Comfy Cloud 잡 취소·로컬 CLI 호출 가능. → `CONTENT_HUB_EXTERNAL_RECOVERY=0`
   게이트 신설(config.py), `comfy.recover_interrupted_run_jobs` 취소 생략 + `main.py` CLI
   신원 캡처 생략 + 드릴 env 주입. 테스트: test_comfy_router.
2. **DashboardView 워크스페이스 전환 경합** — in-flight 응답·stale 클로저 재호출이 다른
   워크스페이스 숫자를 덮음. → 요청 시점 스코프 가드 + `reloadRef.current()` 재호출
   (WorkBoard 의 scopeKey 패턴과 동일).
3. **작업 목록 GET 의 쓰기 증폭** — `sync_folder_tasks_batch` 가 매 읽기마다 폴더 수만큼
   무조건 UPDATE(같은 값 재기록=WAL 증가·쓰기락 경합). → 기존 값 선조회 후 달라진 행만
   쓰기, 백필·보관 UPDATE 는 대상 존재 확인 후 실행. 테스트: 정상 상태 두 번째 동기화
   total_changes 0 고정(test_manage_workspace_scope).
4. **이동 프로젝트의 이전 공간 유령 합계** — summary totals 는 생성물 workspace 기준인데
   행 모집단은 프로젝트 현재 workspace 기준이라 이동 시 합계>행합. → 기록이 남은 공간에
   `workspace_moved=True` 행으로 표시(정책: 생성물 workspace=과금 스냅샷은 이전 공간 유지).
   **잔여**: WorkBoard 작업(project_task) 행·컷 파생의 workspace 귀속 모델 — RL-02 행 참조.

## P2 중 즉시 수정 완료 (2026-08-17 후속, Claude — 설계 결정 불필요 항목)

- ✅ repair(캔버스 복구) 경로 RL-04 우회 — unknown workspace payload 는 재큐잉하지 않고
  placeholder 를 명확한 실패로 종결(구 에이전트 오귀속 실행 차단). 회귀 테스트 포함.
- ✅ `"workspace": null` 키가 배치 컨텍스트 폴백을 죽이던 것 — 값이 None 인 키는 명시로
  치지 않음(빈 문자열 등 깨진 명시값은 종전대로 unknown fail-closed).
- ✅ RL-24 — CLI 파라미터 스키마 조회 실패는 캐시하지 않고 120초 백오프 후 재조회.
- ✅ MANAGE off + 프록시 드레인의 사이드카 테이블 몰래 생성 — 계정 보고 드레인도 MANAGE
  게이트로 묶어 ingest 인라인 레거시 계약 복원.
- ✅ 팀 탭(서버 UUID) unpublish 의 final 가드 재검사 — 되찾은 로컬 행이 final 이면 409 로
  드러냄(무음 확대 차단).
- ✅ finalize 보상 실패 `except: pass` — logger.error 로 관측 가능(보상 outbox 영속화는 후속).
- ✅ comfy /run 원시 경계 오버헤드 2→16MiB(워크플로우 JSON 폼 필드 여유). 파일 합계 상한은 불변.
- ✅ `_MultipartFileBody` docstring 의 리다이렉트 재생 주장 정정.
- ✅ 복원 드릴 finally 의 종료 예외가 원인 예외를 가리던 것 — 로그로 강등.

## 추가 발견·수정 (2026-08-18, 사용자 지적에서 출발)

- ✅ **에셋 탭의 워크스페이스 경계 미구현(P1)** — 사용자가 설명한 의도("선택 워크스페이스의
  프로젝트 폴더만 보이고, 직접 추가한 폴더는 항상 보인다")가 코드에 전혀 없었다. 프론트가
  workspace 를 보내지 않고(`assetsApi.ts`), 백엔드도 `repo.list_projects` 에 `workspace_id`
  를 넘기지 않아(`routers/assets.py`) **팀 A 를 선택해도 팀 B 프로젝트 폴더가 노출**됐다.
  read_all 보유자(admin·PM·PD)에게는 전 조직 폴더가 보였다. 수정: 목록 API 2종에
  `workspace_id` 쿼리 추가 + 자동(프로젝트 유래) 폴더에만 필터 적용, 수동 등록 폴더는
  종전대로 무필터. 에셋 분리창은 관리창과 같은 storage 동기화로 메인 창의 팀 전환을 따른다.
  규칙은 생성 탭과 동일(팀 선택 시만 좁히고 개인·미선택은 전체). 계약 테스트 3건 추가.
  **참고(작업 항목 아님)**: 파일 접근 경로(`/tree`·`/file`·`/thumb`)는 멤버십만 검사하고
  워크스페이스는 보지 않는다. 다만 이를 이용하려면 ①로컬 허브에서 ②이미 멤버인 프로젝트를
  대상으로 ③API 주소를 직접 입력해야 하는데, 그 사람은 워크스페이스만 바꾸면 같은 폴더를
  정상 화면으로 볼 수 있다(폴더 자체도 본인 PC의 실경로다). 새로 열리는 권한이 없으므로
  보안 항목이 아니며 별도 수정 계획을 두지 않는다. 에셋 쪽을 다른 이유로 손볼 때, 규칙을
  한쪽만 적용해 혼동하지 않도록 이 차이만 기억한다.

## P2 백로그 (수정 안 함 — 항목별 합의 후)

> **2026-08-20 P2 소진 라운드로 대부분 종결.** 항목별 최종 현황·커밋은
> [P2_CLOSEOUT_PLAN_2026-08-20.md](P2_CLOSEOUT_PLAN_2026-08-20.md)가 권위 장부다(아래 원문은
> 발견 근거 보존용 — 재구현 금지). 이 라운드 이후의 잔여는 셋뿐: ①preparing+placeholder
> 유령(측정 후 수동 격리) ②레거시 workspace/시각 보정(배치 0 측정 후) ③보존 쿼터 증분
> 회계(B2 — 설계 선행). 나머지는 배치 1~8·B-P1·B-P2(W1 dfba06d7·W2 868d6a89·프론트
> d9e8c58b)로 해소 또는 조건부 보류(D)로 문서화됐다.

### 생성·과금 (에이전트 리뷰 1)
- `ensure_ingested_tracked`(RL-03 백필 API)는 호출처 없는 데드 코드 — 실백필은 CLI 100-window 한정.
- 혼합 배포(구 에이전트+신 서버)에서 격리↔지연 create 이중 과금 창(30분+수동 확인 타이밍 필요,
  신 에이전트는 begin CAS 로 차단) — 배포 순서(에이전트 동시 업데이트)로 흡수.
- preparing+placeholder 쌍이 활성화 직전 크래시 시 영구 잔류(과금 무해, 유령 카드) — 보수적 sweep.

### 텔레메트리·인증·공유 (에이전트 리뷰 2)
- RL-11 보상(재-finalize)은 여전히 1회성·비영속 — 근본 해결은 보상 outbox(설계 필요).
- 계정 보고 outbox poison 행(디코드 불가·영구 409) 정리 경로 부재 — dead-letter 표시.
- 재시도 사이 creator_uid remap 시 거래 중복 삽입 가능(저확률, uid 전환 운영 시 주의).

### 자산·업로드·Comfy (에이전트 리뷰 3)
- 외부(탐색기)에서 감시 폴더 삭제·재생성 시 죽은 핸들이 남아 감시 영구 정지 + Windows
  delete-pending — `asset_watcher.py:258` 재스케줄 감지 필요(Windows 실측 동반 권장).

### 운영·백업·WS (에이전트 리뷰 4)
- 작업자 허브 부팅이 백업 전체 복사+해시에 await 로 묶임(중복 판정이 비용 지불 후) —
  `main.py:332`, `worker_backup.py:204` 선-중복판정 또는 백그라운드화.
- `media_preservation`·`worker_backup` 의 동기 SQLite 호출이 이벤트 루프 위(busy 시 최대 5초
  정지) — `to_thread` 래핑.
- 보존 쿼터 검사가 다운로드 1건마다 MEDIA_DIR 전체 스캔 — 증분 회계로.
- 워치독: HTTP 만 응답하면 무조건 busy — 포트 뺏김 영구 오진 가능(busy 경로에도 저비용
  점유자 확인). PID kill 직전 CommandLine 재확인(TOCTOU).
- ✅WS 2초 전송 예산이 락 대기 포함 — 겹친 broadcast 에서 정상 클라이언트 과잉 격리 가능.
  → 배치 6(2026-08-20, 클로드 구현·코덱스 적대 리뷰 P0 1·P1 3 반영): 연결별 단일 sender +
  크기 제한 큐(64) + reload 신호 큐 병합(빈 배열='전체' 보존), timeout 은 순수 전송에만.
  수거는 sender 실제 종료 대기 후 close(동시 close 로 인한 close 유실 방지) + 백그라운드화.
  원격 브리지는 재연결 시 3도메인 출처 없는 reload 로 따라잡기. 잔여 P2 기록: ①lifespan
  전체 sender 정리 shutdown 없음(uvicorn teardown 이 태스크 취소로 흡수) ②broadcast payload
  는 enqueue 후에도 참조되므로 호출측 수정 금지(docstring 계약으로 종결, 전 호출처 새 literal).
- 세트 백업의 DB 간 스냅샷 시점 미세 창(이동 순서 확인/문서화 수준).
- `_mark_success` 예외 시 세트가 재시작까지 running 고착(드묾) / restore 드릴 finally 예외가
  원인 가림 / AUTH-off 원격 1008 문구 오해 소지.

### 관리 대시보드 (에이전트 리뷰 5)
- 레거시 unknown/personal 스코프 생성물이 팀 프로젝트 작업 파생에서 상시 제외(백필 미적용 행은
  gen_count=0 유령).
- 관리창 셀렉터 선택이 메인 창 filters 재저장에 의해 소리 없이 리셋(storage 이벤트 역기록 없음).
- 과거 기록 뷰에서 archived/활성 행 구분 UI 없음(`Task.archived` 미사용).
- `source_last_seen_at` 문자열 MAX 가 created_at 포맷 혼재에 취약(오차 <1일).
- 대시보드 탭(프로젝트 요약·시퀀스)에 데이터 출처 라벨 없음(작업 탭만 있음).
