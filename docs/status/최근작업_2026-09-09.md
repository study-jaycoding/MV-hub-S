---
aliases:
  - 2026-09-09 적대적 리뷰 라운드
tags:
  - mvhub
  - mvhub/기록
status: snapshot
updated: 2026-09-09
---

# 2026-09-09 — 100명 동시 사용 전제 적대적 리뷰 라운드 (Claude + Codex gpt-6-astra)

기준선 `b917b0f0`(Last viewed 배지 완료) → `7e9c542f`. Jay 지시: "기능이 계속 잘 작동하는 것이 최우선,
100명 동시 사용 전제, 볼트도 함께 점검". 재제안 금지 목록(최적화 R1~R14·휴면코드 후속·반입 타임아웃·
줄수 리팩터·A5·멤버 등록부 B안)과 보류 결정(startup 백그라운드화·scenes merge·comment 정규화)은
리뷰 범위에서 뺐다. 8월 17일 이후 519 커밋과 P2 백로그 잔여를 우선 대상으로 삼았다.

## 진행 방식

Codex 4개 레인을 읽기 전용으로 **병렬** 실행(A 동시성·부하 / B 프런트 체감 / C 백엔드 정확성·권한 /
D 볼트 대조) → Claude 가 판정마다 코드로 재확인 → 최소 수정 + 회귀 테스트 → Codex 코드 리뷰(수정 후
머지, P2 3건) → 후속 3건 반영 → 전체 회귀. 100명 부하 자체는 7월 31일 실측(186 RPS·p95 61ms·락 0)과
8월 soak 이 유효하므로 재실행하지 않았다. 폴링 부하는 1명당 유휴 분당 14~16건(작업 중 ~30)으로
100명이면 초당 25~50건 — 실측 여유 안이라 손대지 않았다.

## 판정 결과

| 레인 | P0 | P1 | P2 | 비고 |
|---|---:|---:|---:|---|
| A 동시성 | 0 | 0 | 3 | 비동기 라우트 안의 동기 호출(DB 쓰기·DNS·소켓 읽기) |
| B 프런트 | 0 | 2 | 3 | 전부 "늦은 응답·이벤트가 새 상태를 덮는" 계열 |
| C 백엔드 | 0 | 2 | 2 | **권한 경계 3건** + 이전 도구 부분 복사 |
| D 볼트 | — | — | — | 위키 42쪽: 유지 30·수정 12·삭제 0, 신설 4·항목 추가 4 |

`generation_view`(Last viewed) 는 A·B·C 모두 "문제 없음"으로 확인(각 PC 로컬 DB 처리, 인덱스·직렬화·
알림 제외 확인).

## 수정 (dev 수정 커밋 8개 + 문서 1개, 전부 최소 변경)

| 커밋 | 내용 | 회귀 위험 |
|---|---|---|
| `203905e9` | auth 관리자 발급 DB 쓰기 → `to_thread_non_abandon` · 썸네일 DNS → `to_thread` · 프록시 `e.read()` → `to_thread` | 낮음(응답 의미 불변) |
| `3711ff52` | **C1** 계보 부모는 열람 가능한 것만(404) · **C3** `folder-counts` tab 정규화 | 낮음 |
| `c79a10c4` | **C2** 알림 센터 목록·모두 읽음에 생성물 가시성 절(team 탭 경계 재사용) | 낮음~중간 |
| `a4352934` | 프런트 5건 — 모달 Ctrl+Z 캡처 격리 · 탭 캐시 복귀 시 seq++ · 마퀴 그룹 보존 조건 · 잔액 키 가드 · 공지 조회 세대 | 낮음~중간 |
| `456db9be` | **C4** server_move 추가 폴더 임시→rename, 실패 시 잔재 제거·예외 전파 | 낮음(도구) |
| `02200240` | 리뷰 ① **C1 회귀 정정** — 검사를 전이 축소 **뒤** 남은 부모로(멤버십은 라우터가 닫아 넘김) · 리뷰 ③ 공지 모두 읽음 완료 시 재무효화·재조회 | 낮음 |
| `9b193b60` | 리뷰 ② 전역 통계 미확인 수도 같은 가시성(해소 불가 벨 숫자 방지) | 낮음 |
| `7e9c542f` | 위 헬퍼의 `_visibility → projects` 지역 import 순환 제거(경계 테스트가 잡음) — 멤버십을 라우터가 내려줌 | 없음(동작 불변) |

C2 의 `ALERT_COMMENT_*` 상수(카드 배지·패널 공유)는 건드리지 않았다 — 알림 센터·통계만 같은 절을 덧붙임.

## 검증

- 백엔드: 새 테스트 — 권한 경계 7(`test_lineage_parent_visibility`)·알림 가시성 7(`test_notification_visibility`)·
  server_move 실패 주입 1. 관련 묶음 수십 개씩 매 커밋 통과. 전체 회귀 결과는 아래 표.
- 프런트: `tsc` 깨끗 · vitest 701 · production build 통과.
- 손 검증 항목(커밋 메시지에 기재): 모달 Ctrl+Z 뒤 캔버스 undo 스택 불변 / Workspace→Team→15초 내 복귀 시
  목록 섞임 없음 / 그룹 A 선택 후 카드 B 만 감싸기 → A 해제 / 프로젝트 탈퇴 후 답글 알림·벨 0.

| 전체 회귀 | 결과 |
|---|---|
| 백엔드(6건 수정 시점) | 1,939 통과 |
| 백엔드(최종 `7e9c542f`, `-x` 없이·종료 코드 파일 확보) | **1,943 통과 · 46 서브테스트 · 실패 0** (3분 19초) |
| 프런트(최종) | tsc 깨끗 · vitest 701 · production build 통과 |

## 교훈 (재발 방지)

- **`pytest -x` + `| tail` 은 실패를 가린다** — 첫 실패에서 멈춘 "1 failed" 가 파이프 뒤 종료 코드 0 으로 보였다.
  전체 회귀는 `-x` 없이 돌리고 종료 코드를 파일로 받는다.
- **경계 테스트는 지역 import 도 간선**(`ast.walk`). "지역 import 로 순환 회피"는 여기서 통하지 않는다 —
  공용 헬퍼가 저장소 모듈을 필요로 하면 호출자(라우터)가 값을 읽어 내려준다.
- **권한 검사는 "실제로 저장될 것"에**. 후보 전체에 걸면 정상 요청(잉여 조상 포함 선택)을 깨뜨린다(리뷰 ①).

## 볼트

Codex 레인 D 판정을 Codex 가 `--sandbox workspace-write` 로 직접 반영(수정 12·항목 추가 4·신설 4,
밀려난 원문 49개 `## 지난 판단` 보존). Claude 가 사실 2건을 직접 검증해 넣었다 — cp949 에 없는 것은
em dash·en dash·불릿, 곡선 따옴표·말줄임표·→ 는 있다 / `\u` 이스케이프는 정확히 4자리. 독립 검증: 46=46,
유령 링크 0, 프론트매터 누락 0. **볼트는 커밋하지 않았다**(다른 세션의 미추적 MV-ch 페이지 4개 포함 — Jay 판단).

## 남은 것 · 하지 않은 것

- `main` 병합 안 함(Jay 요청 시). 실서버 반영은 다음 릴리스 절차로.
- 통계 `ALERT_COMMENT_*` 상수 자체의 가시성 통합은 하지 않았다(4곳 공유 상수 — 카드 배지는 목록이 이미
  가시성 스코프라 노출 없음). 필요 시 별건.
- 8시간 soak 은 이번 범위 밖. 100명 부하는 같은 날 오전의 실측 재검증(아래 절)에서 다시 쟀다.

## 실측 재검증 (같은 날 오전 — Codex gpt-6-astra 4레인 실행, Claude 원본 대조)

리뷰 라운드의 수정이 실제로 도는지 Codex 가 dev(`208dcb72`)에서 직접 실행·측정하고, Claude 가 원본 로그·JSON 으로
다시 셌다. A(백엔드 회귀)·B(프런트)·D(격리 서버 HTTP 실측)를 병렬로, C(100명 부하)는 CPU 경합을 피해 단독으로.
Jay 의 dev 세션(8012)·`data_test` 는 건드리지 않았다.

| 레인 | 결과 |
|---|---|
| A 백엔드 전체 회귀 | 샌드박스 안 1,938 통과·5 실패 → 5건 전부 프로세스 권한 제약(CIM·`taskkill` 거부·로컬 git push). 비샌드박스로 같은 6개 파일 재실행 **126/126 통과**. 코드 회귀 없음 |
| B 프런트 | build 2.23초 · vitest 101 파일 / **701 통과** · 아키텍처 lint 위반 0 |
| D 격리 서버 HTTP 실측 | 임시 계정 3·생성물 24·요청 152회, 판정 51개 중 49 통과. 2건은 요청서 기대값이 계약과 달랐던 것(없는 생성물 PUT → 200 `recorded:false` 가 현행 계약, `/api/ready` 는 `status:"ready"`). Last viewed(씬/`@team` 분리·400·소유자 분리)·계보 부모 404·`?tab=invalid`=my·알림 가시성(멤버 제거·공유 해제 뒤 목록·unread 0, 모두 읽음 뒤 잔존 0)을 **실제 HTTP 로 확인** |
| C 100명 격리 부하(게이트 조건: 2코어·below-normal·60초×2) | **acceptance 17/17**. 2회차 11,560건 전부 200 · 190.47 RPS · p50 9.27 / p95 36.87 / p99 181.6ms · 로그인 p95 7.8초 · WS 100 · 롱폴 100 · SQLite 잠금 0 · RSS +2.01%(최대 260.7MiB / 상한 512) |

07-31 기준(186 RPS · p95 61.6ms) 대비 회귀 없음. 08-14(p95 23.3ms)보다 높지만 조건(4코어·TLS·8시간)이 달라 비교 대상이 아니다.

### 실측이 드러낸 결함 2건 (둘 다 도구·테스트 — 앱 코드 아님)

- `test_gate_reads_the_real_update_state_file` 이 `UPDATE_STATE_BASE` 를 격리하지 않아 매 실행마다 실제
  `%LOCALAPPDATA%\MVHub\updates` 에 `update-*.json` 을 남겼다(60개 확인). 쓰기가 막힌 샌드박스에서는 Windows `mkstemp` 가
  PermissionError 를 이름 충돌로 보고 무한 재시도해 "멈춤"으로 보였다. → `271c58a2` monkeypatch 격리(`test_release_update` 와
  같은 방식). 남은 파일 60개 삭제는 Jay 확인 후.
- `tools/endurance_probe.py` 의 백업 래퍼가 BK-1(`5b68cee7`) 이후 바뀐 `_backup_once(scope) -> bool` 을 못 따라가 TypeError —
  프로브에서 백업이 한 번도 돌지 않았다(08-14 소크 이후 첫 재실행에서 발견). → 인자·반환값 통과로 수정, 120초 재실행 verdict passed.

### 교훈

- Codex 실행 레인은 `workspace-write` 샌드박스가 LOCALAPPDATA 쓰기·자식 PID 조회/종료를 막아 **가짜 실패**를 만든다 —
  프로세스를 띄우는 레인은 full access 로, 샌드박스 실패는 비샌드박스 재실행으로 걸러 확정한다.
- Codex 는 요청서의 기대값이 계약과 다르면 "실패"로 적는다 — 판정은 원본 JSON 으로 다시 센다.

## 캔버스 손질 3건 (같은 날 오후 — Jay 요청, Codex 3회 적대적 코드 리뷰) `5123f0c4`

| 요청 | 결정·구현 |
|---|---|
| 리스트 순서가 오락가락 | 원인: `edge.order` 없으면 소스 카드 y·x 로 정렬 → 카드를 옮기면 순서가 바뀌고, 한 번 순서를 바꾸면 고정. → **항목 순서 = 연결한 순서**(`sortByConnectionOrder`). 두 축: ① 새 연결은 연결 시점에 order(`withCollectorOrder`) ② 기존 씬은 열 때 옛 표시 순서를 order 로 한 번 고정(`freezeLegacyCollectorOrder`) — 생성 카드 refs(@image 번호)는 옛 순서를 보존하므로 표시만 바꾸면 화면 번호와 실제 제출이 어긋난다(Codex P1). fan-in 도 표시 항목 순번(중첩·무선 포함) |
| 'c' 자동연결 방향 | 텍스트+생성은 항상 텍스트→생성(예전엔 생성이 왼쪽이면 뒤집힘). 레퍼런스/빈·레퍼런스 리스트→텍스트, 텍스트→텍스트 리스트, 리스트를 거치는 사슬이면 직접 연결 생략, 리스트를 혼합으로 만드는 연결 금지(반대 방향 유효 시 그쪽), 텍스트 리스트→생성 고정. `lib/sceneAutoConnect.ts`(순수) + 테스트 21개. 텍스트 없는 조합은 예전 층 규칙 그대로 |
| Set 노드 | 기본 160×136. 생성 카드에 **세트 전용 4번째 입력 포트**(금색, role `"set"`, 저장된 role 무시). 포트는 카드별 레인 목록을 세로 중앙에 대칭(-39·-13·+13·+39), 짧은 카드는 간격 축소 — 포트와 선 끝점이 같은 `laneDelta(lane, card)` |

Codex 리뷰 3회에서 잡힌 것: 기존 씬 refs 불일치(P1) · 새 연결까지 y 순 재고정(P1) · T·G·빈 L 순환 · 레퍼런스·텍스트·렌더 빈 계획 ·
텍스트 리스트→생성 차단 · comfy 혼합 · 중첩/무선 fan-in · 작은 카드 포트 이탈. 전부 반영·테스트로 고정. 검증: tsc · vitest 734 · build · lint 0.
후속(Jay 이미지): 렌더가 선택에 있으면 comfy 를 생성과 같은 층으로 봐 **New·comfy 출력이 모두 렌더 입력으로**(예전엔 comfy→New 로 새어 렌더는 New 만 받음). 렌더 없으면 comfy→New 그대로.
Codex 4차(후속 2건 대상): 폴백·렌더 규칙이 기존 연결과 순환을 만들 수 있고(실행 계획에서 comfy 가 빠짐), comfy 에 텍스트가 리스트 경유+직접으로
중복 투입 → 계획의 모든 연결에 **순환 검사**(기존+계획 엣지), comfy 도 텍스트 리스트를 거치면 직접 연결 생략. 테스트 3개 추가(총 26), vitest 739.
Codex 5차: 순환 검사가 무선(Input→Output) 경로를 못 봄 → `resolvePortEdges` 로 해석한 엣지도 따라간다. 테스트 1개 추가(총 27). 5차 판정 외 남은 지적 없음.
손 확인: Vite 라이브 세션에서 리스트 연결 후 카드 이동 → 순서 유지 / 텍스트+New 에 C → 텍스트→New / New 맨 아래 금색 포트로 Set.

## Seedance 2.5 `mode` 누락 — 레퍼런스 붙인 제출이 t2v 로 거부 (같은 날 늦은 오후 — 팀원 로그, Codex 리뷰)

**증상**: 에이전트 로그 `CLI 실패: mode 't2v' does not accept reference media — cmd: generate create seedance_2_5 … --image-references …`.
명령줄에 `--mode` 가 없다(payload 는 `sort_keys` 저장이라 플래그가 알파벳순 → `generate_audio`·`resolution` 사이에 없으면 params 에 없던 것).

**원인(실측 2건)**: ① 힉스필드 잡 기록(`generate list --video --json` 50건)의 Seedance 2.5 params 에는 `mode` 키가 **없다** — 레퍼런스 10장 잡도.
mode 는 제출 스위치일 뿐 기록되지 않고 힉스필드는 붙은 미디어로 판단한다. ② 우리 동기화(`repo/generation_sync.py`)가 기존 행 params 를 그 값으로
**통째로 덮어써** 우리가 제출한 `omni_reference` 도 사라진다(로컬 DB seedance 생성물 79건 전부 mode 없음). → 옛 params 를 복사하는 경로
전부가 `--mode` 없이 제출: 재생성(`gen_recipe` 가 부모 params) · 레시피→씬 모델 카드(`recipeScene`) · 모델 카드 연결 시 하단 바 통째 교체
(`applyModelCfg`) · 캔버스 실행(`resolveSceneGenerationModel` 노드 params 그대로) · 8-13 이전 저장 모델 카드(`t2v` 명시). 하단 바 새 생성만
안전(`MODEL_DEFAULT_OVERRIDE`). 화면은 빈 값일 때 기본값을 대신 보여줘 "omni 로 켜져 보이는데 t2v 로 나감" 으로 보였다.

**수정(세 겹, 최소 변경)**:

| 겹 | 파일 | 규칙 |
|---|---|---|
| ① 제출 직전 보정(필수) | `agent_push.py` `_seedance_mode_for_refs` | 스키마 mode enum 에 `omni_reference` 가 있는 seedance 만. 레퍼런스 미디어(@start/@end 제외) 있음 + mode 없음/`t2v` → `omni_reference`; 없음 + `omni_reference` → `t2v`; `video_edit`·`video_extension` 은 그대로. `_param_flags` 를 `_refs_for_cli` 뒤로 옮겨 플래그·제출 지문 모두 보정값. 스키마 캐시를 `_MODEL_PARAMS_CACHE`(params 목록)로: 성공은 영구, **실패는 60초 TTL 로만 기억**(전엔 빈 집합을 영구 박제 → 세션 내내 필터·보정 꺼짐), 모델별 락으로 같은 모델 동시 조회는 하나, 메인 루프의 사전 조회 제거(워커 안에서 조회 → 60초 타임아웃이 진행 조회를 막지 않음). `_param_enum` 은 캐시만 읽어 CLI 호출 수 불변 |
| ② 동기화 합치기 | `repo/generation_sync.py` `_merged_params` | 기존 행 갱신 시 `{**stored, **incoming}` — 힉스필드가 안 주는 키(mode)는 우리 값 유지 |
| ③ 프론트 기본값 병합 | `useModels.withEffectiveDefaults` · `SpotlightPrompt.applyModelCfg` · `recipeScene.withModelDefaults` | 같은 모델 분기도 기본값 위에 저장값(모델 카드 편집창과 같은 규칙) · 레시피→씬은 `MODEL_DEFAULT_OVERRIDE` 만 정적으로 채움 |

테스트: `test_agent_contracts`(순수 3 + `_submit_one` 종단 — 명령줄 `--mode omni_reference`·`--image-references`·지문, 반대 t2v) ·
`test_generation_sync`(합치기) · `recipeScene.test`(+1) · `useModelsDefaults.test`(2). 검증: 백엔드 전체 pytest · 프론트 tsc · vitest 743 · eslint 0.
Codex 적대적 리뷰 3회: 1차 P2 — 스키마 조회 실패를 캐시하지 않아 동일 모델 8건 배치에서 `model get` 9회(사전 1+워커 8, 타임아웃이면 제출까지 +120초·진행 조회 정지) → 실패를 60초 TTL 로 기억 + 조회 단일화. 2차 P2 — 전역 락이라 혼합 모델 배치(A·B 각 60초 타임아웃)에서 직렬 대기 뒤 A 의 TTL 만료로 재조회(최대 240초) → 모델별 락 + 메인 루프 사전 조회 제거. 3차 판정 머지 가능·남은 문제 없음. 제출 지문은 별도 저장이라 서버 `_expected_create_recipe` 와 충돌 없음(Codex 확인).
★에이전트는 팀원 PC 배포 파일 — 릴리스+업데이트 뒤에야 팀원 PC 에 적용. 임시 처방: 모델 카드 더블클릭 → 모드 `omni_reference` → 저장.

## 폴더 우클릭 메뉴 — '팀에 공유' / '최종 경로로 저장' (같은 날 저녁 — Jay 요청, Codex 설계 검토 1회 + 코드 리뷰)

**요구(Jay)**: 사이드바 폴더 트리(라이브러리·캔버스 공통)에서 폴더를 우클릭하면 폴더 이름과 버튼 하나. 내 작업 탭은 "팀에 공유" —
확인창 뒤 그 폴더(하위 폴더 포함)의 결과물을 모두 팀에 공유. Share&Review 탭은 "최종 경로로 저장" — 최종 경로(프로젝트 렌더 폴더)에
폴더 구조대로 저장. 버튼 색은 폴더 자체의 아이콘 색(최상위 하늘색·하위 라임), 선택 분홍과 무관.

**설계·구현**

| 층 | 내용 |
|---|---|
| 트리 | `FolderTreeView` 에 `onContextMenu(path, name, depth, e)` — 브라우저 메뉴 대신, 선택(onSelect)·드래그 상태는 건드리지 않음. 사이드바만 씀(어셋·관리자 트리는 무변) |
| 메뉴 | `ProjectSection` 이 body 포털·고정 위치로 그림(`lib/folderContextMenu.ts` 순수 규칙: 탭→동작·깊이→색조·확인 문구·가장자리 보정). Esc 캡처·소비, 바깥 mousedown, 드래그 시작, 탭 전환에 닫힘. 열리면 버튼에 초점, Esc·확인창 뒤엔 연 행으로 초점 복귀(body 로 빠지면 `d` 가 전역 카드 단축키로 샘) |
| 공유 | `POST /api/publish-to-shared/folder {project_id, folder_path}` — 후보 조회부터 마지막 배치까지 한 계정으로 고정(`publish._pinned_account_scope` — share 의 것과 같은 규칙, share 를 import 하면 순환이라 로컬 사본. 아키텍처 경계 테스트가 잡았다). 로컬 허브 DB 에서 후보(`repo.folder_share_candidates`: 폴더 접두·done·휴지통 아님·미공유·**모든 모드에서 is_mine 규칙**)를 뽑아 200건씩 기존 `publish_to_shared` 경로로 순차 발행. 중간 실패는 앞선 성공 보존 + `error`·`unprocessed`, 자동 재전송 없음, 같은 폴더 동시 실행 409. `_proxy._LOCAL_EXACT` 에 등록(안 하면 서버로 프록시돼 0건) |
| 저장 | `POST /api/manage/save-finals?project_id&folder_path`(선택) — 저장 대상(final_export 판정 그대로: 최종 컷+done)을 폴더 접두로만 좁힘. 경로·멱등·잠금·관리 권한 그대로 |
| 배선 | `useGenerationShareActions.folderShare`(성공 수=`accepted`: 프록시는 배치별 서버 수락 수 합산, 비프록시는 로컬 표식 수) · `manageApi.saveFinals(projectId, folderPath?)` · `App.onFolderAction` → `FilterSidebar`·`CanvasFolderSidebar` |

Codex 설계 검토(P1 2·P2 3) 전부 반영: 프록시 exact 누락 · 비계정 모드 소유자 판정 · 대량 분할 · 응답 수치(후보≠성공) · 메뉴 열린 동안 단축키.
열린 질문 답: 최종 저장은 **최종본만**(렌더 폴더 오염 방지) — 확인 문구에 명시.
테스트: `backend/tests/test_folder_share.py`(9: 후보 판정·분할 합산·중간 실패 보존·409·0건 정상·로컬 경로 등록·폴더 필터·프록시 서버 수락 수 합산·계정 고정) ·
`frontend/tests/folderContextMenu.test.ts`(4). 검증: 백엔드 전체 pytest · 프론트 tsc · vitest 747 · 빌드 · eslint 0.
Codex 코드 리뷰 2회: 1차 '수정 후 머지' — P1 배치 사이 계정 전환(후속 배치가 B 계정 DB·토큰을 읽음) → 계정 고정 블록으로 후보 조회~마지막 배치 고정 · P2 성공 수 과대(attempted−blocked 가 배치에서 제외된 후보까지 셈) → `accepted`(프록시 배치별 remote_accepted 합산) · P2 메뉴 닫힌 뒤 초점이 body 로 빠져 `d` 가 전역 카드 단축키로 샘 → 연 행으로 초점 복귀. 2차 '머지 가능·남은 문제 없음'. 반영 뒤 아키텍처 경계 테스트가 publish→share 순환을 잡아 계정 고정 헬퍼를 publish 로컬 사본으로 바꿈.
★Jay 실행 세션: 프론트(Vite)는 즉시 반영, 백엔드(8012)는 재기동해야 새 API 가 뜬다. 관리 권한 없는 사용자의 '최종 경로로 저장'은 서버 403 사유를
그대로 보여준다(프론트 Project 타입에 역할 정보가 없어 버튼 비활성은 미구현).

## 캔버스 '폴더 보기' 플로팅 창 (같은 날 밤 — Jay 요청, Codex 코드 리뷰)

**요구**: 캔버스(활성 씬)에서 사이드바 폴더를 눌러도 내용이 안 보인다(계보 보드는 씬이 없을 때만). 폴더를 고르면 가운데 플로팅 창에
라이브러리 격자(툴바 필터·날짜 구분 그대로)를 띄워 워크스페이스로 가지 않고 그 폴더에 뭐가 있는지 바로 본다.

**구현(App.tsx + `useGenerationLibraryData` + CSS, 새 API 없음)**: `folderPeek` 상태 — `CanvasFolderSidebar` 폴더 선택 시 활성 씬이면 열림. compose 탭은 평소 목록을 안 받으므로 창이 열려 있을 때만 훅이 목록·추가 페이지를 조회(`composeListEnabled`, 서버 tab 은 my|team 뿐이라 `tab: "my"` 로 정규화), 열릴 때·필터 바뀔 때 목록 소유 탭을 compose 로 넘긴 뒤(`beginComposeList` — 그냥 비우면 캐시 동기화가 이전 탭 캐시를 빈 목록으로 덮는다) 비우고 다시 받는다. 라이브러리 본문의
`<ThumbnailGrid>` JSX 를 `thumbnailGrid` 변수로 빼 라이브러리 탭과 창이 **같은 요소**를 쓴다(두 분기는 동시에 마운트되지 않음, `tab` 은
compose→my). 데이터는 폴더 클릭이 이미 패치하는 `filters.project_id/folder_path` → `gridGens` 그대로라 상단 캔버스 툴바의
타입·컬러·태그·공유·코멘트·골드·회색 필터가 동일하게 먹는다. 닫기: ✕·Esc(미리보기/정보 팝업이 위에 있으면 양보)·바깥 클릭·씬 전환·탭 이탈·
프로젝트만 선택. 창은 열릴 때 초점을 받고(`tabIndex=-1`), 창 안 Esc 는 직접 닫고 나머지 키는 stopPropagation 으로 캔버스 전역 단축키에 안 샌다(미리보기/정보 팝업이 위에 있으면 Esc·화살표는 그 팝업으로 흘려보냄). z 150/151(씬 패널 20 위·씬 모달 200 아래) — 등급 확인창(60/61→160/161)·코멘트 패널(82→165)을 창 위로 올렸다.
**2차 요청(Jay)**: 창에도 라이브러리 툴바(타입·컬러·태그·공유·코멘트·골드·회색·크기·배치)를 넣고 워크스페이스처럼 조작 — 라이브러리 툴바·선택 액션바 JSX 도 변수로 빼 창과 공유(`libraryToolbar`·`librarySelectionBar`, 창 바닥에 액션바). 단축키: 씬 단축키·붙여넣기 판정(`isSceneTextEntryTarget`)이 `.folder-peek` 안 키를 무시하고, 라이브러리 색·비활성 단축키(`useGenerationKeyboardActions`)는 창이 떠 있으면 compose 에서도 동작(`composeGridActiveRef`). 창의 Esc 는 팝업 → 텍스트 입력 → 선택 해제(슬라이더 등 비텍스트 입력 초점 포함) → 창 닫기 순. compose 의 라이브러리 단축키는 키 타깃이 창 안일 때만(창 밖은 씬 몫 — 이중 실행 방지). 창이 열리면 캔버스 상단 툴바는 태그 패널을 창 툴바에 양보하고, 창은 transform 없이 inset 으로 가운데(창 안 고정 패널이 화면 좌표를 쓰게).
검증: tsc · vitest 747 · 빌드 · eslint 0. 순수 로직이 없어 테스트는 추가하지 않았다.
Codex 코드 리뷰: 창 본체 3회(1차 P1 목록 미조회·P2 3, 2차 P2 2, 3차 머지 가능) + 툴바·단축키 증분 2회(1차 P2 3 — 창 밖 단축키 이중 실행·태그 패널 중복/잘림·슬라이더 초점 Esc → 타깃이 창 안일 때만·상단 툴바 태그 패널 양보+transform 제거·section 이 선택 해제까지 직접, 2차 머지 가능·남은 문제 없음) + 트레이 수정 확인 1회(머지 가능).
**후속 버그(Jay)**: 창에서 '프롬프트 재사용' 뒤 창을 닫으면 레퍼런스 알약이 ⚠ — 원인은 캔버스에서 씬 카드가 선택되지 않았을 때의 트레이 바인딩이 매 렌더 새 빈 배열(`refs: []`)을 줘 `useSpotlightTray` (A) 동기화가 App 재렌더마다 다시 돌아 재사용 refs 를 비우던 것(창 닫힘은 재렌더의 한 예). 모듈 상수 `EMPTY_SCENE_REFS` 로 참조 고정.

## '관리 요약' 사용량을 팀 기록 장부(팩트)로 (같은 날 밤 2 — Jay 결정, Codex 설계 검토 1회 + 코드 리뷰)

**발단(Jay)**: 관리 화면이 "공유된 생성물만 세고, 폴더에 넣어 둔 미공유 생성물의 크레딧·기간은 안 잡힌다". 검토 결과 데이터 경로가 둘이다 —
① 콘텐츠 경로(`/api/manage/summary`·`project-summary` → 서버 content DB `generation` = **발행분만**), ② 텔레메트리 팩트(`manage_hub.db::team_generation_fact`,
각 PC 허브가 공유 여부와 무관하게 자동 push). '관리 요약'(프로젝트 요약 카드·에피소드·시퀀스 표)은 ①을 읽고 있었다.
**실측(프로덕션 앱 8010 → 공유 서버 프록시, 읽기 전용 GET)**: 뻘뻘뻘 = ① 455건·21,209cr·생성시간 0 vs ② 653건·23,605cr·생성시간 50.8h(9명).
e020(오지짱) = ① 73건·16폴더 중 크레딧 있는 폴더 1개(153cr) vs ② 74건·17폴더 전부(8,451cr). 이유: 공유 번들(`share.export_bundle`)에 metrics 가 안 실리고
서버는 자체 거래 대조만 하는데 대부분 실패, 생성시간은 ①에 아예 없다. 9월 팩트 1,519건·12명 도착(전송 살아있음). Jay 결정: **"출근부 장부(팩트)로 봐야 한다."**
개발 세션(`test_dev_server.bat` = data_test + NO_PROXY 격리)은 로컬 DB 를 읽어 미공유 컷이 이미 보였고, `WorkBoard` 부제 "공유 서버 작업 기록"은 고정 문구라 오해를 부른다(미수정).

**구현**: `manage_db.fact_usage(workspace_id, project_ids, budget_periods)` — 한 읽기 스냅샷(`BEGIN`)에서 프로젝트 stats(gen/done/final/real/credits/
metric·**credit_real/est/unknown_count**·elapsed)·모델·예산 주기 모델(day/week/month CASE 를 content 식 그대로)·폴더×작성자×모델 원시행·작업자를 센다.
크레딧 = `COALESCE(real, est, 0)`·tombstone 포함·워크스페이스 = team+id(team_overview 와 동일). `created_start/end` 는 `datetime(created_at,'localtime')` 로
통일(팩트 created_at 은 'YYYY-MM-DD HH:MM:SS' 와 'YYYY-MM-DDTHH:MM:SSZ' 가 섞여 문자열 MIN/MAX 가 같은 날 안에서 뒤집힌다 — 코덱스 P2). 인덱스
`idx_tgf_project_folder(project_id, folder_path)`. `repo/manage.py`: `dashboard_summary`·`project_dashboard_summary` 가 사용량 전부를 팩트로, content 는
레지스트리·planning·**공유(발행) 수(share 표)**·빈 시퀀스(project_task)·표시이름 폴백·출력타입/영상길이·거래 입출에만. 이동 프로젝트 판정도 팩트 pid 집합.
`_project_model_breakdowns` 제거(죽음). 응답에 `usage_source: "facts"`. 라우터: 두 요약 모두 조회 직전 `_refresh_isolated_telemetry()`(격리 test_dev 최신
반영, async 는 `to_thread`). 프론트: `manage/usageSource.ts`(라벨·커버리지 순수 함수) — 출처 라벨 "팀 기록 장부(공유 무관·삭제분 포함·각 PC 보고분)"(구서버는 종전
문구), 크레딧(사용) 호버에 "실제 N · 견적 M · 미상 K", 미상>0 이면 셀에 "· 미상 K"(0원이 아니라 '모름').
**노출 범위(Codex P1 → Jay 결정 2026-09-10 아침)**: 처음엔 멤버도 미공유분 포함으로 두었으나, Jay 가 "팀원의 공유 안 한 작업은 매니저만" 으로 좁혔다 —
`project-summary` 는 일반 멤버(read_all 없음)에게 **내 작업 전부 + 팀원의 공유분** 만 SQL 에서 센다(`fact_usage(viewer_uid=…, shared_job_ids=…)`), 매니저는 전부.
공유분 판정은 팩트의 `is_shared`(PC 보고값 — 공유 해제·발행 직후엔 다음 보고까지 어긋남, Codex P1)가 아니라 **서버 share 표의 job_id**(임시 표로 올려 대조).
응답 `usage_scope: all | mine_plus_shared`, 라벨도 갈린다("내 작업 전부 + 팀원 공유분"). 테스트: 멤버/무작업 멤버/매니저 3범위(PC 보고와 서버 원장이 어긋난 두 방향 포함) + 권한 테스트.
**중복 정책(Codex P3, 테스트로 고정)**: 같은 계정+job_id 는 수렴, 계정이 다르거나 job_id 없는 같은 잡은 두 번 센다(team_overview 와 같은 한계).

**검증**: backend 전체 pytest · `test_manage_fact_usage.py` 6건 신설(미공유·tombstone·미상 구분·빈 시퀀스·이름 폴백·워크스페이스/허용 범위·이동 프로젝트·삭제→복원→재삭제 비용 불변·중복 정책) + 기존
`test_project_budget_period.py`·`test_manage_workspace_scope.py` 팩트 시드로 갱신(MANAGE_DB_PATH 임시 격리) · tsc · vitest 751 · 빌드 · eslint 0 ·
**격리 서버 실측**(data_test 사본, 8019): `project-summary` 뻘뻘뻘 369건·20,657cr = `team-overview` 동일, e001/c0020·c0030(미공유) 에 6cr·4cr, 기간 KST, 멤버 이름.
Codex: 설계 검토 1회(수정 후 진행 — P1 3: 멤버 노출 범위·async 차단·테스트 DB 격리, P2 5, P3 1 → 전부 반영), 코드 리뷰 1회(수정 후 머지 — P2 1: `test_identity_permissions` 가 실제 관리 DB 를 읽음 → MANAGE_DB_PATH 임시 격리 + 허용/비허용 프로젝트 팩트 시드로
범위 검증; 덤으로 관리 DB 미초기화(팩트 표 없음) 설치본에서 요약이 500 으로 죽지 않게 빈 사용량 가드). SQL 자리표시자·정렬·0/NULL·잘못된 날짜는 Codex 가 메모리 SQLite 로 검증.

**남은 것**: 리버 e003(07-23~08-07) 148건이 팩트에 크레딧 0(콘텐츠엔 9,963cr) — 리버 PC 거래 대조 실패 추정, 별건 조사. 공유 번들에 metrics 싣기(콘텐츠 경로
보강)는 선택. '작업' 탭은 여전히 content 파생(발행분). `WorkBoard` 부제 고정 문구. 서버 배포 전엔 팀원 화면은 구서버 응답(종전 라벨) 그대로.

## 사이드바 우클릭 '모두 확인' — 공유&리뷰 +N 배지 일괄 확인 (2026-09-10 아침 — Jay 요청, Codex 코드 리뷰)

**요구**: 공유&리뷰 탭에서 프로젝트 행("뻘뻘뻘 +406 411")을 우클릭하면 "모두 확인" 단추가 나오고, 누르면 새로 들어온(+N) 항목을 전부 확인한 것으로.
**구조(기존)**: +N = 서버 `team-fresh`(기준선 이후 공유 항목, `ack_key=COALESCE(job_id,id)`·`shared_at`)에서 내가 확인(카드 클릭)한 항목을 뺀 수. 확인 기록은
계정별 localStorage(`teamSeen.ts`, 항목 단위 ack 모델) — 서버 호출 없음.
**구현**: `teamSeen.ackTeamFreshKeys(entries)`(여러 항목 한 번에, 변경 있을 때만 저장·bump 1회) · `folderContextMenu.freshItemsInScope`(path ""=프로젝트 전체
(폴더 미지정 포함), 폴더=하위 포함, 확인분 제외)·`folderMenuHeight`·`ackAllLabel` · `ProjectSection`: 프로젝트 행 `onContextMenu`(팀 탭 + +N>0 일 때만, depth -1)
→ 기존 폴더 우클릭 메뉴 포털 재사용. 메뉴: 폴더 행 = 탭별 단추(공유/최종 저장) + "모두 확인 (+N)", 프로젝트 행 = "모두 확인"만. 확인 창 없이 즉시(로컬 기록,
재공유되면 다시 새것). 단추 색 = +N 배지 라임(`tone-ack`). 카드 글로우도 같은 저장소라 함께 꺼진다.
검증: tsc · eslint 0 · vitest(folderContextMenu +2, teamSeen +1) · 전체 vitest · 빌드. Codex 코드 리뷰 1회(머지 가능 — 행 이벤트·포털 닫기·초점 복원 충돌 없음, 500건 페이지 전체 수집, 글로우와 같은 저장소 구독 확인).

## 대시보드 열람 범위 — 멤버=내 사용량 · 매니저=팀 전체 (2026-09-10 오전 — Jay 결정, Codex 설계 검토 1회 + 코드 리뷰)

**발단(Jay)**: 오지짱 계정(테스트 사본에서 member 만 남김)으로 들어가 보니 '프로젝트 요약'이 팀 기록 장부 형식 그대로였다. 결정 — "팀 기록 장부는 매니저만.
일반 멤버는 프로젝트 요약에서 **내 작업물만**. 그 대신 멤버에게 숨겨 두었던 '사용 현황' 구역도 **내 개인 사용량으로** 보여주고, 매니저는 지금처럼 전체."
같은 날 아침의 중간 규칙('내 것 + 팀원 공유분')을 대체한다. 프로젝트 요약의 '크레딧(한도)'(팀 전체 예산) 열과 '멤버' 열(인원·역할 툴팁)은 남기고 그대로(Jay).

**왜 싸게 되나**: 사용 현황 패널이 부르는 `team-overview`·`team-timeseries`·`usage-export` 는 이미 `creator_uid` 필터(매니저의 작업자 드릴다운)를 받는다.
멤버용 '내 사용량' = **서버가 범위를 강제한 같은 화면**. 새 API·새 화면 없음.

**구현**: 라우터 `_usage_viewer(request)` → None(AUTH off·read_all) 또는 `(account_scope_uid, norm_email(세션 이메일))`. 네 라우트(team-overview·timeseries·export·workspaces)가
`_require_manage_read` 대신 이걸 쓴다 — 멤버는 쿼리 `creator_uid` 를 버리고 `manage_db._viewer_clause` 가 `creator_uid=? AND account_email=?` 를 `_agg_where` 에 붙인다.
`project-summary` 도 같은 쌍을 `_usage_scope_where` 에 붙이고(temp.shared_jobs 대조 코드 제거), 멤버의 공유 수는 팩트 `is_shared`(같은 WHERE)로. `/workspaces` 는 멤버에게
`workspace_member.account_email=본인 AND is_available=1` 인 것만(`list_workspace_options(member_email)`), 이메일 없으면 빈 목록. 응답 `usage_scope: all | mine`.
프론트: `DashboardView` 의 read_all 게이트를 둘로 — 패널은 항상 렌더(`scope`), 전체 요약 API(summary) 만 매니저. `WorkspaceUsageDashboard(scope="mine")`: 제목 '내 사용 현황',
"N members" → '내 기록', 통계 격자에서 멤버·인원당 평균 → 프로젝트 수·생성 시간 합, 멤버 카드 숨김(첫 줄 모델 카드 한 칸 `usage-two-columns.single`), 구서버 403 이면
"서버 업데이트 뒤 표시" 안내, 정상 0건이면 "아직 보고된 내 기록이 없습니다". 프로젝트 요약: 한도 툴팁에 "팀 전체 한도 (내 사용과 기준이 다름)", 범례 '사용=내 누적 크레딧',
출처 라벨 "팀 기록 장부 (내 작업만 · 삭제분 포함)".

**Codex 설계 검토(수정 후 진행 → 전부 반영)**: P1 ① **uid 하나로는 부족** — `ingest` 가 미링크 계정을 요청 본문의 creator_uid 로 연결하므로 남의 uid 로 연결한 뒤 조회할 수 있다 →
팩트 `account_email`(push 때 세션 이메일로 강제)까지 동시 대조. ② 세션 uid 가 `__none__` 이면 `_agg_where` 가 `IS NULL` 로 해석 → 권한용 값은 특수 해석 없이 문자열 그대로.
P2 ③ 공유 수가 팀 전체 값으로 남음 → 본인 것만. ④ 구서버 403·정상 0건 구분 안내. ⑤ "미링크=\x00" 전제 정정(실제는 `acct:이메일` — 어느 쪽도 팩트와 안 맞아 빈 결과).

**검증**: `test_manage_my_usage.py` 8건 신설(남의 uid 필터를 보내도 본인만·세부 배열 전부·매니저는 종전·AUTH off 전체·uid 만 맞고 이메일 다르면 0·`__none__`·미링크·timeseries/export·
workspaces 소속/미소속/이메일 없음) + `test_manage_fact_usage`(멤버=내 것만 5건·22cr, 공유 수 1, 이메일 불일치 0)·`test_identity_permissions`(river 1건·5cr·mine) 갱신.
backend pytest 전체 · tsc · vitest 756 · 빌드 · eslint 0.
Codex 코드 리뷰(수정 후 머지 → 반영): P1 없음, P2 1 — 공유 수를 content 원장에서 uid 만으로 세면 uid 위조 계정에 남의 `shared_count` 가 샌다(메모리 DB 재현)
→ 멤버 공유 수는 팩트 `is_shared` 합(uid+이메일 절 안)으로, content 원장은 매니저만. 이메일 불일치 테스트에 `shared_count == 0` 추가.

**남은 것**: 서버 배포 전엔 팀원의 멤버 뷰는 구서버 403 → 안내 문구만 보인다(프로젝트 요약은 종전 응답 그대로). 매니저 화면은 변경 없음. dev 8012 는 백엔드 재기동 필요.
