---
status: active
updated: 2026-09-20
owner: Claude
reviewer: Codex
touched_paths:
  - frontend/src/lib/useEscapeClose.ts
  - frontend/tests/escapeCloseNesting.test.tsx
  - docs/기능설명서.md
  - docs/사용설명서.md
  - docs/TESTING.md
  - docs/CODE_MAP.md
---

# 실시간 브라우저 실측 — 전 기능, Claude·Codex 각자 (2026-09-19)

Jay 지시: "실시간 브라우저 실측. 클로드·코덱스 모두. 모든 기능. 문서(옵시디언)·graft 로 잘 정리·관리되는지 점검하고, 빠졌거나
낡은 정보는 모두 반영." + "서로 정보를 모았다가 코덱스와 함께 검토한 뒤 정리."
절차: ① 각자 독립 수집 → ② 모인 정보를 Codex 와 합동 검토(소스 대조) → ③ 정리(문서·수정).

## 환경 — 운영 서버·유료 호출·사용자 데이터에 닿지 않았다는 근거

방법은 [TESTING.md](../TESTING.md) "격리 브라우저 실측"에 남겼다. 요점:

- `backend/data_test/db` 의 **읽기 전용 스냅샷 복사본**(돌고 있던 다른 세션의 `test_dev` 8012/5173 은 건드리지 않음), 토큰 삭제,
  공유 서버 주소 = 죽은 주소, `NO_PROXY=1`·`SERVER_SYNC=0`·`EXTERNAL_RECOVERY=0`, 루프백 전용, CLI 폴더를 뺀 `PATH`.
- 격리 증명: 두 서버 `/api/health` 의 `cli_available=false`·`remote_realtime=stopped`, 서버 로그에 `192.168.`·`higgsfield` 0줄.
- 허브의 팀 서버 로그인은 **내 격리 서버로만** 갔다 — 격리 S 로그에 루프백발 `POST /api/auth/login` 1건, 허브 설정에 루프백 주소 저장,
  비-루프백 연결 0.
- Claude 쪽은 모든 비-GET 요청을 기록했다 — `POST /api/gen-requests` **0건**. Codex 도 자기 서버 로그로 0건을 확인했다(Codex 보고).
- 누르지 않은 것: Generate · `Alt+Enter` · 카드 ↻ · Comfy 실행 · Resolve로 보내기 · 업데이트 · HF 체크 · Assets 파일 조작.

규모: Claude = S 4회(88+71+73+31단계)·허브 17단계·원인 추적 프로브 5개. Codex = 2라운드(1라운드는 쓰기를 전부 피해 얕았고,
"임시 복사본 안의 쓰기는 안전하다"를 명시한 2라운드에서 카드·공유·휴지통·프로젝트 배정까지 쟀다).

## 통과 (양쪽 확인)

로그인 · 허브 로그인 게이트→서버 주소 변경→로그인 · 그리드/리스트·썸네일 크기·타입 눈금 · 툴바 필터(골드·R/G/B·S·T·C·워크스페이스) ·
검색(3자 이상 FTS / 미만 LIKE) · 사이드바(미분류·휴지통·프로젝트·폴더·생성자·컬러) · 클릭 선택 / 더블클릭 미리보기(←→·Esc) / 휠클릭
정보 팝업 · `r`/`g`/`b`(칠한 직후 컬러 필터에 1장) · `#` 태그 · `c` 코멘트 작성(직후 C 필터에 1장) · Shift/Ctrl+클릭·Ctrl+A·Esc ·
선택 막대 · 공유(S)·최종(골드) · 삭제→휴지통→복구(33→32건) · 알림 센터 탭·모두 읽음 · 계정 메뉴 · 설정 · Host 콘솔 ·
프롬프트 도크(Ctrl+K·`@`·`#`·Enter 계열은 제출하지 않음·Image/Video·모델 드롭다운·레퍼런스 트레이) · 씬 추가(+ → `PUT /api/scenes/backup`) ·
기존 씬 렌더(노드 83·연결선) · Assets 분리창(읽기 + DB 메타 컬러) · PM 분리창 3탭·보드 보기.

Claude 만 확인: `d` 비활성화(라이브러리) — `.gen-cell.deactivated` + 회색 필터, 다시 누르면 원복 · 공유 탭 "전체 보기" → 카드 표시.

## 결함 1건 — 고쳤다

**페이지를 연 뒤 처음 연 관리자 창·설정 창이 Esc 한 번에 안 닫혔다**(두 번째 Esc 에 닫힘, 두 번째로 열면 한 번에 닫힘).

- 추적(페이지 로드 전에 `window.addEventListener` 를 감싸 기록): 첫 Esc 에서 App 의 키 리스너(`useGenerationKeyboardActions` —
  Esc → `clearSelect()`, 선택이 비어 있어도 새 `Set` 을 넣는다)가 먼저 돌아 상태를 바꾼다 → keydown 은 discrete 이벤트라 React 가
  **다음 리스너를 부르기 전에** 동기 렌더+효과를 끝낸다 → `App.tsx` 의 `onAdminClose` 가 렌더마다 새 함수 → 창의 `useEscapeClose` 효과가
  재구독하며 **전달 도중인 자기 리스너를 제거**(전달 중 제거된 리스너는 불리지 않는다) → 그 Esc 가 사라진다. 재구독 뒤에는 순서가 뒤집혀 정상.
- 수정: `frontend/src/lib/useEscapeClose.ts` — 콜백을 ref 로 읽고 리스너는 `[enabled, capture, consume]` 에만 묶는다(호출처 17곳이 함께
  고쳐진다. 같은 저장소의 `useSceneKeyboardShortcuts` 가 이미 쓰는 방식). Codex 가 원인·패치·시험을 검토해 합의.
- 검증: 회귀 시험을 **먼저 써서 옛 구현에서 실패**(닫기 0회)를 확인한 뒤 수정 → 통과. 프런트 전체 184파일·1,912 통과, 경계 검사·빌드 통과.
  고친 빌드로 브라우저 재실측 — 새 페이지에서 처음 연 관리자 창 3/3·설정 창 2/2 가 **Esc 한 번에** 닫힘.
- 도입 시점은 확정하지 못했다(옛 빌드로 재현하지 않음). 2026-09-18 의 `closeTopmost` 도입 이전에도 효과 deps 가 같은 불안정 `onClose`
  였으므로 그 이전부터 있었을 것으로 **추정**한다.
- 방아쇠도 없앴다(같은 날, 아래 "남은 위험 해결" R7): `clearSelect` 가 선택이 비어 있어도 새 `Set` 을 넣어 Esc 마다 App 을 다시 그리던 것.
  `useGenerationKeyboardActions` 자체의 재구독은 그대로 두었다 — 실측된 피해가 없다.

## 서로 어긋났던 것 (합동 검토로 정리)

| 주장 | 결론 | 근거 |
|---|---|---|
| Codex P1 "라이브러리 `d` 비활성화 미구현" | **오탐 — Codex 철회** | `lib/useGenerationKeyboardActions.ts:109-111`(전역 window 키 처리). Codex 는 `ThumbnailGrid.tsx` 의 요소 수준 처리만 봤고, 같은 실행에서 `c`·`#` 도 안 먹었다(키 주입 방식 문제) |
| Codex P2 "캔버스에 씬 편집기가 없다" | 씬이 0개인 계정의 화면 — 결함 아님(발견성) | `SceneBar` 는 늘 `+` 를 그린다. 문서에 "씬이 없으면 +"를 추가 |
| Claude "Assets 에 업로드가 없다(범위 밖)" | **Claude 오류 — 끌어 놓기 업로드가 있다** | `useAssetDropImport` → `POST /api/assets/upload` → **실제 폴더에 저장**. TESTING 에 경고 추가 |
| Claude "서버 주소 입력칸에 팀 기본 주소가 미리 채워져 있다" | **Claude 오류 — 그건 placeholder** | `ServerLoginScreen.tsx:34` `useState(url \|\| "")` = 현재 주소. 내 조사 스크립트가 placeholder 를 먼저 출력했다 |
| Claude "Host 콘솔은 ✕ 로만 닫힌다" | 창 안에서는 맞음 + 상태줄 Host 토글로도 닫힌다 | `ServerConsolePanel.tsx:148-157,230-235` |

## 설계대로인데 문서에 없던 것 → 문서에 넣었다

공유 중 삭제 409("먼저 공유 해제") · 삭제·실패 정리의 확인창 · 휴지통과 복구 · 공유 탭의 워크스페이스 확인 게이트와 "전체 보기" · 알림 센터의
탭·모두 읽음·모으는 것 · 선택 막대 항목 · Esc/✕ 닫기 규칙 · 설정의 네 묶음과 "더보기" · PM 창 3탭과 작업 보기 · CLI 가 없을 때 보이는 것 ·
허브 로그인 게이트와 서버 주소 변경 · 씬 자동 백업 지연(2초)·`+`·"캔버스 노드로 열기" · Assets 끌어 놓기와 단축키. 위치는 Codex 와 합의:
기능의 정본 = [기능설명서.md](../기능설명서.md), 절차·증상 = [사용설명서.md](../사용설명서.md). Codex 가 문장마다 소스 근거를 달았고,
Claude 가 위험한 문장(업로드·주소 입력칸·버튼 문구·백업 지연·Assets 단축키)을 소스로 다시 확인했다.
Codex 최종 리뷰: 코드·회귀 시험 **승인**(허용된 시험 3 passed), 문서 문장 5곳 **수정 요청** → 반영 — 메뉴의 닫기 방식(✕ 없는 메뉴는
Esc·바깥 클릭), "전체 보기"는 **접근 권한 안에서**만, 설정의 외부 프로그램에 Comfy 미회수 실행 추가·"더보기"의 범위, TESTING 의 환경변수
묶음과 CLI 차단 **확인 절차**, 이 문서의 호출처 수(17)·문서 수(77).

## Jay 결정 (2026-09-19)

- **모델 목록 오류 문구는 그대로 둔다** — `HttpError: 502: Model catalog refresh failed` 를 안내 문구로 바꾸자는 제안은 채택하지 않음(재제안 금지).
  대신 증상과 해결은 [사용설명서.md](../사용설명서.md) §8-b 에 적혀 있다.
- **실측 도구를 저장소에 보관** — `tools/browser_measure/`(`servers.py`·`cdp.py`·`walk.py`·`smoke.py`). 보관본으로 처음부터 끝까지 다시 돌려
  확인했다: 기동·격리 확인 통과 → 41단계·실패 0·생성 요청 0건 → 종료·복사본 삭제. 안전 장치(토큰·주소 제거, PATH 에서 CLI 제거)는
  `backend/tests/test_browser_measure_isolation.py` 가 지킨다. 쓰는 법은 [TESTING.md](../TESTING.md).
- **볼트화 진행** — 재사용 지식 5건을 `D:\vault\01-Wiki` 에 반영(신설 4 · 기존 페이지 고쳐 쓰기 1). Codex 의 목록 단계 교차 점검을 거쳤고
  경위는 볼트의 `log.md` 에 있다. 저장소 문서는 볼트로 복사하지 않았다(경로만).

## 관찰만 (고치지 않음)

- CLI 가 없으면 `/api/models` 502 가 목록을 다시 읽을 때마다 반복되고(1차 실측 29회), 도크 아래에 `HttpError: 502: Model catalog refresh failed`
  원문이 남는다. 격리 조건이 만든 상황이지만, CLI 없는 PC 의 사용자도 같은 글자를 본다 — 문구는 그대로 두기로 했다(위 Jay 결정).
- 서버 본체 로그인 화면에서 로그인 전인데 API 9건 이상이 401 로 콘솔 오류를 남긴다.
- 휴지통: 원격에서 사라진 영상은 `ERR_BLOCKED_BY_ORB`, `/api/media-thumb` 502.
- 미측정: 아래 "남은 위험 해결" R8 의 목록으로 갱신했다.

## 남은 위험 해결 — Codex 합동 검토 (Jay "남은 위험을 코덱스랑 같이 검토해서 해결")

Claude 가 위험별 해결안을 내고 Codex 가 소스로 검토(R1·R4·R6 동의, R2·R3·R5·R7·R8 수정 요구) → 반영.

| # | 위험 | 결론 |
|---|---|---|
| R1 | 실측 시나리오가 CSS 클래스에 기대 UI 를 고치면 실패 | **해결** — 러너가 `stale`(대상을 찾지 못함 = 시나리오가 낡음)과 `failed`(기대와 다름 = 제품 확인)를 가르고 종료 코드도 2 / 1 로 나눈다. `data-testid` 심기·클래스 이름 고정 시험은 하지 않음(제품 코드·프런트 변경을 수동 도구가 막게 된다) |
| R2 | 결과·스크린샷에 시험 DB 의 실제 이름·이메일이 남음 | **해결** — 기본 모드는 **자유 문장을 남기지 않는다**(수치·클래스 요약, 이벤트 종류·주소 경로, 고정 어휘의 실패 사유만. 단계 메모·예외·원문·검색어가 든 주소는 `--verbose` — Codex 최종 리뷰 P1), 스크린샷은 `--shots` 일 때만, `servers.py stop` 이 결과·스크린샷까지 삭제. ★실측 중 실제로 새는 것을 봤다: 계정 상태 저장 요청의 **경로 안에** 이메일이 `%40` 으로 인코딩돼 있었고 `@` 꼴만 잡던 가림을 통과했다 → 두 꼴 모두 가리고 터미널 출력에도 적용, 시험으로 고정 |
| R3 | 볼트의 "React 플러시 시점"이 관찰로만 적힘 | **해결** — react-dom 18.3.1 소스의 세 조각(`getEventPriority`/`getCurrentEventPriority`·`ensureRootIsScheduled`·`commitRootImpl`)과 HTML 규격의 microtask checkpoint 로 근거를 달되, Codex 지적대로 "항상"이 아니라 **구현이 열어 둔 경로**로 한정 |
| R4 | 도구 보관 뒤 전체 회귀 미실행 | **해결** — 아래 검증 |
| R5 | 씬 노드 단축키 "미확정" | **해결(통과)** — 완료된 생성물이 묶인 카드에서 `d` → 비활성 저장소에 그 id + 안쪽 계보 노드에 `.disabled`(회색), 다시 `d` → 원복. 2개 이상 선택 뒤 `a` → 위치 이동 + `PUT /api/scenes/backup`, `Ctrl+Z` → 원위치. 앞서 "미확정"은 결과가 없는 빈 생성 카드를 골랐기 때문(그 카드에 `d` 가 안 듣는 것은 설계). "회색 표시가 없다"는 Claude 전제도 틀렸다 — 회색은 씬 카드가 아니라 안쪽 `HistoryBoardNode` 에 붙는다(Codex). 문서에 조건을 적었다 |
| R6 | Esc 결함의 도입 시점 미확정 | **받아들임**(Codex 동의) — 현재 결함은 고쳤고 회귀 시험이 있다. 옛 빌드를 만들어 재는 비용에 비해 얻는 것이 없다 |
| R7 | `clearSelect()` 가 빈 선택에서도 새 `Set` 을 넣어 Esc 마다 재렌더 | **해결** — `setSelected(prev => prev.size ? new Set() : prev)` 한 줄. Codex 가 `selected` identity 에 기대는 곳을 전수 확인(`App.tsx` 의 `[gens, selected]` 하나뿐, 빈 Set 끼리는 값이 같다)해 동작 보존으로 판정. `resetKey`·mousedown 쪽은 건드리지 않음. ★회귀 시험은 처음에 **렌더 호출 수**를 세어 수정 뒤에도 실패했다 — 같은 상태를 돌려주면 React 는 함수를 한 번 더 부를 수 있어도 커밋하지 않는다. 재구독을 일으키는 것은 효과 재실행이므로 **커밋 수**를 세도록 고쳤고, 옛 구현에서 실패·새 구현에서 통과를 확인 |
| R8 | 안전선 때문에 재지 않은 기능 | **일부 해결** — Codex 가 소스로 "격리 DB 안에서 끝나는 쓰기"와 "누르면 안 되는 것"을 갈랐다(누르면 안 됨: **팀에 공지**=릴리스 폴더 쓰기·자식 프로세스, **DB 내보내기**=격리 폴더 밖 파일). 실측한 것: 영구 삭제(`DELETE /api/trash/<id>`, 28→27건) · 계정 상태 저장(`PATCH /api/auth/accounts/<email>/status`) · 전역 역할 칩(`PATCH /api/members/<uid>/global-roles` — 누르면 저장 요청을 보내고 성공하면 목록이 갱신된다) · PM 프로젝트 만들기(`POST /api/projects` + `PUT /api/manage/planning/<id>`) · 언어 전환(`lang=en`). 생성 요청 0건 |

**검증(R4) — Claude 실행 보고(Codex 검토에서는 재실행하지 않음)**: 프런트 전체 185파일·1,913 통과 · 경계 검사·빌드 통과 → **새 빌드**로 보관 도구의 시나리오 41단계: 실패 0 · 낡은 단계 0 · 생성 요청 0건,
`stop` 뒤 작업 폴더에 결과가 남지 않음을 확인 → 백엔드 전체 `4170 passed · 1 skipped · 실패 0`(7분 53초, `CONTENT_HUB_NO_PROXY=1`, 서버·Codex 를
모두 내린 뒤 단독 실행).

**여전히 미측정**: 실제 생성·재생성 · Comfy 실행 · Resolve 전송 · 프로그램 업데이트 · HF 체크 · Assets 파일 가져오기(실제 폴더에 쓴다) · 팀에 공지 · DB 내보내기(이상 안전선) /
업데이트 공지 작성 · 공유 서버 이름·주소 저장(허브 전용 화면) · PM 작업 생성·편집 · 크레딧 플랜·그룹 한도·허용 모델 저장 · 강조색(선택자로 변화를 확인하지 못함)(이상 시간 안에 못 잼 — 격리 서버에서 재도 되는 것들이다).
→ 뒤의 다섯은 같은 날 2차에서 쟀다(아래 "남은 위험 해결 2차").

## 남은 위험 해결 2차 — 시험 격리를 구조로 막음 (Jay "위험 요소들 해결하는거 설계해서 코덱스랑 검토후 정리" → "코덱스랑 같이 진행")

owner Claude · reviewer Codex(설계 2회·구현 diff 3회 검토, 마지막 판정은 아래) · status 완료 · touched_paths `backend/tests/conftest.py`,
`backend/tests/test_pytest_isolation_defaults.py`, `tools/browser_measure/*`, `tools/load_test_100.py`, 관련 시험 4개, `docs/TESTING.md`·`CODE_MAP.md`·`기능설명서.md`.

**먼저 잰 것(Claude 직접 실행).** `CONTENT_HUB_DATA` 만 빈 임시 폴더로 돌려 백엔드 전체를 돌리면 `4168 passed · 2 failed`, 실제 `backend/data`
변경 0 이었다. 임시 폴더에 생긴 파일 = 평소 실제 폴더로 가던 쓰기: `cost_cache_v2.json`·`device_identity.json`·`db/content_hub.db`·`db/manage_hub.db`.
실패 2건(`test_generation_media_cache.py`)은 머리말이 "DB 에 접근하지 않는다"인데 `repo.apply_generation_media_cache_updates` 가 **기본 DB 에
`BEGIN IMMEDIATE`** 를 건다 — 평소엔 실제 기본 DB 에 쓰기 잠금을 잡고 통과하던 시험이다(행은 안 바뀐다). `tools/predeploy_gate.ps1` 은 환경변수를
하나도 넣지 않는다. 즉 "돌리는 쪽이 `CONTENT_HUB_NO_PROXY=1` 을 넣는다"는 규칙은 사람·에이전트·게이트 누구에게도 구조로 지켜지지 않았다.

| # | 고친 것 | 확인(실행 수치는 Claude 직접 실행 보고) |
|---|---|---|
| 1 | `conftest.py` 가 `CONTENT_HUB_NO_PROXY=1` 과 실행마다 새 임시 `CONTENT_HUB_DATA`(+`db/`)를 기본으로 넣는다. 빈 문자열은 기본값으로 바꾸고 비어 있지 않은 명시값은 존중, 임시 폴더는 끝날 때 지우기를 시도한다(0.2초×10 재시도, 끝내 못 지우면 ASCII 경고만 남기고 폴더가 남을 수 있다). 가드 `test_pytest_isolation_defaults.py` | **환경변수 없이** 전체 `4171 passed`, 실제 `backend/data` 의 DB·json 수정 시각 변화 0, 임시 폴더 잔존 0. 빈 값으로 돌려도 통과, 대소문자만 다른 실제 폴더(`backend/DATA`)를 명시하면 가드가 실패(`samefile`) |
| 2 | 실측 도구: 기본 결과의 경로를 **라우트 틀**로 치환(`docs/inventory/endpoints.md`, 틀에 없으면 `/api/{…}`) — 경로의 동적 조각에 마운트·프로젝트 이름이 실렸다. 격리 서버는 부모 셸의 `CONTENT_HUB_*` 를 **전부** 버리고 뜬다(`server_env`). 스냅샷이 `project_folder_link`·`project.render_root_path` 를 비운다. 생성 요청 수는 가리기 전 원문에서 센다 | `test_browser_measure_isolation.py` 가 단언한다(경로 속 이름·percent-encoding·틀 불일치·오염된 부모 env·폴더 경로·13번째 이후 생성 요청). 실제 복사본에서 폴더 링크 0·렌더 루트 0 확인, `smoke.py` 41/41 |
| 3 | 부하 도구: 서버 환경을 부모 `CONTENT_HUB_*` 없이 다시 짜고 `EXTERNAL_RECOVERY=0`(기동 때 실제 CLI 계정 조회 없음)·`NO_PROXY=0` 고정 | `test_load_tool.py`. ★`NO_PROXY=1` 을 넣자는 Codex 1차 안은 Claude 가 반대해 철회 — AUTH on 이라 위임은 어차피 불가능하고, 켜면 `is_shared_team_server()` 가 뒤집혀 운영 공유 서버와 다른 경로를 잰다. 거꾸로 **부른 셸의 `1` 을 물려받던 것**이 문제였다(TESTING 이 게이트 전에 그 변수를 넣으라고 안내해 왔다) — 이전 게이트 결과가 어느 경로였는지는 알 수 없다 |
| 4 | 간헐 실패 재발 처방: `test_db_restore_account_boundary.py` 의 `rglob("*.restore-*.tmp")` 4곳을 제품이 만드는 이름(`.<DB>.restore-<소문자 hex 16>.tmp`)만 보게 좁힘 | 첫 게이트 리허설이 `1 failed`(`…security_failure_rollback`)로 멈췄다. 남은 파일 `.CONTENT_HUB.DB.RESTORE-<HEX>.TMP.tmp` = 09-18 에 처방한 외부 파일과 같은 꼴(제품 이름을 대문자로 + `.tmp`). Windows 의 glob 이 대소문자를 안 가려 걸렸다. Codex 소스 판정: 이 시험의 설비는 `tmp_path`·`tempfile.tempdir` 를 직접 잡으므로 conftest 기본값에서 이 실패로 이어지는 **직접 경로는 확인되지 않았다(인과는 미확인)**, 제품은 `finally` 에서 임시본 2개를 지운다. 만든 쪽은 여전히 **미확정**. `*.bak-*.db`·`*.bak` 단언은 그대로 둔다 — 관측된 외부 파일은 전부 `.tmp` 로 끝나 그 패턴에 걸릴 수 없다(Codex 는 좁히자고 했다가, 관측된 두 파일이 모두 `.tmp` 로 끝난다는 근거에 보류를 받아들였다) |

**배포 게이트 리허설**(Claude 직접 실행 보고 — `predeploy_gate.ps1 -AllowDirty`, 환경변수 없이, 단독, HEAD `cdb5b634`): 문서·아키텍처 린트 → 백엔드 `4172 passed · 1 skipped`
→ 프런트 `185 files · 1913 passed` → production build → 백업·복원 훈련 → 100명 부하 `acceptance.passed=true`(작업 p95 56.9ms · 로그인 p95 7.5s ·
최대 RSS 254MB · SQLite 잠금 0) → **PREDEPLOY GATE PASSED**. 실행 전후 실제 `backend/data` 수정 시각 변화 0. `-AllowDirty` 는 다른 세션의 미커밋
문서 4개 때문이다 — **리허설이지 배포 승인 근거가 아니다**(정식 게이트는 배포 지시 때 고정 릴리스 폴더에서).

**미측정 5건 실측**(Claude 직접 실행 보고 — 격리 서버, 생성 요청 0건·이사 공지 발행 0건. Codex 는 실행하지 않고 소스·문서만 검토했다):
- **강조색**: 설정 → 기본 설정 → 색 단추 5개. 누르면 `localStorage['ch_accent']` 와 `:root` 의 `--accent` 가 즉시 바뀐다(`#c4e84a` → `#4a9eff` 확인 후 되돌림). 서버 요청 없음.
- **PM 작업**: **만들기 화면은 없다** — 작업은 생성물 폴더에서 자동으로 생기고, 표 위의 `+` 는 필터 추가(`work-chip-add`)다. 설명 칸은 벗어날 때 `PATCH /api/manage/tasks/{tid}` 로 저장되고 값이 남는다. 보드 보기 = 열 4개. 상태 바꾸기(카드 끌기)는 CDP 로 HTML5 끌기를 재현하지 않아 **미측정**.
- **크레딧 그룹·허용 모델**: 그룹 창의 [저장] 은 초안만 바꾸고(요청 0), 프로젝트 설정 창의 [저장] 이 `PATCH /api/projects/{pid}` + `PUT /api/manage/planning/{pid}` + `PUT /api/manage/credit-plan/{workspace_id}` 를 보낸다. 다시 열면 그룹이 남아 있다. 렌더 폴더 경로 칸은 비어 있었고 건드리지 않았다.
- **공유 서버 이름·주소**(허브 → 내 격리 S 로 로그인): [저장] → `POST /api/shared-server/url`, `/api/shared-server/status` 에 이름·주소 반영. 누르기 전에 단추 글자가 "저장"이고 "공지"가 없음을 검사했다 — 같은 줄의 **팀에 공지는 누르지 않았다.**
- **업데이트 공지**: 고정 체크 → `PUT /api/update-notices/admin/{notice_id}/pin`, 행 안의 [공지] → `POST …/announce` → 단추가 재공지로 바뀜. **"최신 업데이트 등록" 단추는 개발 클론에 나오지 않는다**(릴리스 설치본에서만) — 그 단추 자체는 미측정.

**이번에도 고치지 않은 것**: 제품 코드는 한 줄도 바꾸지 않았다(시험·도구·문서만). 실측 전용 차단 분기를 제품에 넣자는 안(수동 폴더 등록 403)은 Claude·Codex 합의로
버렸다 — "Assets 폴더 등록 금지"는 **운영 규칙이지 도구의 보장이 아니다.** 코드 지도 pytest 가드(Claude 안)는 Codex 반대(파일 이름 존재만 보는 약한 검사)로 철회.
`tools/endurance_probe.py` 도 부모 env 를 복사하는 같은 꼴이지만 게이트 밖 도구라 관찰로만 남긴다. 수용 유지: Esc 결함 유입 시점 · 증상 없는 재구독 리스너 3곳 ·
smoke 의 CSS 의존 · `--verbose`/`--shots` 의 실명. 알려진 사용자 노출 문제(Jay 결정으로 수용): 로그인 전 401 소음 · CLI 없는 PC 의 `/api/models` 반복 502 와 원문.
보류 API 4개는 유지로 종결(서버 접근 로그를 볼 기회에 재평가).

## 성능 점검 — "우리 프로그램이 PC 를 느리게 하는가" (2026-09-19~20, Jay 지시 · Claude·Codex 각자 실측)

owner Claude · reviewer Codex(독립 실측 1회 + 설계·diff 검토) · status 완료(골드 광택) · touched_paths `frontend/src/styles/generations.css`·`history.css`·`frontend/tests/goldSheenCss.test.ts`(같은 날 `idleCssAnimations.test.ts` 로 이름을 바꿨다)

**계기.** `dev` 의 깨끗한 복제본으로 배포 게이트를 `-AllowDirty` 없이 돌리니(HEAD `df8fe405`) 백엔드 4173 통과 뒤 **프런트 시험에서 멈췄다** — `seedanceSceneErrorsRender`(5초 제한에 5037ms)·
`resolveSelectionRender` 의 시간 초과. 같은 코드가 오후에는 2회 통과, 저녁에는 폴더 셋(C: 임시·D: 임시·원래 작업 폴더)에서 8회 중 0회 통과였다(순수 시험 시간 44초 → 60~70초). 빌드·복원 훈련·100명 부하는
같은 복제본에서 따로 돌려 통과. 백엔드 시험(458초 vs 449~484초)과 부하 p95(56.7 vs 56.9ms)는 그대로라 **PC 전체가 느려진 것은 아니다.** 프런트 시험만 느려진 원인은 **못 찾았다**(CPU 3.5%·메모리 88GB 여유·
디스크 유휴·고성능 전원·보안 프로그램 0%). 두 시험의 시간 여유가 얇다는 것만 확실하다 — 미해결.

**찾은 것: 최종(골드) 카드의 광택이 가만히 열어 둔 탭의 CPU 를 계속 쓴다.** `@keyframes goldSheen` 이 `background-position` 을 2.3초 주기로 끝없이 움직였다(★ 단추의 `::after`·카드 하단 띠·히스토리 노드 띠).
합성으로 처리되지 않는 속성이라 골드 카드가 한 장만 있어도 페이지가 매 프레임 다시 그려진다.

| 조건(격리 서버·헤드리스 크롬·브라우저 프로세스 트리, 코어 1개=100%) | 수정 전 | 3안 적용 뒤 |
|---|---|---|
| 골드 0장 | 1.2% | — |
| 골드 1장(시험 DB 의 원래 화면) | 24~38% | 2.4% |
| 골드 19~20장 | 96~110% | 2.3% |
| 골드 42장 | 133% | — |
| 같은 화면에서 제품의 모션 끄기만 켬(3회) | 33·24·30% → **0.8·0.3·0.3%** | — |

- **Codex 독립 실측**(Claude 의 결론을 알려 주지 않음, 샌드박스 없이 — Jay 지시): "내 작업" 카드 그리드에서만 23~34%, 나머지 화면(공유&리뷰·캔버스·휴지통·히스토리·분리 창 둘·관리자·설정·알림)은 1% 미만,
  백엔드 단독 0.00~0.06%. 미디어 주소를 막아도 20% 가 남아 다운로드는 원인이 아님. Codex 는 "카드 그리드 어딘가"까지 좁혔고, 광택으로 좁힌 것은 Claude 의 모션 끄기 대조다.
  Codex 1차는 workspace-write 샌드박스가 DevTools WebSocket 을 끊어 브라우저 쪽을 하나도 못 쟀다(기억해 둔 함정 그대로).
- Codex 가 보고한 "쓰기 17MB/s" 는 **디스크가 아니다** — 같은 구간의 실제 물리 디스크 쓰기는 0.4~1.7MB/s(모션 끄기 때 0.2~0.4). Windows 의 프로세스 IO 집계는 파이프·프로세스 간 전달도 센다(헤드리스가
  그린 화면을 프로세스끼리 넘기는 양으로 해석 — 해석은 추정). 이 PC 의 실제 크롬에서 CPU 를 쓰던 탭의 쓰기는 0.01MB/s 였다.
- 문제없던 것(Claude 10분 idle, 광택 끔): 탭 0.3%·서버 0.31%·요청 0.28건/초(`POST comment-counts` 10초 · `GET agent/status` 12초 · `GET scenes/cards` 30초 · 알림 4종 60초) · 힙 7.6→7.7MB·DOM·리스너 수 변동 없음.
- **실제 환경 증거**: 이 PC 의 크롬 렌더러 하나가 09-19 09:06 부터 하루 종일 코어 1~1.5개(누적 약 44,800 CPU초). 크롬은 시험 서버(5173)에 연결돼 있었지만 **그 탭이 우리 앱인지는 확정하지 못했다.**
  ~~GPU 가 있는 실제 크롬에서의 수정 전후 수치도 미측정(둘 다 헤드리스).~~ → **정정(09-20)**: "헤드리스라 GPU 가 없다"는 Claude 의 전제가 틀렸다. CDP `SystemInfo.getInfo` 로 확인하니
  이 PC 의 `--headless=new` 크롬은 `gpu_compositing=enabled`·`rasterization=enabled`(RTX 4090)이고 창 있는 크롬과 값이 같다 — 위 수치는 이미 GPU 조건이었다. 창 있는 크롬의 실측은 아래.

**처방(Jay 가 모의 이미지 `_shots/gold-sheen-options.png` 에서 3안 선택. Codex 도 3안 추천, Claude 의 첫 추천은 2안이었다).** 빛은 평소 멈춰 있고 카드·노드에 마우스를 올리거나 ★ 에 키보드 포커스가 갈 때 한 번(1.4초).
밝은 띠를 `transform` 으로만 옮기고 `will-change` 는 걸지 않는다(골드 수만큼 레이어 메모리를 잡는다). 모션 끄기는 `animation: none !important`(hover 규칙의 특이도를 이겨야 한다).
검증(Claude 실행): 가만히 둘 때 running 애니메이션 0 · hover 때 두 애니메이션이 돌고 2초 뒤 0 · 모션 끄기+hover 0 · 프레임이 모의 이미지와 같은 모양 · 새 시험은 수정 전 CSS 에서 4건 중 2건 실패 ·
프런트 전체 1916 통과(실패 1 = 위의 `resolveSelectionRender` 시간 초과 — 이 변경과 무관) · 아키텍처 린트 통과. Codex diff 리뷰: 빛이 닿아야 할 컨테이너 네 자리 확인, 지적 2건(주석의 출처 표현·시험이 문자열에 과하게 묶임) 반영.

**Codex 의 창 있는 실제 크롬 실측**(09-20, Jay "코덱스 실시간 브라우저로 재볼 수 있지 않나" — 전용 프로필·전용 포트의 headed Chrome, 1680×1000, Windows 보고 주사율 59Hz, GPU 합성·래스터 enabled,
가려져도 그리게 하는 플래그 3종, 21초 × 3회, 브라우저 프로세스 트리): 수정 전 재현(옛 규칙 주입 — `getAnimations()` 로 `goldSheen` 2개/40개가 도는 것 확인) 골드 1장 **26.6~28.1%** · 20장 **140~143%**,
수정 후 1장 **0.2~1.0%** · 20장 **0.2~0.5%**. hover 직후 `goldSheenStar` 1개 running → 2초 뒤 0 · `reduce-motion`+hover 0. Claude 의 헤드리스 수치(24~38% → 2.4%, 110% → 2.3%)와 방향·규모가 일치한다.
(다수 조건은 시험 DB 에 골드가 1장뿐이라 같은 카드 DOM 을 화면 안에 19장 복제한 것 — 운영 화면의 절대값으로 일반화하지 않는다.) Jay 의 크롬 탭에 직접 붙는 길은 원격 디버깅이 꺼져 있어 쓰지 않았다.

**"새 항목" 글로우도 같은 방법으로 고쳤다(2026-09-20, Jay A안 + 밝기 선택).** `card-fresh-glow` 가 `box-shadow` 를 1.4초 주기로 끝없이 움직였다. `fresh` 는 팀 탭에서 기준선 뒤에 공유됐고 아직 클릭하지 않은
카드에 붙어 **클릭할 때까지 남는다**(`lib/teamSeen.ts`) — 공유가 잦은 팀에서는 상시 비용이다.

| 조건(브라우저 프로세스 트리, 코어 1개=100%) | 새 항목 1장 | 19장 |
|---|---|---|
| 수정 전 — Codex, 창 있는 실제 크롬 | 42~49% | 113~124% |
| 수정 전 — Claude, GPU 합성이 켜진 헤드리스 | 44% | 86% |
| B안(같은 맥동을 `::before` 층의 opacity 로) — Claude | 25% | 46% |
| A안(정지) 모의 — Claude | 4% | 2% |
| **수정 후, 빌드된 제품** — Claude | **1.7%** | **3.0%** |
| 새 항목 0장(기준선) | 1~2% | — |

B안에서 메인 스레드는 11.8~16.5% → 0.3~0.5% 로 내려갔지만 절반이 남았다 — **끝없이 도는 한 매 프레임 화면을 합치는 비용은 남는다.** 멈춘 빛은 밝기와 상관없이 비용이 없다(가장 밝은 후보를 19장에 붙여 2.6%, 애니메이션 0).
Jay 가 모의 이미지(`_shots/fresh-glow-options.png` → `fresh-glow-brightness.png` → `fresh-glow-many.png`)에서 A안과 밝기(종전 최댓값 18px·5px·0.55 와 24px·7px·0.70 의 중간 = `0 0 21px 6px rgba(210,255,60,0.625)`)를 골랐다.
키프레임과 모션 끄기 규칙 2개는 돌릴 것이 없어 지웠다. 시험 파일은 `goldSheenCss.test.ts` → `idleCssAnimations.test.ts` 로 이름을 바꾸고 새 항목 계약을 더했다(수정 전 CSS 에서 실패 확인).
검증(Claude 실행): 프런트 전체 **1918 통과·실패 0** · 아키텍처 린트 통과. Codex diff 리뷰 승인(`fresh` 는 팀 탭에서만 전달되고 팀 탭은 항상 가상 그리드라 번짐이 잘리지 않는다 · hover 때 그림자는 종전에도 fresh 가 이겼다).
미측정: A안의 창 있는 실제 크롬 수치(골드 때는 헤드리스와 일치했다).

**남긴 관찰(고치지 않음)**: ★캔버스 '방금 생성' 글로우 `.scene-card.glow::after`(`scene.css`) — `opacity` 를 1.4초 주기로 끝없이 움직이고, 카드를 클릭하기 전까지 **최대 24시간** 남는다(`lib/sceneRecentDoneStore.ts` `RECENT_TTL_MS`).
Codex 가 소스로 짚고 Claude 가 재 봤다(격리 서버의 카드 92장짜리 씬, 화면에 보이는 카드 1장에 제품의 `glow` 클래스를 붙임, 15초 × 2): 글로우 없음 **0.2~0.7%** → 1장 **7.4~8.6%**(메인 스레드 0.1%, 도는 애니메이션 `scene-card-glow` 1개).
그리드 카드의 B안(1장 25%)보다 작은 것은 빛나는 요소가 작아서로 보인다(추정). 여러 장일 때는 화면에 보이는 씬 카드가 1장뿐이라 **미측정**. Jay 결정 대기. `comfy-loading-pulse` 는 실행 중에만 붙는다(Codex 소스 확인). 라이브러리는 가상 스크롤이라 화면 밖 카드는 비용이 없다 · Assets 폴더 감시(1초 타이머·5초 `stat`)의 실제 NAS 비용,
권한 승격 중 관리자 창의 1초 타이머, 작업자 에이전트 장시간 비용은 미측정 · 서버의 60초 런타임 지표가 미디어 폴더 크기를 재귀 합산한다(대용량에서 읽기 부담 가능 — 코드 확인만).

## 부하 전수 조사 (2026-09-20, Jay "글로우 모두 찾아, 하나씩 하지 말고" → "부하되는 부분 모두 찾아라")

owner Claude(실측·구현) · reviewer Codex(독립 전수 조사 — Claude 의 목록을 주지 않음 · 설계 검토 · diff 리뷰) · status 완료(Jay "코덱스랑 검토진행")

**방법.** Codex 가 프런트 추적 파일 전부(CSS 13·TSX 175·TS 363·HTML 1·SVG 2)에서 "가만히 둔 화면에서 계속 도는 것"을 30항목으로 분류했다(CSS 애니메이션·타이머·폴링·rAF·영상 반복 재생, 각각 붙는 조건과 지속 시간).
Claude 는 격리 서버에서 제품이 쓰는 클래스·마크업으로 상태를 만들어 12초씩 재고(브라우저 프로세스 트리, 코어 1개=100%, GPU 합성 켜짐), 조작 비용은 메인 스레드 시간과 긴 작업(50ms 초과)으로 쟀다.

| 부하 | 조건 | 실측 | 언제까지 도나 | 상태 |
|---|---|---|---|---|
| 골드 광택 | 골드 카드 1장 / 19장 | 24~38% / 110% → **2%** | 상시 | 고침(`22f92504`) |
| 팀 탭 새 항목 글로우 | 1장 / 19장 | 42~49% / 86~124% → **2~3%** | 클릭할 때까지 | 고침(`8804b2ce`) |
| ★라이브러리 '생성 중' 로고 맥동 `gen-comfy-pulse` | 1장 / 6장 | 17~20% / 19~25% → **3~4% / 4.3%** | 생성이 끝날 때까지(멈춘 생성중이면 끝이 없다) | 고침 — 계단식 `steps(4)` |
| ★캔버스 '방금 생성' 글로우 `scene-card-glow` | 보이는 카드 1장 | 7~10% → **0.0%** | 클릭할 때까지(최대 24시간, 만료를 알리는 타이머는 없다 — Codex) | 고침 — 정지(가장 밝은 지점으로) |
| 캔버스 **Comfy 노드 카드**의 '생성 중' 막대 5개 `gen-wave` | Comfy 노드 실행 중에만(막대를 그리는 곳은 `ComfyCard.tsx` 한 곳 — 생성 카드는 `e52bf3cf` 부터 로고만 그린다) | 막대만 카드에 직접 얹어 7~8% → **1~2%** · 제품 클래스로 만든 Comfy 카드(로고+막대) 1.6~2.8% → **0.7~1.1%** | 실행이 끝날 때까지 | 고침 — 계단식 `steps(4)`, 막대 시차는 0.125초의 배수 |
| 캔버스 생성 카드의 '생성 중' 로고 `comfy-loading-pulse` | 보이는 카드 1장 | 0.7~1.8% → **0.5~0.6%** — 캔버스에서는 원래도 부하가 거의 없었다(기준선 0.2~0.9%) | 생성이 끝날 때까지 | 같은 규칙으로 맞춤(`steps(4)`) |
| 알림 스피너 `notification-spin` | 업데이트·서버 이전 진행 중 | 선형 1.6~2.0% · steps(8) 1.6~1.7%(기준선 1.7~2.3%) — **비용 없음** | 업데이트 15분 상한 · 이전은 상한 없음(Codex) | 그대로 둠 — 11px 라 다시 합치는 넓이가 작다 |
| 기준선 | 내 작업 그리드 / 캔버스 / 팀 탭(15초 자동 새로고침 포함 35초) | 2~4% / 0.3% / 1.6% | — | 문제없음 |

**공통 원인.** 끝없이 도는 애니메이션이 화면에 **하나라도** 있으면 브라우저는 페이지 전체를 초당 60번 새로 합친다. 속성을 합성 가능한 것(opacity·transform)으로 바꿔도 이 비용은 남는다 — 남은 세 곳은 이미 opacity·transform 인데도 7~25% 다.
**줄이는 방법 실측**(라이브러리 로고 맥동 1장, 10초 × 2): 지금 17.2·17.6% · **계단식 `steps(4)`(1.4초에 8번만 값이 바뀜) 2.9·3.6%** · `steps(2)` 2.3·2.5% · 주기만 2배 느리게 18.7·17.3%(효과 없음) · 일시정지 1.5·2.0%.
계단 구간 사이에는 합성할 값이 바뀌지 않아 이 환경의 실측에서 합성 작업량이 크게 줄었다(브라우저 내부 동작의 보장은 아니다 — 판단은 전후 실측으로 한다). 움직임이 의미를 갖는 '진행 중' 표시는 멈추지 않고도 비용을 1/5 로 줄일 수 있다.

**일괄 수정(같은 날).** 원칙 = **장식(클릭할 때까지 남는 글로우)은 멈추고, '진행 중'처럼 움직임이 뜻을 갖는 것은 계단식으로.** Codex 설계 검토의 P1 을 반영해 `jump-none` 없이 기본 `steps(4)`(0.125초 격자에 맞고 오래된 브라우저에서도 안전) — 값은 0.5→0.625→…→1→…→0.5 로 이중 점프 없이 오르내린다. 회귀 시험 `frontend/tests/idleCssAnimations.test.ts` 에 전역 계약을 더했다: `frontend/src` 의 모든 CSS 에서 `infinite` 애니메이션은 같은 선언에 `steps(` 가 있어야 하고, 예외는 **재서 비용이 없던 선언 하나**(파일·선언 전체 일치 — Codex 지적으로 이름이 아니라 선언으로 좁힘)뿐이다. 수정 전 CSS 에서 새 시험 2건이 실패함을 확인. 검증(Claude 실행): 위 표의 전후 수치(빌드된 제품 vs 옛 규칙을 되살린 것, 10초 × 2) · 프런트 전체 **1920 통과·실패 0** · 아키텍처 린트·빌드 통과. Codex diff 리뷰 승인(계단값 순서·막대 0.5초 역위상·모션 끄기에서 글로우 밝기 0.8→1.0 모두 문제없음). 계단식 움직임은 정지 그림으로 못 보여 줘서 `_shots/working-indicator-demo.html`(앱의 실제 CSS 를 읽는 전후 비교 화면)을 만들었다.
★정정(같은 날, Jay "막대 5개는 지금 안 쓰는데"): Claude 가 처음 만든 비교 그림은 임의의 카드에 힉스필드 로고와 막대를 함께 얹어 **제품에 없는 조합**을 보여 줬다. 막대는 Comfy 노드 카드의 상태 줄에만 있고 Comfy 를 실행하지 않으면 나오지 않는다 — 그림(`_shots/idle-load-changes-all.png`)을 생성 카드(로고만)와 Comfy 카드(로고+막대)로 나눠 다시 찍고 위 표도 나눴다. 같은 로고 맥동이 라이브러리에서는 17% 인데 캔버스에서는 1% 안팎인 이유는 확인하지 않았다(라이브러리 카드는 `clip-path` 로 잘린 영역 안이라는 차이가 있다 — 추정).
넣지 않은 것: `sceneRecentDoneStore` 의 24시간 만료를 능동적으로 알리는 one-shot 타이머(Codex 제안) — 정지 글로우면 CPU 와 무관하고 제품 로직 변경이라 범위 밖. 표시가 24시간을 넘어 남을 수 있다는 관찰로만 둔다.

**문제없던 것(재조사 금지).** 조작 비용: 탭 전환 30~255ms(긴 작업 최대 111ms, 첫 전환 1회) · 빠른 스크롤 20회 398ms(긴 작업 0) · 검색 84ms · 카드 92장 씬 열기 37ms · 캔버스 휠 12회 35ms.
폴링·타이머(10초 배지·12초 에이전트 상태·30초 씬 카드·60초 알림 4종, 합계 0.28건/초): 10분 idle 탭 0.3%·서버 0.31%, 힙·DOM·리스너 변동 없음. 백엔드 단독 0.00~0.06%(Codex).
서버의 60초 폴더 크기 합산(`runtime_metrics._directory_size`): 파일 14,281개·1.36GB 트리를 같은 방식으로 훑어 22~24ms = 코어의 0.04%(별도 스레드). Codex 목록의 나머지 타이머류는 숨은 탭에서 요청을 쉬고 화면 애니메이션이 아니다.
**못 잰 것.** 큰 미리보기에 반복 재생 영상을 열어 둔 채 방치(선택자 실패) · 실제 NAS 의 Assets 폴더 감시 · 작업자 에이전트 장시간 · 권한 승격 중 관리자 1초 타이머 · 숨은 탭에서도 도는 WebSocket ping(25초)·부분 편집 2초 폴링(Codex 가 코드로 짚음, 비용은 미측정).

## 지연·DB·저장 부하 점검 (2026-09-20, Jay "다른 곳에 딜레이가 있는지, DB 흐름·저장할 때 부하가 있는지 코덱스랑 같이")

진단만 했고 코드는 바꾸지 않았다. **Codex = 읽기 전용 코드 감사**(내 가설·범위를 주지 않음, 09-12 점검의 재감사 금지·기각 목록은 제외시킴) → 후보 9건.
**Claude = 격리 서버 실측**(generation 1,350행·`content_hub.db` 48MB·`manage_hub.db` 22MB·`scene_backup` 13행. 페이지의 `fetch`·`localStorage.setItem`·longtask 를 감싸 크기와 시간만 기록) → Codex 가 실측을 보고 심각도를 다시 매김.
공유 서버 프록시 홉은 격리(죽은 주소)라 **재지 못했다**.
아래 표의 목록 크기(1.6~6.6MB)는 **브라우저가 압축을 푼 뒤의 크기**다 — 목록 응답은 gzip 으로 나가고(`backend/app/list_gzip.py`, 09-12 C-14 실측 기록: 1,664,434 → 85,501B = 5.1%) 이번에는 전송 크기를 따로 재지 않았다.

| 본 것 | 실측(Claude) | 판정 |
| --- | --- | --- |
| 단건 저장 — 색·태그·열어 봄 기록·씬 백업·코멘트 목록 | 서버 14~21ms · `sqlite_locked_total` 0(전 구간) · 비활성(d)은 요청 0건 | 문제없음 |
| 저장 뒤 재조회 | 색·태그 한 번 → 재조회 8건(목록 1쪽 1.6~2.4MB, 서버 52~125ms + 메타 7종). 화면은 먼저 바뀐다(낙관적). r 6번 연타 = PUT 6 + 목록 재조회 **1번으로 합쳐짐** · longtask 0 | 낭비지만 체감 없음 — 그대로 |
| 목록 1쪽(200건) 2.4MB 의 구성 | `prompt` 36.0% · `display_prompt` 18.7% · `references` 16.3% · `params` 15.1%(`medias`·`input_images`) · 건당 12KB | 09-12 C-1(`params.prompt`)과 다른 항목. 두 프롬프트는 쓰임이 달라 계약 변경 없이 못 뺀다(Codex) — 그대로 |
| 캔버스 저장(Codex #1·#2: 편집마다 `ch.scenes` 전체 재기록 · 쓰기 잠금 안에서 JSON 파싱·해시) | 씬 데이터 최대 47KB·합 153KB · `setItem` 최대 0.1ms · 씬 백업 PUT 14~17ms | 구조는 Codex 말대로지만 이 규모에서 1ms 미만 — **P1 → P3**(Codex 동의). 씬이 MB 단위가 되면 다시 본다 |
| 여러 개 삭제·복원(Codex #3: 항목별 요청 · 쓰기는 직렬) | 동시 6(브라우저 상한): 10개 0.19초 · 40개 0.50초 · 100개 1.02초(복원 0.90초). 동시 32 는 2.46초로 **더 느림** · 잠금 실패 0 · 409 는 '공유 중 삭제 불가' 설계 응답 | **P1 → P2**(Codex 동의). 100개에 1초 — 대량 정리가 잦아지면 서버 배치 API |
| 탭 전환·팀 '전체 보기' | 전체 보기 목록 6.6MB·서버 156~274ms·longtask 1회 133~152ms. 그 밖의 전환 longtask ≤133ms | 위 "문제없던 것"(≤255ms·111ms)과 같은 수준 |
| 첫 화면 | 첫 카드 782ms · 요청 38건 · JS/CSS 1.17MB. 그중 `/api/models*` 502 는 격리(CLI 제거) 때문 | 문제없음 |

**의견이 갈린 것 1건.** Codex 는 팀 '전체 보기'(6.6MB + longtask 133~152ms)를 "지금 원인을 분해해 볼 가치"로 올렸다. Claude 는 열 때 한 번 0.15초이고 위에서 이미 닫은 탭 전환 비용과 같은 수준이라 보류 의견. Jay 결정 대기.
→ **결론(같은 날, Jay "2번 더 들여다보자" — Claude 실측·Codex 코드 추적 후 합의): 6.6MB 는 멈칫의 원인이 아니다. 고치지 않는다.** 첫 화면 0.78초도 Codex 가 같은 칸에 올렸으나 요청 38건 중 `/api/models*` 502 는 격리 탓이라 Claude 는 문제없음으로 닫았다.

| 가른 것 | 실측(Claude, Chrome 153 헤드리스·GPU 합성 켜짐, long-animation-frame + CPU 프로필) |
| --- | --- |
| JSON 풀기 | 팀 목록 6,555,755자·200건 `JSON.parse` = 6.9~13.2ms(내 작업 1.6M자 1.8~3.4ms). 전송은 gzip 1,059,126B → 푼 크기 6,637,507B(16% — 09-12 의 5.1% 보다 덜 줄어든다) |
| 멈칫하는 프레임의 속 | 114~134ms 중 **그리기 단계가 110~131ms**. 그 안의 스크립트는 virtua 의 항목 크기 측정 콜백(`ResizeObserverCallback`, `setUtils` 청크) 57~72ms, 나머지는 스타일·레이아웃 |
| 응답 크기 vs 보이는 카드 수 | **같은 6.6MB 응답인데** 창을 900x560(카드 12장)으로 줄이면 가장 긴 프레임 중앙 0ms(8번 중 1번 79ms). 기본 창(카드 42장)은 팀 중앙 108ms(94~120)·내 작업 중앙 60ms(53~103). 목록을 다시 받았는지와 무관(3/8) |
| 자동 새로고침·스크롤 | 가만히 62초 = 새로고침 4번·long task 0·50ms 넘는 프레임 0 / 20초 연속 스크롤 = long task 50·61ms 두 번 |
| 팀이 ~50ms 더 드는 이유 | 팀 화면 42장 중 37장이 영상 → `<video preload="none" poster>` 37개 + 포스터 실패 감지용 숨은 `<img>`(`MediaThumbnail.tsx`), 내 작업은 0. `creator-badge`·`play-badge` 도 팀에만. CPU 프로필 3회 평균: 팀 229ms(JS 31·브라우저 내부 147·GC 10) / 내 작업 161ms(24·109·6), `appendChild` 24.9 vs 8.9ms. **단, `<video>` 37개만 따로 만들어 붙이면 6ms(img 0.6ms)** — 차이 전체를 영상 요소로 설명하지는 못한다(나머지는 브라우저 내부 시간이라 미귀속). "영상이 많은 카드 구성과 함께 나타난다"까지만 확인 |

즉 탭을 열 때 **보이는 카드 ~42장을 한 프레임에 마운트하는 비용**이고 어느 탭에나 있다(위 "문제없던 것"의 탭 전환 비용과 같은 것). Codex 의 사전 예측 1순위(JSON 파싱·객체 할당)는 틀렸고 Codex 도 철회했다. Codex 가 앞서 낸 후보 2개(목록에서 `params` 큰 입력 빼기·자동 새로고침 ETag/304)는 **이 멈칫을 줄이지 못한다**(전송량 절감용으로만 의미).
검토하고 버린 처방: 영상 카드를 평소 `<img>` 로 그리고 hover 때만 `<video>` — 기대 이득이 분리 실험 기준 6ms 인데 hover 즉시 재생(`GenerationCard.tsx` 의 `videoRef.play()`)·포스터 실패 폴백·빠른 이동 시 마운트 경합·`MediaThumbnail` 공용 소비처를 건드린다 / `content-visibility` 재활성(가상 격자에서 의도적으로 끔)·virtua `bufferSize` 축소도 같은 이유로 제외.
재개 조건: 실제 사용 환경에서 탭 전환 0.1초대 멈칫이 반복 체감되거나 스크롤 중 long task 가 잦아질 때 — 그때는 실제 카드 마크업·실제 포스터로 `<video>+probe` 와 `<img>` 를 A/B 한다.

**한 번 보고 재현 못 한 것.** 팀 전체 보기 직후 캔버스 탭으로 갈 때 작은 요청 5개가 1.3~2.2초(1회). 같은 순서 5번 더 = 17~28ms·longtask 0. 그때 서버는 새 프로필의 썸네일 271건을 처리 중이었고 내 계측이 6.6MB 응답을 복제해 읽고 있었다 — 원인 미확정, 제품 결함으로 올리지 않는다.
**Codex 후보 중 규모가 커지면 볼 것(전부 P3, 미측정):** 500개 초과 색·태그 청크 직렬(`api.ts:635`·`:733`) · 수백 건 프로젝트 귀속의 행별 UPDATE+outbox(`repo/projects.py:505`) · PM 작업 순서 전체 스냅샷(`manageApi.ts:109`) · 통합 Assets 메타의 소스별 순차 프록시(`routers/assets_metadata.py`) · 프록시 timeout 60초(`_proxy.py:171`)·120초(`:559`).
**문제없다고 확인(Codex, 코드):** WAL·`synchronous=NORMAL`·`busy_timeout=5000`·스레드별 연결 재사용 · 동기 라우트는 스레드풀, `async def` 경로의 파일·DB 작업은 `to_thread` — 이벤트 루프를 막는 새 경로 없음.
**못 잰 것(결론을 뒤집을 수 있는 자리, Codex 지적 포함).** 실제 공유 서버 프록시 홉(정상 왕복·장애 시 대기) · `busy_timeout` 안에서 **기다린 시간**(`sqlite_locked_total` 0 은 실패가 없었다는 뜻일 뿐) · 서로 다른 쓰기의 동시 조합(에이전트 ingest + 씬 백업 + 삭제) · WAL 크기·체크포인트 · MB 단위 대형 씬 · 2,000개 PM 작업.
실측 중 실수 1건: 삭제 실측 첫 시도에서 복원 주소를 틀려(405) 격리 사본의 휴지통 28건까지 함께 복원했다 — 격리 사본이라 실제 데이터와 무관하고 사본은 종료 때 삭제됐다.

## 크게 보기의 첫 로딩 (2026-09-20, Jay "동영상이나 이미지 볼 때 처음 로딩이 좀 걸리는데" → "a안으로 진행")

**진단(Claude 실측·Codex 코드 추적).** 시험 DB 의 원본 1,345개 중 1,196개(89%)가 Higgsfield CloudFront 의 https URL 이고 브라우저가 직접 받는다(원본 보존은 설계상 기본 꺼짐 — `config.py`). CDN 표본(최근 이미지 6·영상 6, 이 PC 1회): **엣지에 없으면(Miss) 첫 바이트 810~870ms, 있으면(Hit) 15~30ms** · PNG 0.09~29.7MB(Hit 이면 29.7MB 를 0.3초 = 회선은 빠르다) · **영상 6개 전부 moov 가 파일 뒤**라 앞부분 → 끝부분 Range 재요청 뒤에야 재생(영상 10~54MB). 그동안 크게 보기는 원본만 부르고 아무것도 안 보여 줬다(이웃 프리페치는 이미지만, 첫 장은 없음).
**처방 = A안(빈 화면만 없앤다).** 연 곳이 **이미 화면에 띄운 것과 같은 썸네일 URL**(`PreviewItem.thumb`)을 넘기고, `MediaPreview` 가 그림은 원본이 놓일 크기의 바탕으로·영상은 `poster` 로 깐 뒤 원본이 오면 걷는다. 넘기는 곳: 라이브러리 격자(`previewTargetFromGenerations(…, thumbSize)` — 카드와 같은 폭이어야 캐시에서 뜬다)·카드·`HistoryBoardNode`(캔버스 생성 카드 포함)·캔버스 변형 묶음. 안 넘기는 곳(Assets·레퍼런스·정보 팝업)은 마크업까지 종전 그대로. dev `5f970c2f`.
버린 것: 선택만으로 원본 미리 받기(29MB 헛받음·영상 Range 는 캐시 재사용 보장 없음) · 영상 이웃 프리페치(첫 장엔 효과 없음) · 원본 보존 기본 켜기(디스크 계약) — Codex 합의.
**실측으로 잡은 결함 1건(내 구현).** 영상의 고정 크기를 `loadedmetadata` 에 풀었더니 창이 636×164 로 줄었다 — 표지를 보여 주는 동안 `<video>` 의 제 크기는 **표지 크기**(HTML 규격)라서다. 재생 정보가 오면 영상의 실제 크기로 바꿔 잡고(키우지 않음 = 종전 CSS 와 같은 크기) `playing` 에서만 CSS 에 돌려주게 고쳤다. jsdom 시험으로는 안 잡히고 실제 브라우저를 수정 전 빌드와 나란히 찍어서 나왔다.
**검증(Claude 실행).** 격리 서버·느린 회선(지연 0.4초·8Mbps): 그림 = 수정 전 0.35초 시점 본문 830×20(빈 창) → 수정 후 1220×690(썸네일), 원본 도착(≈8초) 뒤에도 1220×690 으로 크기 변화 없음 / 영상 = 수정 전 636×170(빈 상자) → 수정 후 1211×690(표지), 최종 1211×690(수정 전 최종과 동일). 전후 그림 `_shots/preview-placeholder-before-after.png`. 새 시험 `frontend/tests/mediaPreviewPlaceholder.test.tsx` 7개(수정 전 코드에서 동작 시험 3개 실패 확인) · tsc·아키텍처 린트·빌드 통과 · 프런트 전체 **1927 통과·실패 0**(단독 실행. Codex 리뷰와 겹쳐 돌린 1회는 알려진 `resolveSelectionRender` 5초 초과 1건 → 단독 재실행 통과). Codex 리뷰: P2 2·P3 1 → 2건 반영(A→B→A 에서 준비 표시가 남던 것·썸네일 없는 그림의 마크업 보존), 1건은 근거와 함께 현행 유지(아래) → 재검토 **승인**.
**알고 둔 한계.** 받는 시간·영상 재생 시작 시점(2~3초)은 그대로다 · 포스터가 없는 원격 영상은 깔 것이 없어 종전과 같다 · 원본 치수를 몰라 썸네일을 상자 끝까지 키운다 — 원본이 상자보다 작으면(옛 512px 생성물 등) 올 때 한 번 줄어든다(`ponytail:` 주석, 목록 응답에 원본 치수가 실리면 교체) · 자동 재생이 막힌 영상은 고정 px 가 남아 창을 넓혀도 따라 커지지 않는다(Codex P3, 관찰만) · CDN 표본은 이 PC 12개 1회라 팀원 PC·Miss 빈도는 미측정.

## 전 기능 실측 2차 (2026-09-20, Jay "코덱스 실측 진행. 모든 버튼과 기능들 이상 없는지 검토… 같이 검토해서 수정 진행")

**범위 판단.** 09-19 전수 실측(`ba028c68`) 뒤 제품 코드 변경은 `frontend/src` 13파일뿐이었다(CSS 5·`MediaPreview` 와 thumb 을 넘기는 4곳·`types.ts`·`useGenerationSelection.ts`, 백엔드 0) — 그 변경분은 이번에 전부 밟았고, 나머지는 09-19 통과 기록 위에 **이번에 새로 누른 범위만** 아래에 적는다("전부 눌렀다"가 아니다).
**Codex 실행 레인(격리 서버 8241/8242, 헤드리스) 2회.** smoke 41/41 · 선택/색/태그/코멘트/크게 보기/정보 · 관리자·알림·계정·설정 열기와 Esc · H 로그인 게이트 7단계 · 공유→최종→최종 해제→공유 해제(서버 쓰기 4종 관측) · 삭제→휴지통→복원→영구 삭제(확인창 수락). **이상 0건·생성 요청 0건.** 그 밖의 확장 자동화는 상태 전환 뒤 대상을 잃어 Codex 스스로 '미측정(시나리오 한계)'로 분리했다.
**Claude(8231/8232, 동시에).** 캔버스: 노드 11종 만들기(Tab → 키)·느린 끌기와 Undo/Redo·마퀴·Ctrl+클릭·그룹 만들기/해제·변형 팝업·노드의 정보/코멘트/태그 단추·노드 크게 보기(표지 확인)·히스토리 보기 / 비교 창 / **17개 화면 × (1664×905·1100×700) × (한·영)** 의 가로 넘침·화면 밖 창·글자 잘림·상단 겹침 자동 검사 + 스크린샷 판독 / 정적 점검(폼 안 type 없는 button 0건·영어 사전 누락 43문장 — 설정에 "순차 적용"이 명시돼 결함 아님). 예외 0·생성 요청 0.

| # | 찾은 것 | 재현 | 원인 | 수정(dev `4d76b5c3`) |
| --- | --- | --- | --- | --- |
| F1 P2 | **정보 팝업이 화면 아래로 나간다** — 1664×905 에서 아래쪽 줄 카드를 휠 클릭하면 210~217px 이 화면 밖, 단추 8개 중 4개가 안 보임. 1100×700 은 가운데 줄 카드로도 136px | 3/3 | `InfoPopup.tsx` 의 시작 위치는 높이를 모른 채 top 을 `innerHeight - 200` 까지만 막는데 팝업은 `80vh` 까지 자라고 견적·썸네일이 늦게 채워져 열린 뒤에도 커진다 | `ResizeObserver` 로 크기가 바뀔 때마다 '들어갈 수 있는 가장 아래 top' 까지만 올린다(절대 상한이라 StrictMode 에서 두 번 불려도 한 번만 — Codex P2). 끌어 옮긴 뒤에는 건드리지 않는다 |
| F2 P3 | **캔버스에서 Ctrl+A 를 누르면 화면 글자가 통째로 파랗게 선택**된다(씬 탭·검색칸·상태줄·오류 토스트) — 카드는 0장 | 2/2 | `sceneKeyboard.ts` 에 전체 선택 의도가 없어 브라우저 기본 동작으로 떨어졌다(라이브러리는 정상) | 씬의 모든 카드 선택 + 그룹·행 선택 해제. 결과 팝업·노드 선택기가 떠 있으면 선택은 그대로 두고 기본 동작만 막는다. 입력칸에서는 손대지 않는다 |
| F3 P3 | 설정 "카드 장식 애니메이션" 설명이 낡음("흐르는 빛을 재생") + 앱의 모션 끄기가 **라이브러리 '생성 중' 로고만 못 멈춤**(OS 설정에서만 멈췄다 — Codex 가 코드로 짚음) | 정적 | 같은 날 빛을 hover 한 번으로 바꾸며 문구를 안 고쳤다 | 문구를 실제 동작대로 + 영어 항목 · `.reduce-motion .gen-comfy-logo` 규칙 |

문서 어긋남 1건도 고쳤다: 캔버스의 `Shift+클릭`은 누적 선택이 아니라 **레시피 선택**(그 카드 + 입력 조상, `useSceneCardMove.ts`)인데 단축키 표가 다중 선택으로 뭉뚱그려 적고 있었다 → `기능설명서.md`.
**검증(Claude 실행).** 브라우저 재검증: F1 두 창 크기의 아래쪽 카드 6회 모두 잘림 0·가려진 단추 0, 끌어 내린 뒤에는 그대로 / F2 93/93 선택·글자 선택 0·Esc 로 해제·검색칸에서는 입력 글자만 / F3 `html.reduce-motion` 에서 로고 `animationName: none`. 새 시험 `infoPopupFitsViewport`(4, StrictMode — 수정 전·'두 번 올라가는' 버전 모두에서 2개 실패 확인)·`sceneSelectAllShortcut`(3)·`sceneKeyboard` +1. tsc·린트·빌드 통과. Codex 코드 리뷰 P2 1·P3 2 → 전부 반영 → 승인.
**같이 드러난 시험 문제(dev `71818d86`).** App 전체를 렌더하는 `seedanceSceneErrorsRender`·`resolveSelectionRender` 첫 시험이 **시험 파일이 하나 늘 때마다** 전체 실행에서 기본 5초를 넘었다 — 새 파일 2개를 빼면 3/3 통과, 하나라도 넣으면 7/7 실패, 단독으로는 1.4~1.8초(3/3). 파일 내용이 아니라 병렬 실행의 jsdom 기동 경합이다(09-19 부터 "PC 가 느릴 때"로 적어 둔 그 간헐 실패). Codex 와 검토해 **그 두 시험에만 `{ timeout: 20_000 }`**(전역 제한·병렬도는 그대로 — 다른 시험의 무한 대기를 가리지 않게). 뒤 전체 실행 **3/3 · 1935 통과**.
**못 본 것(이번에도).** 프로젝트·폴더 만들기/이름/이동과 카드 끌어 넣기 · 자동 태그 · 날짜 묶음·무한 스크롤 끝 · 리뷰 메뉴 전 항목과 `.card.fresh` 의 새로고침 뒤 소거 · 코멘트 수정/삭제·배지 · PM 작업 CRUD·보드 드래그·캘린더·크레딧/그룹/모델 저장 · 관리자 역할 변경 · 설정의 강조색·단축키 편집 · H(AUTH off) 쓰기 동등성 — 전부 09-19 통과 기록이 있고 그 뒤 코드가 안 바뀐 영역이지만 **이번에는 다시 누르지 않았다**. 영상 비교 창 · 창 있는 실제 크롬에서의 확인도 미실측.
관찰(고치지 않음): 전체 선택하면 카드가 92→93 으로 보인다(선택돼야 그려지는 comfy 카드 1장 — 기존 마퀴 전체 선택과 같음) · `i18n.ts` 에 안 쓰이는 옛 모션 문구 항목 2개가 남아 있다.

## 문서·색인 점검

- `docs/` 의 Markdown 77개(이 문서 포함) 중 `docs/inventory/` 자동 생성 목록 4개에는 프런트매터가 없고(설계), 나머지 73개에는
  status·updated 가 있다 · 색인·루트 지침에서 닿지 않는 문서 0 · 깨진 상대 링크 1건 수정(`status/성능_CLI정확도_점검_2026-09-12.md`).
- graft: `check` = 코드와 동기화, 1,019 파일 색인, 전날 추가한 도구·`backend/serve.py` 도 찾는다(질의 때 자동 갱신). 의미(deep) 층은 미구축 —
  프로젝트 규칙상 보조 색인이라 그대로 둔다. Codex 도 읽기 전용 질의로 기능 코드 위치를 찾는 데 썼다.
- 볼트(`D:\vault`)는 이번 범위에서 읽지도 쓰지도 않았다(코드와 함께 바뀌는 문서는 저장소에 둔다는 규칙).
