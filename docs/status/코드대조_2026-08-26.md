---
aliases:
  - 코드 대조 2026-08-26
tags:
  - mvhub
  - mvhub/현황
  - mvhub/검증
status: snapshot
updated: 2026-08-27
---

# 코드↔문서 대조·위키화 작업 기록 (2026-08-26)

> [!NOTE]
> Claude·Codex 협업 작업의 인계 기록이다. 문서 `status: snapshot` 은 이 노트가 특정 기준선의 감사 기록이라는
> 뜻이고, 아래 표의 작업 상태와는 다른 축이다.

## 작업 인계

| 항목 | 값 |
|---|---|
| owner | Claude |
| reviewer | Codex |
| 작업 상태 | 완료 (Codex 3라운드 조건부 승인 → 조건 반영 → 매니페스트 39개 양측 승인. 이후 Jay 지시로 Resolve 문서 4개 후속 정리 → 매니페스트 42개 재승인) |
| 대조 기준선 | 커밋 `8133c625` — `git archive` 스냅샷만 읽음. 작업 트리의 타 세션 미커밋 17개는 근거로 쓰지 않음 |
| touched_paths (실제) | 루트 `ARCHITECTURE.md` · `docs/README.md` · `docs/CURRENT_STATUS.md` · `docs/AI_CONTEXT.md` · `docs/ARCHITECTURE.md` · `docs/DATA_OWNERSHIP.md` · `docs/WORKSPACE_DATA_CONTRACT.md` · `docs/신원과_모드_가이드.md` · `docs/GENERATION_SUBMISSION_RECOVERY.md` · `docs/CANVAS_GENERATION_IDEMPOTENCY.md` · `docs/TELEMETRY_DRAIN_LIFECYCLE.md` · `docs/SHARE_STATE_RECONCILIATION_DESIGN.md` · `docs/WORKER_OFFDISK_BACKUP_CONTRACT.md` · `docs/SERVER.md` · `docs/SERVER_RECOVERY.md` · `docs/SERVER_RELOCATION.md` · `docs/HF_CLI_UPGRADE.md` · `docs/TESTING.md` · `docs/RISK_REDUCTION_PLAN_2026-08-15.md` · `docs/PM_DASHBOARD_DESIGN.md` · `docs/관리대시보드_통합계획.md` · `docs/사용설명서.md` · `docs/기능설명서.md` · 이 노트(신규) · `D:\vault\01-Wiki\*`(13개 신규) · `D:\vault\index.md` · `D:\vault\log.md` |
| 접촉하지 않음 | 타 세션 미커밋 17개(`CLAUDE.md`·`AGENTS.md`·`DESIGN_RESOLVE_QUEUE_V3`·Resolve 코드·테스트·bat) · 모든 코드 · `backend/data` · `D:\vault\02-Projects` |
| 커밋 | Jay "dev 기준으로 진행" 승인 → `ff2e9f30`(문서 25) · 타 세션 17개 3커밋 · 볼트 `78d3969` |
| updated | 2026-08-26 |

## 왜 했는가

- 앞선 6라운드 교차검증(같은 날 오전)은 문서↔문서 정합(status·잔재)이었고 코드↔문서 전수 대조는 없었다.
- `docs/ARCHITECTURE.md`·`AI_CONTEXT.md` 의 마지막 전면 작성은 06-24 이고 이후 부분 패치만 있었다. 그 사이 코드는
  최적화 R1~R14·위험 축소 RL-01~25 로 대량 변경됐다.
- `updated` 이후 코드 커밋: `GENERATION_SUBMISSION_RECOVERY` 37 · `DATA_OWNERSHIP` 14 · `WORKER_OFFDISK_BACKUP_CONTRACT` 11.
  단 `updated: 08-26` 은 프로퍼티 일괄 작업의 흔적이지 코드 대조의 증거가 아니라, 커밋 수는 우선순위 참고로만 썼다.

## 대상 29개

active 25: `ARCHITECTURE`(루트) `README`(루트) `docs/README` `CURRENT_STATUS` `RISK_REDUCTION_PLAN_2026-08-15`
`docs/ARCHITECTURE` `AI_CONTEXT` `DATA_OWNERSHIP` `WORKSPACE_DATA_CONTRACT` `신원과_모드_가이드` `AUTH_FAILURE_SEMANTICS`
`CLI_ESTIMATE_LIFECYCLE` `CANVAS_GENERATION_IDEMPOTENCY` `TELEMETRY_DRAIN_LIFECYCLE` `GENERATION_SUBMISSION_RECOVERY`
`SHARE_STATE_RECONCILIATION_DESIGN` `WORKER_OFFDISK_BACKUP_CONTRACT` `SERVER` `SERVER_RECOVERY` `SERVER_RELOCATION`
`TESTING` `HF_CLI_UPGRADE` `UPDATE_ANNOUNCEMENTS` `사용설명서` `기능설명서`
review-required 4: `DESIGN_id_unification` `ROADMAP_SCALE` `PM_DASHBOARD_DESIGN` `관리대시보드_통합계획`

## 절차 (합의안 v2.1)

1. 읽기 전용 에이전트 6개가 분야별 묶음(A 구조 지도·B 신원/데이터·C 기능 계약·D 운영·E 사용자/현황·F 설계 문서)으로
   기준선 스냅샷과 대조. 산출 = finding(문서 file:line · 코드 file:line · 판정 · 심각도 P1/P2/P3/U · confidence) + 문서별 커버리지.
2. Codex 가 finding 전체를 반박 검토(read-only). 양쪽 일치만 확정.
3. Claude 가 확정분을 문서에 반영(작업 트리). Codex 가 수정문을 다시 코드와 대조(2·3라운드).
4. 재사용 가능한 일반 지식만 `D:\vault\01-Wiki` 에 기록(구현 복사 금지, 출처는 저장소 경로). Codex 3라운드 검토.
5. 종료 조건: 열린 P1/P2 = 0 · P1/P2 가능성 U = 0 · 동일 매니페스트 양측 승인 · 최대 3라운드.

## 결과

### 대조 결과

| 단계 | 결과 |
|---|---|
| Phase 2 심층 대조 | 에이전트 6개 · 29개 문서 전부 커버리지 보고 · finding 114건(P1 1 · P2 24 · P3 약 70 · U 18) |
| Phase 3 Codex 반박 | P1/P2 25건 → **확정 21 · P3 하향 2(A-03·E-01) · 반박 2(F-05·F-10)** · U 2건(D-09·E-16)은 P1/P2 가능성 없음 · 신규 N-01 · P3 표본 30건 중 반박 6 |
| Phase 4 수정 | 확정분 **49건**을 문서 23개에 반영(+167/-83). Codex 2라운드에서 수정문 자체의 오류 11건(P4-01~11)·미완료 1건(F-07) 재지적 → 3라운드에서 전부 해소 판정 |
| 최종 | Codex 3라운드 **조건부 승인**(조건 = 이 노트 확정) → 매니페스트 39개 승인. 코드·문서 사실관계 잔여 문제 없음 |
| 후속(Resolve 정리) | Jay 확인: origin dev/main·릴리스 `2026.08.25-0847`·작업자 설치본(`Desktop\MV-hub-S`) 모두 `aa0985b9` 직접 전송. 큐 v3 는 이력 → `CURRENT_STATUS`·`docs/README`·`ARCHITECTURE §7.6`·`status/최근작업_2026-08-24 §2` 에서 "재도입 중" 문구 제거·시점 기록. `DESIGN_RESOLVE_QUEUE_V3` 는 타 세션 파일이라 당시 미접촉 → 타 세션 17개를 dev 에 커밋(`0ab260f1`·`c243b211`·`fe1efacf`, 테스트 247 통과)한 뒤 `superseded` 로 전환 |

### finding ledger 요약 (115건)

- **수정 49** — A-01~07·A-11 · B-01~08·B-12 · C-01~06·C-09·C-11·C-13 · D-01~06 · E-01~05·E-14 · F-01~04·F-06~09·F-19·F-26 · N-01
- **오탐 10** — F-05·F-10·F-12·F-14·F-24(초기안·이력 문서를 현행 계약으로 오독) · A-18 · B-09 · E-07(공통 `mutation_notify` 방송이 즉시 반영을 성립시킴) · D-09·E-16(U → P1/P2 가능성 없음)
- **보류 56** — Codex 표본 밖 P3 40 · U 15 · 미분류 1(E-17). **미검증 backlog** 이며 "해결됨"이 아니다. 다음 라운드에서 E-17 심각도부터 확정한다.
  - 보류 ID: A-08 A-09 A-10 A-12 A-13 A-14 A-15 A-16 A-17 A-19 A-20 A-21 A-22 · B-10 B-11 · C-07 C-08 C-10 C-12 C-14 C-15 C-16 C-17 C-18 · D-07 D-08 D-10 · E-06 E-08 E-09 E-10 E-11 E-12 E-13 E-15 E-17 E-18 · F-11 F-13 F-15 F-16 F-17 F-18 F-20 F-21 F-22 F-23 F-25 F-27 F-28 F-29 F-30 F-31 F-32 F-33 F-34
  - 원문(문서 file:line·주장·코드 근거)은 세션 스크래치의 묶음 보고서에 있었고 저장소에는 ID·심각도만 남긴다. 재현하려면 같은 기준선에서 해당 문서·코드를 다시 대조한다.

### 문서별 핵심 수정 (P1·P2)

| 문서 | 무엇을 바로잡았나 |
|---|---|
| `WORKSPACE_DATA_CONTRACT` | **P1** 규칙 9 — `#+`/`#-` 가 프로젝트 소속을 거부가 아니라 이동·해제(`folder_path` 초기화)하는 현행 계약으로 교체(`e53c8497`) |
| `docs/ARCHITECTURE` | 에이전트 목적지 = 로컬 허브(서버 아님) · 미들웨어 7개 · repo 12모듈 추가 · Resolve 큐 잔존(워커 미기동) · 개발 포트 8000 · 제목 MV Hub |
| `DATA_OWNERSHIP` | 비공개 코멘트·슈퍼 관리자 세션·업데이트 공지·이사 공지·audit 행 추가 · 워크스페이스 등록부는 서버 · test_dev 모드 행 |
| `신원과_모드_가이드` | 슈퍼 관리자 가드 행 · AUTH-off 통과 규칙의 예외 · `X-MVHub-Super-Session` 은 권한 세션(신원 축 아님) |
| `AI_CONTEXT` | `gen_request.status` 에 `preparing`·`blocked` |
| `GENERATION_SUBMISSION_RECOVERY` | 복구 절차를 자동 조사(`generate list`)·유일 후보 자동 anchor·`multiple` 보류·fresh `no_match` 2분·409 규칙으로 교체(`a5fec866`) · 배포 정지 503 · 재시도 정책 |
| `CANVAS_GENERATION_IDEMPOTENCY` | workspace scope 미확정이면 `failed`(RL-04) |
| `TELEMETRY_DRAIN_LIFECYCLE` | dead-letter(409×3) 규칙 |
| `SHARE_STATE_RECONCILIATION_DESIGN` | `blocked` 는 미사용, 실제는 `waiting_local(≤5)→rejected`; origin 불일치는 release 만 |
| `SERVER` | `MV_server.bat` 은 `HOST`/`PORT` 만(CONTENT_HUB_* 덮어씀) · PowerShell 표기 · HOST 기본값 |
| `PM_DASHBOARD_DESIGN` | 초기안(create --wait) 미채택 표시 + 현행(create-first · claim→완료 elapsed · 전량 매칭) · 직접 생성물 견적 없음 · `link_generations` id 전용 · manage 로컬 예외 2개 · 텔레메트리 채널 `/api/manage/telemetry/push` |
| `관리대시보드_통합계획` | folder 축 구현됨 · 담당 배정 폐기(`d23d4993`) · bulk API 권한 · `/breakdown` 미사용 |
| `사용설명서`·`기능설명서` | 관리자 창 탭 이름(승인 · 멤버 · 전역 역할 · **공유 서버**) · `Ctrl+K` 는 표시/숨김 · `c`/`h` 단축키 |
| 루트 `ARCHITECTURE` | 로컬 라우트 가드 3종의 범위 차이(Host 검사는 `publish.py` 한 곳) |

### 볼트 위키 (`D:\vault\01-Wiki`, 13개 신설)

기준선 코드를 출처로 한 재사용 지식. Codex 3라운드(21 + 6 + 2건 반영) 후 조건부 승인 → 문구 2건 반영.
SQLite WAL과 foreign_keys 재설정 · Windows asyncio subprocess와 Proactor 루프 · npm 전역 CLI의 CMD 셰임 · 배치 파일 인코딩과 코드 페이지 ·
NoDefaultCurrentDirectoryInExePath · DaVinci Resolve 스크립팅 Python 버전(confidence low) · HTML5 드래그 함정 · 전역 button 규칙과 아이콘 버튼 폭 ·
상태 수명을 컴포넌트 수명과 분리하기 · CLI 메이저 업그레이드 절차 · 원격 생성물 URL 만료와 바이트 보존 · 가짜 진행률 대신 상태 전이 ·
동시 작업 트리의 감사 기준선(confidence low).
경계: 구현 세부(수치·컬럼·정책)는 이 저장소 문서에만, 볼트에는 일반 원리만. `02-Projects/MV-hub-S.md` 허브 페이지는 Jay 승인 후 신설(볼트 `78d3969`).

### 이번 작업에서 밟은 함정 (다음 세션용)

1. **bash heredoc 은 본문 바이트가 크면 종결자가 잘려 `unexpected EOF`** — 한글 많은 스크립트는 파일로 써서 실행한다.
2. **Python 삼중따옴표 안의 `" + BS + "` 는 문자 그대로 들어간다** — 백슬래시는 문자열 밖에서 `chr(92)` 로 이어 붙인다(P4-09 로 실제 유입, Codex 가 잡음).
3. **`git ls-files` 는 한글 파일명을 8진수로 이스케이프** — `-c core.quotepath=false` 없이는 존재 검사가 실패한다.
4. **finding 을 문서로 옮길 때 과장·확장** — "약 3.5초", "이벤트 로그", "요청→완료", 엔드포인트 이름을 근거 없이 넓혀 11건이 재지적됐다. 코드 근거 file:line 의 문자 그대로만 옮긴다.
5. **`git archive` 스냅샷에는 미추적 파일·`.git` 이 없다** — 리뷰어가 인계 노트·커밋 해시를 못 보므로 별도로 넘긴다.

### 다음 라운드 후보

- 보류 56건 재검증(E-17 심각도 확정 우선). F 묶음은 문서 위상(초기안/현행) 먼저 판정.
- Codex 가 제안한 위키 후보 10건: `async_tools` 취소 자원 소유권 · `atomic_io` 원자 쓰기 · `path_safety` · `net_guard` SSRF · `upload_limits` · single-flight(`cli_bridge`) · 디바운스 writer revision · SQLite 유지보수 게이트·pool epoch · WS bounded queue
- 코드 사안(문서 밖 — 별도 결정): `resolve_queue*` 잔존·`/api/resolve/queue*` 라우트 노출 여부 · `release_update.py` 큐 스캔 · 루트 `README.md` 줄바꿈 혼합(CRLF/LF) · `schema.sql:321` 상태 주석 `running` 누락 · `rbac.py` docstring `product_director` → **3차(아래)에서 처리**
- Jay 결정: Seedance r2v 판정 기억을 볼트 `00-Raw` 에 던질지 (02-Projects 허브 페이지는 신설 완료)

## 2차 라운드 (2026-08-27, 기준선 `5f7a1853`)

Jay 지시로 "코드 안 건드리는 작업 5개"를 병렬 진행. 에이전트 4개 + Codex 4라운드.

| 작업 | 결과 |
|---|---|
| 전체 회귀(격리) | 백엔드 **1,854 + 29 subtests** 통과(3분 29초) · 프론트 **98파일 679** 통과 · `lint:architecture` 이상 없음 · `build` 성공 → `CURRENT_STATUS` 회귀 표·기준선 갱신 |
| 위상 확정 | `PM_DASHBOARD_DESIGN` → archived · `ROADMAP_SCALE` → active · `관리대시보드_통합계획`·`DESIGN_id_unification` → draft. `review-required` 0개 남음 |
| U 16건 재판정 | 일치 6(오탐으로 닫음) · 불일치 P3 4 수정(A-22·C-18b·D-08·F-33a) · 회귀로 확정 2(A-21·E-15) · 승인된 보류 4(A-20·E-17·E-18·F-32 — 외부 데이터·문서 밖). Codex 전건 확정 |
| 표본 밖 P3 추가 수정 | B-10·B-11(DESIGN_id_unification) · F-27(ROADMAP C 대조 포인터) — Codex 확정 |
| Codex 가 잡은 내 과장 | `fe1efacf` "동작 영향 없음"(큐 API 응답은 바뀜) · `workspace*` 를 CLI 1.x 출력으로 오기 · 테스트 격리를 setUp 단일 방식으로 일반화 · 관리대시보드 담당 축 잔재 3곳 |
| 위키 | 코드 근거 명확한 후보 9개를 에이전트 초안 → Codex 3라운드(승인 3·수정 6 → 재작성 → 1건 재수정) → 볼트 통합 `80674a9`(총 27페이지) |

ledger 누계(115건): **수정 58 · 오탐 16 · 승인된 보류 4 · 보류 37**(표본 밖 P3, 행동 영향 없음). 열린 P1/P2 없음.
보류 37 ID: A-08 A-09 A-10 A-12 A-13 A-14 A-15 A-16 A-17 · C-07 C-08 C-10 C-12 C-14 · D-07 · E-06 E-08 E-09 E-10 E-11 E-12 E-13 · F-11 F-13 F-15 F-16 F-17 F-18 F-20 F-21 F-22 F-23 F-25 F-28 F-29 F-30 F-31

## 3차 — 코드 사안 6건 (2026-08-27, Jay 가 코드 수정 허용)

| 항목 | 결과 | 커밋 |
|---|---|---|
| (c) 낡은 주석 6곳 | manage 기본 on · `product_manager`(구 product_director) 주석 10곳 · 잡 큐→주기 작업 · gen_request 흐름 anchor/reconcile(`/fulfill` 은 구버전 호환) · status `running` · `resolve_queue*` 휴면 표기. 동작 변경 0 | `f78eafe4` |
| (d) 테스트 DB bat 하드코딩 | `test_pull-db.bat` 기본값 유지 + `MVHUB_SNAPSHOT_SERVER` env 덮어쓰기(명령줄 인자는 파싱 시점에 확장돼 일부러 없음) · `!SERVER!` 파싱 후 확장 · `tools/refresh_pm_test_data.py` 가 `http://host:port` 만 허용(원문 그대로 검사, 종료코드 2) | `e3d802ac` `0090935b` `3fa4e862` |
| (e) 생성 bat 한글 | `/api/agent/run-bat` 본문 ASCII 영어 · server·email·pin 필드별 allowlist(선행 `-`·`%` 거부, 위반 400) · 본문 ASCII 가드 테스트 | `ab7fe9be` `0090935b` |
| (b) 업데이트 잠금의 큐 스캔 | v3 manifest 스캔 → 진행 중 직접 전송 카운터(`resolve_transfer.track_active`, `threading.Lock`). 순서 계약 = 카운터 증가 → `update_in_progress` 게이트(409). 실제 `checking` 상태 파일·취소·동시·반대 순서 테스트. 프론트 `resolve_active` 타입·차단 문구 분리 | `e5d246b5` `0090935b` `3fa4e862` |
| (a) 큐 잔존 코드 | 라우트 3개·워커 훅·`/status` v3 재평가·미호출 helper(`_accept_account_scope` 등)·`idempotency_key`·프론트 큐 헬퍼·고아 CSS 제거(−800줄). 모듈은 유지(현행 사용: `run_non_abandon`·`/locks`·status runner 자식). `/resume` 의 v3 manifest 수동 복구 경로 손실을 문서에 명시 | `f47104e0` `0090935b` |
| (f) 루트 README 줄바꿈 | **오탐** — 인덱스(`git ls-files --eol`)는 전부 LF, 혼합은 작업 트리 체크아웃 현상으로 추정. 저장소 변경 없음 | — |

절차: Claude 설계 v1 → Codex 반박(사실 정정 13건 — `build_account_scope` 는 미호출 helper 만 씀, 프론트 테스트는 `frontend/tests/`, 카운터는 워커 스레드에서 읽힘, 업데이트 진입 경쟁, `product_director` 잔존 10곳 …) → v2 → Codex 수정 요청 4건(카운터→게이트 순서·`/resume` 손실 명시·bat 입력·필드별 allowlist) → v3 델타 전부 수용 → 구현 5커밋 → Codex diff 리뷰(P2 2·P3 4) → `0090935b` → 재리뷰(P3 2) → `3fa4e862` → **승인**(미해결 0).
회귀(최종 `3fa4e862`, 격리): 백엔드 **1,862 + 46 subtests**(3분 26초) · 프론트 **98파일 672** · `lint:architecture`·`build` 이상 없음.
남은 위험: Resolve 자식 반입에 timeout 이 없어 자식이 멈추면 카운트가 유지돼 업데이트가 막힌다(종전 `_IMPORT_LOCK` 과 같은 성질, 범위 밖). `resolve_queue_worker.py`(휴면 1,127줄)·워커 의존 테스트는 2026-08-27 Jay 지시로 삭제(호출자 0 확인, AST 로 워커 없이 못 도는 테스트만 제거). `resolve_queue.py` 도 같은 날 기준선 정리 — 현행 경로가 쓰는 `run_non_abandon` 만 남기고(2,005→33줄) 그 코드 없이는 못 도는 테스트 57개 제거(섞여 있던 살아있는 계약 2개는 큐 코드 없이 재작성), 동작 코드 무변경. 반입 자식 시간제한 없음은 Jay 결정으로 현행 유지(종결).
함정(다음 세션용): bat 의 `%~1` 은 delayed expansion 보호를 받지 못한다(파싱 시점 확장) — 외부 값은 env 로 받고 `!VAR!` 로만 쓰며 형식 검증은 Python 에서. `git diff`·`grep` 은 저장소 루트에서(셸 cwd 가 `backend`/`frontend` 로 남으면 빈 결과가 "이상 없음" 으로 읽힌다). "import X" 존재 단언은 문자열 안의 텍스트에도 걸린다 — 실제 import 는 `ast` 로 확인.

### 3차 실측 (2026-08-27, Jay 지시 — Claude·Codex 각자)

| 항목 | Claude | Codex(독립) |
|---|---|---|
| 게이트 409 | 격리 서버(8000, `data_test`, AUTH 끔)에서 `write_state("checking")` → `POST /transfers` **409** "프로그램 업데이트가 진행 중이라 Resolve 전송을 시작할 수 없습니다" → 상태 파일 삭제·`update_in_progress()` False | 1차는 AppData 쓰기 제한으로 불가 → `MVHUB_UPDATE_STATE_DIR` 을 작업 폴더로 옮겨 재기동한 뒤 **409 재현**, 삭제 후 2차 POST 404 |
| 진행 카운터 | '뻘뻘뻘' 렌더 루트를 스크래치로 바꿔 40건(1,138MB) 전송(0.95초): 12샘플 중 8회 `resolve_active=1`·`can_update=false`(0.23~1.02초), 종료 후 0. Resolve 는 대상 불일치(`MVHUB-MEASURE-NO-SUCH-PROJECT` vs 열린 "Untitled Project 11")로 거부 → 클립 미삽입. 복사본 40개 전부 스크래치(NAS 0). 렌더 루트 `Z:\PROJECT_MUDX\10_ai` 원복 | 같은 방식 40건(1.76초): 19샘플 중 15회 `resolve_active=1`(0.11~1.70초), `active_total=2`(잔존 생성 1 + Resolve 1)·`can_update=false`, 종료 후 0. 거부 `project_changed`, 렌더 루트 원복·복사본 삭제 확인 |
| 런처 창 닫기 | `test_dev` 사본(포트 3173) 실제 콘솔 창 → 8012 health 7.8초·Vite 9초·`agent_push` 기동·Chrome 32→37 → `CloseMainWindow` → 11초 뒤 8012/3173 리스너 0, 트리 0(무관한 08-24 잔존 `test_dev.bat` 창 41332 만 남음), Chrome 37 유지. 로그 끝 "[stopped] Agent and local services stopped. The browser remains open." | 샌드박스가 콘솔·CIM 접근 거부 → 단위 테스트 `-k breakaway` 2 passed. 내 증거 감사: 결론 지지, 단 "10초 이내"(실제 11.0초)·특정 탭 생존·Chrome 미실행 케이스·반복성은 미증명, PASS 조건이 기존 프로세스를 제외하지 않아 정상인데 CHECK 표기 |

환경 함정: 5173 이 Windows 예약 포트 범위(5141–5240) → `test_dev.bat` Vite `EACCES`(코드 아님). 4173 은 다른 앱의 클라이언트 소켓이 점유 → 실제 bind 시험으로 3173 선택.
안전: 실사용 8010·팀 서버·NAS·Resolve 프로젝트·`backend/data` 미접촉. `data_test` 변경은 렌더 루트(원복)뿐. 임시 bat 사본·스크래치 복사본 삭제.
