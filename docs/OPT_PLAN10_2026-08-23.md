# 최적화 라운드 11 (2026-08-23) — Opus 주도·코덱스 선별 투입 체제

★진행 현황: 프런트 23건(6cd662ca·b248d01b·4a8fd250)+백엔드 17건·A7 잔여·C1
(5660e7ea·14917a40·93ef219b·e7936a32 직전·e7936a32) 반영. 회귀 백엔드 1,500·프런트 638.
적대 리뷰(코덱스) P1 3건 수정 중: ①finalize 라우트 전체 scope 고정(캡처가 add_task
직전뿐) ②history acc/key·scope 원자 캡처 ③씬 저장 실패를 성공처럼 반환(R11-14 회귀).
리뷰 통과 확인: A1 캐시 키 정합·C1 부수효과 동등·모듈 캐시 계약.

리뷰 P2 11건 → R12 이월: 게이트 중 401 vs 503 구분 · 코멘트 mutation 후 refresh 의
전환 경합 2곳 · DashboardView finally loading · 워크스페이스 전환 시 drill 필터 미초기화 ·
assetVersions 영속 실패 후 재시도 없음·손상 버킷이 전체 로드 중단 · busyUid 행 단위 한계 ·
_history_account·_agent_acc 루프 위 SQLite · starter 예약-등록 경계 boot epoch ·
_rl 축출이 미만료 키 제거(제한 초기화 가능).

★체제 재편(Jay 지시): 서베이·구현 주력=클로드 Opus 서브에이전트, 코덱스(gpt-5.6-sol)=
까다로운 핵심(C1 등)+적대 리뷰, Fable=승인·diff 검토·회귀·커밋. 종료 조건="최적화가
필요 없어질 때까지"(서베이 2회 연속 실질 후보 0=수렴 판정). 서베이 상세=scratchpad
codex_r11 아님 — Opus 에이전트 결과(이 장부가 권위 요약).

## 프런트엔드 (Opus 서베이: 후보 23건 — 핵심부는 수렴, 결함은 신규 코드 집중)

- 웨이브 1 ✅완료(6cd662ca): R11-1 배지 폴 500 분할(P1급) · R11-2/3/4 응답 순서 가드 ·
  R11-5 역할 칩 잠금 · R11-6 프로젝트 로더 가드. vitest 618.
- 웨이브 2(진행 중): R11-7/8 워크스페이스 전환 잔존 수치·loading 소유권 · R11-9 씬 실행
  무음 거부 안내 · R11-10 죽은 계산 제거 · R11-11 Intl 재사용(출력 동일성 조건) ·
  R11-12 마퀴 저장 throttle.
- 웨이브 3(대기): R11-13 assetVersions 누적·persist 분리(썸네일 정책 불변) · R11-14 씬
  저장소 3파싱→1 · R11-15 sceneEdges 인덱스(실측 조건) · R11-16 모델 모달 캐시 모듈화 ·
  R11-17/18/19/20/21/22/23 소형.
- 기각 12건(WS setGens·누수 전수·무한루프·언마운트 setState 등 — 근거 확인 완료, 재제안 금지).

## 백엔드 (Opus 서베이: 수렴 아님 — auth.py 가 전 라운드 미조사였음)

- A1(★) auth 시크릿 조회가 인증 미들웨어에서 루프 위 동기 SQLite×2 + maintenance gate
  무한 대기 → 복원 중 서버 전체 정지. 처방=(db_path, pool_epoch) 키 프로세스 캐시.
  ★캐시에 pool_epoch 필수(복원 시 auth_secret 회전 = R7 0-B 전 토큰 무효화 계약).
- A2 get_secret read-generate-write 경합 → DO NOTHING+재SELECT(A1 묶음).
- A3 롱폴 사유 강등 "event" → None 1줄(에이전트 idle 폴백 복구).
- A4 수제 non-abandon 3곳(db_backup×2·auth login limiter)이 반복 취소에 뚫림 —
  R7 헬퍼의 원조 선례가 미이관. to_thread_non_abandon 으로.
- A5 /api/db/import 업로드 복사 1곳 plain to_thread → 임시파일 최대 512MiB 잔존.
- A6 finalize 백그라운드 보존이 루프 위 동기 DB+계정 미고정 → 캡처+override+to_thread.
- A7 history 라우트 2개가 루프에서 transition_lock → 캡처를 to_thread 로(라우트는 async 유지).
- A8 history 자동시작 starter 미등록 → _HISTORY_STARTERS+선취소(백로그 REP-HAF-2 채택).
- A9 auth _rl_fails 무제한 증가(비인증 원격 트리거) → 기존 상한+스윕 패턴 재사용.
- B1~B5 저비용: 레거시 백업 비밀검증 1줄 · validate_hub_db mode=ro · mcp_ingest 방어 2줄 ·
  _NOTIFY_EXCLUDE 경로 불일치 · _probe_fps Stream 앵커.
- C1(코덱스 담당) finalize↔unpublish TOCTOU(서버 본체) → repo transaction-root 2개.
- C2 sync_now 계정 범위 캡처+override(CLI·캐시 정책 불변).
- ⏸C3 Resolve import 큐잉 제거 — 실기기 확인 필요, Jay 결정 대기.

## 반대 확정(재제안 금지)

백로그 3건(RR BUSY sentinel·RR lock 아래 2회 조회·purge 고아 intent — 발현 경로 없음/
무해/대안이 더 위험) · rbac ADMIN create_project(설계 의도, projects.py:310 주석만 정정
대상) · comfy node_id 정렬·redact 후치·agent_signals waiter 일괄 해제 등(서베이 §5).

## 하지 말 것(서베이 자체 명시 — A1 pool_epoch 필수·Resolve timeout 절대 금지·RR
blocking=False 불변·plain to_thread 전수 교체 금지·_rl in-flight 예약 의미 불변·
R1~R10 완료·반대 재조사 금지 + 누적 금지 전부)
