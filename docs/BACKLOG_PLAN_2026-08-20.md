---
updated: 2026-08-20
status: archived
---

# 백로그 정리 실행 계획 (2026-08-20, 클로드 초안 → 코덱스 xhigh 적대 검토 → 합의)

기준 커밋 `0797f80b`. 잔여 상태의 단일 출처는 RISK_REDUCTION_PLAN Gate 0 표(유일 잔여 RL-23 🔶).
이 문서는 REVIEW_2026-08-17.md P2 백로그 + 수신보장 잔여 + 청소류의 실행 순서를 확정한다.
코덱스 검토 원문: 세션 scratchpad `backlog_plan_codex_out.md` (항목별 파일:줄 근거 포함).

## 검토에서 확정된 사실 (초안 대비 변경)

- **제외(이미 해결)**: restore finally 원인 가림(`restore_runtime_verify.py:213-229`에서 이미 보존).
- **성격 변경**: preparing+placeholder 잔류 — 정상 재시작·재접속은 이미 복구됨. 자동 sweep 은
  위험(서버가 브라우저 로컬 씬을 모름 → 복구 가능하던 카드 소멸). 측정+수동 격리로 축소.
- **성격 변경**: 부팅 백업·쿼터 스캔 — 이벤트 루프는 이미 to_thread. 실제 문제는 readiness 대기와
  "비용 지불 후 중복판정". local_stamp 선확인/추적형 백그라운드 초기화로 접근.
- **확대**: 워치독 — 포트 오진·TOCTOU 외에 더 큰 구멍 2개: ①HTTP 200 본문 미확인(다른 서비스
  200을 정상 오판) ②PID 판별이 "serve.py" 부분문자열(`server_watchdog.py:258-263,299-306,347-360`).
  `restart_server_task.ps1:36-60`의 절대경로 판별 재사용.
- **신규 추가(초안 누락)**: `_mark_success` 고착 — 서버 ACK 후 로컬 done 기록 실패 시 자식 종료,
  부모는 비정상 종료에 recover 안 함(`worker_backup.py:764-787,853-856,1000-1021`). 우선 배치에 포함.
- **신규 추가**: 혼합 배포 이중과금은 "배포 순서 주의"로 부족(구 에이전트는 submission-stage 미사용,
  `routers/gen_requests.py:338-356`). 유료 claim 최소 에이전트 버전 게이트 또는 배포 중 생성 일시중지.
- **HF 멱등키**: CLI 1.1.23 `generate create --help` 실측에 멱등키 옵션 없음 → **지원 확인 전까지
  미지원으로 취급**. 대신 MV-hub 자체 요청 멱등키(HTTP 재시도·더블클릭 중복 방지)는 지금 구현 가능
  (일반 요청은 매번 새 ID INSERT, 캔버스만 유니크 키 존재 — `db_migrations.py:482-484`).
- **poison 행**: "409면 전부 dead-letter" 금지 — 인증·계정 불일치 409는 전체 설정 오류일 수 있음.
  로컬 디코드 오류만 즉시 격리, 서버 오류는 항목별 ACK/분할 재전송으로 원인 행 특정.
- **reconciliation reducer**: 이미 공통 reducer 존재·9개 화면 사용 중(`stateReconciliation.ts`).
  전면 통일이 아니라 실측된 hotspot(예: ProjectManagerPanel, MountManager)만 적용.
- **고아 파일**: "261개"는 낡은 수치 — data_test 거친 재측정 = 영구 406/참조 99/후보 307.
  trash·manage 미대조 상태이므로 삭제가 아니라 분류 보고서까지만.
- **D:\ClaudeCode\MV-hub-S 정리 = 고위험**: backend/data 1.25GB·8,772파일, **최신 변경 2026-08-18**
  (아직 뭔가 쓰고 있을 가능성 — 실사용 배포본일 수 있음). 예약작업·바로가기·데이터 확인 +
  Jay 승인 전 절대 삭제 금지. 코드 배치와 완전 분리.
- **Resolve CF-PC01 실측 ≠ RL-23**: Resolve 가져오기는 이미 통과 기록. 별도 배포 점검 트랙.
- **팀탭 비공개 뱃지 합산**: viewer별 규칙이 맞음(중앙 집계는 비공개 누설 위험) — 문서화로 종결.

## 실행 순서 (배치별 별도 커밋·별도 리뷰 게이트, 한 배치=한 장애 영역)

| # | 배치 | 구현 → 리뷰 |
|---|---|---|
| 0 | 읽기 전용 기준선 측정: HF 계약 보존, 고아 분류 보고서, 레거시 workspace/시각 포맷 분포, 부팅 백업·쿼터 스캔 소요. RL-23 물리 훈련은 별도 병렬 트랙 | 코덱스 조사 → 클로드 판단 |
| 1 | ✅과금 경계 완결: 일반 생성 요청 멱등키 + 혼합 배포 게이트 A안(2026-08-20 Jay 결정→코덱스 구현·클로드 검토·전 회귀 통과 — submission-stage+agent_id 미달 에이전트는 pending/pending-exists 에서 차단, WS flash "업데이트 필요" 안내 5분 스로틀) | 코덱스 → 클로드 완료 |
| 2 | ✅수신·전달 무결성(2026-08-20 코덱스 구현·클로드 검토·전 회귀 통과): `_mark_success` 비정상 종료 복구 + poison 행 dead-letter(디코드 오류만, 409는 재시도 유지, 행별 전송) + 거래 stable ID(이메일 기반, 옛 ID 재사용 이중 대조 + 중복 병합 마이그레이션) | 코덱스 → 클로드 |
| 3 | ✅구현 완료(2026-08-20, 설계 v2 계약대로 3a 원장 838bdae2 + 3b reconciler 9034aaa3 — feat/share-reconciliation 에서 코덱스 구현·클로드 검토, 리뷰가 신원 리맵 누락·계층 역전 2건 포착·수정, 전 회귀 1106 통과 후 dev 병합) | 코덱스 → 클로드 완료 |
| 4 | ✅이벤트 루프·부팅(2026-08-20 코덱스 구현·클로드 검토·백엔드 1070 통과, 커밋 700315ed) | 코덱스 → 클로드 |
| 5 | ✅워치독 단독(2026-08-20 코덱스 구현·클로드 검토, 커밋 81f7b4ca): 200 본문 계약 검증 + 절대경로 정확 일치 PID 판별 + kill 직전 재확인(TOCTOU) + port_hijacked/identity_mismatch 경보 전용, fail-closed | 코덱스 → 클로드 |
| 6 | ✅WS 단독(2026-08-20 클로드 구현·코덱스 적대 리뷰 P0 1+P1 3 반영·전 회귀 통과): 연결별 단일 sender + 크기 제한 queue(64) + reload 병합(빈 배열='전체' 보존) + 수거 시 sender 종료 대기 후 close + overflow 수거 백그라운드화 + 원격 브리지 재연결 catch-up | 클로드 → 코덱스 완료 |
| 7 | ✅asset watcher 단독(2026-08-20 코덱스 구현·클로드 검토·전 회귀 1096 통과): 1초 건강검진 + 핸들 세대 번호 + 폴더 ID(inode·ino=0 폴백) + NAS 백오프 1→8초·유예 30초 + delete-pending 옛 핸들 선해제. Windows tmp 실측 반복 20/20. 잔여: 실 NAS 실측(유예 적정성·stat 비용) | 코덱스 → 클로드 완료 |
| 8 | ✅4종 완료(2026-08-20, 05fa13a0 — feat/manage-ui 에서 클로드 구현·코덱스 리뷰 P1 3건 반영 후 dev 병합): selector 독립선택(A안·pinned) + archived 배지·흐림 + 출처 라벨·실크레딧 미매칭 건수 + AUTH-off 1008 reason 구분. **잔여: 레거시 시각 보정(배치 0 측정 후)·시퀀스 카드 출처 라벨** — P2 소진 계획으로 이관 | 클로드 → 코덱스 완료 |
| 9 | 측정 후 성능·청소: 쿼터 증분 회계(시작 시 전체 재계산+주기 drift 대조 필수) → hotspot reducer → `ensure_ingested_tracked` 제거 | 코덱스 → 클로드 |
| 10 | 전략 P2 착수 조건 문서화 후 보류: 외부 보존 tier·provider webhook·scenecard job_id 앵커 | 클로드 → 코덱스 |
| 11 | 외부 파일 정리(고아 삭제·낡은 배포본): 코드 배치 종료 후, 백업·복구 가능 절차로 | **Jay 승인 필수** |

## Jay 결정 (2026-08-20 확인)

1. **배치 1 — 혼합 배포 게이트 방식**: **A안 확정(2026-08-20)** — 유료 claim 최소 에이전트
   버전 게이트. 도장(submission-stage) 미지원 구 에이전트의 유료 생성은 서버가 거부하고
   "업데이트 후 이용" 안내. 기준은 도장 버전 이상(이후 버전 전부 통과). 이번 전환 1회용 강제.
2. **배치 8 — 관리창 워크스페이스 선택 정책**: **A) 독립 선택 확정** — 열 때는 메인 창 팀으로
   시작하되, 관리창에서 바꾼 선택은 그 창에서 유지(메인 창 저장이 덮어쓰지 않음).

## 진행 규약

- 격리 워크트리에서 구현(커밋 금지) → 반대측 diff 적대 리뷰 → 본 저장소 반영 → 전체 회귀
  (백엔드 pytest·프론트 vitest·tsc) → 배치별 커밋. 실사용(8010)·test_dev(8012/5173) 무간섭.
- 각 배치 완료 시 REVIEW_2026-08-17.md 해당 항목에 상태(유효/부분 해결/제외/조건부)와 확인
  커밋을 기록해 재구현 방지.
