---
aliases:
  - 2026년 9월 종합 점검
tags:
  - mvhub
  - mvhub/검증
status: active
updated: 2026-09-18
---

# 프로그램 종합 점검 — 2026-09-17

## 범위·협업·권한

- owner: Codex (통합·백엔드/실측), reviewer: Claude (독립 화면 조사·설계 및 구현 검토).
- status: 완료(이번 격리 종합 점검의 증거·수정 종결, 배포 승인 아님). B1/B3/E1/M1/B2/B4/B5/D1/D2/C1/E4/C6/U1/R1/W1/C4/C5 및
  C4 hover/F1·C3/E3·U2/R1 후속 코드 독립 승인. E3 후속 완료,
  R1/C3 검토 권고 시험 완료. E2/E5 접수 CAS 후속 독립 승인·관련 시험 완료.
  2회차 B6/B7/B8/B9/F2와 B4 댓글 후속까지 구현·독립 검증 완료. 최종 전체 백엔드 통과,
  최종 15분 실측 통과와 실제 Claude R8 승인·기록 보완 재확인까지 완료했다(09-18).
- touched_paths: 이 문서, `docs/CURRENT_STATUS.md`, `docs/README.md`,
  `backend/app/active_account.py`, `backend/app/routers/projects.py`, `share.py`,
  `resolve_integration.py`, `backend/tests/test_project_assign_account_pin.py`,
  B3 `backend/app/repo/projects.py`·멤버 이름 회귀, E1 `backend/app/routers/comfy.py`·접수 회귀,
  M1 `frontend/src/lib/modelCatalog.ts`·이름 캐시 회귀. 이후 승인 묶음의 소유 경계는 아래 기록한다.
- 기준: `dev` `4df0eb93d397826507d3a1be881814a94455147e`, 시작 시 작업 트리 clean.
- 사용자 목표: 전체 안정성·같은 기능의 유해 중복·최적화를 실측하면서 반복 점검한다.
  문제 수집 → 설계 → 실제 Claude/Codex 합의 → 구현 → 독립 검토 → 재측정 순서를 지킨다.
  일반적인 설계 선택은 두 검토자가 근거를 비교해 결정하되 운영/데이터 안전선을 넓히지 않는다.
- 운영 서버, 사용자 `backend/data`, 설치본, 실제 Resolve 프로젝트를 변경하지 않는다.
  유료 생성·외부 복구·배포·앱 재시작은 이번 점검의 자동 단계가 아니다.
  합성 데이터와 격리 서버에서 실제 HTTP/브라우저/프로세스 자원을 측정한다.
- 이전 [최적화 시리즈](OPT_PLAN12_2026-08-23.md)는 종료된 별개 작업이다.
  종결·반대된 후보는 새로운 재현/실측 근거 없이 다시 제안하지 않는다.
- 위험 상태의 정본은 [위험 목록 Gate 0](RISK_REDUCTION_PLAN_2026-08-15.md)다.
  이 장부는 이번 조사의 증거·후보·합의·검증 회차를 관리한다.

## 완료 기준과 진행률

12개 영역마다 다음 네 증거를 각각 확인한다. 전체 **48개 확인 항목**이다.

1. 경계: 호출 흐름·데이터 소유자·공통/별도 구현 및 의도적인 차이 확인.
2. 정상/경계: 현재 기준 코드에서 일반 동작과 빈 값·대량·다중 사용자 경계 검증.
3. 실패/경합: 취소·재시도·오류·동시 실행·오래된 응답·재시작 중 해당 위험 검증.
4. 실측: 격리 실제 실행에서 지연·자원·종료/정리 관측. 외부 실기기가 필요한 부분은 한계를 명시.

진행률은 **증거를 충족한 항목 수 / 48**이다. 조사 착수나 단순 코드 읽기는 통과로 세지 않는다.
수정 완료율은 별도로 **독립 검토와 재검증까지 끝난 승인 문제 / 전체 승인 문제**로 표시한다.
문제가 아직 확정되지 않았으면 수정 완료율은 산정 중이다. 전체 범위를 줄여 완료율을 높이지 않는다.
해당 없는 위험은 이유와 Claude 검토가 있어야 항목을 닫는다.
완료는 48개 증거 점검과 발견된 큰 문제의 해결, 다음 회차에서 큰 회귀가 없다는 재확인까지다.
실측 불가능/권한 밖의 증거는 통과로 바꾸지 않고 제한과 필요한 다음 행동을 남긴다.

| ID | 영역 | 경계 | 정상/경계 | 실패/경합 | 실측 |
|---|---|---|---|---|---|
| A01 | Workspace·Share & Review 카드 표시/상태 | 확인 R1-F | 확인 R2-F | 확인 R3 | 확인 R3(단일 카드) |
| A02 | Canvas·History·씬 저장/복원 | 확인 R1-F | 확인 R2-F | 확인 R2-F | 확인 R3(합성 1/50장) |
| A03 | 공통 작업: 확인·다운로드·원본 열기·폴더 이동 | 확인 R1-F/B | 확인 R3 | 확인 R3 | 확인 R5(ASGI/SQLite 이동) |
| A04 | 폴더·생성자·태그·색상·검색/필터 집계 | 확인 R1-F/B | 확인 R2-F | 확인 R3(로컬 계정 고정) | 확인 R3(합성 집계/폭) |
| A05 | 계정·워크스페이스·권한·인증 경계 | 확인 R1-B | 확인 R4* | 확인 R8(최종334PASS 대표 집합) | 확인 R6(AUTH-on ASGI) |
| A06 | 관리창: 작업·대시보드·테이블/보드/캘린더·보관 | 확인 R1-F/B | 확인 R2-F/B2 | 확인 R2-F/B2 | 확인 R4(WorkBoard30/300) |
| A07 | Higgsfield·에이전트·생성 제출/추적/재시도 | 확인 R1-E | 확인 R4* | 확인 R4* | 확인 R4(합성provider/실제agent) |
| A08 | Comfy 실행·취소·복구·결과 연결 | 확인 R1-E | 확인 R4* | 확인 R4* | 확인 R5(profile-off 대조) |
| A09 | Resolve 진단·전송·선택 복원·프로세스 정리 | 확인 R1-L | 확인 R4* | 확인 R4* | 확인 R3(FakeResolve/Windows 자식) |
| A10 | Assets·미디어·썸네일·경로·가져오기/내보내기 | 확인 R1-D | 확인 R4* | 확인 R4* | 확인 R3(92B loopback) |
| A11 | 실시간 갱신·조회/캐시·응답 순서·장시간 자원 | 확인 R1-W | 확인 R2-F | 확인 R2-F | 확인 R5(2창/WS/해제) |
| A12 | DB·백업/복원·기동/종료·업데이트/배포 안전성 | 확인 R1-L | 확인 R4* | 확인 R4*(209보완) | 확인 R7(합성 복원/실제 자식 기동) |

현재: **48/48 (100%)**. 승인 수정 묶음은 **27/27 (100%)** 독립 검토·재검증 완료.
실제 Claude R8 `a94242c6-4e78-4210-8de7-24f39b3e9837`가 마지막 A05 칸과 R4* 최종전체 조건의
충족을 승인했다. 아래 최종 통합 결과에 필수 기록 보완 5건을 반영했고,
Claude `8f39cf94-42be-452d-969d-b357c9cac881`가 기록 보완·세 사실 정정·종결 문구를 재확인 승인했다.
B4 댓글 후속은 정정 BEFORE의 7개 반례를 최소 수정한 뒤 112개 관련 검사·실제 Claude 독립 승인·
루트의 473파일 해시/원자료/종료 대조를 마쳤다. 기존 묶음의 파생 수정으로 중복 계수하지 않는다.
R4* 정상/실패11칸은 기존 전체 결과와 후속209/98을 연결한 대표 계약 승인이다. 최종 전체에서
같은 결과를 재확인하는 조건은 단일 최종전체 실행과 R8 대조로 충족했다. 개별 묶음을 합쳐 전체 green으로 표기하지 않는다.
승인27은 B1/B2/B3/B4/B5/B6/B7/B8/B9, E1/E2/E3/E4/E5, C1/C3/C4/C5/C6, M1/W1/U1/U2/R1, D1/D2, F2다. 최종 전체와 15분 부하는 통과했고 R8의 기록 보완을 반영했다.
초기22묶음 완료 후 2회차 B6/B7/B8/F2 네 건을 승인해 분모를26으로 갱신했다. 후속 계정3건도 실제 Claude 승인·420건 통과와 루트 검토를 마쳤다.
C2는 결함이 아닌 의도 차이로 제외했다. 파생 수정은 해당 초기 묶음에 포함해 중복 계수하지 않는다.
R3 B9의 동적 반례를 확정해27번째 승인 묶음으로 추가했다. F2 후속은 기존 묶음에 포함하며 완료했다.
이는 현재 승인된 묶음의 수정 완료율이며, 추가 발견이나 최종 전체 재검증 완료를 미리 보장하지 않는다.
이는 이번 종합 점검의 증거 진척이며 제품 품질 점수가 아니다.
경계12개와 R2 정상/실패8칸, R3 정상/실패4칸 및 실측5칸을 확인했다. 대표 계약의 검증이며 전체 App E2E·실기기·모든 조합의 보증이 아니다.
R3는 실제 Claude 응답 `11e569cd-64ce-4b90-a3f8-2bedc7b460b8`을 루트가 전문 대조했다. 응답 수신 후 재계산에서 프런트551파일과 B6–8 소유4파일의 검증 당시 해시가 모두 동일했다.
UI 실측3칸은 DOM/RAF와 종료 정리의 좁은 관측이다. 실행 중 브라우저 RSS/heap·실제 페인트·백분위수·실서버는 미측정이며 실사용 보증으로 확대하지 않는다.
R3의 팀 프록시 신원/비정상 이동 응답 후속은 아래 B9/F2 기록대로 독립 검증을 마쳤다.
R4가 정상/실패11칸과 실측2칸을 추가 승인했고 당시 대기했던 A05 실패는 R8에서 신규B9·댓글후속 포함 최종전체 근거로 닫았다.
R4 응답 `7ef02f0d-37f7-40ee-90b7-ff52ebaa82c0` 전문대조 뒤 backend472/frontend553파일의
당시 현재해시가 해당 검증 때와 동일함을 루트가 재계산했다. 이후 B4 댓글 후속 변경은 아래 별도 근거로 연결한다.
R5 실제 Claude가 A03/A08/A11 대표 실측3칸을 승인했다. A03/A08/A11 제품19/6/12파일의
당시 현재해시도 루트가 다시 계산해 불일치0을 확인했다. 이후 R6 인증·R7 복원 실측을 승인받았고 A05실패 최종전체 조건도 R8에서 충족했다.
교차 영역의 계정 고정·실행 진입점 대장도 경계 증거에 포함한다. 일반 부하 시험은 요청 중 계정 전환,
자동 보관 시각 경계, 이름 없는 계정을 커버하지 않으므로 각 전용 재현과 구분한다.

## 직전 검증 기준선 (이번 포괄 점검의 완료를 대신하지 않음)

- `1262b249` 전체 사전 게이트: backend 3,259 +815 subtests PASS, Windows symlink 1 SKIP;
  frontend 165파일/1,555 PASS, 타입 포함 빌드·아키텍처 검사 PASS.
- `4df0eb93`까지 추가 변경은 검증 문서뿐이다. 합성 SQLite 백업/복원과
  100명·60초×2회 부하 22,585건 모두 200, 작업 p95 61.99/62.09ms를 확인했다.
- 근거: `predeploy-reports/release-20260917-1262b24/`의 게이트·부하 JSON/로그.
- 릴리즈 `2026.09.17-1900`은 19:11:54에 NAS 게시까지 완료됐다.
  `D:\ClaudeCode\MV-hub-S-release\predeploy-reports\release-2026.09.17-1900\REPORT.md`가
  실제 ZIP 검증·게시 기록이다. 운영 적용과 유료 생성 왕복은 별도 미검증이다.

## 1회차 조사·실행 기록

- 19:43 Claude CLI에 읽기 전용 화면/공통 UI 독립 조사 요청.
  `Read/Glob/Grep`만 허용하고 MCP·브라우저·코드 실행을 차단했다.
  응답 회수 전에는 Claude의 설계 승인으로 기록하지 않는다.
- Codex 독립 조사 분담: 상태/신원/관리 집계 계약, 생성 에이전트/Comfy 수명 주기.
  담당을 분리하고 파일 쓰기·운영 접근·유료 실행을 금지했다.
- Codex 통합 담당: 미디어/조회/캐시·기동/복구 및 실제 격리 측정 계획.
- 시간별 보고: 현재 작업에 연결된 `MV Hub 종합 점검 시간별 보고` (`mv-hub`) 활성화.
  1시간 간격으로 근거 있는 진척·발견·설계/구현/검증 상태를 보고하고 중복 실행하지 않는다.

## 1회차 근거와 문제 후보

R1-F: Claude 실제 독립 응답은
`predeploy-reports/program-audit-20260917/claude-initial-review.md`에 보존했다.
R1-B: Codex 상태/신원/집계 독립 조사 및
`backend-repro/results.json`(위 보고서 폴더 기준). 실제 라우트·저장소와 합성 A/B SQLite,
가상 시계만 사용해 아래 B 세 건을 재현했다(1.834초, 외부 호출/프로세스 차단).
R1-E: Codex 실행 수명주기 독립 조사 및
`execution-repro/isolated-ecqmiwtk/results.json`. 52개 함수 검증, 38.084초, exit 0.
이 두 시간은 재현 도구 실행시간이며 사용자 응답 지연이 아니다.

| ID | 심각도/판정 | 후보·실제 근거 | 최소 설계 방향 (승인 전) |
|---|---|---|---|
| B1 | P1 재현 | `routers/projects.py:449 assign_project`: A 요청 중 전환 시 B DB 폴더와 프로젝트 캐시 변경 | 요청 시작의 계정 키+uid를 원자 캡처하고 모든 DB/후속 요청에 고정 |
| B2 | P2 재현 | `routers/manage.py:104/1158`: 자동 보관 경계 뒤 조건부 GET 304/미보관, 일반 GET은 보관 | 시간 의존 ETag에 제한된 재평가 기한, 폴링마다 전체 재계산 금지 |
| B3 | P2 재현 | `repo/projects.py:900/925/944`: 이름 없는 계정이 단건에는 이메일 앞부분, 배치에는 null | 공용 이름 해석기를 UID 집합에 한 번 적용, 가시성/역할 유지 |
| E1 | P1 재현 10/10 | `routers/comfy.py:1066/1087`: 입력 복사 중 업데이트가 시작된 뒤 실행 worker도 시작 | 실행 등록 후 업데이트 재검사, 거부 시 임시 입력/등록 회수 |
| E4 | P1 재현 10/10 | `agent_push.py:2223/2908`: 앵커 ACK 후 outbox 삭제 오류가 실제 진행 작업을 failed로 변경 | 과금 후 로컬 기록 오류와 제출 전 실패 분리, 추적·앵커 보존 |
| E3 | P2 함수 재현 | `agent_push.py:2774`, `repo/generations.py:324`: 삭제 본체 64개가 192회 조회 후도 용량 64개 점유 | 소유자/같은 job/휴지통을 검증한 명시적 종결 계약, 복원 기록 유지 |
| E2 | P1 모사 원격 접수 재현 10/10 | Comfy 접수 후 응답 단절을 일반 failed 502로 표시, prompt/영속 흔적 없음 | 제출 결과 불명 상태·재실행 경고, 자동 재제출 금지. 제공자/영속 계약 별도 설계 |
| E5 | P2 실제 worker 재현 각 10/10 | 모사 원격 완료 뒤 상세/다운로드 일시 오류가 전체 failed, 결과 조회 재시도 0·취소 시도 1·영속 기록 삭제 | 원격 완료와 결과 회수 분리, 같은 prompt 결과만 제한 재시도·근거 보존 |
| C1 | P2 브라우저 재현 | 팀 카드 S 더블클릭의 두 번째 클릭이 Unshare 행에 도달, Yes/No 단계로 이동 | 메뉴 즉시 표시 유지, 두 번째 클릭 오전달 방지; Yes 확인은 유지 |
| C2 | 의도 차이 검토 | ↑ 보류→공유와 S 더블클릭→최종의 차이 | 기존 사용자 요청은 상대 이동과 최종 선택을 구분. 곧바로 규칙 통합하지 않음 |
| C3 | P2 함수 재현·HTTP 실측 | `SceneBoard.tsx:2311 applyColorToGids`: N개 단건 저장, 실패는 console.warn만. 역순 완료 시 UI 초록/서버 빨강 | 배치+직렬화+최신 상태 복구/알림. 단순 배치화만으로 응답 역전을 해결할 수 없음 |
| C4 | P2 후보 | `HistoryBoardNode.tsx:179`: 본인/권한 조건 없이 S 노출, 핸들러는 무음 거절 | 공용 표시 자격과 읽기 전용 상태 표식을 구분 |
| C5 | P3 확인 | 선택 액션바 두 종류에 다운로드/비교/삭제 등 번역 누락 | 공통 문구/작은 버튼 단위 재사용, 보드 삭제 의미는 유지 |
| C6 | P3 확인 | AssetCell/InfoPopup 원본 열기 3곳 alert와 raw error, 폴더는 토스트 | 프런트 실패 안내 재사용. 백엔드 탐색기/파일 구분은 이미 있으므로 결함으로 단정하지 않음 |
| M1 | P2 함수 재현 | `modelCatalog.ts` 실패/빈 목록 뒤 HTTP 재요청 없음, 별도 modelCatalogCache는 다음 요청 복구 | 공용 카탈로그 HTTP·성공 캐시 재사용, 실패/빈 목록 고정 방지 |
| R1 | P1 실제 복원 재현 5/5 | `routers/db_transfer.py:755/773` A 백업 다운로드 중 B로 전환하면 B DB에 A 백업을 설치하고 성공 응답. B 원본 .bak은 보존, 현재 B 포인터 삭제 | 접수 계정·대상 경로 캡처, 설치 잠금 안에서 일치 확인 후만 교체. 단순 override로 현재 계정 포인터를 지우지 않기 |
| W1 | P2 실제 훅 재현 5/5 | `useGenerationProgress.ts`의 늦은 done 상세가 후속 공유/보류/색상/태그 전체를 옛 값으로 덮음 | 화면 생존/계정 범위·요청 후 변경 검증, 실행 결과 필드와 사용자 메타 갱신 경계 분리 검토 |
| U1 | P1 상태 판정 재현 | 업데이트 상태 파일이 Windows 배타 잠금 중이면 `_read_state`가 빈 상태로 삼켜 실행 게이트가 열림 | 최초 파일 없음과 읽기/파싱 불가 구별, 안전한 거부 및 상태 조회 덮어쓰기 방지. 상세 설계 대기 |
| B4 | P1 실제 라우트 재현 5/5 | 비공개 댓글 앵커 조회 중 A→B 전환하면 B DB에 메모 저장 | 댓글 읽기/쓰기의 개인 계정 범위를 기존 고정 계약에 맞춤. 설계 검토 중 |
| B5 | P2 실제 라우트 재현 각 5/5 | A 원격 재인증/해제 응답 후 B DB에 권한 토큰 혼입/잘못된 로컬 정리 | 현재 계정 유지 기능인 재인증/해제만 계정 고정. 명시 로그인 전환은 별개 |
| D1 | P2 실제 HTTP 재현 | 선언 92바이트·수신 40바이트 이미지가 정상 보존본으로 확정, 다음 호출 already | 선언 길이 일치 후만 확정·재시도 가능 오류·part 정리 |
| D2 | P2 실제 라우트 재현 | 병합 입력 3개 중 유실 1개를 무음 제외하고 2개 정상 MP4 응답 | 하나라도 소스 확인 실패면 병합 전에 전체 실패·순번 안내 |
| U2 | P2 함수 재현 후보 | 실제 PowerShell 상태 쓰기 함수가 동시 읽기 중 Move-Item 오류로 중단·tmp 잔존 | 원본 보존형 교체·제한 재시도·임시 정리 별도 검토. 업데이트 본문은 실행하지 않음 |

E1 대조군: Comfy 먼저/업데이트 먼저 각각 10/10 정상 차단. E4 오류 없는 대조군 10/10 정상.
E3은 본체 없음 응답을 모사한 함수 단위 근거이며 실제 UI 삭제 경로는 아직 미검증이다.
후보를 모았다는 사실은 설계 승인이나 제품 수정 완료를 뜻하지 않는다.

### 추가 실행 경계와 복원 재현

R1-L: Resolve는 로컬 머신 권한 → 활동 등록/업데이트 재검사 → 계정 key+uid 고정 →
원본 권한 검증/안전 복사/manifest → 선택 감시 중지 → 직렬 import → 별도 Python 자식으로 이어진다.
자식도 전송 lease를 보유하고 Resolve 연결 전 journal, 대상 프로젝트 확인, 이전 Bin 복원을 한다.
8초 상태 probe와 무제한 실제 import는 ABI 크래시 격리/중간 파괴 방지 목적의 의도적 차이다.
근거(backend/app 기준): `routers/resolve_integration.py`, `services/resolve_transfer.py`,
`services/resolve_status_runner.py`, `services/resolve_import_worker.py`, `services/resolve_bridge.py`,
`services/resolve_selection_monitor.py`의 진입·종료 경계.

로컬 backup은 content/trash/manage 온라인 세트, 작업자 offdisk는 정제된 content/trash 세트다.
전자는 로컬 전체 복구, 후자는 계정별 오프디스크 계약이므로 임의로 합치지 않는다.
기동/종료는 생산자 정지 후 전송·선택 감시 회수, 업데이트는 부모/자식 lease를 확인한다.
RL-23 실물 다른 디스크/예비 PC 복원과 운영 Gate 6-B/6-C는 미검증 상태를 유지한다.
Resolve 파일 lease는 Windows 및 릴리스 표식이 있는 설치 루트에서만 작동한다.
표식 없는 dev 실행의 PASS를 프로세스 간 실제 상호배제 증거로 사용하지 않는다.

`restore-repro/isolated-gp3sym8g/results.json`: 실제 version 복원 라우트·ZIP 해시/SQLite 검증·
설치/보안 리셋/포인터 정리를 합성 A/B DB에 실행했다. 다운로드 중 A→B 전환은 **5/5 잘못된 B 교체**,
전환 없음과 설치 잠금 안 전환 대기는 각각 **5/5 정상**이다. 총 15회 26.64초(하네스 실행시간).
모든 교체 경로를 전용 격리 폴더 하위로 검증했고 네트워크/하위 프로세스는 차단했다.
사용자 DB·실제 서버·Resolve 접근 없음, 원본 합성 DB/가짜 ZIP 보존, 테스트 스레드 모두 종료.
앞선 두 하네스 오류는 별도 기록했으며 제품 결함으로 계상하지 않는다.
`/server-restore`, DB `/import`는 같은 앞단의 코드 영향범위이며 실제 재현은 version 라우트에 한정한다.
기존 `test_r12_import_account_pin.py`는 생성물 복제 API의 보호로서 DB 전체 복원과 다른 경계다.

### 실시간 갱신 경계와 응답 역전

R1-W: `ws.py`는 인증 계정별 FIFO/단일 sender와 64건 상한, 데이터 없는 갱신 신호 병합,
시간 제한/느린 연결 정리를 소유한다. `remote_realtime.py`는 한 원격 연결을 유지하고 설정 변경,
인증 만료 대기, 재연결 후 3개 도메인 재조회를 전달한다. `progressSocket.ts`는 브라우저 연결,
5초 안정 연결 판정/지터 백오프, 1008 인증 종료, 재연결 catch-up과 타이머 회수를 소유한다.
메인 App과 독립 관리창의 연결은 서로 다른 문서 수명이라 중복으로 합치지 않는다.
`useGenerationProgress.ts`는 400ms 변경 병합/자기 요청 출처 생략과 완료 상세를 적용한다.
`useGenerationLibraryData.ts`는 계정·워크스페이스·탭 범위 및 요청 순번, 단일 진행 조회/후속 병합,
페이지 추가와 탭 캐시를 관리한다. 일반 목록/활성 작업/관리창의 폴링 간격 차이는 의도적이다.
현재 W1은 목록 로더의 보호를 거치지 않는 완료 상세 적용 경계에서 발생했다.

`realtime-repro/isolated-a7c0uD/results.json`: 실제 React 훅/DOM, API와 WS만 deferred mock.
25개 검증에서 최신 공유/보류/색/태그가 늦은 상세에 덮임 5/5, 정상 완료 5/5,
enabled=false 정리 뒤 살아있는 동일 카드 덮임 5/5, 정리 뒤 실패 reload 5/5를 확인했다.
완전 언마운트 5/5는 React가 변경을 무시하므로 별도 누수/오류 주장을 기각했다.
실제 App 로그아웃은 목록/인증 가드가 있으므로 정보 노출이나 서버 데이터 손실로 확대하지 않는다.
OPT_PLAN10의 과거 언마운트/성능 기각과 구별되는 새 상태값 퇴행 증거다.

### 추가 재현·교차 경계

R1-D: Assets는 `safe_join`·허용 미디어/응답 헤더·트리 깊이/노드 상한을 공용으로 사용한다.
업로드/참조/capture는 공용 `asset_io`의 청크 상한·동명/원자 확정을 쓰고, 참조만 내용 중복 제거한다.
원본 영구 보존, 썸네일 원본 LRU, 썸네일 결과 LRU는 삭제 권한/수명이 달라 합치지 않는다.
공개 댓글은 서버 프로젝트 권한, 개인 태그/색/댓글은 로컬 DB, 단건/ZIP/골드/병합 내보내기는 의도가 다르다.

- `media-repro/results.json`: 실제 urllib+루프백에서 짧은 body가 예외 없이 반환돼 D1이 발생.
  실제 보존 usecase의 DB 갱신 요청은 기록용 stub으로 확인했다. 정상 PNG 대조군 유효.
  D2는 원본 소스 해석/라우트, ffmpeg만 stub이며 정상 순서/전부 유실 400 대조군도 확인.
  영구 보존 기본 비활성, 실제 캐시 호출 경로만 영향. 인코딩 품질/실제 운영 빈도는 미검증.
  리스너·프로세스·스레드·part·병합 임시 파일 회수, 제품 변경 없음.
- `account-sibling-repro/isolated-0cs2su2s/results.json`: B4/B5와 대조군 **45개**, 12.73초.
  실제 라우트/합성 DB, 메모리 포인터/가짜 인증 응답만 사용. 외부 부수효과 시도 0.
  B5는 서버 email+uid 방어로 서버 권한 상승을 주장하지 않는다. 실제 로그인은 B 권한을 지우므로
  B의 유효 권한 삭제까지는 응답 대기 중 재인증 등의 추가 조건이 필요하다.
- `comfy-result-repro/isolated-gdccz72f/results.json`: **35개** 실제 worker/impl/_wait/SQLite/다운로드 검증,
  원격 접수/상태/결과 스트림만 stub. E2/E5 각 실패 조건 10/10, 모든 경우 submit=1/자동 재제출=0.
  상태 조회 일시 오류는 기존 재시도로 복구되는 대조군. 실행 슬롯/활성 잡/part 잔여 0.
  프런트는 일반 실패 후 실행 버튼을 다시 켜지만 자동/무조건 재실행을 권유한다고 주장하지 않는다.
  새 복구 상태는 현재 부팅 시 in-flight 취소/삭제 규칙과 함께 설계해야 한다.
- `update-gate-repro/results.json`: U1 상태 대조 14개·Windows 배타잠금 10회·get_status 덮어쓰기 1회.
  Python 원자 쓰기 잠금 실패는 원본 보존/tmp 정리 10/10. PS 함수 추출 시험은 reader 없음 400회 성공,
  reader 있음 2회 오류. JSON 절단/일시 missing은 관측되지 않았으므로 원인으로 단정하지 않는다.
  실패 tmp 2개는 보고서 하위 증거로 보존, 업데이터 본문/실제 설치본/운영 상태 접근 없음.

교차 쓰기 대장: 개인 메타 단건/배치·폴더 공유·sync는 기존 계정 고정이 있고,
로그인/register는 명시 대상 계정 전환이므로 고정 대상이 아니다. create/reorder/멤버 쓰기의
proxy 즉시 반환과 update/delete의 watcher 정리는 assign의 DB 후속 쓰기와 구별한다.
workspace batch, manage render_root 미러/골드 저장, HF-missing 정리, direct publish-to-shared는
고정 범위와 uid/CAS 방어가 섞인 정적 추가 경계다. 아직 새 오귀속 재현으로 승격하지 않았으며
다음 실패 검증에서 각각 보호 여부를 확인한다. DB 복원은 업데이트 활동 게이트와 별개인 경계도 남아 있다.

## 실제 부하 측정 1회차

`predeploy-reports/program-audit-20260917/load-baseline.json`: 현재 제품 코드,
100명·2코어·낮은 우선순위·120초×2회·합성 생성물 2,000개, HTTP 혼합 읽기/쓰기.
총 **45,395건 모두 200**, 작업 p95 **54.74/58.41ms**, 5xx·전송·SQLite 잠금·WS/롱폴 오류 0.
실제 LISTEN PID 27368에 제약 적용, 최대 RSS **257,179,648바이트**, 사이클 간 종료 RSS 증가 **8.91%**.
수치는 지정한 20%/512MiB 기준 안이지만 4분 표본만으로 장시간 누수 없음이라고 판단하지 않는다.
동시 100명 로그인 p95 7,547.8ms, 로그인 중 제어 요청 p95 41.77ms.
측정 프로세스 39552/27368 및 포트 3645가 종료됐음을 재확인했다.

### 색상·카탈로그·카드 재현 및 관련 회귀

- `frontend-domain-repro/results.json`: 원본 TypeScript 변환 및 Scene/API 함수 AST 추출,
  React/HTTP만 mock한 11개 검증. Scene 1/50/200개 선택은 1/50/200요청, 라이브러리는 각각 1요청.
  Scene 전체 실패 시 화면의 낙관적 색 유지·경고만, 라이브러리는 재조회·알림 1회.
  빨강→초록 저장 순서를 뒤집으면 Scene UI 초록/서버 빨강, 직렬 큐 경로는 초록으로 일치.
  전체 앱의 다른 갱신 구독자까지 측정한 결과는 아니다.
- 같은 재현에서 이름 카탈로그는 첫 실패·빈 목록을 고정하지만 공용 목록 캐시는 재시도해 복구했다.
  이름 표시 1곳+선택기 50곳 동시 호출은 HTTP 2회다(서버의 유료/CLI 실행 횟수가 아님).
- `color-http-results.json`: 실제 격리 서버·동일 소유 합성 카드 200개·2코어,
  단건 HTTP 동시성 6개와 기존 batch 엔드포인트를 각 3회 비교했다.
  50개 전체 완료 중앙값은 **263.70 → 3.80ms**, 200개는 **1,044.04 → 6.12ms**.
  모두 200·저장 색 확인. 이 값은 localhost HTTP 비교이며 전체 캔버스 렌더 시간이나 LAN 지연이 아니다.
  프로세스·리스너·포트·임시 데이터 회수 확인. 첫 도구 실행의 종료 관측 경합은 별도 기록하고 제외했다.
- `ui-probe/results.json`: 실제 GenerationCard/CSS+합성 이미지, 브라우저 S 더블클릭이
  grid 140/340px, list 컨테이너 420/640px에서 Unshare 확인으로 이동. 최종 변경 0회.
  list 140/340 선택은 둘 다 컨테이너 420px이므로 서로 다른 크기 조건으로 중복 집계하지 않는다.
  시험 탭·Vite PID 3076은 종료했다.
- 관련 백엔드 12파일: **211 PASS +5 subtests**, 37.79초.
  최초 격리 호출의 `BACKUP_INTERVAL=0`이 비공개 `_run()` 직접 호출 테스트의 시작 임계도 0으로 만들어
  1개가 3회/2회 기댓값 불일치로 실패했다(210 PASS). 기본 하루 간격과 격리 백업 경로를 명시한
  재실행은 제품/테스트 수정 없이 통과했다. 이 설정 의존성은 제품 백업 장애로 분류하지 않는다.
  두 로그와 XML 모두 `boundary-tests/`에 보존.
- 프런트 관련 8파일 **89 PASS**, 2.81초. `sceneModelModalRestore`의 기존 act 환경 경고는 남아 있다.
  근거: `frontend-baseline.json`/`.log`. PASS는 위 새 실패 경로가 기존 테스트에 없다는 사실과 양립한다.

## 설계 교환 상태

Claude 초기 응답을 Codex가 대조한 후 B1~B3/E1~E5/C1~C6/M1의 최소 설계와 반례를 보냈다.
요청은 `design-review-request.txt`, 실제 응답은 `claude-design-review.md`다(보고서 폴더 기준).
실행 71945는 exit 0으로 완료됐다. B1 공통 캡처/context(데코레이터 이동 금지), B2 라우터 응답 키의
60초 버킷만, E1 pending 등록/기존 이중 검사, C2 상대/절대 의미 유지에는 합의했다.
C3은 씬별 큐가 같은 생성물을 여러 씬에서 편집할 때 역전을 막지 못하는 반례,
M1은 catch에서만 Promise를 비우면 빈 목록 성공이 계속 고정되는 반례를 Codex가 제시했다.
C1은 팀 공유 카드의 onSDouble에 명시적 return이 있어 더블 최종 복원 제안을 수용하지 않았다.
즉시 메뉴+pointer 오전달 방지/키보드 보존으로 범위를 맞춘다. B3 정렬의 NULL 순서 설명도 교정 중이다.
E4는 과금 후 메모리/영속 기록/앵커 재시도 책임까지 최소 설계를 보완하며,
R1 실제 복원 근거와 함께 `design-followup-request.txt`로 2차 합의를 요청했다.
실행 53661 exit 0, 실제 `claude-design-followup-review.md`에서 C1 오전달만 차단/팀 더블 비활성 유지,
C3 인스턴스 단일 큐, M1 finally/실패·빈맵 1초 간격, B3 NULL 마지막 표시이름 정렬,
E4 메모리 anchor/persist pending과 추적 패스 재시도, R1 설치 전 snapshot 대조에 합의했다.
R1은 AUTH on/고정 DB 경로의 포인터 비의존성과 모든 설치 호출 진입점, A→B→A 경계를 검증한다.

추가 U1/W1 설계 회신 `claude-realtime-update-design-review.md`(21411 exit 0)에 대해
W1은 기존 구조 비교 3-way 병합+생존/순번/scope 가드, U1은 파손도 force 재설정 없이 안전 거부를
Codex가 선택했다. 기존 force도 진행 중 업데이트를 무시하지 않으므로 권한을 넓히지 않는다.
세부 회신 `claude-remaining-design.md`(76228 exit 0)에서 B4/B5/D1 승인,
D2는 ViewTimeline의 무음 실패도 토스트로 안내하는 조건으로 승인했다.
U1/W1은 승인을 받았지만 Codex 재대조에서 잔여 반례를 찾아 구현 전에 다시 합의한다:
U1 최종 재검사와 쓰기가 무잠금이면 active 덮어쓰기가 남고, get_status 쓰기는 2곳이 아니라 5곳이다.
최초 missing은 정상이며 오류 응답도 기존 base 필드를 보존해야 한다. 서비스만 바꾸어서는
라우터 3곳의 하드코딩 안내를 바꿀 수 없다. W1 순번 삭제 후 1부터 재사용하면 ABA가 생기고,
baseline 미확보 시 current 폴백은 사용자 변경을 보존하지 못한다. 이견을 숨기고 구현하지 않는다.

E2/E3/E5는 `execution-recovery-design-request.txt`로 실제 Claude 별도 설계 검토 중(41622).
사용자 보류인 Comfy 강화 계획의 비용·출처·실행시간 기능은 재개하지 않는다.
미접수/제출 불명/원격 완료/결과 회수 및 계정·대상·부팅 복구의 최소 안전 계약만 검토한다.

### B1 첫 구현 묶음

owner Codex, reviewer Claude, status 완료(이 수정 묶음만). 전체 제품 완료가 아니다.
`active_account`에 공용 캡처/context를 추가하고 share/Resolve 기존 래퍼는 위임했다.
FastAPI 데코레이터는 같은 모듈에 유지, async 캡처는 worker에서 수행한다.
프로젝트 이동은 첫 proxy 판정부터 캐시·DB 이동·후속 서버 미러·return까지 같은 key+uid를 본다.
DB 스키마/권한 규칙/서버 API/명시적 로그인 전환은 변경하지 않았다.
신규 6개 회귀는 변경 전 **5 FAIL/1 PASS**(새 helper 부재 포함), 변경 후 기존 검사 포함 **56 PASS**,
4.89초. 첫 실행 위치 오류는 별도 수집 실패 기록으로 유지. `b1-before/`, `b1-after/` 근거.
독립 코드 리뷰 **72550 exit 0**, `claude-b1-code-review.md`: 승인·차단 결함 없음(정적 리뷰).
비차단 권고에 따라 상류 키+uid 쌍 고정 전제를 설명하고, 팀 조기 반환·예외 내부 A 범위·
다른 스레드에서 네트워크 구간 락 미보유 검증을 보강했다. 기존 관련 검사 포함 **77 PASS**, 10.28초.
`b1-reviewed/tests.log`/`results.xml` 근거. 공용 capture를 아직 사용하지 않는 형제 scope는
후속 계정 고정 대장으로 남겼고 일괄 리팩토링하지 않았다.

### B3/E1/M1 독립 구현 묶음

- B3: 멤버 단건/전체/가시 프로젝트 조회가 공용 이름 해석기를 1회 호출한다.
  합의된 이름 없는 사람 마지막 정렬, 연결 1개/SELECT 3개 고정. 신규 전 10 FAIL/1 PASS →
  후 11 PASS, 기존 프로젝트·권한 포함 **147 PASS**, 79.48초. `b3-after/` 로그/XML.
- E1: 준비 전 pending 등록, 준비 후 업데이트 재검사, 거부/준비 오류에서 업로드·잡 회수.
  경합 양방향·예외 정리 신규 전 22 FAIL/22 PASS → 후 44 PASS, 관련 **161 PASS**, 16.66초.
  `e1-after/` 로그/XML. force/U1/E2/E5는 이 수정의 보장 밖이다.
- M1: 공용 HTTP 캐시 합류, 성공 이름 맵만 loaded, finally로 진행 Promise 해제.
  실제 빈 목록/실패는 이름 쪽 1초 뒤 다음 호출, 형식 불완전 원시 목록은 공용 60초 TTL 뒤
  다음 호출에서 복구한다. picker 캐시/명시 재시도는 변경하지 않았다. 신규 전 8 FAIL,
  기존 cache 포함 후 **18 PASS**, 1.68초. 타입·변경 파일 ESLint 통과. `m1-after/` 근거.
- 세 묶음 6파일 독립 리뷰 **54495 exit 0, 모두 승인**.
  `claude-b3-e1-m1-code-review.md`에 실제 정적 검토를 보존했다.
  E1 권고대로 Thread 생성자도 정리 try 안에 넣었고, 생성자 실패 전 FAIL → 후 관련 **162 PASS**,
  15.23초(`e1-after/constructor-regression.xml`). B3의 매우 큰 UID 집합 SQL 변수 상한과
  두 화면의 미상 이름 UID 폴백은 비차단 후속 후보로 남긴다. 실제 팀 100명 통과로 무한 확장을 주장하지 않는다.
- 다음 독립 구현 소유: B4/B5는 generation/publish의 댓글·재인증 경계와 신규 회귀만,
  B2는 manage 라우터 응답 키와 시각 경계 회귀만. 운영/사용자 DB/커밋은 범위 밖.

15분 기준선: 실행 66749 exit 0, 같은 서버 100명·300초×3회·2코어/낮은 우선순위,
총 **170,886건 모두 200**, p95 **50.81/52.42/51.90ms**, 5xx/전송/SQLite 잠금/WS·롱폴 오류 0.
최대 RSS **273,092,608바이트**, 워밍업 뒤 종료 RSS 증가 **3.78%**, 30초 표본마다 연결 100개 유지.
로그인 p95 7,610.6ms, 로그인 중 제어 요청 p95 41.58ms. 512MiB/20% 기준 통과.
제품 수정 전 종료했고 프로세스34160/40328·포트8593 모두 종료 확인했다.
`load-15min-baseline.json` 및 `.progress.json` 근거. 15분은 근무일 전체/실제 제공자/실물 Resolve
또는 전용 계정전환/파일오류 재현을 대신하지 않는다.

### 후속 구현·독립 검토 기록 (21:23 갱신)

- B2: 응답 키에 60초 버킷만 추가, 저장소 0.75초 캐시/권한/DB stamp 유지.
  전 3 FAIL/7 PASS → 후 신규 10 PASS, 관련 **46 PASS +9 subtests**, 13.56초.
  B4/B5: 기존 계정 범위를 댓글 읽기/쓰기 전체 및 재인증/해제 응답까지 적용.
  전 22 FAIL/37 PASS → 신규 59 포함 관련 **94 PASS**, 180.07초.
  실제 Claude **76889 exit 0, 세 건 모두 승인**, `claude-b2-b4-b5-code-review.md`.
  보관 판정의 시각 경계/작성자 귀속/fixture 고정을 테스트에 보강 중이다.
  재평가 상한 설명은 60초 버킷 + 30초 폴링 + 저장소 TTL 0.75초로 **90.75초 이내**다.
- D1/D2: 양수 선언 길이 불일치 파일은 확정 전 거부하고 기존 재시도를 사용한다.
  병합 일부 유실은 인코더/작업 폴더 생성 전 전체 400, 프런트는 안내 후 버튼을 복구한다.
  전 백엔드 6 FAIL/10 PASS·프런트 2 FAIL/1 PASS → 관련 **50 PASS**(16.94초)·프런트 **3 PASS**,
  타입·변경 ESLint PASS. 첫 임시 DB 미초기화 2건과 fixture 경로 오류는 하네스 오류로 따로 보존했다.
  실제 Claude **61249 exit 0, 두 건 승인**. 최종 응답에는 비차단 5건 중 상세가 유실돼 별도 회수 요청(42891).
  확인된 테스트 권고 2건(기본 인자에 이미 캡처된 대기 상수/사용량 전역 원장)을 수정했고
  신규 **16 PASS**, 0.73초(`d1-d2-reviewed/`). 제품 변경 없는 시험 격리 보강이다.
  unknown 길이/요청하지 않은 압축/과거 잘린 파일은 이번 길이 가드의 보장 밖이다.
- C1: 팀 카드 메뉴를 즉시 열되 첫 220ms의 pointer 오전달만 capture 단계에서 막는다.
  전 신규 3 FAIL/기존 16 PASS → 관련 **48 PASS**. 실제 브라우저 grid 140/340px,
  list 실제 420/640px 모두 두 번째 포인터가 Unshare에 닿아도 3개 선택 유지·확인 질문 없음·변경 0.
  키보드 Enter/No/Esc와 Yes 단일 적용 확인, 340px 화면도 육안 점검했다.
  `c1-after/browser-results.json`; 시험 탭·Vite 세션80008/포트8597 종료 확인.
- E4: job ID를 먼저 메모리 추적하고 outbox/앵커/추적 저장 오류를 제출 실패와 분리했다.
  전 38 FAIL/2 PASS → 신규 43 포함 관련 **345 PASS +6 subtests**, 60.40초.
  `e4-after/results.json`/`final-related.xml`. 신규 due 재앵커 1회와 기존 replay 1회는 별개다.
  디스크·서버 ACK 모두 실패할 때 메모리만 보존하며 크래시 내구성을 보장하지 않는다.
  C1/E4는 실제 Claude 독립 리뷰 **96975 진행 중**, 완료 전 승인으로 세지 않는다.
- U1: `claude-u1-w1-counterexample.md` 실제 최종 승인에 따라 상태 쓰기 5곳의 최종 재검사와
  쓰기를 동일 잠금으로 묶고, 5초 잠금 실패는 무쓰기, missing은 정상/읽기·파싱 불가는 안전 거부한다.
  구현 소유는 release_update 서비스/회귀와 생성·Comfy·Resolve 게이트 안내 문자열만이다.
  실제 코드에서 begin-submission의 같은 안내 1곳도 확인돼 문구만 일치시키기로 했다.
  실제 업데이터·설치본·운영 상태 파일은 실행/수정하지 않는다.
- W1: 추가 **33개** 설계 검증(`w1-design-repro/isolated-Fp0fLj/results.json`)은 raw-only 기준이
  done 상태와 fresh 실패 phase를 섞을 수 있음을 보여 준다. raw/예상 progress 적용값의 쌍 기준과
  후속 non-done 시 무효화, 256건 순번 제거의 큰 burst 유실을 구현 전에 다시 합의 중(35880).
  저우선 lane skip은 설계 스트레스이며 현재 WS에서 그 경로가 실행된다고 주장하지 않는다.
- E2/E3/E5: 초안의 비-2xx 미접수 단정, 미회수 원장 자동 축출, legacy 이중 쓰기,
  terminal 요청만으로 추적 해제, 복원 후보/결과 저장 ACK 누락을 Codex가 반대했다.
  실제 코드 대조 후 **68154 설계 2차 요청**. 자동 재제출 금지·원격 완료 후 취소 금지·계정/대상 고정은 유지하고,
  사용자 보류인 비용/시간/출처 강화 계획을 재개하지 않는다. 구현은 아직 없다.

B2 실제 비교는 같은 167개 소스 파일 사본 두 벌에서 manage 응답 키 3줄만 다르게 고정했다.
각 100명·2코어·낮은 우선순위·task-read 120초×2회, 원 HTTP 200/304와 gzip 바이트를 별도 관측한다.
기준선 세션54364/포트14649는 종료 확인, 이후 세션69457/포트2465에서 수정판 측정 중이다.
앞선 15분 혼합 프로필은 이 전용 비교의 대조군으로 사용하지 않는다.

### 후속 통합·실측 기록 (21:47 갱신)

- B2/B4/B5 검토 권고를 반영한 관련 시험 **141 PASS +9 subtests**, 211.07초.
  AUTH on 대표 경로 **3 PASS**, 15.32초. 시각 경계의 실제 보관 변화, 작성자 귀속,
  AUTH 설정 격리/범위 해제까지 고정했다(`reviewed/b2-b45-after-*`, `reviewed/b45-auth-on-*`).
- D1/D2 실제 Claude 상세 재검토 **42891 승인**, `claude-d1-d2-review-detail.md`.
  시험 격리 보강 16 PASS를 확인했다. 길이 미선언·이미 저장된 과거 불완전 파일은 여전히 보장 밖이다.
- C1/E4 실제 Claude 독립 검토 **96975 승인**, `claude-c1-e4-code-review.md`.
  C1 권고인 메뉴 열린 상태의 직접 더블클릭 가드를 추가하고 Workspace 단일/더블클릭 대조군을 보강했다.
  전 신규 1 FAIL/21 PASS → 관련 **51 PASS**(`c1-after/review-after.json`).
  220ms는 기존 제품의 빠른 클릭 분리 기준이며 모든 OS 더블클릭 간격을 보장하지 않는다.
  E4는 영속 저장·서버 ACK 동시 실패 시 메모리만 남는 한계와 구형 에이전트 계약을 유지한다.
- C6: AssetCell grid/list 및 InfoPopup 원본 열기 3곳을 같은 비차단·번역 오류 안내로 통일했다.
  전 **24 FAIL/6 PASS** → 신규30+관련3=**33 PASS**, 타입 검사/제품 ESLint 통과.
  시험 파일은 ESLint 설정 대상 밖이므로 시험 린트 통과라고 주장하지 않는다.
  실제 Claude **49578 승인**, `claude-c6-b2-cost-review.md`. API/OS 호출·범위 밖 복사 alert는 변경하지 않았다.
  403의 실제 로컬 허브 전용 제한을 더 구체적으로 안내하는 일은 비차단 후속 후보다.
- U1: 신규 **105** 포함 관련 **213 PASS**, 16.73초(`u1-after/final-related.xml`).
  Windows 실제 배타 잠금, 병렬 읽기, 쓰기 5곳, 5초 잠금 실패, 실행 진입점 안전 거부를 포함한다.
  제한된 작업 범위상 기존 batch/추출 PowerShell/자식 프로세스 시험 **9건은 제외**했으며 전체 백엔드 게이트가 아니다.
  실제 Claude **15767 독립 리뷰 중**. 대상은 release_update 서비스, 실행 게이트 안내 5곳, 신규 회귀다.
- 프런트 1회차 전체 **1,602/1,602 PASS**, 아키텍처 검사 및 타입 포함 빌드 PASS.
  `frontend-round1/` 근거. C1 마지막 권고 3건 보강 전의 전체 검사이며, 최종 통합 때 다시 실행한다.
  기존 App 묶음 572.72kB(gzip190.53kB)의 500kB 경고는 남고 이를 오류로 숨기거나 전면 구조 개편하지 않는다.
- W1: token 비교 **105개**(알려진 결함 대조군 포함) PASS, 1.26초.
  `w1-design-repro/token-isolated-J6Ycqt/results.json`. 일반/StrictMode 각각 3회에서 무관한
  높은 우선순위 commit이 미완료 updater 토큰을 조기 제거하는 layout-retire 반례를 확인했다.
  현재 표시 ID의 최신 토큰을 유지하는 대안은 최신 에셋/300건 burst/반복100회 map1을 보존했다.
  제품 WS의 실제 스케줄링 재현이 아니라 명시적 우선순위 스트레스이며 제품 3파일은 변경하지 않았다.
  실제 Claude **68416**에 재합의를 요청했다. 승인 전 구현하지 않는다.
- R1: 실제 Claude **33298 최종 동의**, `claude-r1-final-boundary.md`.
  고정 DB여도 AUTH off면 머신 포인터를 함께 검사한다. 서버 다운로드 자격증명도 한 쌍으로 캡처하고
  fallback에서 새 계정 정보를 다시 읽지 않는다. 4개 설치 진입점과 임시파일 정리만 구현 범위다.
- E2/E3/E5: 실제 Claude 2차안 `claude-execution-recovery-counterexample.md` 수신.
  과거 job 앵커만으로 현재 요청 종결, 여러 출력의 부분 저장 ACK, 재시작 후 미저장 출력 복원 경계를
  추가 검토 중이며 아직 최종 설계 승인/제품 구현 완료로 세지 않는다.

B2 전용 실측 완료: 같은 167파일 사본에서 manage 응답 키 3줄만 다르게 한 100명·2코어
120초×2회 전/후 비교(`b2-measurement/REPORT.md`, `comparison.json`). 모든 오류 0.
작업 조회 p95 **95.67/95.92 → 121.18/120.07ms**, p99 **252.20/234.09 → 603.07/719.49ms**.
200 본문 응답 **218→612**, gzip **29,251,894→82,268,100바이트**(240초 합), 최대 RSS
**289,341,440→304,574,464바이트**. DB 2,000행 불변·WS/롱폴100 유지, 모든 시험 프로세스/포트 종료 확인.
p95 500ms/RSS512MiB/사이클간20% 기준은 통과했지만 경계 순간 지연·전송량 증가는 실제 비용이다.
실제 Claude49578은 기능 보장을 위한 현재 비용을 수용했다. 캐시128상한까지의 장기 체류는 이번4분의 보장 밖이다.
동시 열람20명 이상/본문gzip300KB 초과/LAN밖 사용은 콘텐츠 유래 ETag 개선 재검토 기준으로 제안됐으며,
실사용이 해당되는지 별도 확인 전 임의로 제품 계약을 바꾸지 않는다. 앞선 혼합15분은 이 비교의 대조군이 아니다.

### 후속 독립 검토·경계 보강 (22:15)

- U1 실제 Claude **15767 승인**, 상세 `claude-u1-code-review.md`. 후속 관련 **215 PASS/9 제외**,
  16.90초(`u1-after/FOLLOWUP.md`). 뒤늦게 보강한 20개는 baseline에서 모두 실패함을 별도로 확인했다.
  최초 하네스의 downstream 준비 오류는 보존하고 제품 결함 재현과 구분했다.
  원래 제외한 Windows 시험 9개는 root가 본문과 대상 폴더를 확인한 뒤 임시 설치/자식 프로세스만으로
  **9 PASS/34 제외**, 25.64초(`u1-nine-isolated/`). 실제 업데이트·설치본 실행은 하지 않았다.
  새로 확인된 N9: 실제 비동기 접수 함수 + 읽기 PermissionError 5회 동시 호출에서
  heartbeat 최대 지연 **507.955ms**(정상 5회1.624ms). HTTP 부하 결과가 아니라 실제 coroutine 계측이다.
  동기 재시도 sleep을 이벤트 루프 밖으로 옮기는 범위는 별도 합의 전이다.
- R1 실제 Claude **96343 승인**, 차단0/비차단7(`claude-r1-code-review.md`).
  4개 복원 접수의 계정/DB/서버 자격증명 고정과 설치 전 이중 검사를 구현했다.
  전82개 **66 FAIL/16 PASS** → 신규111 포함 관련 **188 PASS/2 제외**, 95.29초.
  추가29는 전 증거 없는 보강이며 82개 전체 실패라고 세지 않는다. 기존 시험2개는
  필수 expected 인자/합성 머신 포인터 준비만 보정했고 단언을 약화하지 않았다.
  관련2개 제외와 기본값 호환 시험1개는 진행 중인 전체 백엔드 검증에서 다시 확인한다.
  비차단 NB1 동시 import 잠금의 루프 정지, NB3 포인터 읽기 실패 구분, NB4 백업의 늦은 자격증명 재독은
  별도 설계 조사로 넘겼다. multipart 수신 완료 후가 핸들러 접수 시점이며 외부 포인터 수정 감시는 보장 밖이다.
- W1 실제 Claude **68416 최종 합의** 뒤 구현. 실제 제품 훅을 직접 검사한 신규 **58 PASS**,
  관련 **142 PASS**(실제 App 렌더39 포함), 17.85초. 타입/시험 타입/구조 검사 PASS.
  `w1-after/related-final-Hr1pwe/`, `checks-nCAKUA/`. Map 삭제 전 무효화, 현재 표시 ID 토큰 보존,
  후속 메타데이터 편집 보존, 300건/StrictMode/계정 전환을 포함하며 실제 WS 스트레스와는 구분한다.
  독립 Claude **84956 진행 중**, 제품4파일 동결. metadata ABA와 목록 밖 settled 토큰의 다음 commit까지 체류는 남는다.
- C4 공통 표시 조건/History 권한 전달: 전4 FAIL/16 PASS → 관련 **64 PASS**.
  C5 선택 바 번역: 전4 FAIL → 관련 **29 PASS**, 타입/제품 lint 통과.
  첫 C5 after 하네스는 기존 클릭 이벤트 인자를 오해한 단언으로 실패/2 pending이 남았다.
  제품 변경 없이 시험을 실제 계약에 맞춘 `c5-after/final-after.json`이 최종29 PASS 근거다.
  실제 Claude **46862 승인/차단0 요약**은 받았으나 상세 본문 유실로 **13731**에 상세 재확인을 요청했다.
  Canvas 두 wrapper의 mayFinalize 전달 누락(C4-F1)은 작은 후속 설계 대상으로 분리했다.
- C3 추가 모델/실제 API 재현 **13 PASS**: 이전 계정 finally가 새 pending을 줄이는 문제,
  committed A→B→A 뒤 옛 큐 재실행, 3-worker 조회1501번째 ID가 새 계정으로 전송되는 경계를 확인했다.
  장부 자체의 scope/epoch 소유와 조회 청크 직전 가드가 필요해 실제 Claude **47374** 재합의 중.
  `c3-design-repro/BOUNDARY_FOLLOWUP.md`. 제품 변경 없이 해시/프로세스 종료를 확인했다.
- E3 실제 앵커/삭제/purge 기반 설계 재현 **32 PASS**: 과거 이벤트는 현재 job 증거가 될 수 없다.
  Claude3차안은 trash exact-job 종결과 영구삭제된 역사 tracker 해제(현재 요청 무변경)를 분리했다.
  missing success의 자산 지연과 이미 terminal 요청의 복원 marker 경계는 **1972**에 추가 합의 요청.
  E3 제품은 전체 백엔드 시험 종료/최종 합의 전까지 변경하지 않는다.
- E2/E5 **51732**에 남은 설계 반례를 전달했다. 부분 출력 저장의 계정 귀속, 원장200 상한의 경쟁,
  TLS 실패의 접수 불명성, 스트림의 실제 시간 상한, 공용 HTTP 처리 유지가 미합의이며 구현 전이다.
- U2 실제 Windows 잠금에서 Python 상태 쓰기가 raw PermissionError/ASGI500으로 끝나고,
  PS Move-Item은 오류183과 임시파일을 남기는 경우를 확인했다. 250ms 재시도 예산은 읽기 부하에서 부족했다.
  5초/250ms 후보는 Python/PS 각각100회 성공했지만 PS100회56.25초의 지연 비용이 생겼다.
  오래된 writer가 최신 상태를 덮는 대조는 함수/파일 수준 순서 반례이며 실제 진입점 도달성 증거는 아니다.
  최종 U2 설계는 아직 미승인이다. 실제 업데이트·NAS·운영 앱은 변경하지 않았다.

전체 백엔드 시험은 격리 DATA/DB/MEDIA/UPDATE_STATE에서 실행 중이다(`backend-round1/`, 세션10231).
모든 결과가 닫히기 전 전체 통과로 기록하지 않는다. 개별 통과 수는 겹치므로 합산하지 않으며,
영역 전체 증거 **12/48(25%)**를 개별 수정 통과만으로 올리지 않았다.

### 통합 시험·후속 합의 (22:46)

- 백엔드 1회차는 **3,662 PASS +815 subtests PASS /7 FAIL /1 SKIP**, 2,215.05초로 종료했다.
  실패7은 모두 `test_server_move.py`에서 runner의 명시 `CONTENT_HUB_DB`와 시험 자체의
  임시 `data-dir`가 충돌했다. 제품/기존 시험을 수정하지 않고 해당 환경 지정만 해제한
  별도 격리 실행은 **26 PASS**, 18.01초였다(`server-move-isolated/`).
  첫 실패 결과를 보존하며 두 실행을 한 번의 전체 green 실행으로 표기하지 않는다.
  Windows symlink 권한 SKIP1은 기존과 같다. R1에서 제외했던2건도 전체 실행에서는 통과했다.
- 프런트 2회차 **171파일/1,687 PASS**, 17.20초, 빌드·구조 검사 PASS.
  뒤이어 C4-F1과 hover 후속을 포함한 실행은 **172파일/1,727 PASS**, 17.05초였다.
  동시 작업 트리에서 시점별 검증한 결과이며 다음 C3/E3 변경까지 보장하지 않는다.
- W1 실제 Claude **84956 승인**, 차단0. 비차단 N1(빈 토큰 장부에서도 목록을 순회)을
  기존 제안대로 1줄 guard로 줄였다. 신규 회귀 전58 PASS/1 FAIL → 관련 **73 PASS**.
  `w1-after/review-before.json`·`review-after.json`. 토큰/계정 계약은 그대로다.
- C4/C5 실제 Claude **13731 상세 승인**, 차단0. 캔버스 C4-F1은 기존 권한 함수를
  부모2곳→wrapper2곳→History 카드에 전달하는 코드5줄과 주석1줄로 수정했다.
  최종 동일36건 전4 FAIL/32 PASS → 관련 **117 PASS**, 두 실제 SceneBoard 경로의
  false→true 전달도 통과했다(`c4-f1-after/REPORT.md`).
- 별도 실제 hover 검증에서 C4 읽기 배지가 사라지는 회귀를 발견했다.
  실제 Claude **44005 설계 승인** 후 액션 없는 공유 배지를 유지하고 T/C 앞 공간을 확보했다.
  동일23건 전5 FAIL/18 PASS → 관련 **71 PASS**. 실제 제품 컴포넌트/CSS + 합성 카드의
  9관측에서 History/Canvas 읽기 배지 opacity1, Canvas 배지 오른쪽57/T왼쪽62로 겹침0,
  권한 허용·보류·최종은 기존 액션 유지, 일반·진행 중·비활성 대조도 확인했다.
  `c4-after/browser-hover-after.json`. 시험 서버8597/탭 종료·listener0.
  키보드/모든 카드 크기/실사용 서버 검증으로 확대하지 않는다. 후속 코드 독립 리뷰 **50068** 진행 중.
- C3 실제 Claude **47374**의 authEpoch와 씬별 장부2축, committed cardsSceneId,
  ticket 자기 장부 정리, 조회 청크 직전 검사 합의 완료. F1 동결 뒤 구현에 착수했다.
  같은 계정의 씬 전환만으로 이미 접수한 색 저장을 버리지 않는다.
- E3 실제 Claude **97229→1972→7751→19448** 최종 합의 완료.
  trash exact-job 종결과 영구삭제된 역사 tracker 해제는 분리하고, 증명 없는 결과는 무변화 거부한다.
  history-only는 DB 이벤트도 쓰지 않는다. 이미 보유한 유효 자산만 복원에 사용하며 새 provider URL 저장0.
  VERIFYING_NOTE 단일 상수 이동과 기존 공개 이름 유지까지 합의했다.
  전체 백엔드 종료 후 제품 쓰기 gate를 해제했다. 신규 실패 우선 시험과 구현 진행 중이다.
- E2/E5 실제 Claude **66453**에서 AUTH on의 논리 소유자는 요청 계정 email,
  DB 선택 pin과 별개로 판정하기로 합의했다. 스트림 read1 내부 chunked trailer는 전체 시간 상한을
  보장하지 않으므로 기존 hard deadline 주장을 철회하고 반복 경계 예산·제한 재시도로 범위를 명확히 했다.
  계정/manifest/collect 소유 계약을 통합해 구현 전 마지막 검토 중이다.
- R1 후속 실제 Claude **43482**는 수동 백업에서 source A를 늦게 선택한 B의 outbox에 넣을 수 있음을
  확인했다. 자동 백업은 이미 계정이 고정돼 있어 같은 문제로 묶지 않는다.
  import 잠금 즉시 거부·손상 포인터 분류·백업 접수와 자식의 기대 소유자 대조를 설계했다.
  자식 인자 개인정보 노출과 오염 캐시의 500 경계는 추가 정리 전이며 후속 제품 변경0.
- U2 실제47사례 이후 Claude **45619** 설계의 오류183 예외 범위, 원래 PS 실패 전파,
  새 await 취소 정리와 비용 주장에 좁은 정정을 요청했다(**65775**). 제품은 아직 변경하지 않았다.
  원자 파일 교체가 프로세스간 상태 순서를 보장하지 않는다는 한계를 유지한다.

### 독립 코드 검토·Windows 실측 후속 (23:15)

- C4 hover/Canvas 전달은 실제 Claude **50068 승인**, 차단0/비차단7.
  `:not()`로 CSS 우선순위가 증가하지 않는다는 종전 설명은 철회한다. 현재 충돌은 없으나
  110px 캔버스/키보드 접근성/Library 읽기 배지까지 모두 해결했다고 주장하지 않는다.
- C3 실제 Claude **31511 승인**, 차단0/비차단7. 최종 신규54개, 관련143개,
  전체 **175파일/1,781 PASS**, 16.77초, 타입·구조 검사 PASS.
  `c3-after/full-final.json`이 신규54의 최종 근거이며 앞선53개 스냅샷과 구분한다.
  씬 전환 뒤 실패 고지, 삭제된 ID의 복구 건수, 토큰 변경 후 폴 재시작 시험을 후속 설계·검토한다.
  공유 확정 후 단건 전체 레코드 교체는 별도 잔여 색 덮어쓰기 후보로 기록하고 즉시 수정하지 않는다.
- E3 최종 신규 **106 PASS**, 210.46초, 관련 **441 PASS +6 subtests**, 107.86초.
  관련 실행의 명시 제외5개는 기존 자식/kill4개와 버전 조회 mock 누락1개다. 후자는 실제
  `push_once`+버전 캐시 대조2개로 별도 보강했다. 차단된 첫 시험 도구 결과도 보존한다.
  실제 Claude **37170 승인**, 핵심 계약 차단0. 운영 뷰어의 신규 이벤트3종 누락,
  복구 문서 예외·한계와 에이전트 non-200/비-dict 응답·복원 충돌 자산값 시험을 보완한다.
- E2/E5 최종 **66453→33910→75208** 합의 완료. AUTH on owner-unknown legacy는
  원본을 보존하되 v1에 이관하지 않고, AUTH off legacy만 해당 DB 소유자로 불완전 증거 이관한다.
  DB당200개 상한과 전체 manifest를 유지한다. 근거 없는 1MiB/32출력 절단 제안은 반려 후 철회됐다.
  lazy 복원은 로컬만, key는 DB경로+AUTH mode다. 원격 취소는 기존 eager boot 자격/락 밖만.
  같은 경로의 프로세스 중 DB 교체 감지는 이번 보장 밖이다. 실패 우선46개 중42 FAIL/4 PASS를
  보존한 뒤 제품 구현 gate를 해제했다. 새 유료 제출로 복구하지 않는다.
- E2 device identity 공용 진입은 실제 Claude **25700 설계 승인** 뒤 동시 초기화 잠금을 추가했다.
  동일 시험 전5 FAIL/2 PASS → **7 PASS**. DB/원장/전환 잠금 밖에서만 캡처한다.
  단일 프로세스 초기화 계약이며 프로세스간 원자성을 추가 보장하지 않는다.
- R1 후속 **43482→62898** 최종 합의 후 구현 중이다. 수동 백업 접수 계정과 자식 기대 계정을
  고정하고, 자식 인자에는 원문 email 대신 기존 계정 key의 SHA256과 backup set을 전달한다.
  해시는 인증 수단/강한 개인정보 보호가 아니다. falsey 손상 캐시가 public accessor에서
  None으로 소실되는 경계를 추가 발견해 제품 확장 전에 Claude 교정 검토를 요청한다.
- U2 실제 Claude **65775** 최종 합의 후 구현했다. 새 writer21개 포함 **133 PASS**,
  async gate8개 포함 **177 PASS**. 두 묶음에는 중복 시험이 있어 합산하지 않는다.
  Python/PS 잠금5·32·33만 5초/250ms 재시도하며, 최초 Move의 create-race183은
  실제 대상이 새 파일로 생긴 경우에만 같은 예산 안에서 재시도한다. 나머지는 즉시 실패한다.
  공유 atomic helper/전체 updater/기존 rollback 정책은 바꾸지 않았다.
- U2 실제 Windows **14사례 완료**, `u2-actual/isolated-e709vy56/results.json`.
  Python transient647ms/permanent5,233ms, PS transient625ms/permanent5,297ms,
  동시 읽기 중100회 쓰기63,261ms/344시도/정상 읽기10,204/읽기 오류46/**손상 JSON0**.
  생성 경합·디렉터리·112/123/183 오류 대조와 자기 임시파일만 정리/타인 임시파일 보존을 확인했다.
  종료된 자식·합류된 reader thread를 확인했다. 전체 updater body는 실행0.
  첫 harness는100회 제한25초가 짧고 실패 cleanup에 stop-event 누락이 있어 중단됐다.
  정확한 시험 자식 PID만 검증 후 종료하고, harness만 수정한 재실행이다. 첫 결과는 보존한다.
  5초는 재시도 예산이며 OS I/O를 중단시키는 절대 상한이 아니다. 새 Python+구 worker 혼합 기간의
  기존 writer 한계와 프로세스간 상태 순서 문제도 해결했다고 주장하지 않는다.

영역 전체 증거 **12/48(25%)**는 유지한다. 개별 수정 승인·테스트 수를 전체 영역 완료로 환산하지 않는다.
이번 후속 제품 변경은 커밋·배포·운영 적용하지 않았다.

### 추가 실패 경계·측정·잔여 UI (23:32)

- U2 신규44개 포함 통합은173 PASS/2 FAIL(45.61초). 실패2는 동시에 변경 중인 E2의
  `comfy.run` 필수 Request 인자를 기존 U1 fixture가 전달하지 않은 TypeError다.
  E2 담당자가 해당2호출만 합성 Request/identity로 보정하고 기존 assert를 유지한다.
  해당2개 명시 제외 재검증은 **173 PASS /2 deselected**, 36.58초이며 제품5+신규시험2 해시 전후 동일.
  `u2-final-scoped/verification.json`. 이를 전체 green으로 표기하지 않고 최종 통합에서 다시 포함한다.
- 실제 SQLite의 begin→gate 취소/오류에서 pending 반환, cleanup WS 실패 뒤에도 DB 반환,
  DB 실패 시 기존 submitting/lease 유지 및 원오류 보존을 확인했다. 실제 Windows lease의
  Resolve 전송/재시도6경계에서 카운터 복귀와 핸들 해제를 확인했다. 실제 Resolve 실행0.
- U2 동일 sample_case 후속 측정은 PermissionError5 동시조건에서 heartbeat 최대
  **507.955→15.721ms**, 묶음 완료507.919→105.385ms였다. 정상5는1.624→3.754ms.
  `u1-after/measurement-followup.json` 대 `u2-loop/measurement.json`.
  실제 라우트/상태 읽기에 합성 오류를 주입한 코루틴 실측이며 HTTP/운영 p95가 아니다.
  네트워크·자식·DB 생성0, 새 스레드0, 측정 제품 해시 동일.
- PS AST 파싱 및 writer함수 외 전체 source가 HEAD와 같음을 확인했다. 실제 구 writer→새 writer,
  키 집합/BOM 호환, 원본 HEAD의 `_read_state` 추출 함수가 새 결과를 읽는 대조를 통과했다.
  `u2-actual/contract-result.json`, `old_reader_contract.py`. 독립 코드 리뷰 **49866** 진행 중.
- R1 후속 최종 **211 PASS**, 202.33초 + 기존 관련 **108 PASS**, 112.58초 +
  기본환경 poll 원본1 PASS(0.65초), 중복 없이320개. 최종외부네트워크·차단시도0,
  실제 합성 계정불일치 자식1개는 account_changed/exit0·outbox불변이었다.
  실제 HTTP multipart·기동드릴2개는 제외해 최종 전체 드릴에 남긴다.
  `r1-followup-after/review-z86ikef2/REPORT.md`와 고정6파일 해시를 root가 대조했다.
  실제 Claude63730 원시 cache 보강 승인 및 root 교정(I/O 증가0 일반 주장 철회)을 반영했다.
  R1 추가 코드와 device wrapper는 독립 리뷰 **34542** 진행 중.
- C3 NB2 실제 SceneBoard 재현: 공유 확정 뒤 색 저장 ACK보다 늦은 단건 조회가 도착하면
  UI 회색/캐시 빨강으로 갈라진다. 정상 순서는 둘 다 빨강이었다.
  `c3-followup/results.json`의3 PASS는 버그 재현2/대조·인증 경계 증거이지 수정 완료가 아니다.
  NB2는 작은 단건 반영 계약으로 별도 Claude **72384** 설계 중이며 제품 추가 수정 전이다.
- C3 NB1/4의 기존 세대별 무음/조회수 정책과 새 리뷰 권고가 충돌함을 actual **11495**가 인정했다.
  root는 auth-live 실패수F와 실제 반영수R 분리, 같은 티켓세대의 더 새 revision만 제외하는 B′를 승인했다.
  revision은 새 의도 접수이지 성공 증명이 아니며 후속 실패가 관측되는 대조도 요구한다.
  복구 쓰기는 여전히 현재 세대/현재ID/동일 revision에 한정한다. 계정 전환 뒤 침묵은 유지한다.
- C3 인증401 회귀는 AUTH on 경로로 한정한다. 초기 훅 조합의 요청2회는 App authPending
  게이트가 빠진 harness 수치라 실 앱 요청 횟수로 쓰지 않는다. AUTH off의 계정null·토큰 변경
  폴 정지 후보는 App의 !has_token 언마운트 완화까지 확인한 뒤 판정한다.

### 시간별 보고

- 20:41 첫 1시간 보고: 증거 **12/48(25%)**, B1 구현/56개 검사 통과·독립 리뷰 대기,
  나머지 후보는 합의/추가 재현 중. 15분170,886건 정상. 제품 전체 안전/수정 완료 선언은 하지 않는다.
- 21:41 두 번째 보고: 증거 **12/48(25%)**, 프런트1,602개·빌드·구조 검사 PASS,
  B2 전용100명 비교 오류0 및 지연/전송량 증가 공개, W1 추가 설계 반례 확인.
  개별 수정 통과를 영역 전체 통과로 올리지 않았으며 점검 중 변경은 배포하지 않았다.
- 22:41 세 번째 보고: 증거 **12/48(25%)**, 프런트1,727 PASS,
  백엔드3,662+815 subtests PASS와 경로 환경 충돌7건을 공개하고 분리 재검증했다.
  C3/E3 최종 합의 후 구현 착수. 운영 데이터·설치본·배포 변경0.
- 23:41 네 번째 보고: 증거 **12/48(25%)**, 초기 수정 묶음16/22(72.7%) 완료.
  R1/U2 독립 승인 도착·권고 보강 중, E3 후속186개 통과. 색상 경합/Comfy 회수와
  최종 전체 회귀·15분 후속 실측이 남아 전체 완료로 올리지 않았다. 운영/배포 변경0.

### 독립 검토 후속 (23:45)

- 실제 Claude49866(U2),34542(R1+device),18390(C3 NB2 최종 보완) 응답을 root가 전문 확인했다.
  U2/R1은 계약 승인·차단0이며 정적/실측 산출물 검토이지 Claude가 시험을 직접 실행한 것은 아니다.
- E3 실제37170 권고 후속은 표시3줄·회귀32개 순증이다. E3 138+기존로그48=186 PASS,
  248.43초. 변경 전 라벨3 FAIL/나머지24 PASS. root가 후속 diff와 제품6+후속2 해시를 대조했다.
  `e3-after/review-followup-bundle-0x37gfhf` 및 복구 계약§11에 잔여 한계를 기록해 E3 묶음을 닫았다.
- R1은 warm 캐시0→1 파일 읽기, import 접수/재대조 총3회, 게이트 안 디스크 I/O와
  수동 success가 같은 계정 이전 대기 세트일 수 있음을 계약에 추가했다. 제품2파일 동결 유지,
  실제 child 시험 env 키를 명시적으로 정정하고 재대조503·done set·main 일치·None token 회귀를 보강한다.
- U2는 recovery 호출부 failed 불변식과 begin BaseException 한정 AST 검사, 재시도 중 합성 파일
  삭제/재생성, 안전 로그 검사를 보강한다. 제품5파일은 독립 승인본을 유지한다.
  Python 주입시험의 가짜 tmp 부재 단언은 제거하고 실제 Windows14사례의 위생 증거와 구분한다.
  재시도 코드 집합 인라인/상수 차이, 예외 cause 보존, PS delete-race 즉시 실패,
  best-effort 쓰기 지연, 원예외 우선/후속 취소 흡수는 비차단 한계로 남긴다.
  fixture는 표준 time 전역을 바꾸지 않기 위해 모듈 clock 대역을 유지한다.
  후속 신규47개 PASS(6.26초), 실제 PS writer 한정 재실행/AST·recovery 호출부 불변식 PASS.
  root가 제품5파일 해시 동일을 확인했다. `u2-review-followup/results.xml`과
  `u2-actual/verify_contract.ps1`이며 전체 updater 본문은 실행하지 않았다. U2 묶음을 닫는다.
- C3 B′35건 및 관련61건 통과. NB2는 실제4콜백의 첫 await 이전 동기 capture만 보장,
  updater 실행 시 pending/revision 재평가, ref/cache 쓰기는 밖에서 수행하는 최종안을 승인했다.
  동일 gid 재추가 및 비색 메타의 전역 최신 순서는 보장하지 않는다. 추가 liveSet identity 가드는 반려했다.
- C3 NB3 7사례는 actual hooks+복사한 App 게이트로 검증했다. 정상 preserved401/토큰 제거 대조는
  복구된다. AUTH off·headerless401·백엔드 has_token 유지 조합에서만 폴 정지 조건이 재현됐으며
  현행 정상 프록시에서 영구 정지가 증명된 것은 아니다. 실 App 전체 재현으로 과장하거나 제품 범위를 넓히지 않는다.
- E2 신규 flat 경로의 대소문자/인코딩/상대경로 별칭이 legacy 분류로 빠질 가능성을 담당자가 발견했다.
  제품 변경 전 실제 경로 해석 재현과 Claude의 좁은 검토를 요청했고, 정상 legacy 보존을 조건으로 유지한다.

### 최종 통합 준비와 추가 반례 (23:55)

- R1 tests-only 후속: 신규14 포함225 PASS+관련108 PASS=333개, 실제 자식 AUTH 키 정정은
  포함 시험이다. 기존 기본환경 poll1건을 이번 실행 수에 더하지 않는다. 제품 추가 변경0.
  root가 보고서·추가 diff·제품2/시험2 SHA를 직접 확인해 R1 묶음을 닫았다.
  근거 `r1-followup-after/reviewed-bundle-9_rtkzfd`. 실 HTTP multipart·기동 드릴은 최종 전체에 다시 포함한다.
- C3 B′/NB2는 동결, 관련120 PASS/타입·구조 검사 PASS. 실제 Claude16884 독립 코드 검토 진행.
- 프런트 전체179파일/**1,819 PASS**, 타입 포함 build/구조 검사 PASS. 기존500kB 번들 경고 유지.
  `frontend-final/vitest.json`·`build.log`·`architecture.log`. 아래 E2 후속 뒤 다시 전체를 실행한다.
- E2 경로 별칭은 Windows 실파일/StaticFiles에서 실제 도달하는6개 비정규 주소가 다른 계정 save를
  통과하는 반례로 확정했다. 1회 URL 인코딩은200,2회는404인 실제 loopback 대조도 확인했다.
  경로를 임의로 재작성하지 않고 신규형식 비정규 입력을400 거부하는 안은 실제 Claude1979 검토 중이다.
- root가 E2 동일계정 목록 응답 순서 후보를 찾아 실제 hook에서 재현했다: 수동 옛GET→dismiss후
  최신빈GET→옛GET응답 시 정리된 행 재출현(1 FAIL/기존5 PASS). epoch+요청번호 보호를 별도 Claude와 합의 전 제품 동결한다.
- 최종 전체 backend runner를 준비했다. `test_server_move.py`만 테스트가 소유한 --data-dir를
  쓰도록 외부 CONTENT_HUB_DB 상속을 시험 전후 잠시 분리한다. 테스트의 자체 override/단언은 유지한다.
  같은 runner로26 PASS(25.84초), 전후 source해시동일. 최종 전체 결과로 과장하지 않는다.
  `final_backend.py`, `whole-backend-u7fyzyii/verification.json`. 전체는 E2 동결 뒤 실행한다.
- 후속15분 부하는 원래 도구/동일100명·300초×3회·2코어·낮은우선순위·512MiB 기준을 유지한다.
  ignored wrapper는 합성 DB/log를 지우지 않고 보고서 아래에 보존하는 것만 다르다. 아직 실행 전이다.

### 동결본 전체 검증 착수 (2026-09-18 00:10)

- E2/E5 소유 제품10·시험8파일 동결. 관련 backend **338 PASS, 제외0, 132.07초**,
  관련 frontend **35 PASS**. `e25-after/final-handoff.md`, `owned-file-hashes.json`과 고정 diff 보존.
  실제 Claude 독립 코드 리뷰17921과 root 전체 backend4791을 시작했다. 완료 전 통과로 세지 않는다.
- 경로 후속은 실제 Claude1979 합의 뒤 원문 ASCII segment 화이트리스트로 구현했다.
  URL 인코딩/상대경로/대소문자 별칭 및 Windows 별칭 입력을 거부한다. **비정규 legacy의 새 저장도
  400으로 좁혀진다.** 공식 legacy UUID 경로·정상 샤딩 경로는 유지하며 기존 파일/행/GET/입력 계보
  변경은 없다. Win32 ADS/8.3는 입력 거부만 검증했으며 해당 media GET200을 실증했다고 하지 않는다.
- 동일계정 목록 순서 후속은 실제 Claude1558 합의 후 seq+epoch로 보호했다.
  변경 전4 FAIL/6 PASS → 관련35 PASS. E2 수정 뒤 프런트 전체 **179파일/1,824 PASS**,
  타입 포함 build·구조 검사 PASS(`frontend-final-after-e2`). 기존500kB 번들 경고는 남는다.
- 실제 합성 SQLite에서 **200 records×100 outputs=20,000 entry** 전량 보존,
  JSON3,558,239bytes. 3회 read57.626–98.375ms, 한 행 수정 시 전체 봉투 read/check/write
  200.780–274.230ms(`e25-after/manifest-measure-results.json`). 절단/새 상한을 도입하지 않았고
  단일 프로세스·이 PC의 관측값이며 전체 실사용 지연 보장이 아니다.
- E2 실제 컴포넌트/훅·제품 CSS의 격리 브라우저에서 KO/EN, 360/700px 가로 넘침0,
  확인 후 정리·동일 job 회수·로컬 저장의 호출 배선을 확인했다. API4개만 메모리 대역이며
  서버 권한/ACK 실증과 구분한다(`e25-ui/REPORT.md`). 소유 탭을 닫고8599 서버를 종료해 리스너0 확인.
- R1에서 좁은 검증에 빠졌던 실제 loopback multipart와 복원본 기동/ready/login/count 두 훈련을
  전체 runner로 별도 확인했다: **2 PASS, 41 deselected, 6.07초**, 외부 접근 차단 발생0,
  전후 해시 동일(`whole-backend-lw9whwdr`). 최종 전체 결과에 숫자를 합산하지 않는다.
- C3 실제 Claude16884: B′/NB2 **승인·차단0**. root가 전문을 읽었고 제품 동결을 유지한다.
  GET 중 언마운트, batch missing 삭제와 HTTP404/410의 차이, 단건401, 실제 방송 후 조회를
  tests-only로 보강한다. 리뷰의 “항상 POST+단건+배치” 표현은 성공 경로에 한정해야 한다:
  publish는 succeeded>0, 나머지는 API/reload 성공 뒤 방송한다. 실패해도 항상 방송하는 것은 아니다.
  Library 키보드 색 실패의 한국어 고정 문구는 새 P3 후보로 기록하며 이번 동결 범위를 임의로 넓히지 않는다.
- root 전체 backend는 `whole-backend-hyfa8lxx`에서 실행 중이며 SQLite는 합성 루트,
  소켓은 loopback만 허용하는 감사 가드를 추가했다. 관측 중 실패2건은 완료 뒤 원인을 분류한다.
  전체 부하15분은 무거운 시험 종료 뒤 실행해 부하 간 간섭을 줄인다. 운영/NAS/설치본 변경0.

### C3 권고 회귀 완료 (00:15)

C3 제품 변경 없이 공유 후 조회 시험13개를 추가했다. 단독36 PASS, 관련6파일133 PASS,
타입/구조 검사 PASS. 실제 batch missing 삭제와 HTTP404/410 재시도, GET 중 언마운트,
401 공통 인증 정책, 실제 publish action의 성공/차단/HTTP 실패/reload 실패 방송을 대조했다.
root가 추가 시험 본문·보고서·보존 JSON과 제품12/시험1 현재 해시를 대조해 불일치0을 확인했다.
`c3-followup/reviewer-regressions-*`에 보존했고 C3 초기 수정 묶음을 닫는다.
StrictMode 공유 단건 및 전체 App 인증 게이트를 새로 검증했다고 주장하지 않는다.
실제 callback의 성공조건·방송·비색 응답 순서 한계는 보고서§6을 따른다.
최종 프런트 전체는 순증13개를 포함해 별도로 재실행 중이다(`frontend-final-reviewed`).

### 통합 프런트 완료·E2 접수 경합 추가 재현 (00:33)

- 프런트 최종 전체 **179파일/1,837 PASS**, 타입 포함 build·구조 검사 PASS.
  `frontend-final-reviewed`에 보존했다. 기존500kB 번들 경고는 유지하며 이후 코드/시험 변경은 재검증한다.
- Claude17921 독립 리뷰 전문을 root가 읽었다. 조건부 승인·차단0 의견이지만 stale 실행 snapshot
  경합은 단순 TTL 잔여 한계로 닫지 않았다. 실제 Event 순서 제어로 두 collect 모두200/worker2,
  dismiss200/원장0행인데 첫 worker는 active인 반례를 확인했다. 외부 호출6종0, thread 회수,
  제품 SHA 불변(`e25-after/stale-claim-snapshot-results.json`).
- 기존 원장 토큰 `(collect_job_id, collect_seq, phase)`을 먼저 읽고 실행 snapshot 뒤 원장 lock
  안에서 동일한지 재확인하는 최소안으로 실제 Claude 재검토를 요청했다. 아직 제품 수정 승인이 아니다.
  새 시각 기반 대기/lock 중첩/DB 열 추가는 도입하지 않는 방향이다. 목록의 순간적인 표시와
  변경 요청의 안전한 거부를 구분하며, collect와 dismiss 둘 다 보호해야 한다.
- 카드 실제 브라우저 합성 shell에서 140/220/340/640px 각각 S→Final→No,
  총12입력의 DOM 변경0.7–2.5ms·다음 frame callback9.2–14.2ms를 관측했다.
  페인트 완료/전체 App/백엔드 지연이 아니고 같은 PC의 backend 시험 간섭이 있다.
  확정 변경0, 소유 탭·Vite PID54700·8597 listener 종료 확인. `ui-probe/latency-report.md`.
- 프런트6·백엔드6영역의 시험/실측 근거 연결 문서를 읽었다. 실제 DOM/HTTP/함수 대역의
  범위와 미측정 칸을 분리하며 아직48칸을 선통과시키지 않는다. mixed load 도구는 캐시304를
  논리200으로 반환하므로 과거 기준선의 “모두200”은 raw 전송200만 뜻하지 않는다.
  B2 wire 비교만 raw200/304를 별도로 실측했다. Resolve 전용 실측 공백은 격리 가능한 계획만 준비한다.

### E2 접수 CAS 합의와 시험 재기준화 (00:39)

실제 Claude18594 응답(UUID `a23978ca-1222-4227-83c4-97a2615ceac6`) 전문을 root가 읽었다.
원장 read→실행 snapshot→원장 edit 재판독의 tuple 비교에 합의했다. seq는 새 helper에서
`int(value or 0)`로 통일하고, dismiss는 입력 순서 중복제거·missing sentinel로 edit의404를
유지한다. tuple 불일치는 기존409 문구를 재사용하며 completion CAS/TTL/DB 구조는 변경하지 않는다.
phase가 완료로 바뀐 직후의 보수적409도 UI가 finally에서 최신 목록을 다시 읽어 재시도할 수 있다.
리뷰 I4의 “살아있는 worker는 TTL로 사라질 수 없다”는 표현은 채택하지 않는다.
기존 blocking 호출/soft deadline 한계와 다중 프로세스 한계를 유지한다.

루트 전체 시험4791은63%에서 Ctrl+C 중단(exit1). 관측 실패2·skip1의 최종 traceback/XML은
생성되지 않아 통과 집계하지 않는다. PID51228/53388·고유 경로 helper/리스너0 확인,
합성 흔적은 `whole-backend-hyfa8lxx/INTERRUPTED.md`와 함께 보존했다.
초반 실패 분류용28파일 -x 실행80018을 별도로 시작했다. 다음 runner는 실패 즉시 기록만 보강했다.
E2 담당자에게 합의된 CAS와 관련 회귀·언마운트/손상 원장/409 재조회 및 EN 한 키 보완만 승인했다.

추가 변경 전 손상 봉투4형×5진입점은 모두409, 원문 보존·생성0·외부0을 확인했다.
별도 언마운트 후 늦은 GET 성공/실패2사례는 실제 setter 호출0을 확인했다.
phase 복원 기록 쓰기가 실패한 뒤 메모리 failed owner가 남으면 즉시 회수가409일 수 있다.
이는 기존 실패 기록 보존/TTL의 잔여 한계로 분리하며 새 시간 정책으로 바꾸지 않는다.
`e25-after/remaining-review-boundaries-results.json`, `unmount-late-get-results.json`.

### 2회차 증거 독립 판정과 추가 후보 (00:41 시간별 보고)

R2-F: Claude37625 실제 응답(UUID `25a85de5-1c7c-4239-bc8b-c3c5ec66c755`) 전문을 root가 읽었다.
전체179파일1,837 PASS에서 매핑된 **42파일565사례**를 정확한 `frontend/tests` 경로로 연결했다.
`frontend-evidence-summary.json`의 미해결 파일0. 코드·시험·경계 문서를 함께 정적으로 검토한
Claude 판정은 정상5칸(A01/A02/A04/A06/A11), 실패3칸(A02/A06/A11) 통과다.
root가 이8칸을 수용하여 **20/48(41.7%)**로 갱신했다. A06의 실제 자동 보관 경계는 이미 검토한
B2 관련 시험과 결합한다. 실 App/운영/장시간 브라우저/픽셀/실네트워크는 잔여로 유지한다.

보류는 A01 공유 다건, A04 공간 귀속의 계정 전환 후보와 A03 실제 폴더 이동 훅의 성공·실패
배선 공백이다. `publish_to_shared` 비proxy 루프, `set_generation_workspace_batch` 원격 왕복,
`put_project_folder` 미러(주변 설정의 잔여) 세 곳을 합성 A/B DB에서 추가 재현한다.
이 세 후보는 아직 재현/설계 승인 전이며 초기22묶음 분모에 섞지 않는다. A03 이동 경고의
한국어 고정/raw 오류도 별도 후보로 유지한다. 근거 `claude-frontend-evidence-response.json`.

초반 실패 진단80018은 `whole-backend-9vg8nx5s`에서 **125 PASS/1 FAIL**로 중단했다.
`test_execute_pending_claims_only_current_submit_worker_capacity`가 기본 LOCALAPPDATA의
실사용 agent_state DB에 접근하려다가 SQLite 감사 가드에 차단됐다. 실제 DB 연결 전에 막혔고
제품 실패로 분류하지 않는다. runner의 LOCALAPPDATA를 합성 루트로 지정했으며 안전 가드는
완화하지 않았다. 수정 전후 제품 해시 동일이고 두 번째 초기 실패는 아직 원인 미확정이다.

### E2/E5 최종 독립 승인과 2회차 구현 승인 (01:06)

실제 Claude의 CAS 코드 리뷰(UUID `61d5bf6c-bf3a-4111-89e4-316d1c507c1c`) 전문과 코드·신규 시험을
root가 읽고18파일 해시 일치 확인했다. backend **352 PASS**, frontend **39 PASS**,
신규 CAS14개는 before5 FAIL/9 PASS → after14 PASS다. 두 stale snapshot 반례는409·seq1·
첫 owner 보존·worker1·provider0·thread 회수로 바뀌었다. I4/살아 있는 worker TTL 보장은 채택하지 않는다.
리뷰의 “동일 바이트 파일” 및 “thread 시작 실패 자체로 collecting 잔류” 설명도 채택하지 않는다.
내용 동일성은 보장하지 않으며 collecting 잔류 반례는 phase 복원 기록 쓰기까지 함께 실패한 조건이다.
기존409 안내/기존 completion 토큰 정규화/다중 오류 우선순위 선택 제안은 범위 확장 없이 유지했다.
근거 `e25-after/cas-final`, `cas-owned-file-hashes.json`, `stale-claim-code-review-response.json`.

초반 backend 실패 재검증은 LOCALAPPDATA를 합성 루트로 추가한 runner로 동일28파일을 실행해
**488 PASS +27 subtests, 164.00초**, source_unchanged=true, blocked=[]를 확인했다
(`whole-backend-p53e5dp6`). 제품 변경/시험 제외/가드 완화는 없다. 중단한 원래 전체의 두 번째 실패
traceback은 없으므로 그 원인을 확정한 것으로 쓰지 않는다. 전체 backend green은 여전히 미확정이다.

| 신규 ID | 재현된 경계 | 승인한 최소안·현재 상태 |
|---|---|---|
| B6 | 비proxy publish에서 A 스냅샷 후 g1은A, 전환 후 g2는B에 공유. B에 행 없으면 FK 오류/A 부분 반영 | 첫 판정부터 요청 계정 pin, 구현 중 |
| B7 | 공간 이동 preview 뒤 원격 응답 대역에서 A→B, B에 같은gid/A creator 복사본이 있으면 B만 변경 | 전체 요청 pin, A 자체 타인403/누락404는 보존, 구현 중 |
| B8 | 폴더 root PATCH 응답 대역 뒤 B의 root/link만 변경. B 프로젝트 없으면 고아 link 생성 | 권한 확인 전부터 전체 요청 pin, 구현 중 |
| F2 | 공통 폴더 이동 안내의 한국어 고정/raw 오류, API 성공 뒤 예외를 이동 실패로 오보 | 번역·고정 오류 안내·API 실패와 reload 실패 분리, 관련41 PASS/독립 리뷰 중 |

계정3건은 최초25+교정13=38개 재현/대조 관측으로 정상·전환을 경로별 총4회 확인했다.
이는 수정 후 PASS 수가 아니다. 실제 원래 SQLite 선택/권한/쓰기 함수, AUTH off·합성 포인터 전환과
proxy 대역을 썼다. 실운영 사고/자연 발생 확률/실서버 원격 쓰기를 입증하지 않았다.
workspace의 B 타인403/누락404는 전환 후 잘못 고른 DB에서 나온 결과이며, patch 후에는 A를 검사한다.
응답 **스키마·호출순서**는 유지하지만 전환 레이스의 상태코드/귀속 결과는 의도적으로 달라진다.
Claude 설계 응답(UUID `d51de7bd-1d6d-4cd1-88b5-96eee9ebb093`)을 읽고 이 정정과 A 자체 권한 회귀,
중첩 pin/락 미보유/AUTH-on 단일 DB/예외 복원 추가 시험 조건을 수용했다.
키만 override한 상류의 현재 uid 혼합 한계·pid 단독 폴더 캐시는 별도 한계로 남기고 제품 범위를 넓히지 않는다.
근거 `cross-account-followup/REPORT.md`, `design-review-response.json`.

F2 설계는 실제 Claude(UUID `f0ab8a88-5545-44fb-9a66-9924afb8250a`)와 합의했다.
400 전용 안내/함수형 폴더명 치환/false만 팀 경고/updated0 성공/자동 재시도0을 유지한다.
새23개+기존4개는 before10 PASS/17 FAIL; after는 E2settings14를 더해41 PASS다.
모든 before 문구 차이를 기존 고장으로 세지 않는다. reload reject는 현재 reload의 일반 배선이 아니라
합성 계약 경화이며 운영 방송 누락을 입증하지 않았다. 새 후속 콜백 throw는 호출자에게 전파될 수 있다.
1501IDs는 프런트 미절단 전달만 검증한다. 실제 HTTP/SQLite 상한은 범위 밖이다.
제품2파일/회귀1파일만 수정하고 i18n의 앞선 E2 키는 그대로 보존했다. `a03-repro/design-agreement.md`.

실제 FilterSidebar/ResizableSidebar 합성 브라우저에서 생성자 A/B/해제에 맞춰
카드3/4/7 및 프로젝트3/4/7, 두 폴더의 ★/R/총계가 함께 바뀌는 것을 확인했다.
DOM 변경8.0–10.7ms/다음 RAF11.9–13.1ms는 해당3입력 표본이며 paint/p95/백엔드 지연이 아니다.
실제 drag200→360과 키보드200/360/480, 긴 이름 겹침0을 확인했고 소유탭·node27404/56936·8601리스너를 정리했다.
API8개/저장소는 대역이며 실제 서버 집계 증거로 바꾸지 않는다. `a04-ui-probe/REPORT.md`.

Resolve 실측 첫 실행은 정상3·no_project1 통과, crash 종료 순간 listener 조회가 NoSuchProcess(null)인
하네스 판정으로5개에서 중단했다. exit7/오류 manifest 저장/lease 해제/자식 회수는 통과했다.
null을0으로 바꾸지 않고 종료 경계 결측과 유효표본을 분리하는 좁은 계측 보강만 승인했고8개 전체를 새 run에서 재검증한다.
제품13해시 불변/guard위반0/소유자식잔류0. 실제 Resolve·사용자 프로젝트는 사용하지 않았다.
근거 `a09-measurement/attempt-1-report.md`. 계측 보강 뒤 결과를 아직 선통과시키지 않는다.

### 후속 프런트 확정·좁은 실행 실측 완료 (01:18)

F2의 실제 Claude 코드 리뷰(UUID `a5a3d059-ef92-43ac-946d-5b2e6ca84926`)는 승인/차단0이다.
root가 전문과 실제 코드를 대조하고 선택 회귀 두 항목(폴더명 literal placeholder, 상태코드 숫자 비노출)을
기존 시험에 보강했다. 제품 변경 없이 최종 프런트 **180파일1,864 PASS**, 타입 포함 build·구조검사PASS,
551파일 해시 전후동일(`frontend-final-676928677a104047adc77e04c1fa98f3`). 기존 번들 크기 경고는 유지한다.
리뷰의 “기존 팀경고 자체 없음”은 반려했다. 기존에는 한국어 경고가 있었고 EN 문자열 검사 실패였으므로
번역 누락이 정확한 판정이다. 잘못된 API 응답을0건 성공으로 숨기는 fallback은 추가하지 않았다.
기존 reload 내부 raw 토스트·2.5초 단일 토스트·미래 번역 어순은 비차단 후보/한계로 분리했다.

A09 최종8개 시나리오 모두 검증 필드 통과(150 Boolean 단언이지 pytest150개 아님).
실제 v2 route/복사·manifest/bridge Python 로직/FakeResolve/실제 Python 자식·Windows lease 경계다.
정상 raw1316.54–1370.94ms에는250ms×2대역hold·계측·manifest재검사가 포함된다.
기본probe child call→reap8030.49ms, 취소는 최종저장 후 전파, 부모lease해제 후 자식보호 유지 관측.
논리15호출/Windows PID30개와 부모51224 종료 확인, listener719유효0+2종료경계null을 원문 보존했다.
guard0/활동·pending·sampler0/소유lease해제, 제품13파일 현재해시까지 동일.
실Resolve ABI/선택worker/사용자프로젝트는 범위 밖. `a09-measurement/final-report.md`와 raw/판정diff.

A10은92B 합성PNG의 실제 loopback urllib/cache 경로를 계측했다. cold3회25.76–53.84ms,
warm3회0.088–0.104ms·추가GET0. 짧은40B응답은 실제3GET 뒤4.33초에transient, 확정파일0;
원래backoff1.2초와대기3초를 줄이지 않았다. 동일URL8동시에서GET1/cached1/already7.
최종 `.part`·lock·refs·추가thread·listener0, guard0, 제품3해시동일, 종료후PID17932/10920listener0.
작은표본/계측포함/SSRFfixture예외이며 CDN·대용량·코덱·실브라우저 성능은 입증하지 않았다.
`a10-measurement/REPORT.md`, `run-7889d2b423a5406a8d171cbd8ed2f5be/results.json`.

B6/B7/B8 새시험25개는 before19FAIL/6PASS→after25PASS다. wrapper3곳만 제거한 역변환파일의
SHA가 작업전 dirty3파일과 동일해 앞선승인수정 보존을 확인했다(담당자 인계 후 root재검토 예정).
관련첫420개는408PASS/12FAIL: 관리기능OFF환경10건과 fixture loopback DNS차단2건을 확인했다.
제품/기존시험을 고치지 않고 MANAGE=1 및 시험이 직접bind한소유endpoint만 허용해 동일묶음을 재실행한다.
첫실패는 보존하고 아직420통과로 세지 않는다. `cross-account-followup/related-jvkw3ybe`.

### 후속 계정3건 독립 승인·캔버스 실측 (01:33)

B6/B7/B8의 실제 Claude 코드 리뷰(UUID `fc124fbe-9880-4fd3-a81b-c0260f019ad5`)는 승인/차단0.
root가 응답 전문·owned.diff 전체·신규25시험 전체·4파일 현재해시를 대조했다. 관련 동일13파일420PASS,
실패/오류/skip0,317.09초. MANAGE 설정과 소유loopback endpoint 가드만 교정했으며 첫408/12실패는 보존.
계정3건 포함 승인26/26묶음의 개별 검증은 완료다. 전체backend/최종15분/48칸은 아직 별도 미완료다.
AUTH-on은 단일DB 선택은 불변이지만 uid pin은 달라지므로 “모든 동작 변화0”이라고 쓰지 않는다.
proxy publish 성공전환·folder응답tree 자체의 판별력은 선택시험 보강 후보이며 현재 저장값 검증을 대신하지 않는다.
key-only상류/폴더캐시pid전역·실서버토큰만료는 기존한계로 유지. `cross-account-followup/IMPLEMENTATION.md`.

A02 실제SceneBoard를 메모리Storage/API 합성환경에서1/50장 mount해 실마우스선택/키보드색전환을 관측했다.
6전환의DOM1.3–11.6ms/다음RAF2.5–17.4ms,50장배치마다정확히50ID/1요청. 선택·색표시 일치.
실saveScenes/listScenes와unmount/remount 후50장유지, 첫카드드래그화면좌표(60,123)→(93,145)가재열기후동일.
메모리저장 약0.1ms는 디스크/DB미러/내보내기 시간이 아니다. 색표시는generationcache/API에서오며씬JSON색영속성주장아님.
사용자데이터/외부통신0,소유탭·node48396/세션17273·8602listener정리. 프런트551현재해시도최종전체검증본과동일.
`a02-ui-probe/REPORT.md`, `observations.json`, `move-restore.json`.

A07 실제agent publicpass 합성실측은5케이스PASS 보고를 수신했으며 root원자료대조 중이다.
A08은 TestClient초기화 중 하네스Popen치환 및Windowsselfpipe차단 두 건을 구분해 최소하네스만 보정한다.
제품회수실행/측정은 아직0이며 오류를 제품결함이나 실측통과로 세지 않는다. 원자료두run은 보존한다.
`a08-measurement/ATTEMPT-2.md`의 정확한소유loop초기화예외만 승인하고 일반loopback/DNS는금지한다.

최종 프런트180파일1864개 중44파일592사례를 새 `frontend-evidence-final-1864.json`에대응했다.
기존4보류칸과5실측칸의 증거를 실제Claude에재검토요청했으며 결과전에는20/48을올리지않는다.

### 생성/회수 실측 인계·최종 전체 검사 시작 (01:40 정기 보고)

승인26/26 개별 검증 완료, 확정증거20/48 유지. 추가9칸 실제Claude 증거검토 진행 중이다.
최종backend는01:38에동결코드로시작했다(session69695,소유launcher53672/Python17876,
`whole-backend-dxrehxkd`). 합성LOCALAPPDATA/DB/미디어/업데이트경로·외부network거부,
MANAGE=1로고정하고기존전체tests를제외없이실행한다. 완료전PASS로기록하지않는다.
후속15분부하는아직시작하지않았다. 실측과중량전체검사를동시에실행하지않는다.

A07 원본agent lifecycle/실time·pool·SQLite를유지한5케이스/20publicpass(+포함wrapper5)모두통과.
합성create10/get30/anchor11,knownID유지·F1메모리보존후원본저장회복·F2같은ID재anchor·마지막active/tracked/outbox0.
정상I wrapper1395.02–1634.81ms에는각20ms대역과실SQLiteFULL·guard/계측비용포함.
listener368유효0+최종1유효0/결측0,신규worker/sampler0,PID55516사후부재. 사후listener조회NotFound의null은0으로변조하지않음.
root가하네스전체/최종보고/종료JSON/5case시간·cleanup·원자료를읽고대조했다. `a07-measurement/final-report.md`.

A08 profile-on1/200기록(각manifest100)두child통과. 실제TestClient/route/worker/slot/downloader와
두512KiB메모리HTTP스트림,100개중98사전저장+2회수. 전체20,000개다운로드시험이아니다.
200기록목록3.67–3.73초/성공회수8.09초/부분실패22.03초(실backoff14초포함).
sys/thread profile·audit·TestClient·sampler비용혼합이므로체감지연이나제품결함확정으로쓰지않는다.
각성공100출력/부분실패99출력과전체원장보존,HTTPResponse총22모두닫힘/part·slot·active·소유thread0,
selfpipe소유socket각3닫힘,childPID4개사후종료/제품6해시동일. `a08-measurement/RESULTS.md`.
profile-off6freshchild대조는최소하네스설계만승인했고최종전체검사종료뒤별도타이밍에실행한다.

비차단문구후보(추가수정미승인): `useGenerationUtilityActions.ts`의bulkDownload/히스토리실패와
`download.ts`의downloadOne/원격실패안내에는번역하지않은문자열이남아있다. `useAppToast`/`flashMsg`는
문자열을그대로표시하므로F2의폴더이동안내와별개경로다. 이번읽기에서는실브라우저재현/독립우선순위판정전이며,
현재통합검사를중단하거나다운로드저장계약까지새설계로넓히지않는다. 원본위치3진입점C6·폴더이동F2수정과혼동하지않는다.

### R3 증거 독립 검토와 후속 후보

`claude-evidence-round3-response.json`의 실제 Claude 응답은 A01 실패, A03 정상/실패,
A04 로컬 귀속 실패 및 A01/A02/A04/A09/A10 실측의 총9칸을 좁은 범위에서 승인했다.
루트가 전문·현재 제품/관련 시험 해시를 대조해29/48로 갱신했다. UI3칸은 브라우저 RSS/heap,
GPU 페인트 완료·실서버를 재지 않았으며 A09는 FakeResolve, A10은92B loopback이라는 제한을 유지한다.
리뷰어는 읽기만 했고 테스트/브라우저/해시 재계산을 실행하지 않았다. 재계산은 루트의 별도 근거다.

추가 후보2건은 승인 수정26묶음과 별도로 판정한다. 제품을 먼저 고치지 않는다.

- 팀 공간 귀속 프록시: `set_team_generation_workspace_batch`의 GET/PUT 사이 계정 전환 시
  호출 시점 토큰이 바뀔 수 있다. 실제 로그인은 B의 기존 elevation을 지우므로 B super-admin이
  항상 붙는다는 설명은 부정확하다. 일반 B 토큰 전환과 재elevate 조건을 분리하여 설계 검토한다.
- 비정상 폴더 이동 응답: API가 계약과 달리 null 등을 반환하면 좁아진 catch 밖의 `result.updated`가
  throw하여 이전의 오류 안내 대신 무토스트 rejection이 될 수 있다. 정상 서버가 이를 반환한다는
  운영 근거는 없다. `updated ?? 0`으로 가짜 성공을 표시하는 제안은 채택하지 않고,
  결과 불명 안내·재조회·자동 재시도 금지를 유지하는 최소안을 검토한다.

전체 backend는 동결 상태로 계속 실행 중이다. A06 합성30/300행 화면과 A08 profile-off 대조는
하네스 읽기 검토를 마쳤고, A11 실제 WS→재조회→DOM의 두 창 하네스는 승인된 격리 계획으로 준비한다.
실행 시점은 중량 검사와 분리하며 아직 실측 성공으로 세지 않는다.

### R3 후속 최소 설계 합의 (02:03 이후)

owner: Codex, reviewer: 실제 Claude, status: 설계 승인·재현 준비(제품 수정/실행 전).
팀 공간 프록시 후보를 B9로 분리하며 R3 응답의 F-1 표기와 같은 건이다.
`claude-team-proxy-design-response.json` UUID `1bc6c601-386f-42df-a7d8-3c594e0ad7eb`:
P2 귀속/정합성 후보로 판정, 첫 proxying 이전부터 기존 pin 한 겹에 합의했다.
실제 getter/raw_request 대역·정상 로그인 elevation 해제·비proxy 위임 전환을 반드시 반례로 확인한
뒤만 구현한다. 주입 시나리오 반복은 각1회, 기존 서명/권한 시험을 재사용하고 새 인증 설계는 만들지 않는다.
리뷰의 “유일한 미pin 변경 라우트” 총칭은 같은 응답의 댓글 경로 관찰과 맞지 않으므로 전체보장으로
채택하지 않는다. 별도 댓글 경로는 관찰 사실/미재현으로 남기고 이번 승인 범위에는 넣지 않았다.

F2 비정상 응답 후속은 `claude-move-response-design-response.json` UUID
`c6f2b779-31bb-4b46-af13-b8536e800985`의 최소안/M1/M2에 합의했다.
API 응답을 `{updated, teamSyncFailed} | null`로 한 번만 읽어 정규화하고 이후 원문을 재접근하지 않는다.
safe integer 0 이상, ok 부재 호환/명시 true 외 거부, false 팀 경고 규칙을 유지한다.
불명 결과도 재조회·board·broadcast 뒤 중립 안내1회, 자동재시도0. 일반 callback broad catch와
backend shape gate는 제외한다. 새 UI 회귀와 실제 API/fetch 경계 회귀를 별도 파일로 준비한다.
null의 hook 무토스트 rejection과 primitive/누락 수량의 오안내를 구분한다. 운영 사고 재현은 아니다.

두 설계의 전문을 루트가 원본/기존 시험과 대조했다. 현 whole backend가 끝나기 전에는 제품·기존
시험을 바꾸지 않고 ignored 준비 파일만 작성한다. 26/26은 앞선 승인 묶음의 개별 검증 시점 기록이며,
이 후속 합의의 구현·재현/최종 전체 통과를 선포하는 숫자가 아니다. 동적 확정 뒤 분모/상태를 갱신한다.

## 남은 위험·다음 단계

### 02:22 이후 후속 검증

전체 backend `whole-backend-dxrehxkd`는 4,120 PASS +815 subtests PASS, 4 FAIL, 1 SKIP으로 종료했다.
2,642.34초는 전체 시험 실행시간이지 사용자 지연이 아니다. 전후 소스 동일·격리 밖 접근 차단 시도0,
소유 launcher53672/Python17876/PowerShell54520 모두 종료를 확인했다. 실패4건 원문은 보존한다.
`backend-evidence-whole-4120-four-failures.json`에 6영역 매핑을 남겼고 A12 실패4를 숨기지 않는다.

4실패는 test_release_update의 두 helper가 RetryableMoveCodes 선언 대신 앞선 사용처부터 코드를
잘라 PowerShell 파싱이 깨진 문제였다. 실제 Claude `32e2b59e-5f4c-410b-b7bc-1737d65dc75d`의
승인대로 시작 표식에 ` =`만 추가했다. updater 자체/시간제한/복구 assertion/skip은 무변경이다.
해당4건 포함 업데이트5파일209PASS,54.70초,source 동일/차단0(`whole-backend-8lyyc2oq`).
updater SHA256 `873b50bd421c6688fd76e082eed7428e43062de49f7a5feeeaff3f3f64ebd5a6`은
수정 전 값과 동일하다. 전체 재검증은 B9 후속까지 반영한 뒤 다시 수행한다.

F2는 backend 검사가 읽거나 해시 계산하는 파일과 겹치지 않음을 확인해 독립 frontend 회귀를
먼저 실행했다. 최초 신규시험 brace 누락으로 수집실패한 before.json은 하네스 오류로 보존한다.
brace만 고친 제품 무변경 재실행은68개 중29FAIL/39PASS(기존23/API7 모두PASS),
합의한 private 정규화·중립 안내·한영2쌍 구현 뒤 같은68개 모두PASS다.
ok 재접근 poison과 team_synced null/undefined/true 정상 문구 대조도 포함한다.
원본 API/서버 계약은 바꾸지 않았으며 비정상 HTTP 성공 응답을 결과불명으로 안내하는 프런트 보완이다.
실제 Claude 독립 코드 검토와 frontend 전체 재검증은 진행 중이다. 승인 완료 건수는 아직 올리지 않는다.

B9 재현 첫 시도는 pytest의 Windows NUL 접근을 격리 가드가 막아 제품 호출 전 종료했다.
run `cross-account-followup/team-proxy/before-r7amtpg5`는 반례가 아니며 원문을 보존했다.
가드 범위는 유지하고 pytest capture만 sys로 바꾼 두 번째 시도에서 실제 반례를 확인 중이다.


전체 영역의 1회차 후보를 모으고, 실측으로 사실/추측을 나눈 뒤 Claude와 설계를 교환한다.
직접 검토 결과 또는 재현 근거 없이 구현을 시작하지 않는다.
운영 적용·유료 제공자·실기기 전용 검증 한계는 숨기지 않는다.

### 02:40 정기 보고 후 현재 검증

F2 후속은 실제 Claude `e881e1f3-b73b-4c90-b3b7-19c53aeed6a6` 코드승인/차단0,
루트 전문대조와 전체 frontend182파일1909PASS·구조/빌드PASS·553파일 해시동일을 마쳤다.
`frontend-final-823aaedf423f42608d8bca8b58e8feb2` 및 정정 매핑
`frontend-evidence-summary-823aaedf-f2-response-corrected.json`(46파일637개,누락0)이 근거다.
첫 매핑에서 직전 extra27개가 빠졌던 원본도 보존했으며 전체1909결과는 변하지 않는다.
빌드500kB 경고는 남고, 성능 개선이나 전체backend 완료를 뜻하지 않는다.

B9는 가드 정리된 before6FAIL/4PASS→기존pin한겹→after10PASS, 관련4파일98PASS를 마쳤다.
`whole-backend-kwwc37l1` 전후472파일 동일/차단0, 소유PID/리스너 종료 확인.
추가한 한 겹만 메모리에서 역변환한 generation.py 전체 SHA가 작업 전 dirty 기준과 동일하여
기존B7/다른 변경 보존을 확인했다. 독립 실제Claude 리뷰는 진행 중이며 완료건수에 아직 넣지 않는다.

A06은 개발 HMR/CSP 충돌 첫시도 제외 후 격리 프로덕션 정적화면에서57표시관측 모두일치했다.
30/300상태·검색·생성자/복합필터·해제 수량일치,대역GET6/오류0,unmount2·최종root/observer/RAF0.
3회표본300행 표전환DOM123.1–135.2ms/RAF2최대188.1ms로 다른 보기보다 무겁다.
계측/DOM스캔비용 포함,실제API/미디어/GPU페인트/RSS/heap미측정으로 제한한다.
소유tab/node54168/session3001/8603리스너 종료,프런트553재해시 동일.
`a06-ui-probe/REPORT.md`·`observations-production.json`·`verification-production.json`에 근거가 있다.

A03/A08 profile-off는 다른 중량검사와 분리한 직렬 timing gate를 열었다.
A11 두창/실제WS는 아직 준비본이고, A05는 기존100명로그인부하 재사용+작은실제인증ASGI계획을
Claude에게 검토 중이다. 최종전체backend와15분부하는 이들실측/후속승인 뒤 별도수행한다.

### 02:51 후속 독립 승인·실측 정리

실제 Claude 응답 `ff072938-e219-467f-b8ca-fd0afdeb8489`은 B9 최소 pin 구현을 승인했다.
루트가 응답 전문과 현재 제품/신규시험 SHA를 대조했고 각각 `7aebc34b…`/`27d9eb33…`로
after/관련98PASS 때와 동일하다. 리뷰 중에는 관련묶음이 진행 중이었으나 최종 인계와 정리 기록은
완료 상태로 갱신됐다. 따라서 B9를27번째 완료 묶음에 넣되 최종 전체 통과와 혼동하지 않는다.
같은 응답의 댓글 edit/delete/read/seen 경로 관찰은 별도 읽기 분류만 하며 미재현 후보로 유지한다.

A05 계획은 조건부 승인됐다. pA에는 A, pB에는 B의 `project_member`와 `creator` 역할을
명시한 뒤 원래 인증 middleware 함수 객체를 작은 ASGI 앱에 등록하는 안으로 준비한다.
선택 대안인 전체 main.app 사용은 채택하지 않아 기존 좁은 경계를 유지한다. 아직 실행 전이다.

A03은 실제 프로젝트 route/SQLite/outbox/로컬 관리 drain으로12요청 oracle 모두 통과했다.
일반1/50개 이동 median327.73/326.29ms, 없는 프로젝트400은1.99/1.88ms이며 DB준비/대조는
요청 타이머 밖이다. source19 동일, guard0, DBpool/context/thread/client/selfpipe 정리 및
PID44300/51484 종료를 확인했다. `a03-measurement/RESULTS.md`와 원자료가 근거다.

A08 profile-off는 A03 종료 뒤6개 fresh child를 직렬 실행해 모두 통과했다.
200×100목록9표본316.08–337.31ms, 성공회수1.95–2.13초, 부분실패16.11–16.63초다.
실패에는 원래2/4/8초 backoff가 포함되고 100출력 중98개는 사전저장·2개만 새 전송이다.
on/off 차이를 전부 profiler 고유 비용이나 제품 최적화 효과로 단정하지 않는다.
제품6/보존원자료12 해시동일, part/slot/active/thread0·응답66개닫힘·소유14PID 종료.
`a08-measurement/PROFILE-OFF-RESULTS.md`에 제한과 원자료를 연결했다.

A11 준비본에서도 Vite `hmr:false`가 개발 WS 시도 자체를 없애지 못하는 경로를 읽기로 확인했다.
제품은 그대로 두고 ignored 하네스만 production build+preview로 바꿔 기존 포트/네트워크/
자격증명 제거 경계를 유지한다. 해당 수정본을 읽고 나서만 브라우저 실행 gate를 연다.
R4의15개 증거칸은 독립 검토 대기라 현재29/48을 그대로 유지한다.

### 02:53 R4 증거 검토 반영

실제 Claude는 정상/실패11칸과 A06/A07 실측2칸을 승인했다. 전부 대표 계약/합성 경계이며
실제 제공자·Resolve·설치본·NAS 복원의 통과 판정이 아니다. A10 symlink1SKIP도 보존한다.
A05 실패/경합은 B9 코드승인 조건을 충족했지만 최종전체에서 기존306+B9신규10 동시 통과가
필요하여 대기한다. 새 시험을 최종 A05 매핑에 연결해야 한다. 기존 사례수와 중복 합산하지 않는다.

A12 실측은 **필수 공백**으로 남았다. 상태 writer의100저장63.26초/읽기실패46/손상0은 유효하지만,
복원 드릴의6.07초는 시험 묶음시간이지 복원·ready·로그인·종료 각각의 관측이 아니다.
이미 검증된 합성 백업1회·원래 `verify_restored_set_runtime` 흐름에 ignored 외부 관측만 붙여
단계별 시간/자원/정리를 기록하는 최소안을 준비한다. 제품·기존 assertion·timeout 변경0,
U2 actual14재실행0, 운영/설치본/실제updater/물리NAS복원은 계속 범위 밖이다.
남은6칸은 A03/A05/A08/A11/A12실측 및 A05실패다. 최종전체backend·최종15분은 별도 미완료다.

### 03:13 R5 대표 실측 승인·남은 검증

실제 Claude 응답 `427670c4-420e-4fcc-8a6f-c3441b811b4c` 전문을 읽고 A03/A08/A11 실측
3칸만 추가했다. 읽기 전용 독립 검토와 루트의 별도 해시/프로세스 확인은 구분한다.
원자료: `claude-evidence-round5-response.json`, `evidence-round5-root-recheck.json`.

A03은 스키마 캐시를 미리 준비하고 DB풀을 비운 조건의 실제 ASGI 이동 시간이다. drain의
개별 시간은 미계측이므로 약327ms를 특정 원인이나 운영 체감 지연으로 분해하지 않는다.
A08 off 실행은 시간/상태/정리 근거이고 코드 경계 통과는 같은 제품해시의 보존 on 실행과
연결한 추론이다. 두 실행의 시간 차이를 전부 profiler 비용이나 제품 최적화 효과로 귀속하지 않는다.

A11은 합성 카드1개로 실제 제품 hook→native WS→재조회→DOM을 연결했다. A색변경1회,
B자동반영, 자기 알림 중복조회0, B해제 뒤 추가알림에도 B의 fetch/layout/event/WS 증가0,
최종 양쪽 타이머/WS 및 서버 연결/sender 종료를 확인했다. 각 시계 안에서 A입력→DOM17.2ms,
서버notify→WS410.05ms, B수신→DOM415.9ms이며 원래0.4초/400ms debounce를 유지했다.
`a11-measurement/REPORT.md`와 관측11개/검증12조건/정리 원자료를 보존했다.
BC는 관측상 없었던 것이 아니라 하네스에서 비활성화한 전제다. 전체App/실DB/인증/재연결/
장시간heap/실LAN은 미측정이다. 첫 빌드 guard 실패는 제품 반례나 PASS로 세지 않는다.

A11 이전 JSON의 현재해시 차이 필드는 null이라 원자료를 덮지 않고 새 사후 기록에 명시적
불일치 배열[]과 제품12개 재계산을 남겼다. 보완된 하네스4파일의 현재 SHA도 기록했지만
이것을 누락된 실행 전 manifest의 소급 증명으로 사용하지 않는다. esbuild는 spawn PID와
사후 부재만 있고 OS생성시각/개별exit event는 없어 다른 프로세스보다 근거가 좁다.

A05는 최종 준비본 정적검토와18파일 해시 대조 후 단1회 격리 timing gate를 열었다.
A12는 원래 백업/복원 시험 assertion·timeout을 바꾸지 않는 관측안의 Claude 검토·준비 중이다.
댓글 edit/delete/read/seen은 아직 미재현 후보이며 설계 검토만 진행한다. 후보만으로
승인 문제수/완료수를 늘리거나 제품을 먼저 수정하지 않는다. 최종전체backend와15분부하는
이 후속 판단 및 소스 동결 뒤 실행하며, 개별 통과를 합쳐 최종전체 green으로 표시하지 않는다.

### 03:24 A05 인증 실측 승인

실제 Claude R6 `d2c4091b-de87-42e9-adef-c65122c3c93d` 응답 전문을 대조해 A05실측 한 칸만
추가했다. 첫 실행 login2+GET30, oracle30/30·흐름9/9 PASS, 200×24/401×3/403×3이다.
계정A cookie→B Bearer 우선/교체, workspace·멤버 가시성, B정지401/A대조200/같은B쿠키복구200을
확인했다. 쿠키/Bearer 관측은 공개 prefix `/api/auth/me`, 401/403 강제는 보호 경로에서 나왔다.
`a05-measurement/RESULTS.md`·run원자료·cleanup/root-recheck가 근거다.

GET median1.3382–2.7064ms(각3회), 20ms27표본 working set 최대89,055,232B이며
ASGI/가드/샘플러 비용 포함, CPU15.625ms 해상도/짧은요청 중간표본 누락 한계를 남긴다.
원본 객체24개/등록 middleware·route 동일,8테이블전후동일,자기DB1개와파일잠금해제,
thread/client/pool/context/selfpipe0 및 실제Python/launcher/wrapper/conhost종료 확인.
HEAD는 `4df0eb93`이나 dirty이므로 실제18파일 SHA가 소스기준이며 루트가 다시 대조했다.
준비 README는 과거 설계 기록임을 명시해 이후 PASS와 혼동하지 않게 했다.

이 결과는 AUTH-off 댓글 계정전환,로그인폭주,fullApp/TCP/실브라우저/장시간메모리를 닫지 않는다.
댓글 추가시험은 합의된18사례 준비 단계이며 제품미변경이다. A12 실측과 A05최종전체 조건,
최종15분부하가 남아 있다. 운영/NAS/설치본 변경0을 유지한다.

### 03:30 B4 댓글 후속 반례·시험 도구 실패 분리

실제 Claude 설계 `85432ca2-b7e0-4a15-8d5d-d92fca656edc`와 최소 정정 합의
`ad1b9433-6d76-4b33-94e3-708b9baae23f`를 반영한 신규18사례를 제품미변경으로 실행했다.
`whole-backend-q9sj8rd9`는8FAIL/10PASS(20.58초), source동일이다. 이 중 원격3사례는
원래요청에 포함된 `worker_id:null`을 테스트예상에서 빠뜨려 대역입구에서 실패했다.
그3개는 제품반례로 세지 않고 테스트1줄만 고친 뒤 새 BEFORE실행을 별도승인한다.

로컬5실패는 JUnit의 A/B 실제저장값으로 루트가 대조했다. A요청 중 B포인터로 전환하면
B대상없음에도 read1/seen0 또는 seen1이 생기고, B대상있음이면 B댓글 seen1이 생겼다.
같은ID·요청작성자 댓글이 B에 있으면 B본문이 수정됐으며, B의 동일작성자 비공개답글이 있는
삭제 사례에서는 B부모/답글이 함께 없어졌다. A는 원래값을 유지했다. actor는 계속 요청의
`me`이고 B는 DB귀속이다. 합성스케줄의 반례이며 운영사고/빈도/임의타인삭제를 주장하지 않는다.
scope/전환thread 정리 관측도 보존한다. B4의 파생 후속으로 재검증을 다시 열었으며
수정 방향은 기존 `_personal_meta_account_scope`를4경로에 적용하고 edit의빈400은 밖에 유지하는
합의안이다. 아직 제품은 바꾸지 않았다.

A12 첫시도는 pytest logging의기본NUL파일을가드가거부해시험진입0으로멈췄다.
원자료/172소스동일/부모·launcher종료를보존하고 제품실패나PASS로 세지 않는다.
시험이의존하지않는 pytest logging plugin만 제외한1옵션교정을 읽기검토했다.
NUL/UNC/외부읽기 허용은 늘리지 않았으며 댓글 BEFORE 종료 뒤 새단1회실측gate를 열었다.

### 03:49 B4 후속 종결·A12 R7 승인·최종 전체 시작

B4 정정 BEFORE `whole-backend-obji777f`는 사전 선언한 **7FAIL/11PASS**였다.
처음의 원격 대역 입력 오류 기록은 보존하고, 같은 스케줄에서 송신 전 getter의 B/B 귀속과
401 후속 probe의 B URL/A token을 실제 원본 getter·프록시로 재현했다(raw HTTP만 메모리 대역).
제품의 edit/delete/read/seen 네 경로를 기존 개인메타 계정 scope로 감쌌다. 빈 edit 400은
밖에 유지하고 요청 actor는 바꾸지 않았다. 네 scope와 설명 한 줄의 메모리 역변환 해시가
이전 전체 파일과 같아 기존 B9 등 다른 변경을 보존했음을 확인했다.

실제 Claude 코드 리뷰 `73a6328b-be37-4d2c-8031-744bd6886756`는 차단 이슈0으로 승인했다.
AFTER `whole-backend-ztynr21o`는 **112PASS**(신규18+관련94), 269.62초/runner269.867초다.
루트가 JUnit의 A만 read/seen·편집·삭제, B 원문/부모/답글 보존, 원격·401 probe A/A,
18건의 override 해제/전환 스레드 종료를 직접 읽었다. 473개 전후/현재 해시 동일,
격리 밖 차단 시도0, 소유4PID 종료를 확인했다. 실제 전송 실측은 edit 대역3경우이며,
나머지 원격 경로는 공통 helper와 코드 대조다. 이 한계를 전체 댓글 보안 보증으로 확대하지 않는다.

A12 성공 run `a12-measurement/runs/1d5e64be90cd44cd8049e8f903062744`는 원본 두 시험
**2PASS**/6.75초다. backup 발행441.0685ms, 별도 복원 세트 verify430.9328ms,
serve 생성→첫 ready200 2527.079ms, checks 확인→login 진입 경계2530.9135ms,
login140.9228ms, 원래 terminate 회수1.0983ms를 관측했다. 두 fixture는 별도 세트이고
중첩 단계들을 합산하지 않는다. 첫 ready 요청 하나가 listen 전 시작해 재시도0회였으며
기동·연결 대기·소유권 조회 비용이 포함된 상한이다. graceful 종료나 p95/RTO가 아니다.
실제 serve 최대 RSS90,484,736B, 표본 최대 간격75.6086ms, CPU 미측정이다.
원본 백업3개 재해시 동일·임시 파일/소유 PID/포트 정리와 가드 오류0을 확인했다.

실제 Claude R7 `36cdc59d-dbb2-4b85-9683-4faadb4d5de6`는 **실측 한 칸만** 승인했다.
검토는 코드·JSON 문자열 대조이고 실제 해시 재계산·PID/포트 외부 확인은 루트 기록이다.
Python 가드는 runner/실제 serve에 적용되며 venv launcher는 marker/부모/인수/생성시각의
사후 admission으로 확인한다. 보존 자식 로그는 전체 stdout이 아니다. 당시172소스 불변과
03:32 재확인 뒤 generation.py의 댓글4scope가 바뀌었으며 복원 경로 변경은 없다.
현재 전체 시험으로 최신 조합을 재확인한다. 최초 pytest logging NUL 가드 실패와
`no:logging` 단일 옵션 교정 기록도 보존하고 제품 PASS와 섞지 않는다.

최종 백엔드는 03:47:28에 시작했다. 세션 `52658`, controller57916→launcher21480→Python51712,
결과 `whole-backend-8lwer3pd`, 로그 `whole-final-env-bdbe9d0704d94168a997f83f57c6c498/whole.log`.
앞선 발사 준비 한 번은 비어 있는 환경 변수 목록의 PowerShell 처리에서 중단돼 Python/pytest
진입0이었다. 루프만 정정하고 새 격리 경로에서 시작했으며 기존 runner 해시·473소스는 동일하다.
제품/시험은 동결한다. 완료 XML에서 기존 A05 매핑에 B9와 신규 댓글18사례를 함께 확인한 뒤
15분/100명 부하를 순차 실행한다. 개별 PASS를 합산해 최종 전체 통과라고 하지 않는다.
NAS `2026.09.17-1900`은 이번 점검 수정과 무관한 이전 배포본이며 크기/해시만 읽기 재확인했다.
이번 점검에서 운영 DB·설치본·NAS 쓰기는0이다.

### 04:40 시간별 보고·최종 전체 통과·부하 준비 교정

최종 백엔드 `whole-backend-8lwer3pd`는 04:32:10 정상 종료했다. **4,152PASS +815 subtests,
1SKIP**, pytest2678.46초/runner2681.032초다. 제외는 기존 Windows 심볼릭 링크 생성 권한
부재이고 실패/오류0이다. 라이브러리 폐기예고 경고2개는 남았다. 473파일 전후와 현재 해시
동일·격리 밖 차단 시도0·controller/launcher/Python 종료를 확인했다. 앞선 전체에서 실패한
updater 표식4시험도 이번 XML에 각1번씩 PASS다. 프런트182파일1909PASS·빌드/구조검사와
553소스 동일은 03:50 루트 재계산으로 현재 코드에 연결했다.

`backend-evidence-final-8lwer3pd-case-sensitive.json`이 현재 집계다. A05는 기존10파일+B9+
댓글후속 파일의12파일/334PASS이며 신규28개 모두 포함, missing0/중복0이다.
다른 영역은 A07 314, A08 244, A09 155, A10 174PASS+1SKIP, A12 555PASS다.
영역은 겹칠 수 있으므로 합산하지 않는다. XML testcase4153개와 suite tests4968의 차이는
815 subtests이며 별도 누락이 아니다. 최초 분석의 기본 PowerShell 대소문자 무시 그룹화가
`None`/`none`와 Windows 경로 대소문자 대조를 잘못 합쳐 중복으로 표시했다.
원 XML을 보존한 채 대소문자 구분 tuple 식별로 다시 집계했다. 최초 분석도 별도 남겼다.
최종 종결 검토 전이므로 증거 진척은47/48, 승인 수정은27/27로 보고했다.

첫 부하 `whole-load-n5q913yk`는 **invalid / harness-misconfigured (aborted), 제품 no verdict**다.
원본 시드는 `data/media/lo/load.bin` 262144B를 만들지만 ignored wrapper는 `CONTENT_HUB_MEDIA`
값을 `RUN/media`로 지정했다. 서버 media_bytes0, 미디어 비중과 일치하는 unmatched4xx를 관측했다.
04:41:20 보존한 부분 스냅샷은 cycle2/60.05초, 누적2xx68302/4xx2815이다. 완주 결과가 아니며
개별 미디어 HTTP 상태코드나 첫 cycle의 정확한 최종 집계는 보존하지 못해 수치로 주장하지 않는다.
첫 중단 소유권 검사는 DateTime을 문자열로 바꾸며 소수초가 유실돼 안전하게 거부했다.
원래 DateTime의 정밀도를 유지해 PID 생성시각·부모를 다시 대조한 뒤 소유 서버/runner만 중단했다.
소유6PID 및14125리스너 종료 확인, 임시 DB/로그/부분 스냅샷/실행 wrapper 사본은 보존했다.

실제 Claude `57ff1250-7593-479b-aeec-74660c40c6a1`가 제품·원본 부하 도구를 바꾸지 않는
`CONTENT_HUB_MEDIA=RUN/data/media` **한 줄 교정**을 승인했다. 시간/임계값/인증/단언은
그대로다. shared/assets는 이 workload에서 참조하지 않으므로 관례 통일을 위한 추가 변경은 하지 않았다.
wrapper SHA는 `d76a8cbe…`→`8b6dbb7d…`이고 첫 실행 사본의 해시도 원본과 일치한다.

새 run `whole-load-s5vy_dvm`은04:42:59 시작, 세션44048,
로그 `load-final-env-b7efd1eef99443b0a2ad1adbcc575de7/load.log`다.
발사 전에 상속된 제품/provider/proxy/credential 환경변수 이름 목록이 빈 배열임을 실제 검사해
`root-launch.json`에 기록했다(값 기록0). 소유 트리22244→21560→54044→30344→57452,
conhost45292, listen1957을 `root-owned-start.json`에 보존했다. 첫30초 스냅샷은
media_bytes262144, 2xx6476/캐시3xx163, 4xx없음, WS/agent 각100이다. 아직 최종 PASS가 아니다.
원래100명·300초×3·20생성물·2코어·낮은 우선순위·512MiB 조건을 유지한다.
운영 DB/설치본/NAS 쓰기0이며 최종 부하와 독립 종결 검토가 남는다.

## 최종 통합 결과와 종결 검토 (2026-09-18)

이 절과 위 48칸이 최신 판정이다. 앞선 시간별 기록의 진행 중·미승인·실패는 해당 시점의 이력이며
삭제하지 않는다. owner Codex, reviewer Claude. R8은 코드 변경·추가 시험 요구 없이 승인했고,
최신 장부·A05 집계 의미·부하 지표/범위·중복/최적화 결론의 기록 보완 5건을 요구했다.
본 절에 모두 반영했다. R8 원문은 `predeploy-reports/program-audit-20260917/claude-evidence-round8-final-response.json`이다.
후속 `claude-r8-record-confirmation-response.json`(동일 폴더)은 **기록 보완 승인·추가 코드/시험 요구0**이며,
원자료/현재 소스 대조와 최종 종료 기록은 `final-completion.json`에 연결한다. 이로써 이번 점검을 종결한다.

### 최종 시험·실측

| 구분 | 단일 최종 실행 결과 | 범위·한계 |
|---|---|---|
| 백엔드 전체 | 4,152 PASS +815 subtests, 1 SKIP, 실패/오류 0, 2 경고 | `whole-backend-8lwer3pd`, Windows symlink 권한 제외; websockets/uvicorn 폐기 예고 경고 |
| 프런트 전체 | 182파일/1,909 PASS, 타입 포함 빌드·구조 검사 PASS | `frontend-final-823aaedf423f42608d8bca8b58e8feb2`; 기존 500kB 번들 경고 유지 |
| 혼합 부하 | 100명·300초×3, 171,283건 논리 정상, 17개 수용 기준 PASS | `whole-load-s5vy_dvm`; 정상 캐시304 재사용 포함, 유료 생성 없음 |
| 지연·메모리 | 일반 요청 p95 50.82/50.47/50.53ms, p99 81.79/84.76/82.52ms; 최대 269,979,648B = 257.5MiB, 워밍업 후 +3.77% | 기준선 p99보다 1.7–5.5ms 높다. 완전 통제 A/B나 전체 앱 성능 개선 주장이 아님 |
| 동시 연결·정리 | WS/agent long-poll 각100, 요청/전송/DB잠금 오류0; 소유6PID·포트1957 종료, 추가 kill0 | 무효 첫 부하의 별도6PID·포트14125도 종료; 원자료 보존 |

A05는 **단일 XML `E2C5DCEDAF102344CE61443FC52B2CF97C482FE04E6D51FDE43A40D948F18917`의
12파일/334PASS, 신규28 포함, missing0, 대소문자 구분 중복0**이다. 기존10파일306 + B9신규10 +
댓글후속18 =334이며 기존10파일 수의 감소가 없다. **대표 계약 집합이지 권한 전수 검증이 아니다.**
다른 계정 고정 시험은 최종 전체에 포함되지만 이 대표 집합에는 모두 들어 있지 않다.
6영역 집합은 겹치므로 합산하지 않는다. XML testcase4153와 suite tests4968의 차이는815 subtests다.

부하 지연은 다음 서로 다른 표본으로 분리한다. 마지막 cycle의 100명 동시 **로그인 p95 7660.6ms**,
로그인 중 일반 제어 요청101건 **p95 46.74ms**, 서버 전체 **p99 25012.77ms(during) /
25014.67ms(after)**다. 서버 전체 표본에는 약25초 대기하는 `/api/agent/wait` long-poll이
포함되므로 마지막 값을 일반 화면 요청 지연으로 읽지 않는다. 단일 경로의 p99를 별도 측정한 것은 아니다.

혼합 workload의 마지막 cycle은 list31,386 / facets8,495 / stats5,796 / detail5,675 /
media2,318 / write_color3,441의 **6개 작업 종류**와 별도 로그인·WS·agent 연결이다.
Comfy 실행, share/publish, Resolve 전송, 백업/업데이트, gen_requests 제출은 이 부하에
포함되지 않는다. 해당 경로의 회귀·개별 실측과 혼합 부하를 서로 대체하지 않는다.
`_unmatched`는 이 도구에서 라우트 템플릿이 없는 정적 미디어 요청 버킷이며 그 자체가 오류가 아니다.
첫 잘못된 media 경로에서는 해당4xx가2815였지만 교정 후에는 미디어262144B·4xx/5xx없음을 확인했다.
`task_read_commits_zero`와 `task_rows_unchanged`는 mixed 모드에서 null(미판정)이지 PASS가 아니다.

### 유해 중복 후보와 처분

| 영역 | 후보/공통 경계 | 처분과 근거 |
|---|---|---|
| A01 카드 검토 | 절대 상태 선택과 ↑↓ 상대 이동, 카드별 S 처리 | C1 오클릭만 보완. C2는 사용자 계약상 의도 차이로 통합 기각; 확인 UI·표시 자격은 공용 사용 |
| A02 Canvas·History | 캔버스만 단건 색 요청·무음 실패, 표시 자격 불일치 | C3 공용 배치/API·색 상태 규칙 재사용 + 보드 수명에 맞는 직렬화/복구; C4 공용 자격으로 정렬 |
| A03 공통 액션 | 선택바 번역·원본 열기 실패 표시의 화면별 복제 | C5 공용 문구, C6 `sourceLocationFeedback` 재사용. 이미 공용인 다운로드/프로젝트 이동 경로는 유지; F2는 그 경로의 응답불명 안내 보완 |
| A04 폴더·필터 | 일반/캔버스 사이드바 및 서버/화면 필터 | 이미 `ProjectSection`·`ResizableSidebar` 공용. 생성자 집계 opt-in·노드 dim과 목록 SQL의 의도 차이를 보존 |
| A05 신원·권한 | 여러 라우트의 계정 캡처·개인 메타 scope | B1 공용 capture/context로 위임, B4–9의 재현된 경로만 고정. 요청 actor와 DB 귀속, 명시 로그인 전환은 분리 |
| A06 관리 | 검토 상태 파생과 이름 표시·시각 캐시 | 구서버 호환 타입/관리 스냅샷은 라이브러리 카드와 통합하지 않음. B3 이름 해석 공용화, B2 시각 경계 재평가 |
| A07 에이전트 | HF 추적과 Comfy 실행의 유사 실패/재시도 | 제공자 접수/앵커/ACK 계약이 달라 무리한 통합 없음. E3/E4는 대상 소실·접수 후 로컬 실패 경계만 수정 |
| A08 Comfy | 제출 결과 불명과 원격 완료 후 결과 회수 | E2/E5 공통 영속 원장·기존 클라이언트 사용, 불명 제출의 자동 재실행과 같은 prompt 결과 회수는 분리 |
| A09 Resolve | 공용 계정 보호와 호스트 프로세스 처리 | 신원 캡처는 공용 위임, 실제 호스트/lease/자식 회수 수명은 생성 worker와 별도 유지 |
| A10 Assets·미디어 | 업로드·참조·capture, 영구 보존과 두 LRU 캐시 | `asset_io`는 이미 공용. 참조 중복 제거·보존/캐시의 삭제 권한과 수명은 달라 통합하지 않음; D1/D2 무결성만 보완 |
| A11 실시간 | 메인 App·독립 관리창의 소켓/폴링 | 문서 수명이 달라 연결 통합 기각. W1 완료 상세의 오래된 사용자 메타 덮기만 공용 상태 반영 경계에서 차단 |
| A12 업데이트·복구 | 상태 writer와 범용 atomic helper·버전 marker | U2 지연 재시도는 상태 writer에만 적용. 전체 atomic helper/marker 통합은 다른 경로의 빠른 실패 계약 때문에 기각 |

근거는 이 장부의 최초 경계 조사, 각 C/B/E/U 설계·독립 리뷰, R1-F 원문
`claude-initial-review.md` §3이다. 최초 제안과 후속 합의가 다르면 후속 합의/현재 구현을 우선한다.
전역 confirm/alert 전면 교체, 도메인별 삭제 callback 통일 등은 새 필요성이 없어 이번 범위에서 하지 않았다.

### 최적화 후보와 채택·기각 근거

| 후보 | 판단 | 측정/유지한 계약 |
|---|---|---|
| C3 캔버스 색 요청 배치화 | 채택 + 응답 역전 보호 | 변경 전 선택1/50/200장=1/50/200요청, 최종 실제 SceneBoard50장=50ID/1요청. localhost 단건 대 배치 비교 중앙값은50장263.70→3.80ms, 200장1044.04→6.12ms; 전체 화면/LAN 성능 수치는 아님 |
| B3 멤버 이름·M1 이름 캐시 | 공용 조회/성공 캐시 재사용 채택 | B3 연결1/SELECT3 고정, 이름 없는 계정 일관성 회귀. M1 실패·빈 목록 고착 해소 및 중복 진행 해제; 선택기 별도 수명은 유지. 전체 앱 속도 향상으로 확대하지 않음 |
| U2 관련 async 실행 게이트 | 이벤트 루프 밖 읽기 채택 | 잠금 주입5요청의 heartbeat최대507.955→15.721ms, 정상1.624→3.754ms. 느린 I/O가 빠른 I/O로 바뀐 것이 아니며 동시 핸들/worker 비용은 남음 |
| B2 조회 캐시 시각 재평가 | 정확성을 위해 비용 수용 | task-read p95 약96→120/121ms, p99 약234/252→603/719ms, 240초 gzip29.25→82.27MB. 오류0·합의 기준 내지만 경계 지연/전송량 증가 보존; 원자료 `b2-measurement/REPORT.md` |
| U2 상태 기록의 짧은 재시도·범용 helper 통합 | 짧은 예산/전역 적용 기각, 좁은5초/250ms 채택 | 실제 Windows14사례, 100저장63.261초·읽기오류46·손상JSON0. 제한된 재시도 예산이지 OS I/O 절대상한 아님; 전체 updater 미실행 |
| A08 회수 경로의 profile-on/off 속도 차이 | 측정 도구 영향과 제품 최적화 구분 | profile-off200기록 목록316–337ms, 성공회수1.955–2.131초, 부분실패16.111–16.635초(백오프14초 포함). 다른 실행조건의 차이를 전부 profiler나 제품 개선으로 귀속하지 않음 |
| A11 연결 통합·언마운트 누수 주장, 번들 전면 개편 | 근거 없는 통합/누수 주장은 기각, 번들 경고는 보존 | 문서별 수명 유지; 단기2창 WS/해제 관측만 확인. 전체heap/페인트·장기누수 미검증, 큰 번들 경고만으로 구조 재작성하지 않음 |

### 한계·변경 파일·다음 단계

검증 기준은 `dev` HEAD `4df0eb93` 위 **미커밋 작업본**이다. 변경105파일은 backend구현22,
backend시험25, frontend구현31, frontend시험18, agent/업데이트/도구3, 문서6이다.
전체 파일 목록과 R8 입력은 `final-closeout.json`, 보완 지표·후속 확인은 `final-review-receipt.json`에
연결한다(모두 `predeploy-reports/program-audit-20260917/` 아래). 원래 검토 입력 JSON은 덮지 않는다.
473/553/167은 각 시험의 선택된 소스 해시 집합이며 **저장소 모든 파일**의 해시가 아니다.
`backend/schema.sql` 같은 비-.py 백엔드 자산은 집합 밖이고 이번 Git 변경 목록에는 없다.

R8 원문의 `269.98MiB`는 MB/MiB 단위 오기로, 원자료269,979,648B는 **257.5MiB**다.
R8 말미의 `U2 최종 설계 미승인`도 과거 시점 혼입이다. 실제 설계 `eac21a11…`, 좁은 정정
`4e693491…`, 코드 승인 `fb66805e…`와 23:45의47개·PS 계약 보강을 근거로 **U2는 승인·종결**이다.
이는 읽기오류46회나 비용을 없앴다는 뜻이 아니며 actual14사례를 이 종결 단계에서 다시 실행하지 않았다.
두 오기와 XML SHA 비교 출처까지 세 건을 원문 보존 후 후속 승인으로 정정했다.
XML 해시는 backend verification의 필드가 아니라 실제 파일 재계산과 집계/receipt의 동일 값이 근거다.

실제 유료 제공자 왕복, 실제 Resolve, 운영 DB/설치본 적용, 전체 App E2E, 8시간 이상 운용,
브라우저 heap/페인트·물리 NAS→예비PC 복원/RTO는 미검증이다. A12는 별개 작은 fixture·강제 자식 종료,
A11은 단시간2창 관측이다. 이 한계와 B2/U2 비용을 종합 점검 완료 또는 혼합 부하 PASS로 지우지 않는다.
다음은 사용자 승인 후 코드/문서 분리 커밋과 Gate6-A 재확인, 이후 명시적 푸시·병합·릴리즈 절차다.
이번 점검은 배포 승인이 아니다. NAS의 `2026.09.17-1900`은 이전 게시본으로 이번 미커밋 수정은 미포함이다.

## 남길 것

- 프로젝트 문서: 기능 중복 여부는 호출 계약과 실패 경로 차이까지 확인한 뒤 판단한다.
- 기억·볼트: 없음.
