# 캔버스(씬) 병합 전 리뷰·최적화 설계도

> 목적: dev(→ main 병합 대상, 165커밋/89파일/+12,709−824)의 **캔버스 전 기능**을 팀 서버(다중 사용자) 배포 기준으로 **안전하게** 리뷰·최적화한다.
> 대원칙: **"지금 잘 되는 기능을 안 깨뜨린다."** 클로드 설계·통합 + 코덱스 방향/불변식 검토 공동 작성.
> 상태: **결정 확정(2026-07-27)** — 아래 4개 추천안 그대로 채택. 병합 전 안전 작업 착수.

## 결정 확정 (Jay, 2026-07-27)
1. **씬 저장** = 로컬 유지 + **계정별 네임스페이스**(서버 저장은 후순위 별도 작업).
2. **팀서버 Comfy 권한** = **제한 없음(전부 열기)**. API키가 있으면 누구나 실행/저장/설정 가능, UI 숨김 없음. (Jay 결정 2026-07-27: 팀은 신뢰관계, 키가 사실상 게이트.) → comfy 라우터 권한 변경 안 함.
3. **병합 전 리팩터 범위** = **Phase 0~1(특성화 테스트 + 순수 추출)만**. Phase 2+ 는 병합 후.
4. **E2E 도구** = **미도입**. 수동 스모크 체크리스트 + 순수로직 단위테스트로 대체.

---

## 1. 핵심 판단 — 대형 파일 구조개선은 "병합 후"

`SceneBoard.tsx` = **5684줄, 내부 함수 ~180, 훅 ~93**. 상태·refs·키보드·마우스·paste/drop·Comfy 실행·카드 렌더가 강하게 얽혀 있고 **브라우저 상호작용 테스트가 약함**.

| 시점 | 권고 | 이유 |
|---|---|---|
| 병합 **전** 대형 분해 | ❌ 비권장 | drag/휠/paste/undo/async 회귀 위험 큼, 안전망(테스트) 부족 |
| 병합 **전** 개선 | ✅ 저위험만 | 순수함수 추출·중복 파싱 캐시·**팀서버 보안/권한 점검**·수동 스모크 강화 |
| 병합 **후** 구조개선 | ✅ 권장 | Phase 단위로 작게, 매 단계 **동일 동작** 검증 |

→ **결론:** 병합 전엔 "위험 잡기 + 저위험 최적화"만. 5천 줄 분해는 병합 후 단계별로.

---

## 2. 작업 구분

### A. 병합 전 (안전·필수)
1. **특성화(회귀 방지) 안전망 확보** — 지금 동작을 고정(§5).
2. **팀서버 보안/권한 점검** — 다중 사용자에서만 드러나는 위험(§3-C).
3. **저위험 최적화** — 무동작변경 순수 추출·캐시(§3-A/B 중 저위험).
4. **팀서버 아키텍처 결정** — 씬 저장 범위·Comfy 권한(§4).

### B. 병합 후 (점진 구조개선)
- SceneBoard 책임 분해 로드맵 Phase 2~6(§6).

---

## 3. 크로스커팅 점검·최적화 항목

### A. 정확성 / 불변식 (위험 높음, 병합 전 점검)
| 항목 | 근거/현황 | 방향 |
|---|---|---|
| 늦은 비동기 응답(stale) | Comfy 실행엔 `sceneIdRef` 가드 있음. paste/drop/upload 경로는 추가 확인 필요 | 모든 비동기 시작 시 sceneId·content 캡처, 완료 시 현재와 다르면 카드 상태 미반영 |
| refs 경합 | `cardsRef/edgesRef` 즉시 갱신으로 setState 지연 회피 | 추출 시 이 패턴 보존(‘setState만 신뢰’ 금지) |
| 리스너 누수 | `beginDrag` 등 cleanup 구조 존재 | 훅 분리 시 cleanup 소유권 명확히 유지 |
| 배치 저장 순서 | 순차 await 로 대표=마지막 보장(최근 수정) | 회귀 테스트로 고정 |

### B. 성능 핫스팟 (대부분 저위험, 병합 전 일부 가능)
| 항목 | 근거 | 방향 |
|---|---|---|
| 워크플로 JSON 반복 파싱 | **해결** — content LRU에서 파싱·class·출력 종류를 공유하고 params 배열별 텍스트 키를 캐시 | 270개 프론트 테스트 + Comfy 카드 실측 |
| 렌더당 재계산 | **해결** — 그래프 파생값 memo, 뷰포트 컬링, 엣지 역할 incoming 인덱스 적용 | 큰 씬 이동 시 불필요한 전체 탐색 축소 |
| N+1 조회 | **해결** — `useSceneGenData`·완료 감시가 generation/history 배치 API 사용 | 진행 중 id만 재폴링 |
| object_info | 백엔드 5분 캐시 있음. 로컬 타깃은 전체(수 MB) 대신 개별조회 여지 | 로컬은 `/object_info/{class}`(작음), 클라우드는 전체 유지 |

### C. 팀서버 보안 / 권한 (위험 높음, 병합 전 필수)
| 항목 | 근거 | 방향 |
|---|---|---|
| AUTH off 원격 차단 | main.py 가 AUTH off 원격 바인딩 차단 | 팀 서버는 `CONTENT_HUB_AUTH=1` 고정(배포 체크리스트) |
| 프록시 경계 | `_proxy.py` 로컬 경로 allow-list | 새 라우터마다 proxy ownership 테스트 필수 |
| 계정 스코프 | `deps.py` account/remap 스코프 | `acct:<email>` fallback·실시간 스코프 정합 유지 |
| SSRF/경로 | net_guard·path_safety 존재. Comfy URL 은 예외적 private 허용 | Comfy 는 net_guard 우회 → **권한으로 막아야 함** |
| **Comfy 팀서버 노출** | `/api/comfy/*`(settings·run·status) | owner/admin/loopback 정책 필요(§4-②) |
| Comfy 저장 권한 | save-to-library 의 `require_project_role(..., read_only=True)` | read_only=True 의도 확인 필요(리뷰 항목) |

---

## 4. 팀서버 아키텍처 — Jay 결정 필요

### ① 씬(캔버스) 저장 범위 ★가장 중요
현재 씬은 **브라우저 localStorage** 저장(개인·PC 로컬).
- **A. 로컬 유지(추천, 최소):** 씬=개인 작업 초안. 단 **계정별 네임스페이스**로 분리(같은 브라우저에서 계정 전환 시 안 섞이게). 서버 저장은 별도 기능으로 후순위.
- **B. 서버 저장(큼):** 계정/프로젝트별 서버 보관·공유. 새 저장소·동기화·권한 설계 필요 = 별도 프로젝트급.
- 👉 추천: **A(로컬+계정 네임스페이스)**. B는 팀 공유 요구가 확실할 때 별도 착수.

### ② 팀서버에서 `/api/comfy/*` 허용 범위
Comfy 는 로컬/클라우드 자원 접근 + 크레딧 소모 → 다중 사용자에 그대로 열면 위험.
- 후보: 비활성 / **owner·admin 만** / loopback 만 / 전체 인증 사용자
- 👉 추천: **owner·admin 제한**(또는 운영자만). 최소 권한.

### ③ 병합 전 허용 리팩터 범위
- 👉 추천: **Phase 0~1(특성화 테스트 + 순수 추출)만**. Phase 2+ 는 병합 후.

### ④ 브라우저 E2E 도구 도입 여부
- 👉 추천: **지금은 도입 안 함.** Playwright 는 무겁다. **수동 스모크 체크리스트 + 순수로직 단위테스트 확대**로 대체. 병합 후 필요 시 검토.

---

## 5. 안전 방법론 — 안 깨뜨리는 절차

### 특성화 테스트(착수 전 확보)
| 구분 | 내용 |
|---|---|
| 프론트 자동 | `cd frontend && npx tsc --noEmit && npx vitest run && npx vite build` |
| 백엔드 자동 | `cd backend && python -m pytest` (CONTENT_HUB_MANAGE=1) |
| 브라우저 수동(스모크) | 씬 생성/전환, 카드 추가/삭제, drag, 휠 줌, 중간버튼 팬, 이미지 paste, asset drop, edge 연결, 그룹/접기, undo/redo |
| Comfy 수동 | 파싱, 시드 무작위, 실행, 배치 실행, 내작업 저장, **실행 중 씬 전환** |
| 팀서버 수동 | AUTH on 로그인, 권한별 조회/수정/삭제, 프록시 경로, WebSocket 스코프, /media, /api/download |

### 검증 게이트(매 단계)
tsc·vitest·pytest·build 통과 + 관련 수동 스모크 1회. **하나라도 실패면 해당 단계 즉시 롤백.**

### 롤백 기준
테스트 실패 원인 불명 / 수동 스모크 1개라도 실패 / AUTH·프록시 회귀 / 씬 전환 중 stale 결과 / localStorage 손상 가능성.

---

## 6. SceneBoard 점진 분해 로드맵 (병합 후)

| Phase | 분리 대상 | 후보 훅/파일 | 위험 | 검증 |
|---|---|---|---|---|
| 0 | 현재 동작 고정 | 스모크 문서 + 기존 테스트 | 낮음 | 씬 생성/전환·drag·pan·zoom·paste·drop·undo |
| 1 | 상수/순수 helper | sceneConstants.ts, sceneRefs.ts, sceneMedia.ts | 낮음 | vitest(렌더 구조 불변) |
| 2 | 저장/undo/씬전환 상태 | useSceneBoardState | 중 | persist/undo/sceneIdRef 보존 |
| 3 | 키보드/paste/drop | useSceneKeyboard, useSceneClipboardDrop | 높음 | Delete·Ctrl+Z/C/V/G·paste·drop |
| 4 | 마우스/카메라/드래그 | useScenePointerController | 높음 | marquee·drag·resize·wheel·pan |
| 5 | Comfy 실행/저장 오케스트레이션 | useSceneComfyOrchestration | 높음 | 전환 중 실행·late 무시·배치 순서 |
| 6 | 카드종류별 렌더 분리 | ReferenceCard/TextCard/ComfyCard/GenerationCard … | 중 | 시각 스모크 + 클릭/포트 |

Phase 1은 저위험(병합 전 후보 가능), 2~6은 신중(병합 후).

> 2026-08-05 `rearch/mv-hub-v` 진행 상태: Phase 1의 시드·그룹 기하 순수 계산,
> Phase 2의 저장/undo(`useSceneHistory`), Phase 3의 키보드 판정·리스너와
> 붙여넣기·파일/에셋 드롭(`useSceneClipboardDrop`),
> Phase 4의 드래그 세션 생명주기, 카메라·좌표 계산, 카드 크기 조절, 카드·그룹 이동과
> 배경 마퀴(`useSceneCardResize`, `useSceneCardMove`, `useSceneGroupMove`,
> `useSceneMarqueeSelection`), Phase 5의 실행 경계
> (`sceneComfyExecutor` + `useSceneComfyExecution`)를 분리했다. 카드 상태 소유자는 여전히
> `SceneBoard` 하나다. 다음 작업은 렌더 핫패스와 워크플로 파싱 캐시를 점검하는 것이며,
> 실제 브라우저 스모크를 각 단계에 병행해야 한다.

> 2026-08-05 렌더 핫패스 1차: 워크플로 원문은 LRU 32개 범위에서 파싱·노드 class·출력 종류를
> 한 번만 계산하고, 파라미터 배열별 텍스트 주입 대상도 약한 참조로 캐시한다. Comfy 카드의 연결
> 텍스트 판정과 입력 미디어 집계는 그래프가 바뀔 때만 다시 계산한다. 다음은 엣지 역할 판정에서
> input·list마다 전체 엣지를 되풀이해 읽는 경로를 인덱스 기반으로 줄이는 작업이다.

> 2026-08-05 렌더 핫패스 2차: 엣지 역할은 타깃별 incoming 인덱스를 한 번 만들고, 같은 input의
> 실제 소스와 같은 list의 수집 결과를 일괄 판정 동안 공유한다. 개별 판정 API와 정렬·색 규칙은
> 유지했으며, 화면 렌더만 `resolveEdgeRoles` 일괄 경로를 사용한다. 다음은 백엔드 `manage` 저장소의
> 쿼리/트랜잭션 경계를 정리하는 P7 단계다.

> 2026-08-05 P7: `manage.py`의 작업 조회·폴더 자동 작업·담당자 배정·CRUD를 `manage_tasks.py`로
> 분리하고 `repo.manage` 파사드는 유지했다. `manage.py`는 959줄에서 515줄로 줄었다. 다음은 P8
> `generations.py`의 남은 쓰기·동기화 책임을 점검한다.

> 2026-08-05 P8: CLI 결과의 known/unknown job 조회와 단건·배치 적재를 `generation_sync.py`로,
> 공용 레퍼런스 upsert/link를 `generation_references.py`로 분리했다. `generations.py`는 1,192줄에서
> 861줄로 줄였고 `repo.X` 파사드는 유지했다.

> 2026-08-05 P9: `assets.py`에 있던 업로드 스트리밍·지문·중복 탐색·원자 파일 확정을
> `services/asset_io.py`로, 계정별 마운트 JSON 저장을 `services/asset_mounts.py`로 분리했다.
> 다중 Assets 창의 동시 마운트 갱신은 파일별 잠금으로 변경 유실을 막고, 라우터 URL·응답은 유지했다.

> 2026-08-05 P10: 개인 메타와 팀 코멘트 API 9개를 `assets_metadata.py`로, 공통 접근 정책을
> `_assets_access.py`로 분리했다. 합본 `imp/cap`의 실제 메타 키 규칙은 순수 서비스 `asset_paths.py`로
> 단일화했다. 기존 URL·메서드·로컬 개인 메타와 원격 코멘트 뱃지 병합 계약은 테스트로 고정했다.

> 2026-08-05 P11: `generations-stats.failed_count`가 공유 DB 전체를 세면서 실패 정리는 현재 계정만
> 처리하던 범위 불일치를 수정했다. AUTH 모드는 현재 `creator_uid`, 단독 모드는 전체라는 동일 규칙을
> 통계와 정리 양쪽에 적용해 다른 사용자의 실패 수가 내 도구막대에 남지 않게 했다.

> 2026-08-05 P12: DB 경로 결정을 `db_paths.py`, generation 물리 삭제를 `generation_delete.py`로
> 분리해 `db ↔ runtime_metrics`, `generations ↔ trash` 지연 상호 의존을 제거했다. 모든 함수 내부
> import까지 포함한 백엔드 import 그래프 순환 0건을 AST 아키텍처 테스트로 계속 강제한다.

> 2026-08-05 P13: PM 대시보드의 다중 작업 담당 해제가 작업 수만큼 DELETE 요청을 보내던 경로를
> 기존 배치 배정 API의 `remove` 모드로 통합했다. 추가·교체·해제 모두 한 요청·한 DB 트랜잭션이며,
> 지정한 담당자만 지우고 다른 담당자는 보존하는 계약을 테스트했다.

> 2026-08-05 P14: Assets 다중 선택에서 색상·태그를 파일 수만큼 저장하던 경로를 배치 API로
> 통합했다. 최대 500개를 한 요청·한 DB 트랜잭션으로 저장하고, 중간 실패 시 전체 롤백한다.
> 기존 `Promise.allSettled`가 일부 실패를 놓치던 문제도 단일 배치 실패 처리로 함께 제거했다.

> 2026-08-05 P15: Assets 다중 소스 지정도 파일별 HTTP 요청 대신 배치 API로 통합했다. 파일
> 지문은 DB 쓰기 잠금을 열기 전에 계산하고, 계산된 소스 메타는 한 트랜잭션으로 저장한다.

> 2026-08-05 P16: PM 작업 보드 드래그 순서와 선택 삭제를 작업별 요청에서 배치 API로 바꿨다.
> 모든 작업의 존재·프로젝트 권한을 먼저 확인하고 한 DB 트랜잭션으로 적용한다. 연속 드래그 저장은
> 요청 순서를 직렬화해 늦게 끝난 이전 요청이 최신 순서를 덮는 경쟁도 막았다.

> 2026-08-05 P17: 라이브러리 다중 색상·개인 태그·전역 태그 변경을 배치 API로 통합했다.
> 항목별 권한 실패를 허용하던 기존 의미는 유지하면서 브라우저 HTTP fan-out을 없앴고, 기존에
> `allSettled` 결과를 무시해 태그 저장 실패가 보이지 않던 문제를 실패 개수 표시로 보완했다.

> 2026-08-05 P18: PM 작업 화면의 `tasks-batch`가 HTTP만 하나로 묶고 내부에서는 프로젝트마다
> `list_tasks`를 다시 호출하던 가짜 배치 경로를 실제 다중 프로젝트 DB 조회로 통합했다. 폴더·시퀀스
> 귀속은 `project_id`를 포함한 키로 매핑해 같은 이름을 쓰는 프로젝트끼리 섞이지 않으며, 컷·담당·
> 메트릭·코멘트·이름 집계는 전체 프로젝트 묶음 기준으로 처리한다. 큰 결과 집합은 SQLite 변수 상한보다
> 작은 단위로 나눠 조회한다.

> 2026-08-05 P19: Assets 메타 편집이 React 재렌더 전에 연속 실행되면 오래된 `metaRef`를 다시 읽어
> 색상 토글·소스 토글·태그 제거의 화면값과 저장값이 달라질 수 있던 경쟁을 수정했다. 낙관적 상태와
> 참조를 동시에 갱신하고, 같은 프로젝트의 저장은 입력 순서대로 실행한다. 중간 저장이 실패하면 대기 중인
> 변경을 마친 뒤 한 번만 서버 상태와 재동기화하며, 재조회 도중 들어온 새 편집은 최신 화면을 복원한다.

> 2026-08-05 P20: 라이브러리의 R/G/B 단축키도 재렌더 전 연속 입력에서 오래된 `gensRef`를 읽어
> 같은 색 두 번 입력이 토글 해제가 되지 않던 경쟁을 수정했다. 생성물 배열 참조를 낙관적 상태와 즉시
> 동기화하고 색 저장을 입력 순서대로 실행한다. 성공 후의 불필요한 350ms 재조회는 제거하고, 일부 또는
> 전체 실패가 있을 때만 대기 중인 저장을 마친 뒤 한 번 재조회한다. P19 큐는 공용 `lib`로 승격했다.

> 2026-08-05 P21: 일반·전역 태그의 단건/다중 낙관적 저장도 공용 순서 큐로 통합했다. 빠른 태그
> 추가·삭제의 HTTP 완료 순서가 뒤집혀 최신 값을 덮거나, 성공마다 예약하던 600ms 재조회가 다음 저장
> 중간에 화면을 되돌리는 경로를 제거했다. 새 일반 태그는 필터 목록에 즉시 합치며, 일부/전체 저장 실패
> 때만 큐가 끝난 뒤 태그 facet을 포함한 전체 재조회로 실제 서버 상태를 복구한다.

> 2026-08-05 P22: PM 작업 보드에서 빠르게 여러 번 드래그하면 모든 중간 순서 스냅샷을 서버에
> 직렬 저장해 마지막 화면 반영이 늦어지던 경로를 최신값 큐로 바꿨다. 실행 중 1건은 완료하되 대기 중
> 스냅샷은 최신 하나로 교체한다. 오래된 저장 실패 뒤 최신 순서가 성공하면 불필요한 재조회 없이 끝내고,
> 최종 저장 실패만 재조회한다. 재조회 도중 새 드래그가 들어오면 최신 낙관적 화면을 복원한 뒤 저장한다.

> 2026-08-05 P23: 라이브러리 색상·일반 태그·자동 태그 배치 API가 HTTP만 하나로 묶고 서버 안에서는
> 단건 라우트를 최대 500번 다시 호출하던 경로를 실제 DB 배치로 바꿨다. 로컬 id/job_id는 최소 행만
> 한 번에 해석하고, 권한을 통과한 로컬 항목과 남의 팀 카드 shadow를 각각 한 트랜잭션으로 저장한다.
> 로컬에 없는 팀 카드 확인도 기존 카드별 GET 대신 공유 서버의 `/api/generations/batch` 한 요청으로
> 합쳤다. 부분 성공, 자동 태그 계정 네임스페이스, 동일 id 마지막 값, 중간 실패 전체 롤백을 테스트했다.

> 2026-08-05 P24: PM 담당자 일괄 배정은 실제 저장은 한 트랜잭션이었지만 권한 검사에서 작업마다
> `task_project_id`를 다시 조회했다. 순서·삭제 배치와 같은 `task_projects` 경계를 재사용해 최대 500개
> 작업의 프로젝트를 한 번에 읽고 프로젝트별 권한도 한 번만 검사한다. 기존의 중복 작업 입력 순서와
> 최대 500개 절단, 작업당 담당자 20명 상한은 그대로 유지했다.

> 2026-08-05 P25: HF 원본 누락 검토가 확인 결과마다 생성물 신원을 다시 조회하고, 재등장한 항목의
> `hf_missing` 표시도 한 건씩 저장하던 경로를 배치 조회·한 트랜잭션으로 합쳤다. 휴지통 이동은 별도
> DB와 복원 자식을 함께 다루므로 안전하게 단건 처리를 유지한다. 로컬 정리에서는 휴지통 이동 결과를
> 확인하지 않고 `trashed`를 증가시키던 집계 오류도 수정해 실제 이동 성공 건만 반환한다.

> 2026-08-05 P26: 복사한 실제 DB에서 일부 옛 생성물의 `prompt`에 에디터 내부 `PromptPart[]` JSON이
> 문자열로 저장돼 카드·캔버스에 그대로 보이는 호환 문제를 확인했다. 전용 배열의 모든 원소가 유효할
> 때만 프론트 API 경계에서 텍스트·`@소스명`으로 복원하고, 일반 JSON·손상 배열은 원문을 유지한다.
> 정상 응답 객체는 그대로 재사용하며 목록·단건·배치·히스토리·소스 API를 같은 규칙으로 통일했다.
> 재생성 시에는 복원된 prompt를 명시해 DB의 옛 JSON 원문이 다시 실행되는 것도 막았다.

> 2026-08-05 P27: `test_push-db`는 콘텐츠·휴지통·팀 통계·계정 DB를 포함한 전체 격리 스냅샷을
> 만들면서도 `test_pull-db`는 `content_hub.db` 한 개만 받아 기존 `data_test` 전체를 교체해 서버와
> 다른 상태가 되던 통합 버그를 수정했다. 스냅샷 서버에서만 환경 플래그로 열리는 관리자 전용 ZIP
> API가 모든 `.db`를 SQLite backup으로 묶고, pull은 manifest·경로·중복·크기·CRC·각 SQLite
> 무결성을 확인한 뒤에만 기존 테스트 데이터를 보관하고 새 폴더를 설치한다. 미디어·에셋은 기존처럼
> 번들에서 제외한다.

> 2026-08-05 P28: 공용 `MediaThumbnail`이 썸네일·원본·영상의 최종 로드 실패 후에도 깨진
> `<img>`/검은 `<video>`를 계속 남기던 표시 문제를 수정했다. 레퍼런스 카드는 썸네일 실패 시 원본을
> 한 번 재시도하고, 그마저 실패하거나 재시도 경로가 없는 화면은 각 소비처가 정의한 fallback으로
> 전환한다. 로드 상태 전이를 순수 함수로 분리해 직접 실패·원본 재시도·최종 실패를 검증한다.

> 2026-08-05 P29: 같은 씬의 Comfy 카드가 실행 중일 때 워크플로 또는 노출 파라미터를 바꾸면,
> 먼저 시작한 실행의 결과·실패 상태·자동 저장 `genId`가 새 설정 카드에 붙을 수 있던 경쟁 조건을
> 수정했다. 직접·배치 실행 모두 시작 입력을 스냅샷으로 고정하고 미디어 다운로드 전후, API 완료,
> 카드 반영, 자동 저장, 하류 생성 직전에 현재 입력과 비교한다. 교체가 감지되면 이미 시작한 원격
> 작업은 그대로 끝내되 낡은 결과는 현재 카드와 하류 실행에 반영하지 않는다.

> 2026-08-05 P30: 워크플로 설정은 같아도 Comfy 실행 중 연결선·연결 텍스트·레퍼런스 순서 또는
> 원본 URL이 바뀌면 옛 입력 결과가 현재 그래프에 붙을 수 있던 남은 경쟁 조건을 수정했다. 카드나 엣지
> 객체 전체가 아니라 실제 API에 전달되는 정렬 미디어와 최종 파라미터만 정규화해 비교하므로, 결과에
> 영향 없는 단일 카드 이동은 실행을 취소하지 않는다. 다운로드 중 텍스트 편집은 기존처럼 호출 직전
> 최신값을 사용하되, API 시작 뒤 입력이 달라지면 성공 결과와 옛 실패를 모두 폐기한다. 라이브러리 자동
> 저장도 실행 당시 미디어 출처·텍스트·배치별 무작위 seed를 사용해 현재 그래프로 잘못 기록하지 않는다.

> 2026-08-05 P31: 비교 모달마다 복제돼 있던 영상 재생·정지·종료 동기화를 공용 바인더로
> 통합하고 수동 탐색도 양방향으로 맞췄다. 같은 절대 초를 기준으로 하되 짧은 영상은 자기 길이에서
> 멈추고, 메타데이터가 아직 없는 영상은 로드 뒤 대기 중인 탐색을 적용한다. 프로그램적으로 발생한
> seek/play/pause 이벤트는 한 번만 소비해 무한 전파를 막으며, 가장 긴 영상 종료 시 함께 되감는
> 기존 동작을 유지한다.

> 2026-08-05 P32: 렌더의 Comfy 실행이 끝날 때 현재도 같은 생성카드가 체크·연결돼 있는지 확인하지
> 않아, 실행 중 체크 해제·렌더 연결 해제·Comfy 의존 교체를 해도 옛 대상에 유료 후속 생성이 이어질
> 수 있던 경쟁 조건을 수정했다. 시작 계획의 대상·종류·의존관계·사이클 제외 집합을 의미 기준으로
> 비교하고, 달라지면 아직 시작하지 않은 Comfy 단계와 결과 저장·후속 생성을 중단한다. 카드 이동에
> 따른 동순위 순서 변경은 결과 의미가 같아 허용한다. 고수준 실행 계획은 무선 Input/Output 해석과
> 렌더 체크 필터도 자체 적용한다.

> 2026-08-05 P33: Comfy 실행 뒤 App이 생성카드의 최신 모델·텍스트·레퍼런스를 다시 읽어, 실행
> 시작 뒤 편집된 새 설정과 이전 Comfy 결과를 섞어 유료 생성으로 제출할 수 있던 남은 경쟁 조건을
> 수정했다. 실제 요청 재료 조립을 `sceneGenerationInputs` 순수 모듈로 옮겨 제출과 변경 검사가 같은
> 규칙을 쓰게 하고, 모델·파라미터·유효 텍스트·수동 및 동적 레퍼런스 순서를 시작 시점에 지문화한다.
> 정상 실행 중 달라지는 Comfy 출력·상태·자동 저장 genId와 표시용 thumb는 제외하며, 같은 상태를
> 반복 검사할 때는 cards/edges 참조 캐시로 그래프 재순회를 피한다. 저장 이력이 있는 Comfy를 List로
> 연결한 배치도 옛 저장 ref 대신 현재 overlay로 교체해 자동 저장 실패 시 이전 이미지가 섞이지 않게 했다.

> 2026-08-05 P34: 캔버스 생성 제출의 전역 `renderingRef`와 선행 `Promise.all` 장벽을 제거했다.
> 실행권은 씬별로 가져 같은 씬의 중복 클릭만 막고, 다른 씬은 느린 요청 중에도 계속 렌더할 수 있다.
> 모델 파라미터 조회·비율 해석·요청 조립·제출은 작업마다 독립 진행하며, 빠른 성공 결과는 배치의
> 마지막 요청을 기다리지 않고 시작한 원래 씬에 즉시 붙는다. 완료 시에는 입력 순서로 변형 순서를
> 다시 맞춘다. 비활성 씬 결과 반영은 현재 씬의 저장 전 입력을 보존하고, 현재 씬 결과를 합칠 때는
> 디바운스 입력을 먼저 확정한다. 대기 중 삭제된 카드·씬은 되살리지 않는다.

> 2026-08-05 P35: 생성 완료 감시를 현재 `SceneBoard`와 App 백그라운드 watcher 사이에서
> id별로 나눠 같은 생성물을 중복 조회하지 않게 했다. 현재 씬을 떠나도 다른 씬의 진행 중 결과는
> App watcher가 계속 추적하며, WebSocket 상태는 생성 등록 순서 경합을 피하면서 가능한 즉시 glow에
> 반영한다. 숨겨진 브라우저 탭에서는 주기 조회를 쉬고, 500개를 넘는 생성물 조회는 서버 계약에 맞춰
> 제한된 동시성으로 나눠 합친다. 읽기용 POST가 `synced`를 방송해 조회→전체 reload→재조회로 이어지던
> 순환도 미들웨어 계약에서 차단했다.

> 2026-08-05 P36: 본 서버와 공유 서버 프록시가 서로 달랐던 라이브러리 변경 판정을
> `mutation_notify` 계약으로 통합했다. 씬 2초 자동 백업·Assets 메타데이터·Comfy 설정·에이전트 깨우기와
> 조회형 POST는 더 이상 generation 전체 reload를 만들지 않는다. 실제 변경 요청에는 탭별 일회성 id를
> 붙이고, 그 변경을 포함한 목록 조회가 성공한 경우에만 뒤늦은 자기 `synced`를 한 번 생략한다. 다른
> 탭·다른 PC·출처 없는 syncer 알림과 실패한 조회는 항상 다시 반영한다. 캔버스의 projects 조회도
> fire-and-forget을 없애 코얼레싱이 완료를 정확히 기다리며, 최초 WebSocket 연결은 초기 목록 조회를
> 반복하지 않고 실제 재연결만 누락 보정한다. 외부 변경은 열린 SceneBoard에도 전용 갱신 이벤트를 보낸다.

> 2026-08-05 P37: 라이브러리·Assets·PM 변경 영역을 분리하고 Assets·PM 독립 창도 로컬 `/ws`를
> 직접 구독하게 했다. 같은 신호가 직접 WS와 BroadcastChannel로 겹쳐도 한 번만 처리하며, 숨김 탭은
> 복귀 때 따라잡고 PM의 느린 집계 요청은 겹치지 않는다.

> 2026-08-06 P38: 위임 모드의 로컬 허브가 공유 서버 `/ws`에 프로세스당 한 번 연결해 다른 PC의
> 데이터 없는 변경 신호를 로컬 창에 중계한다. 토큰은 URL에 넣지 않고, 개인 진행률·결과 URL은 차단한다.
> 로컬 프록시가 즉시 보낸 신호와 원격 echo는 요청 출처로 제거하며, route-level 위임도 출처 헤더를
> 보존한다. 로그인·설정이 라이브러리 reload로 오인되던 판정도 제외했다. AUTH 서버+로컬 허브를 별도
> 포트로 띄운 실측에서 세 영역의 외부 변경과 자기 echo 1회 처리를 확인했다.

> 2026-08-06 P39: 프로젝트 `Render` 폴더 트리가 모든 파일을 정렬하고 파일마다 경로 메타데이터를
> 2~3회 다시 조회하던 병목을 `os.scandir` 단일 열거로 바꿨다. 로컬 9,600파일 격리 실측은 평균
> 991ms에서 109ms로 줄었다. Windows junction이 렌더 루트 밖이나 자기 자신으로 재귀하던 경로도
> reparse 속성으로 차단하고, 생략 사실은 기존 `truncated` 표식으로 화면에 알린다. 폴더 노드 상한에
> 닿은 뒤 남은 형제 폴더를 계속 열거하던 동작도 즉시 중단한다.

> 2026-08-06 P40: 사이드바 폴더 배지가 각 노드마다 모든 `folder_path`를 다시 비교하던
> O(노드×경로) 계산을 순수 `folderTreeModel`로 분리하고, 경로별 개수를 부모 체인에 한 번만 누적한 뒤
> 노드마다 O(1)로 조회하게 했다. 노드·경로 각 2,000개 격리 실측은 중앙값 약 329ms에서 실제 새 모듈
> 4.21ms로 줄었다. 레거시 경로 정규화, 디스크에 없는 가상 폴더, 신규 공유 배지, 접두사가 같은 형제
> 경로와 `constructor`/`__proto__` 폴더명까지 순수 테스트로 고정했다.

> 2026-08-06 P41: 프로젝트 폴더 트리가 처음 열릴 때 모든 부모 폴더를 자동 확장해 큰 트리 전체를
> DOM에 만들던 지연을 제한했다. 250개 이하 트리는 기존처럼 모두 열고, 큰 트리는 표시 예산 안에서
> 첫 단계만 열되 마지막 선택 폴더의 조상은 항상 보이게 한다. 구형 전체 펼침 저장값은 버전 2 전환 때
> 한 번 폐기하고 이후 사용자의 수동 펼침 상태는 그대로 저장한다. 2,220행 React 렌더 격리 실측은
> 전체 펼침 중앙값 43.42ms에서 220행 첫 단계 펼침 3.66ms로 줄었다. 초기 확장과 스크롤 임계값 탐색은
> 반복문으로 바꿔 깊은 트리에서도 호출 스택을 쓰지 않고, 스크롤 판정은 15개를 넘는 즉시 중단한다.

> 2026-08-06 P42: 숨겨진 Assets 창에서 변경 신호를 받은 뒤 복귀하면 변경 큐의 전체 갱신과 별도
> focus/visibility 트리 안전망이 함께 실행될 수 있던 중복 경로를 하나로 합쳤다. 실제 변경이 있으면
> 변경 프로젝트·메타·코멘트를 반영하는 갱신만 즉시 실행하고, 신호가 없으며 마지막 조회 후 30초가
> 지난 경우에만 트리 안전망을 사용한다. 예약된 변경 갱신이 있거나 프로젝트를 막 전환한 경우에도
> 포커스 조회를 추가하지 않는다. 복귀 정책은 순수 함수 4개 테스트로 우선순위와 경계를 고정했다.

> 2026-08-06 P43: 관리 창 작업 탭이 숨김 중 변경 후 복귀할 때 실시간 `reloadSignal`과 자체
> visibility 안전망이 각각 `tasks-batch`를 실행할 수 있던 경로를 수정했다. 30초 폴링·복귀 안전망은
> 300ms 예약 시점이 아니라 실제 실행 직전에 진행 중 요청과 마지막 시작 시각을 다시 확인한다. 먼저
> 도착한 실시간 갱신이 있으면 5초 안의 안전망 요청은 생략하며, WebSocket 변경 자체는 그대로 우선한다.
> 진행 중·경계 시각 정책을 순수 테스트로 고정했다.

> 2026-08-06 P44: 프론트 HMR은 최신인데 재시작하지 않은 백엔드가 P36 이전 알림 계약을 계속 쓰는
> 버전 불일치에서, 조회형 POST가 출처 없는 `synced`를 되돌려 캔버스와 관리 창이 재조회를 반복하는
> 순환을 실측했다. 캔버스는 5초 동안 bare 신호와 프로젝트·폴더·생성 배치 요청이 각각 24회 발생했다.
> 메인 라이브러리는 bare 신호가 시작한 reload 진행 중에는 다음 신호를 기다리고, 성공 직후 1초 안의
> 반향만 생략한다. 1초 뒤의 syncer·다른 기기 변경은 다시 정상 반영하며 실패한 reload도 즉시 재시도한다.
> 같은 구버전 백엔드에서 수정 후 10초 동안 초기 bare 반향 2회는 요청 묶음 1회로 끝났다. 관리 창은
> 출처 없는 라이브러리 신호를 30초 안전망에 맡기고, 출처 있는 브라우저 변경과 `manage_changed`는 즉시
> 반영한다. 실측상 16초간 bare 신호 32회에도 `tasks-batch`는 정기 안전망 1회뿐이었다. Assets 복귀도
> 프로젝트·트리·메타 요청이 각각 1회임을 확인했다. 정책은 순수 테스트로 고정하고 진단 계측은 제거했다.

> 2026-08-06 P45: 63개 카드·54개 엣지 씬에서 선택 1회 렌더는 약 8.6ms였지만, 실데이터가 같은
> 실시간 동기화에도 프로젝트 배열·생성물 레코드·부모 관계를 새 참조로 교체하고, 보이지 않는 히스토리
> 보드와 닫힌 코멘트 패널의 신호 카운터까지 올리며, 생성물 재조회 시작 자체를 state로 표현해 큰
> `SceneBoard`를 반복 통과하는 것을 실측했다. API JSON의 내용이 같으면 배열·레코드의 기존 참조를
> 유지하고 실제로 바뀐 항목만 교체하도록 `stateReconciliation` 경계를 추가했다. 숨은 소비자는 열릴 때
> 자체 조회하므로 현재 보일 때만 갱신하고, 캔버스 재조회는 state 카운터 대신 최신 요청 우선 ref로
> 직접 시작한다. 최종 28초 실측에서 bare 신호 6개와 프로젝트·폴더·생성물 요청 묶음 3회는 정상
> 실행됐지만 동일 응답의 `SceneBoard` 렌더는 0회였다. 진단 UI·계측은 제거했으며 백엔드 375개,
> 프론트 379개, TypeScript/Vite 빌드와 ESLint 구조 검사를 통과했다.

> 2026-08-06 P46: 주기·포커스·실시간 갱신 훅을 전수 점검한 뒤 실제 526개 파일 Assets 화면을
> 실측했다. 트리와 메타가 이전과 같아도 API의 새 배열·객체를 그대로 state에 넣어 새로고침 1회마다
> 전체 화면을 2회 렌더했고, 합계 약 49~51ms가 걸렸다. 캐시 직렬화·저장은 115KB·0.6~0.7ms로
> 병목이 아니어서 유지했다. 프로젝트 목록·트리·메타 state만 구조 비교로 기존 참조를 보존하고,
> 변경된 항목은 정상 교체하도록 했다. 같은 새로고침에서 API와 캐시 저장은 그대로 실행됐지만 Assets
> 렌더는 0회였고, 중첩 트리의 파일 버전 변경은 새 참조로 반영되는 테스트를 추가했다. 진단 계측은
> 제거했으며 백엔드 375개, 프론트 380개, 빌드와 구조 검사를 통과했다.

> 2026-08-06 P47: 실제 6행 관리 작업표와 활성 폴더 약 15행인 사이드바를 점검했다. 관리 작업표는
> 실시간 신호·30초 안전망의 `tasks-batch` 내용이 같아도 정렬된 작업 배열과 중첩 컷·담당자 객체를
> 매번 새 state로 교체했고, 사이드바는 생성물 변경 알림마다 `countTick` state를 먼저 올려 카운트 응답
> 전에 한 번 렌더했다. 작업 응답과 프로젝트 순서는 구조가 같으면 기존 참조를 유지하고, 폴더 카운트와
> 팀 신규 목록도 실제 값이 달라질 때만 state를 교체한다. 카운트 알림은 최신 함수 ref로 조회를 직접
> 시작하며, 탭·핀·활성 프로젝트가 바뀐 뒤 늦게 온 이전 응답은 요청 번호로 무시한다. 브라우저에서 팀
> 기준 25건과 내 기준 3건의 폴더 배지 전환, 관리 작업표 6행과 30초 안전망 1주기 후 동일 6행을 확인했다.
> 백엔드 375개, 프론트 382개, TypeScript/Vite 빌드와 ESLint 구조 검사를 통과했다.

> 2026-08-06 P48: 프론트 전체의 `setInterval`·focus/visibility·WebSocket/BroadcastChannel 및
> `reloadSignal` 소비자를 최종 감사했다. 남은 큰 경로는 통합 관리 대시보드로, 프로젝트 4개·작업 6개
> 실데이터에서 내용이 같은 요약·작업·멤버 Map·팀 통계·주간 추이를 다시 읽으면 화면을 8회 렌더해
> 합계 약 2.10ms가 들었다. JSON 배열·객체뿐 아니라 JSON 값을 담은 Map도 동일하면 기존 참조를
> 유지하도록 확장해 같은 재조회는 0회 렌더가 됐다. 같은 실시간 신호를 받는 완료 탭의 프로젝트·저장
> 상태, 보이는 히스토리 그래프, 상단 계정 메뉴의 30초 동기화 상태도 동일 응답을 보존한다. 임시 계측과
> 강제 진입 코드는 제거했고, 브라우저에서 대시보드 프로젝트 4행과 완료본 저장 대상 2건을 확인했다.
> 백엔드 375개, 프론트 383개, TypeScript/Vite 빌드와 ESLint 구조 검사를 통과했다.

---

## 7. 역할 분담
- **코덱스:** 불변식 검토, 순수 로직 추출, 테스트 설계, 보안/성능 핫스팟 검증.
- **클로드:** 전체 설계 통합, UI 흐름 보존, 병합 순서 조율, 최종 파일 수정.

---

## 8. Jay 결정 필요(요약)
1. **씬 저장 범위**: 로컬+계정 네임스페이스(추천) vs 서버 저장(별도 프로젝트)
2. **팀서버 Comfy 권한**: owner/admin 제한(추천) vs 전체 인증 vs 비활성
3. **병합 전 리팩터 범위**: Phase 0~1만(추천) vs Phase 2까지
4. **E2E 도구**: 미도입(추천, 수동 스모크로) vs Playwright 도입

---

## 리뷰 진행 기록
- **청크 1 (백엔드 보안/권한)**: 완료. Comfy는 개인 로컬+본인 키 사용이라 서버 공유 위험 없음 → 권한 변경 안 함. 씬 계정 네임스페이스 반영.
- **청크 2 (씬 그래프 순수 로직)**: 완료. 반영 3건(이관 경계 #2·리스트 텍스트 상류 #4·import 방어 #3). 결정: **#6 output 다중입력은 그대로 허용**(채널 이름을 다르게 쓰면 겹칠 일 없음, input/output 의도된 설계).
- **청크 3 (SceneBoard 상태·저장)**: 완료. 코덱스 공동 설계로 5건 반영 — **#3 카드 삭제는 캔버스만(서버 생성물 보존, Jay 결정 ⓑ)**, #4 수동 참조 provenance 보존, #5 이동/정렬 후 연결 참조 순서 재계산(withGenRefs), #6 그룹 유령 멤버 정리, #1 비동기 응답의 씬 오염 가드(파일임포트·캡쳐·comfy parse/save/run). #2 undo-vs-외부저장은 보류(이미 완화).
- **청크 4 (입력 이벤트: 키보드·마우스·paste·drop·drag)**: 완료. 코덱스 공동 설계로 3건 반영 — **A** 색(r/g/b)·비활성(d)·그룹(Ctrl+G) 단축키에 `e.repeat` 가드(키 반복이 토글을 깜빡이거나 `setColor` 반복 전송하던 문제). **C** Comfy 노드 드롭존이 `.json` 만 가로채게(이미지·에셋 드롭이 빈 comfy 카드에 삼켜지던 문제, 이제 보드로 통과해 레퍼런스 카드 생성). **B** `beginDrag` 에 `onCancel` + 창 blur 취소 인프라(2-ref: 언마운트=teardown-only, 취소=onCancel; `closed` 멱등). Alt-Tab 등 mouseup 유실 시 리스너가 남아 버튼 안 눌린 mousemove 가 카드를 끌거나 유령 와이어·팬 커서가 고착되던 문제 해결. 호출부 10곳에 상황별 onCancel 배정(포트와이어=연결 안 함·와이어만 정리 / 팬·리사이즈·그룹테두리=좌표무관 커밋 재사용 / 리오더·마퀴·가위=표시만 정리 / **카드·그룹 이동=커밋 헬퍼 추출해 blur 에서도 정상 저장**, up 동작 불변). 검증: tsc·vitest124·build ✓.
- **청크 5 (생성 실행 / App 연결)**: 완료. 코덱스 리뷰 3건 검증 — 반영 1건, 기각/보고 2건.
  - **반영**: 렌더 no-comfy 경로가 `onRenderCards`(=generateCards)를 await 안 해 중복 클릭이 조용히 삼켜지고 에러가 보드와 안 이어지던 문제 → `await` + 타입 `void|Promise<void>` 통일(comfy 경로와 일관).
  - **기각(의도 확인)**: gen_requests 의 `require_project_role(..., read_only=True)` 는 버그 아님 — 주석에 명시된 팀 모델(프로젝트 멤버 또는 전역 read_all=admin/PM/PD 통과, 로컬 AUTH off 즉시 통과). 메모의 "의도 확인 필요" 해소.
  - **Jay 판단 보고**: 단일 생성카드 Generate(comfy 없음)는 makeGenJob(모델 없으면 차단)이 아니라 하단 스포트라이트 submit 을 재사용(197행 의도). 그래서 모델 노드 없는 카드를 생성하면 이전 카드/라이브러리의 '남은 모델'로 나갈 수 있음. 스포트라이트에 그 모델이 보이긴 하나 렌더 경로(차단)와 불일치. 차단으로 바꾸면 "Generate=하단 submit" 의도가 깨져 자동수정 보류 — 원하면 "모델 노드 없으면 Generate 비활성" 정책으로 통일 가능.
- **청크 6 (렌더 / UI)**: 완료. 코덱스 4건 — 반영 1건, 후속/폴리시 3건. XSS·dangerouslySetInnerHTML 없음(깨끗), 리스트 key 대부분 id 기반.
  - **반영**: comfy 입력 엣지 끝점이 base 만 레인별(ref/text)이고 fan 은 전체 입력 기준이라 ref 선이 ref 포트에서 어긋나던 문제 → 생성카드처럼 레인별 fan(inEdgesLaned 에 comfy 포함)으로 포트에 정렬(생성 동작 불변).
  - **✅ 반영(2026-08-05 Phase 6)**: 뷰포트 컬링·그래프 파생값 memo·워크플로 파싱 캐시·Comfy 입력 집계 memo·엣지 역할 incoming 인덱스를 적용했다. 카드별 수집 계산은 연결 순서가 공간 좌표에 의존하므로 이동 시 재계산을 유지한다.
  - **✅ 반영(2026-08-05 P28)**: MediaThumbnail 최종 로드 실패를 화면별 fallback으로 전환하고, 레퍼런스의 썸네일→원본 1회 재시도도 최종 실패 상태와 연결했다. comfy/InfoPopup 비디오 preload·poster 정책은 별도 기능 판단으로 유지한다.
- **청크 7 (Assets)**: 완료. 코덱스 4건 — 반영 1건, Jay 보고 3건. safe_join·loopback 게이트(tree/file/thumb/upload/reveal/zip)·업로드 상한·zip 정리·reveal shell 미경유는 깨끗.
  - **반영**: `/api/assets/projects` 가 loopback 없이 백그라운드 프리워밍(서버 디스크 스캔·썸네일 생성)을 걸어, 공유서버 원격 사용자가 Assets 만 열어도 서버 파일 I/O 를 돌리던 문제 → `is_loopback_request` 일 때만 프리워밍(원격은 thumb 를 못 받으니 기능 손실 0).
  - **Jay 보고(팀 ACL/토폴로지 판단)**:
    - ① Assets 공유 코멘트(`/meta`·`/comments`)에 프로젝트 멤버십 검사 없음 — 비멤버가 project 이름+path 알면 코멘트 열람/작성 가능. "팀 신뢰모델"이면 허용이나, 프로젝트 격리 필요하면 멤버십 ACL 추가. (마운트형 project 는 멤버십 개념 자체가 없어 정책 결정 필요)
    - ③ `GET /mounts` 가 loopback 없이 PM auto-mount 의 서버 절대경로·exists 노출(멤버 프로젝트 한정이나 서버 경로 노출). 원격에서 폴더등록 UI 자체가 무의미하니 게이트 검토.
    - ④ 공유서버(AUTH on)는 account_key=None → 마운트 단일파일. `_owner_mounts` 의 레거시(owner="") 흡수가 첫 요청자에게 쏠림(업그레이드 서버에서 마운트 유실). 로컬 허브 마이그레이션 의도와 얽혀 신중 필요.
- **청크 8 (계정 / DB / 휴지통)**: 리뷰 완료, **코드 변경 없음 — Jay 결정 필요**. 신원·공유 번들·마이그레이션은 가장 민감·정교한 시스템이라 정책/회귀 위험이 커 안전한 원라인 수정이 없음. remap_creator_uid 는 넓게 커버(깨끗), 일반 휴지통 삭제/복원은 owner 스코프됨(깨끗).
  - **★Critical (Jay 결정): publish-bundle 재공유가 남의 생성물 오염** — [share.py] 병합에서 `fact_blocked`(creator≠shared_by)는 프롬프트/에셋만 막고, 그 뒤 `share` 행 INSERT·`_set_auto_tags`(REPLACE)·`_merge_comments` 는 그대로 실행. B가 A의 생성물 id/job_id 를 아는 채 번들을 보내면 ① A의 비공개 생성물이 team 공유로 바뀌고 ② `_set_auto_tags` 가 owner=생성물작성자(A) 기준 REPLACE 라 B 번들의 빈 auto_tags 로 **A의 auto_tag 가 삭제**될 수 있음. 단, 코드가 "남의 것 재공유(COALESCE)"를 의도적으로 다뤄 재공유는 설계된 기능 → 정책 판단 필요. 권고: fact_blocked(비소유) 시 파괴적 오버레이(`_set_auto_tags` REPLACE·강제 share)도 함께 차단(코덱스 안).
  - **High → 현재 미해당**: 부팅 마이그레이션에 프로세스간 락 없음. 그러나 서버는 **단일 워커**(uvicorn.run reload=False, workers 미지정)라 레이스 없음. 향후 멀티워커 스케일 시에만 락 필요.
  - **Medium (Jay 결정): `/api/sources` 개인메타 누출** — search_sources 가 viewer/creator 스코프를 안 넘겨, 같은 프로젝트 멤버 B가 A의 개인 색/태그/auto_tag 를 소스 응답으로 봄(일반 목록에선 숨겨지는데). 수정=sources 쿼리에 viewer 스코프(피커 회귀 주의).
  - **Medium: 팀탭 auto_tags 필터 존재추론** — 값은 숨기나 필터 결과로 남의 auto_tag 부착 여부 노출.
  - **Low: `generation_stats().failed_count` 계정 미스코프** — 남의 실패물 수가 내 카운트에 섞임(clear_failed 는 본인만 → 숫자·조작범위 불일치).
- **청크 9 (미디어 비교)**: 완료. 코덱스 4건 — 반영 3건, 후속 1건. compareDiff 캔버스/ObjectURL/rAF 없음·XSS 없음(깨끗).
  - **반영 ①(높음)**: 비교 모달 위에서 Delete 가 뒤 SceneBoard 로 새어 선택 카드가 삭제되던 문제 → 두 모달(Compare/VideoCompare) keydown 을 capture 단계 + 폼컨트롤 밖 키 stopPropagation 으로 소유(배경 캔버스 단축키 차단).
  - **반영 ②(중간)**: 소스 라이트박스 `.cmp-srcbox` z-index 220 < 모달 231 이라 의도(모달 위)와 달리 뒤에 깔리던 문제 → 240 으로.
  - **반영 ④(낮음)**: VideoCompareModal 이미지 fallback 이 `img.src`(절대)≠`v.fallback`(상대) 비교라 fallback 도 404 면 무한 재요청 → dataset 플래그로 1회만.
  - **✅ 반영(2026-08-05 P31)**: 두 영상의 수동 seek를 절대 초 기준으로 양방향 동기화하고,
    길이 clamp·메타데이터 지연·프로그램적 이벤트 피드백 차단·종료 후 동시 재시작을 공용 바인더와 테스트로 고정했다.
- **청크 10 (Prompt / Spotlight)**: 완료. 코덱스 4건 전부 반영. 칩 렌더는 textContent/React 텍스트라 안전, 트레이 리스너 정리 있음(깨끗).
  - **①(높음) HTML paste XSS**: contentEditable onPaste 가 이미지 없으면 기본 붙여넣기를 허용해 text/html(<img onerror> 등)이 DOM 에 삽입되던 XSS 표면 → 이미지 아니면 preventDefault 후 text/plain 만 insertTextAtCaret. (SpotlightPromptRow 는 이 핸들러를 prop 으로 받아 함께 커버)
  - **②(높음) 깨진 PromptPart crash**: draftParse 가 JSON 배열을 검증 없이 캐스팅 → restoreParts 의 p.t/p.v.split 예외 → 손상 씬/import 로 프롬프트·바인딩 중단. draftParse 에 원소 스키마 검증(text=문자열 v·chip=ref 객체) 추가.
  - **③(중간) JSON 같은 텍스트 손실**: 텍스트노드에 `["prompt"]` 등을 쓰면 PromptPart 로 오인돼 손실 → 유효 PromptPart 가 하나도 없으면 원문을 텍스트로 폴백(빈 배열=의도된 빈 프롬프트는 유지).
  - **④(중간) batch 상한 방어**: submit(batchOverride) 가 Math.max(1,…)뿐이라 Infinity/9999/소수 시 Array.from RangeError·요청 폭주 → 유한·정수화 + [1, MAX_COUNT] clamp.
- **청크 11 (Comfy 재확인)**: 완료. 코덱스 5건 — 반영 1건(치명), Jay 결정/후속 4건. `_LOCAL_PREFIXES` 에 /api/comfy 포함·GET 키 마스킹·PUT '***' 무시·object_info 캐시키 api_key 분리·COMBO 두 형식·_str_set 방어는 깨끗.
  - **★반영(치명): /view 리다이렉트 file:// 로컬파일 읽기** — view_bytes 가 302 Location 을 스킴 검증 없이 urlopen 해, 악의적 Comfy 서버가 `Location: file:///…`(urllib file:// 지원) 나 내부망 IP 를 돌려주면 서버 로컬 파일·내부 서비스를 읽을 수 있었음 → 리다이렉트 대상에 `assert_public_http_url`(http(s)+공개IP) + `guarded_opener`(체인 리다이렉트 차단) 적용. comfy 직결은 기존대로 사설/로컬 허용.
  - **✅ Jay 승인·반영: 공유서버 /api/comfy loopback 게이트** — comfy 라우터 전체에 `_require_local_comfy`(AUTH on 이면 loopback 전용) 부착. 로컬 허브(AUTH off)는 통과, 공유서버 원격의 SSRF·저장키 대리사용 차단. 결정 #2("Comfy=로컬 개인사용") 모델을 강제. (assets 의 _require_local_assets 와 동일 패턴)
  - **후속: DoS 상한** — /run 워크플로 JSON·업로드·/view 다운로드에 크기 상한/스트리밍 없음(정상 대형 영상과 구분 필요).
  - **✅ 반영(2026-08-05 P29): 실행 중 워크플로 교체** — sceneId 가드에 content·노출 파라미터 실행 스냅샷을 더해 옛 결과·실패·자동 저장 연결·하류 생성을 차단했다.
- **청크 12 (검증 매트릭스)**: 완료. 병합 전 청크 5~11 반영 후 전체 자동 검증 **올 그린** — 백엔드 pytest 150 통과, 프론트 tsc 클린·vitest 124 통과·vite build ✓. (수동 스모크는 Jay 실사용 확인 몫: 씬 생성/전환·drag·pan·zoom·paste·drop·undo, Comfy 파싱·실행·배치·내작업저장·실행중 씬전환, 비교모달, 팀서버 AUTH 권한별.)
- **★리뷰 완료 요약(청크 1~12)**: 코드 반영 = 청크 4·5·6·7·9·10·11(+세션 초반 1~3).
  - **Jay 결정 완료**: (B) 공유서버 /api/comfy loopback 게이트 = **✅ 승인·반영**(커밋 8a7959a). (A) 청크 8 신원·공유 번들(publish-bundle 재공유 오염 등) = **그대로 둠**(Jay 결정: 팀은 신뢰관계, 수용 가능한 위험). 인원 확대·외부 노출 시 재검토 대상으로 남김.
  - **후속(기능/정책)**: Comfy DoS 상한.
  - **결론**: 병합 전 위험 반영 완료. dev→main 병합 가능(fast-forward).
- **보류(저위험·후속)**:
  - #1 다중 탭+서로 다른 계정 동시 로그인 시 씬 오염 가능(매우 드묾, keyOf 가 매 호출 activeAccount 를 읽음). 필요 시 탭 시작 시 네임스페이스 고정으로.
  - #7 arrangeNodes 비정상 grid/좌표 방어.

## 2026-08-06 리아키텍처 최종 감사

- `SceneBoard.tsx` 3,552줄(카드·입력·카메라·이동 훅 분리), repo `generations.py` 881줄,
  Assets 라우터 878줄까지 책임을 분리했다. 공개 파사드와 저장 데이터 형식은 유지했다.
- `assets.py`의 디스크 IO·계정별 마운트 저장·개인 메타·팀 코멘트를 하위 서비스와 라우터로
  분리했다. 배치 API 추가 후 라우터는 878줄이며 여러 Assets 창의 동시 마운트 등록도 변경을 잃지 않는다.
- 백엔드 전체 import 순환 0건, 의미 있는 동일 AST 함수 복제 0건, 계층 역방향 import 0건이다
  (주기 백업·동기화 클래스의 동일한 2필드 초기화만 구조상 반복).
- 다중 담당 해제, Assets 색상·태그·소스, PM 작업 순서·삭제, 라이브러리 색상·태그의
  브라우저 HTTP fan-out을 제거했다. PM 다중 프로젝트 작업 조회도 서버 내부의 프로젝트별 반복 조회를
  실제 DB 배치로 바꿨다. 남은 `Promise.all`은 생성 배치·부분 성공 삭제·독립 조회처럼 병렬 실행 자체가
  기능 의미인 경로다.
- P18~P48 자동 검증은 백엔드 375개·프론트 383개, TypeScript/Vite 빌드와 ESLint 구조 검사를
  통과했다. 실제 프로젝트 관리 작업표와 526개 파일 Assets 화면 로딩을 확인했고, P26은 복사 DB의
  문제 카드가 작업 공간·캔버스 모두에서 더 이상 JSON으로 노출되지 않는 것까지 브라우저에서 확인했다.
- 남은 `id/job_id` 데이터 마이그레이션은 구버전 데이터 호환이 선행돼야 하는 별도 고위험 단계다.
  Assets의 남은 파일 라우트는 같은 로컬 디스크 경계를 공유하며 현재 성능 병목은 아니다. Comfy 요청
  크기 상한은 구조가 아니라 별도 운영 정책이다.

## 부록 — 리뷰 청크(별도 세션, 병합 전 위험 점검용)
서브시스템 최종 diff 기준. 위험순: ① 백엔드 보안 경계 → ② 씬 그래프 순수 로직 → ③ SceneBoard 상태/저장 → ④ 입력 이벤트 → ⑤ 생성 실행/App 연결 → ⑥ 렌더/UI → ⑦ Assets → ⑧ 계정/DB/휴지통 → ⑨ 미디어 비교 → ⑩ Prompt/Spotlight → ⑪ Comfy 재확인 → ⑫ 검증 매트릭스.
