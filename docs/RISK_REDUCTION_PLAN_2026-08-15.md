# dev 위험 축소 공동 작업 계획 (Codex ↔ Claude)

작성일: 2026-08-15 · 갱신: 2026-08-16 (Claude 검토 + Codex 문서 모순 정리)
코드 감사 기준: `dev` (`c5127aec`, 코드 변경 32개 커밋)
비교 기준: `main` (`3b7cf077`)
계획 문서 최초 커밋: `9969b129`
상태: **2차 교차 검토 완료(Claude, 2026-08-16) — 상태 어휘 매핑 1건 반영. 구현은 항목별 합의·승인 후.**

## 1. 이 문서의 목적

코드 감사 기준 `dev`는 `main`보다 코드 변경 32개 커밋 앞서 있으며 생성·DB·백업·Comfy·Resolve·
운영 안정성 수정이 한 통합 후보에 모여 있다. 이후 계획 문서 커밋은 코드 변경 수와 분리해 센다.
자동 테스트는 통과하지만, 실제 외부 시스템과 결합된 운영 위험과 대시보드의 데이터 범위 불일치가
남아 있다.

이 문서는 다음 세 가지를 위해 사용한다.

1. 이미 해결한 감사 항목을 다시 수정하지 않는다.
2. 남은 위험을 사용자 영향과 재현 가능성 기준으로 다시 우선순위화한다.
3. Codex와 Claude가 설계 결정을 합의한 뒤 작은 커밋 단위로 구현한다.

기존 감사 이력은 [AUDIT_2026-08-15.md](AUDIT_2026-08-15.md)에 보존한다. 현행 구조와 데이터
소유권은 [ARCHITECTURE.md](ARCHITECTURE.md), [DATA_OWNERSHIP.md](DATA_OWNERSHIP.md),
[WORKSPACE_DATA_CONTRACT.md](WORKSPACE_DATA_CONTRACT.md)를 기준으로 한다.

## 2. 현재 기준선

| 항목 | 현재 상태 |
|---|---|
| 코드 감사 기준 | `c5127aec` (`main` 이후 코드 변경 32개 커밋) |
| 계획 문서 기준 | 최초 문서 커밋 `9969b129` — 코드 변경 수에서 제외 |
| 코드 변경 규모 | 85개 파일, +3,341 / -845줄 |
| 실시간 Git 상태 | 이 문서에 고정하지 않고 작업 시작 시 `git status`, `git branch -vv`, `git rev-list`로 확인 |
| 백엔드 테스트 | 699개 통과 |
| 프론트 테스트 | 518개 통과 |
| 프론트 빌드 | 통과 |
| 아키텍처 검사 | 통과 |
| 아직 하지 않은 검증 | 실제 Higgsfield·공유 서버·Comfy·Resolve·NAS·릴리즈 업데이트 통합 실측 |

자동 테스트 통과는 내부 계약이 유지된다는 뜻이다. 외부 CLI, 네트워크, 다른 PC, 운영 DB까지
안전하다는 증명은 아니다.

## 3. 불변 원칙

다음 원칙에 반하는 제안은 구현 전에 별도 설계 승인을 받는다.

1. 개인 생성물·개인 자산·캔버스는 로컬 우선이다.
2. 공유는 명시적인 publish 동작으로만 이루어진다.
3. 공유 서버는 생성하지 않는다. 실제 생성은 사용자 PC의 에이전트와 CLI가 수행한다.
4. 알 수 없는 계정·워크스페이스·권한은 추측하지 않고 실패 처리한다.
5. `generation.id`는 로컬·서버에서 다를 수 있으며, 경계 간 추적 기준은 `job_id`다.
6. DB 삭제·복원·정리처럼 되돌리기 어려운 작업은 백업과 사용자 확인 없이 실행하지 않는다.
7. 로그에는 인증정보, 전체 프롬프트, 원본 URL 같은 민감정보를 그대로 남기지 않는다.

## 4. 우선순위 정의

| 등급 | 의미 | 처리 원칙 |
|---|---|---|
| P0 | 잘못된 데이터 표시, 오과금·오귀속·데이터 손실 가능 | 운영 배포 완료 선언 전에 해결하거나 사용자가 위험을 명시적으로 승인 |
| P1 | 장애 복구 실패, 장시간 운영 불안, 보안 경계 문제 | P0 직후 작은 커밋으로 처리 |
| P2 | 유지보수·성능·구조 부채 | 기능 동결 구간에 단계적으로 처리 |

P0라고 해서 기존 `main`에도 이미 존재하는 문제 때문에 안정성 개선 코드의 Git 병합을 반드시 막는
것은 아니다. Git 병합, 운영 배포, 배포 완료 선언은 Gate 6의 서로 다른 판단 단계로 관리한다.

## 5. 작업 계획

### Gate 0 — 감사 기준선 재정리

목표: 완료 항목과 실제 잔여 항목을 분리해 같은 문제를 다시 고치지 않는다.

작업:

- `AUDIT_2026-08-15.md`의 완료 표와 잔여 목록을 현재 코드에 대조한다.
- 해결됨·부분 해결·미해결·실측 필요의 네 상태로 표시한다.
- 각 미해결 항목에는 현재 파일·함수 근거와 재현 조건을 붙인다.
- 근거가 오래됐거나 코드가 사라진 항목은 작업 목록에서 제거하고 감사 이력에만 남긴다.

완료 조건:

- Claude와 Codex가 같은 잔여 목록을 사용한다.
- 구현되지 않은 항목만 아래 Gate 1 이후로 이동한다.

#### Gate 0 산출물 — 정규화 잔여 목록 (2026-08-16 문서 모순 정리본)

상태의 단일 출처는 이 표다. 각 Gate 본문은 ID로만 참조하고, 상태 변경은 여기서만 한다.
상태: ✅해결 · 🔶부분/실측 필요 · ⬜미해결.

| ID | 우선 | 상태 | 항목 | 근거·남은 조치 |
|---|---|---|---|---|
| RL-01 | P0 | ⬜ | ingest 배치가 잡 고유 workspace 를 현재 선택값으로 덮어씀 | `generation_sync.py:37 workspace_columns(workspace or g)` — MCP 백필 시 전 이력 오귀속. 최초 적재 후엔 정정 불가(unknown 만 채움) |
| RL-02 | P0 | ⬜ | 작업 대시보드 데이터 의미(파생·수명주기·범위) | Gate 1 본문. "로컬에 없는 폴더가 작업에 표시" 현상 |
| RL-03 | P0 | ⬜ | CLI 주기 동기화 적재분이 텔레메트리 outbox 미반영 | `syncer.sync_now → apply_synced_jobs` 가 dirty 표시 안 함 — 하우스·수동 동기화분 대시보드 영구 누락. Gate 1 완료 조건 |
| RL-04 | P0 | ⬜ | unknown workspace 생성 요청 fail-closed | 직전 team 공간 잔존 과금(`agent_push._ensure_request_workspace`). **첫 커밋**: 에이전트 fail-closed 가드 → 이후 서버측 거절+업그레이드 안내. personal 추측 복귀 금지(방향만 바뀐 오과금) |
| RL-05 | P1 | ⬜ | claim lease 만료 후 안전 복구 계약 부재 | lease 만료만으로 재큐잉하면 이미 제출된 작업이 중복 과금될 수 있음. 상태별 복구표와 외부 제출 확인이 선행돼야 함 |
| RL-06 | P1 | ⬜ | 캔버스 재시도 placeholder 충돌(IntegrityError 500) | `usecases/gen_requests.py` 멱등 검사 gen_request 행만 확인 |
| RL-07 | P0 | ✅ | 완료 작업이 늦은 응답으로 되돌아감 | cb373035 `set_status` done 가드 + 상태엔진 테스트 |
| RL-08 | P1 | ⬜ | 견적 CLI 동시 무제한·취소 시 subprocess 미회수 | `usecases:_schedule_request_estimate`, `cli_bridge` CancelledError |
| RL-09 | P1 | ⬜ | 임의 401 에 토큰 삭제→로컬 모드 전환 | `_proxy.py` — 장애가 "데이터 없음"으로 보임 |
| RL-10 | P1 | ⬜ | 텔레메트리 락 보유 중 네트워크 왕복 | `_telemetry.py` drain_lock 안 proxy_json(60s) |
| RL-11 | P1 | 🔶 | publish 뱃지≠서버 — 잔여는 F7·F8 만 | 0e624b89 가 blocked 구분·finalize 중단 해결. 잔여: 프록시 unpublish 404→골드가드 우회, unfinalize 보상 비대칭 |
| RL-12 | P2 | 🔶 | 재시도 큐 관측 — 잔여는 '마지막 성공 시각'만 | telemetry_outbox_status + /api/sync-status 존재 |
| RL-13 | P1 | ⬜ | 크레딧·거래 보고 무음 유실(재시도 큐 없음) | `ingest.py` account_status fire-and-forget |
| RL-14 | P1 | ⬜ | /api/assets/file 확장자·헤더 무검증(동일 출처 HTML) | `routers/assets.py` FileResponse |
| RL-15 | P1 | ⬜ | multipart/zip 업로드 총량 상한 부재 | Starlette 스풀 후 검사 |
| RL-16 | P1 | ⬜ | asset watcher unregister 없음(폴더 삭제 불가·핸들 누적) | `asset_watcher.py` |
| RL-17 | P1 | ⬜ | 대형 Comfy 입력 다중 메모리 사본 | `comfy.py _read_media_uploads` 외 |
| RL-18 | P1 | ✅ | 임시파일 공통 청소 계약 | a2fbbe0e `temp_sweeper`(24h·앱 접두 한정) — 신규 임시파일은 이 계약 사용 |
| RL-19 | P1 | ⬜ | WS jitter·브로드캐스트 타임아웃·1008 무표시 | `progressSocket.ts`, `ws.py` |
| RL-20 | P1 | 🔶 | 워치독 busy/사망 오판 — 코드 해결, 운영 실측 필요 | 0d7f4223+75a64d6b. 전용 상태머신 테스트·운영 로그 확인 잔여 |
| RL-21 | P1 | 🔶 | 백업 복구 드릴 — 단일 DB 도구만 존재 | `tools/verify_backup_restore.py`. 세트(content·trash·manage) 동시 복원+격리 서버 ready·로그인·수 대조 실측 잔여 |
| RL-22 | P1 | ⬜ | 원격 URL 만료 대비 원본 보존 정책(신규) | 자동 byte-cache 가 골드 중심. "공유·골드 보존 정책 + 용량 한도 + 실패 상태·재시도 노출"로 설계(무제한 자동 캐시 금지 — 저장공간이 새 위험) |
| RL-23 | P1 | ⬜ | 작업자 오프디스크 백업이 자동·필수·관측 계약이 아님(신규) | 백업이 같은 디스크(`data/backups`). 서버용 `backup_replicate` 계약을 작업자에 확장 |
| RL-24 | P2 | ⬜ | CLI 파라미터 스키마 실패 영구 캐시(신규) | `cli_bridge._PARAM_NAMES_CACHE` — 실패는 TTL 또는 미저장 |

### Gate 1 — 작업 대시보드의 데이터 의미 확정 (P0)

현재 위험:

- 작업 화면은 로컬 실제 폴더가 아니라 공유 서버의 `generation.folder_path`에서 만들어진 누적
  작업을 표시한다.
- 현재 선택 워크스페이스가 작업 조회에 일관되게 전달되지 않는다.
- 작업 행은 추가 중심이라 과거 또는 삭제된 기록이 계속 보일 수 있다.
- 프로젝트 수·멤버 수와 시퀀스 작업 집계가 서로 다른 범위를 사용할 수 있다.

권장 설계:

1. `작업` 기본 화면은 **선택한 워크스페이스 + 사용자가 볼 수 있는 활성 프로젝트**로 제한한다.
2. 서버가 확인할 수 없는 사용자 PC의 물리 폴더 존재 여부를 서버 진실값으로 삼지 않는다.
3. 생성 기록에서 유도된 작업에는 `active`, `archived`, `source_last_seen_at` 같은 수명주기 개념을 둔다.
4. 오래된 항목은 자동 삭제하지 않고 `과거 기록`으로 분리한다.
5. 화면에 `공유 서버 작업 기록`처럼 데이터 출처를 표시한다.
6. 프로젝트 요약·멤버·시퀀스·Yield가 같은 workspace/project/visibility 조건을 사용하게 한다.

결정이 필요한 질문과 **검토 합의 권장 답**(최종 확정은 사용자):

- 작업 화면은 현재 작업만? → **기본=현재**(선택 워크스페이스+가시 활성 프로젝트), 과거 기록은
  별도 토글. 자동 삭제 없음(불변 원칙 6).
- archived 판정 시점? → **`max(마지막 생성 기록 후 30일, 프로젝트 계획 마감일)`** 경과.
  30일은 프로젝트 설정으로 조정 가능.
- workspace 변경 시 과거 기록? → **이전 공간에 유지.** 생성물의 workspace 는 생성 당시
  과금된 공간의 불변 사실 스냅샷이다(WORKSPACE_DATA_CONTRACT). 이동은 명시 재배정 작업으로만
  하고 별도 이력/필드로 남겨 과금 스냅샷을 지우지 않는다.

선결 조건: **RL-01**(ingest workspace 덮어쓰기)과 **RL-03**(주기 동기화 텔레메트리 미반영)을
이 Gate 와 같은 묶음으로 처리한다 — 입구 데이터가 오염되면 화면 정의를 아무리 고쳐도 숫자가 틀린다.

검증:

- 두 워크스페이스에 같은 프로젝트명이 있어도 데이터가 섞이지 않는다.
- 개인 모드와 팀 모드 이동 시 행 수와 합계가 안정적이다.
- 프로젝트 총합이 하위 에피소드·시퀀스 합계와 동일하다.
- 삭제/보관된 프로젝트는 기본 목록에서 사라지지만 과거 기록에서 조회된다.

완료 조건:

- 사용자가 화면의 숫자가 어느 DB·워크스페이스·기간에서 왔는지 설명 없이도 알 수 있다.
- 현재 보고된 “로컬에 없는 폴더가 작업에 표시됨” 현상이 정책대로 정리된다.

### Gate 2 — 생성 완료·소유권·과금 경계 강화 (P0)

대상 위험:

- claim 후 에이전트가 사라졌을 때 lease 만료·재할당 계약이 불완전할 가능성.
- 캔버스 재시도 충돌에서 placeholder가 남을 가능성.
- 견적용 CLI subprocess 동시 실행 수가 제한되지 않을 가능성.
- 요청 취소 시 subprocess가 완전히 회수되지 않을 가능성.
- 알 수 없는 workspace가 직전 team workspace로 귀속될 가능성.

권장 순서:

1. 상태 전이표와 DB CAS 조건을 먼저 문서화한다.
2. claim에 소유자, lease 만료, heartbeat, 복구 조건을 명시한다. **lease 만료 자체는 재큐잉
   근거로 사용하지 않는다.**
3. workspace 미확정은 개인/이전 공간으로 추측하지 않고 요청을 거절한다.
4. placeholder 생성과 요청 등록을 하나의 원자적 작업으로 묶거나 실패 보상한다.
5. CLI 실행에 공통 세마포어·취소·프로세스 종료 계약을 적용한다.

RL-05 안전 복구 기준:

| 중단 시 상태 | 자동 처리 | 이유 |
|---|---|---|
| `claimed` | CLI 제출 전임이 확인된 경우에만 재큐잉 | 아직 외부 과금이 시작되지 않았음이 증명돼야 함 |
| `submitting` | 자동 재큐잉 금지, 외부 생성 조회 후 `running` 복구 또는 `recovery_required` | CLI가 생성했지만 `job_id` 저장 전에 끊겼을 수 있음 |
| `running` + `job_id` 있음 | 새 생성 없이 기존 작업 추적 재개 | 이미 제출된 생성의 상태만 이어서 확인 |
| `running` + `job_id` 없음 | `recovery_required`, 자동 재제출 금지 | 외부 제출 여부를 확정할 수 없음 |
| `done` | 변경 금지 | 늦은 lease·에이전트 응답으로 완료를 되돌리지 않음 |

`recovery_required`는 자동 재생성 상태가 아니라 운영자 또는 복구 루틴이 외부 생성 여부를 확인해야
하는 보류 상태다. 외부 생성이 실행되지 않았음이 확인된 경우에만 새 요청으로 되돌릴 수 있다.

상태 어휘 매핑(2차 검토 반영): 현재 코드의 실제 상태는 `pending → submitting → running →
done/failed/canceled`다(`repo/gen_requests.py`). 표의 `claimed`는 아직 별도 상태로 존재하지
않는다 — claim 시 즉시 `submitting`이 되므로, 현재 코드에서 중단 시점 상태는 항상 `submitting`
행의 규칙을 따른다. `claimed` 행을 적용하려면 CLI 제출 전/후를 구분하는 상태(또는 제출 마커)를
신설해야 하며 `recovery_required`도 신설 상태다. 참고로 현재 스윕은 `job_id` 없는
`submitting/running` 30분 초과를 재큐잉 없이 `failed`로 닫고 `job_id` 보유분은 건드리지
않는다 — "lease 만료 자체는 재큐잉 근거가 아니다" 원칙과 이미 일치한다.

검증:

- 에이전트 강제 종료 후 같은 작업이 두 번 과금되지 않고 복구된다.
- 완료된 작업은 늦은 응답으로 running/failed로 되돌아가지 않는다.
- workspace가 없거나 권한이 사라진 요청은 생성 전에 명확히 실패한다.
- 취소 후 CLI 자식 프로세스가 남지 않는다.

완료 조건:

- `pending → submitting → running → done/failed/canceled` 전이가 테스트와 실제 로그에서
  동일하다(신설 상태 도입 시 그 어휘로 갱신).
- 생성 1건마다 workspace, account, job_id, 최종 결과가 한 경로로 추적된다.

### Gate 3 — 공유·프록시·텔레메트리 신뢰성 (P1)

대상 위험:

- 일부 401 응답에서 토큰을 지우고 로컬 모드로 전환하면 장애가 데이터 없음처럼 보일 수 있다.
- 네트워크 호출 중 텔레메트리 락을 오래 잡을 가능성.
- ingest가 기존 workspace 정보를 덮어쓸 가능성.
- 크레딧 보고·주기 동기화 실패가 운영자에게 충분히 보이지 않을 가능성.
- publish/unpublish 실패 후 로컬 뱃지와 서버 상태가 달라질 가능성.

권장 설계:

- 인증 만료, 권한 거부, 서버 장애, 비정상 응답을 서로 다른 오류로 유지한다.
- 네트워크 전송은 DB 락 밖에서 수행하고 결과 반영만 짧은 트랜잭션으로 처리한다.
- workspace/account 필드는 권한 있는 명시적 이동 작업 외에는 덮어쓰지 않는다.
- publish 상태는 서버 ACK를 받은 항목만 확정한다.
- 재시도 큐의 크기, 가장 오래된 항목, 마지막 성공 시각을 운영 상태에 노출한다.

완료 조건:

- 서버가 꺼져도 로컬 데이터가 빈 데이터로 오인되지 않는다.
- 서버 복구 후 중복 없이 전송되고 공유 뱃지와 서버 상태가 일치한다.

### Gate 4 — 자산·미디어 보안과 수명주기 (P1)

대상 위험:

- `/api/assets/file`에서 허용되지 않은 확장자 또는 HTML이 같은 출처로 열릴 가능성.
- multipart/zip 업로드 크기 상한이 불명확하다.
- 삭제된 폴더의 watcher 핸들이 남을 가능성.
- 대형 Comfy 입력이 메모리에서 여러 번 복사될 가능성.
- 캐시 생성·교체 사이 파일 변경 경쟁 조건 가능성.

권장 작업:

- 허용 확장자·MIME·다운로드 헤더 정책을 명시한다.
- 업로드 총량·파일 수·개별 파일 크기 제한을 둔다.
- watcher 등록부에 명시적인 unregister와 종료 검증을 추가한다.
- 대형 파일은 가능하면 스트리밍하고 임시파일은 공통 sweeper 계약을 사용한다.

완료 조건:

- HTML·실행 가능 파일이 앱 출처에서 실행되지 않는다.
- 제한 초과 요청은 메모리 고갈 없이 4xx로 종료된다.
- 폴더 삭제·재등록을 반복해도 watcher와 핸들이 증가하지 않는다.

### Gate 5 — WebSocket·장시간 운영·복구 실측 (P1)

대상 위험:

- WS 재연결에 jitter가 없어 여러 사용자가 동시에 재접속할 수 있다.
- 느린 클라이언트 전송에 타임아웃이 없으면 브로드캐스트가 막힐 수 있다.
- 인증 만료 close code가 사용자에게 보이지 않고 실시간 갱신만 멈출 수 있다.
- 백업이 존재해도 실제 복원 드릴을 하지 않으면 복구 시간을 알 수 없다.

검증 시나리오:

1. 100명 연결을 가정한 WS 재연결·댓글 카운트·team-fresh 부하.
2. 공유 서버 5분 단절 후 복구, 클라이언트 동시 재접속.
3. 8시간 저사양 서버 soak test: 메모리, 핸들, DB lock, outbox, 로그 크기 측정.
4. 백업 복사본으로 별도 임시 경로 복원 후 `/api/ready`, 로그인, 프로젝트 수 대조.
5. 워치독이 busy/maintenance를 사망으로 오판하지 않는지 확인.

완료 조건:

- 재접속 폭주로 서버가 다시 죽는 연쇄가 없다.
- 느린 클라이언트 하나가 전체 사용자 갱신을 막지 않는다.
- 복원 절차와 예상 복구 시간이 문서화된다.

### Gate 6 — 외부 통합과 병합·배포 검증 (P1)

실측 매트릭스:

| 통합 | 최소 검증 |
|---|---|
| Higgsfield | 실제 생성 1건, 완료 판정, URL/자산 저장, 크레딧 기록 |
| 공유 서버 | publish, 다른 계정 조회, unpublish, 권한 거부 |
| Comfy | 병렬 2건, 취소, 재시작 복구, 입력 혼입 없음 |
| Resolve | 설치 탐지, UNC/매핑 경로, 가져오기, 내보내기 |
| NAS | 정상·느림·단절 각각에서 UI가 멈추지 않음 |
| 릴리즈 | 기존 설치 업데이트, 서버 sparse checkout 업데이트, 실패 후 재시도 |

#### 6-A. Git 병합 Gate

`dev` 코드를 `main`에 넣어도 되는지를 판단한다. Git 병합만으로 실행 중 서버나 작업자 PC가
재시작되지는 않는다.

필수 조건:

- 전체 자동 테스트·프론트 빌드·아키텍처 검사 통과.
- `dev` 작업 폴더가 깨끗하고 병합 대상 커밋이 확정됨.
- DB 스키마가 기존 데이터와 하위 호환되거나 명시적인 마이그레이션·롤백 계획이 있음.
- 기존 `main`보다 생성·공유·업데이트 경로가 악화되지 않았음.

Gate 1 대시보드 문제는 기존 `main`에도 존재하므로 안정성 개선 코드의 Git 병합 자체는 막지 않는다.
다만 해결되지 않은 P0로 계속 기록하고, 배포 완료 선언 전 해결 또는 사용자 승인을 받는다.

#### 6-B. 운영 배포 Gate

운영 서버나 작업자 PC가 새 코드를 pull·업데이트하고 프로세스를 재시작해도 되는지를 판단한다.

필수 조건:

- 운영 서버 DB 수동 백업과 세트(content·trash·manage) 복구 검증(RL-21).
- 대상 서버의 non-terminal 요청이 0건이거나 중단·복구 계획이 기록됨.
- 업데이트할 작업자 PC에서 진행 중 Higgsfield·Comfy·Resolve 작업이 없음.
- 배포 전 커밋과 되돌릴 커밋, 재시작 절차가 기록됨.

#### 6-C. 배포 완료 선언 Gate

사용자에게 새 버전 배포가 정상 완료됐다고 선언할 수 있는지를 판단한다.

필수 조건:

- 실제 Higgsfield 생성 1건 왕복(`claim → anchor → ingest → done`).
- `publish → 다른 계정 조회 → unpublish` 왕복(RL-11/F7 실검증).
- 남아 있는 P0가 해결됐거나 사용자가 영향과 임시 운영 방법을 확인하고 명시적으로 승인함.
- 실측 결과와 로그 위치, 배포 식별자와 롤백 기준점이 기록됨.

### Gate 7 — 구조 부채 축소 (P2)

대상 파일은 기능 수정과 섞지 않고 별도 브랜치·커밋에서 다룬다.

| 파일 | 현재 규모(대략) | 분리 기준 |
|---|---:|---|
| `SceneBoard.tsx` | 3,763줄 | 선택, 드래그, 노드 렌더, 저장·복구, 단축키 |
| `App.tsx` | 1,607줄 | 인증, workspace, library, 라우팅, WS orchestration |
| `SpotlightPrompt.tsx` | 1,236줄 | 입력 UI, 명령 해석, workspace/tag 액션 |
| `routers/comfy.py` | 967줄 | job API, upload, execution, recovery, cleanup |
| `repo/identity.py` | 941줄 | account link, visibility, members, identity resolve |
| `services/resolve_bridge.py` | 917줄 | connection, path, import, bin ordering, manifest |

원칙:

- 동작 변경 없는 추출부터 한다.
- 기존 공개 함수와 API 응답을 먼저 테스트로 고정한다.
- 한 커밋에서 한 책임만 분리한다.
- 각 추출 후 전체 테스트와 실제 해당 화면 smoke test를 실행한다.

## 6. 구현 단위와 역할 제안

| 단계 | 주도 제안 | 교차 검토 |
|---|---|---|
| 데이터 소유권·대시보드 의미 결정 | Claude | Codex가 쿼리·경계조건 검토 |
| 생성 lease/CAS·동시성 | Codex | Claude가 전체 흐름·UI 영향 검토 |
| 공유·권한·workspace 계약 | Claude | Codex가 실패·재시도 경로 검토 |
| 성능·락·subprocess·WS | Codex | Claude가 운영 통합 검토 |
| 프론트 구조 분리 | Claude | Codex가 회귀·성능 검토 |
| 실측·배포 승인 | 공동 | 사용자 최종 승인 |

역할은 권장안이며 파일 소유권이 아니다. 어느 쪽이 구현하더라도 다른 쪽이 첫 구현은 틀렸다고
가정하고 논리 오류, 경계 조건, Windows 경로, 장애 복구를 다시 검토한다.

## 7. 각 작업의 공통 완료 형식

모든 작업은 다음 정보를 남긴다.

1. 문제와 재현 조건
2. 변경 전·후 데이터 흐름
3. 변경 파일과 변경하지 않은 범위
4. 자동 테스트 결과
5. 실측 결과와 로그 위치
6. DB·API·배포 호환성
7. 남은 위험과 롤백 방법

커밋은 작업 하나당 하나를 기본으로 하고, 커밋 메시지에는 변경 내용보다 변경 이유가 드러나게 한다.

## 8. Claude 검토 요청

Claude는 구현 전에 아래 형식으로 이 계획을 검토한다.

```text
1. 동의하는 Gate와 이유
2. 우선순위를 바꿔야 할 Gate와 근거
3. 현재 코드에서 이미 해결되어 제거할 항목
4. 빠진 P0/P1 위험과 파일·함수 근거
5. Gate 1 대시보드 결정 질문에 대한 권장 답
6. dev → main 병합을 막아야 하는 조건
7. 첫 번째로 구현할 가장 작은 커밋 범위
```

검토 시 추측으로 항목을 추가하지 않고, 현재 `dev` 코드 또는 재현 결과를 근거로 삼는다.

## 9. 현재 권장 결론 (문서 모순 정리 반영)

1. 코드 감사 기준 `dev`의 32개 코드 커밋은 자동 검증 기준으로 통합 후보 상태다. 이후 계획 문서
   커밋은 코드 변경 수와 분리한다.
2. Gate 0 산출물(§5-0 표)이 잔여 상태의 단일 출처다. 현재 24건 중 ✅ 해결 2건,
   🔶 부분 해결·실측 필요 4건, ⬜ 미해결 18건이다.
3. P0 = RL-01·02·03·04. 첫 코드 작업은 RL-04 unknown workspace fail-closed다. 최소 변경 파일은
   `agent_push.py`와 `backend/tests/test_agent_contracts.py` 2개이며, 이어서 서버측 거절과
   업그레이드 안내 계약을 별도 커밋으로 검토한다.
4. Gate 1(RL-02)은 RL-01·03과 한 묶음으로 처리한다. 입구 데이터 정합 없이는 화면만 수정해도
   집계가 다시 틀어진다.
5. Git 병합은 Gate 6-A, 운영 서버·작업자 업데이트는 Gate 6-B, 배포 완료 선언은 Gate 6-C를
   각각 통과해야 한다. Gate 1은 Git 병합 자체를 막지 않지만, 해결되지 않은 P0로 남아 배포 완료
   선언 전 해결 또는 사용자 승인이 필요하다.
6. RL-05는 lease 만료만으로 자동 재큐잉하지 않는다. 외부 생성 미실행이 확인된 요청만 재큐잉하고,
   제출 여부가 불명확하면 `recovery_required`로 분리해 이중 과금을 막는다.
7. 신규 인지된 데이터 손실 위험 RL-22(원격 URL 만료)·RL-23(작업자 오프디스크 백업)은
   P1로 Gate 3/5 뒤에 배치하되, 사용자 승인 시 앞당길 수 있다.
8. 구조 분리(Gate 7)는 안정성 작업과 섞지 않고 운영 위험을 닫은 뒤 진행한다.
